# CLAUDE.md — 402 Guard (구글 클라우드 X 솔라나 AI 에이전틱 해커톤)

> **이 파일은 개발 에이전트(Claude Code)용 내부 작업 노트입니다 — 제품 설명이 아닙니다.**
> 세션 간 맥락을 잇기 위한 진행 메모라서 미완성 항목·자체 점검 결과·아직 검증되지 않은
> 가설이 그대로 섞여 있고, 아래로 갈수록 오래된 기록입니다.
> 제품이 무엇인지는 [`README.md`](README.md), 기능의 현행 상태는
> [`docs/FEATURES.md`](docs/FEATURES.md), 제출 문안은 [`docs/submission.md`](docs/submission.md)
> 가 각각 단일 출처입니다. **여기 적힌 자체 평가·추정치를 제품의 공식 주장으로 인용하지 마세요.**

이 저장소에서 작업하는 Claude는 아래 규칙을 모든 세션에서 따른다.

## 프로젝트 한 줄 요약

**사용자가 정한 조건·예산 한도 안에서 AI 에이전트가 스스로 주식(토큰화 주식)을 사고팔고 USDC로 정산하는 멀티에이전트 시스템.** Trading Agent(구매) ↔ Broker Agent(판매), A2A 협의 + AP2 한도 + x402 온체인 정산. 언어 Python, 네트워크 Solana devnet(개발은 localnet). 상세는 `docs/architecture.md`.

## 해커톤 고정 제약 (0721 킥오프 세션 기준)

1. 마감: 최종 제출 2026-08-03(월) 23:59 KST. 모든 일정 제안은 이 마감을 기준으로 역산한다.
2. 제출물 3종을 항상 염두에 둔다: ①프로덕트 소개서(타겟 고객·문제/수익모델/아키텍처 필수 포함) ②재현 가능한 코드+README의 GitHub 저장소 ③AI 에이전트 결제 전 과정 데모 영상. 라이브 배포 URL은 가산점.
3. 목업은 심사 제외다. 결제는 반드시 실제로 동작해야 하며, "화면이 아니라 진짜 트랜잭션"이 심사 대상이다. 모든 결제 기능은 실행 로그와 트랜잭션 이력(해시)을 남기도록 구현한다.
4. 네트워크: 개발의 99%는 Localnet, 시연·제출은 Devnet. Mainnet은 사용하지 않는다. 심사 평가 네트워크는 Devnet과 Localnet이다.
5. 심사 4축에 맞춘다: ①혁신성·UX·상업성 ②Gemini 등 AI 활용도 ③인프라 연동(USDC on Solana, Solana Pay, pay.sh) ④실행 로그·이력 기반 실제 구동.
6. 결제 스택: 결제 레이어로 솔라나를 쓰는 것만 필수. pay.sh, x402, Solana Pay, 자체 스마트 컨트랙트 모두 허용된다. (현행: 자체 x402 구현. pay.sh는 비교 검토 대상이며 전환 의무 없음.)
7. 오버 엔지니어링 금지: Gemini는 API 호출 수준이면 충분하고 ADK/Vertex 같은 구조적 활용은 가산점이 아니다. 문제-솔루션 적합성이 명확한 최소 구성을 우선한다.
8. 역할 분담: 코드 개발·설계·문서는 Claude가 담당한다. 프로덕트 런타임의 AI(에이전트 판단·대화)는 Gemini API(무료 티어)를 호출하도록 구현한다. Antigravity/Gemini로 개발하지 않아도 감점 없음이 Q&A로 확인됨.
9. 산출물(소개서·README·데모 시나리오)에는 반드시 당위성을 명시한다: 왜 카드망이 아닌 블록체인인가(무인증 결제, 저수수료, 빠른 속도, 24/7 국경 무관, 에이전트는 계좌 개설 불가하지만 지갑은 코드로 생성 가능), 왜 사람 개입 없는 자율 결제인가.
10. 지갑 개인 키·시크릿은 코드에 하드코딩하지 않고 채팅에 붙여넣지 않는다. 에이전트에게 개인 키를 직접 주는 설계를 피한다. 키 파일은 `secrets/` 아래에 두고 절대 커밋하지 않는다(.gitignore로 차단됨).

## 개발 상세 규칙

- **작업 사이클 규칙 (2026-07-23 확정, 07-23 저녁 푸시 추가)**: "①기능 구현 → ②즉시 검증·버그 수정 → ③커밋 → ④`git push origin main`"을 **기능 단위로 반복**한다. **푸시는 사용자가 상시 승인했으므로 커밋할 때마다 곧바로 푸시한다**(별도로 묻지 않는다). 버그를 여러 기능 뒤로 미뤄 일괄 디버깅하지 않는다(원인 추적 비용이 커짐). **사용자가 보고한 버그는 새 기능 착수보다 항상 우선**하며, 그 코드를 작성한 대화에서 즉시 고치는 것이 원칙(맥락이 신선할 때가 가장 싸다) — 대화가 이미 길거나 이전 단계 코드의 버그면 "버그 수정 전용" 새 대화로 시작한다. **"보기 좋게"(디자인·폴리시)는 기능마다 하지 않는다** — 무디자인 뼈대를 유지하다 디자인 시안 수령 후 일괄 적용한다(`web/static/css/theme.css` 교체 구조 전제). 즉 동작 안정성(버그)은 기능 사이클에 포함, 시각 다듬기는 별도 단계.
- 검증 루틴: 결제 기능은 (1)로컬 전 과정 실행→영수증 확인 (2)상태 변화를 앱 화면과 온체인/DB 양쪽에서 교차 확인 (3)배포 URL에서 동일 검증 반복 (4)트랜잭션 해시·로그를 파일로 아카이빙(`artifacts/tx/`), 이 4단계를 통과해야 완료로 간주한다. `docs/verification_checklist.md` 참고.
- 구현 시작 전에 요구사항이 모호하면 인터뷰(질문 3~4개)로 구체화한 뒤 구현 계획을 먼저 제시하고 승인받는다.
- 독립적인 컴포넌트(에이전트/결제/시세/프론트)는 병렬로 나눠 작업한다.
- 배포 대상: Google Cloud Run(Dockerfile 기반). 데이터는 Firestore, 비정형 파일은 Cloud Storage. GCP 인증(gcloud/서비스 계정)은 사용자가 직접 수행하며, Claude는 명령만 제시한다.
- Gemini API는 무료 티어를 사용한다($300 크레딧으로는 Gemini API를 쓸 수 없음). 무료 티어 초과가 예상되면 사용자에게 "디스코드에 사전 문의해 추가 크레딧을 요청하라"고 안내한다.
- Devnet SOL 퍼셋은 하루 0.5~5 SOL 제한이므로 부족이 예상되면 미리(마감 직전 금지) 디스코드 솔라나 채널에 요청하도록 안내한다. 테스트용 자산은 자체 토큰 발행(무한 발행 가능)으로 해결한다.
- 참고 프로토콜 계층: A2A(에이전트 간 통신)/MCP(에이전트-툴)/AP2(mandate 기반 결제 위임)/UCP(A2A+AP2 통합)/x402·MPP(정산 레일). AP2 안에서 x402를 결제 레일로 사용할 수 있다.
- 데모데이(8/21 금, Google Startup Campus) 라이브 시연을 전제로, 데모는 네트워크 불안정 대비 폴백(로컬넷 재시연 경로)을 준비한다.

## 현재 상태 (2026-07-31 갱신)

> # ▶▶▶ [✅ 완료 — 2026-07-31] 프런트 새 전달본 8묶음 이식 · 커밋 `5a33026` 푸시 ·
> # 리비전 **`synapstock-00025-xvl`** 배포·검증 완료
> **전달서 `web/static/lab/README.md:115` 이후 8묶음을 전부 옮겼다. 다시 하지 말 것.**
> 실험장 전용(`mock.js`·`bundle.py`·`devserve.py`·`data-lab-*`·조작 바·`.fab` 의 목 배선)은
> 가져오지 않았다. 옛 전달본 백업은 스크래치패드 `94455dfd-…/scratchpad/lab_prev/` 에 있다.
>
> | # | 항목 | 결과 |
> |---|---|---|
> | 1 | 맨 위로 버튼 | 글자 삭제·50→**40px**·화면 아래 가운데·호버 scale 1.06 + 화살표 벨트. 벨트 재현 실측: 정지 `[12,52]` → −12 지점 `[0,40]` → 주기 끝 `[-28,12]`(이음매 0) |
> | 2 | 토스트 우하단 → **우상단** | 제목칸 바닥이 실측 **정확히 90** 이라 `top:98`(여유 8) · 얇은 헤더면 `47`. `align-items:flex-end` 로 폭이 제 글 길이를 따라감(실측 짧은 것 52.8 · 긴 것 340) |
> | 3 | 플로팅 버튼 `.fab-stack` | 접힘 36×36 → 펼침 **114/86×36**, 아이콘 오른쪽 끝 **1235.2 불변**(거울상). `app.js` 가 `data-fab-pause` → `btnPause.click()` 배선 |
> | 4 | 얇은 헤더 긴급정지 감춤 | CSS 로만(`display:none`). 짝인 `[data-slim-resume]` 는 남겼다 |
> | 5 | 알림 버튼 종 | `--ci-bell/-off` + `::after`+`order:-1` + 17px. `renderNotifyBtn` 이 `is-notify-on` 을 붙인다 |
> | 6 | 차오르는 색 `#EDEBFF` | 적용. 글자색(`--sweep-fg`)은 그대로(지시가 바탕색 하나) |
> | 7 | 데일리 브리핑 카드 | 간격 16 · 부제 `Gemini 자동` · 생성시각 알약(#f4f5fc/#8f96af·8/11·r8) · `오늘 요약` 감춤 · 메타 `17:58:14 생성 · Gemini` 로 축약 |
> | 8 | 시세 등락 배지 | 큰 숫자 오른쪽 알약(h30·pad 0 11·r20), 방향은 `clip-path` 삼각형(꼭짓점 뒤집기). 오름·내림 양방향 실측 |
>
> **⚠ 이번에 새로 드러난 사실 4건 — 다음 세션이 알아야 한다.**
> ① **`.to-top` 은 `landing.html` 과 공용이다.** 마크업(`.to-top-track` + 화살표 2개)을 랜딩에도
>    함께 넣었다 — 안 넣으면 랜딩만 글자도 모션도 없는 맨 원이 된다. 랜딩 실측도 통과.
> ② **알림 라벨의 `🔔`·`🔕` 이모지를 뺐다**(`NOTIFY_LABEL`). 안 빼면 아이콘과 겹쳐 종이 두 개다.
>    **문구 자체는 한 글자도 바꾸지 않았다**(`'차단됨'을 쓰지 않는다`는 기존 판단 유지).
> ③ **배지에서 `USDC` 를 뺀 것은 정보 손실이 아니다** — 바로 위 큰 숫자가 이미
>    `325.89 USDC` 다(`app.js:566`). 기준 문구는 알약 `title` 로 내렸다.
> ④ **브리핑 본문의 굵은 구간은 이식하지 않았다**(전달서가 예고한 대로). 실험장은 `mock.js`
>    POOL 의 `«»` 를 감싼 것이고 **서버는 평문만 준다** — 서버가 마크업을 주기 전에는 불가능하다.
>
> **게이트 전부 통과**: 훅 128→**130종**(`data-fab-pause`·`-menu` 추가) **누락 0** ·
> CSS 주석 192/192·중괄호 489/489(전달서가 경고한 `*/` 이중 닫기 없음) · `node --check` 3파일 ·
> dry 세션 완주(가드 시도 3 · 차단 0 · **유출 0.00**) · **콘솔 오류 0**(대시보드·랜딩·배포본) ·
> 가로 넘침 0 · 라이트/다크 양쪽 실측.
> **배포 검증**: 엔드포인트 12개 중 11개 200 + `/static/lab/index.html` **404**(실험장 차단 유지) ·
> 정적 파일 4개 **저장소와 MD5 일치** · 배포본 화면에서 8묶음 전부 확인.
>
> **⚠ 남겨 둔 것 2건(전달서가 '사용자 판단 대기'로 넘긴 것)**
> - **`메뉴` 버튼은 아무 동작도 없다** — 누를 곳이 미정이라 배선하지 않았다(모양만).
> - **플로팅 `STOP` 은 세션이 안 돌 때도 눌리는 모양이다.** 진짜 버튼이 `disabled` 라
>   `click()` 이 아무 일도 안 하므로 무해하지만, 회색으로 만들려면 `renderState` 에 한 줄이다.
>
> ---
>
> # ▶▶ 다음 대화 = **devnet USDC 재실증 → A-lite 재검토 → 온체인 결제 확인 → 잔여 점검**
> **⚑ 사용자 지시(2026-07-31): "버그부서·심사부서 돌리기 전에 완벽하게 검토해야 된다."**
> 아래 4건을 끝낸 뒤에 부서를 돌린다. **넉넉한 예산의 새 대화 권장.** 기준 커밋 `5372d4f`.
>
> 1. **devnet USDC 재실증 (Circle 공식 민트)** — ⚑ 사용자: *"데브넷은 실제 돈이 아니니까
>    바로바로 해봐도 되고, 하는 게 좋다."* **실측으로 확인한 현재 상태**: `artifacts/tx/` 에
>    **Circle 공식 민트(`4zMMC9srt5Ri…`)로 정산한 아카이브가 0건**이다. devnet 증빙 1건
>    (`20260724_1643_solana-devnet_live_buy.json`)은 **구매자가 민트 권한을 쥔 자체발행 토큰**
>    이라, explorer 를 영상에 찍으면 "자기가 찍은 돈으로 지불"로 읽힌다. 이게 축③·④ 공통의
>    최대 단일 구멍이다.
>
>    **▶ 2026-07-31 온체인 실측 — 착수 전 상태가 확정됐다(추정 아님).**
>    · 자체발행 민트 `8L9feSSChJHXEF58etFL1zsTzWiggqRdWFwqLM6vgH4u` 의
>      **`mintAuthority` = `GQJo2qcr6nUVjFaHUGsb6ZS5XnQndUCDHTqGgZN5gQX6` = trading(구매자) 지갑**,
>      supply 1,500,000,000(=1500). **explorer 한 줄로 "자기가 찍은 돈" 이 성립한다.**
>    · **Circle 공식 USDC(`4zMMC9sr…`) 토큰 계정이 세 지갑 모두 아예 없다.**
>    · devnet SOL — trading **14.4853** · broker **5.4874** · user **0**(충분. 추가 파우셋 불필요).
>    · 로컬 `secrets/` 세 지갑 = 07-24 증빙의 지갑과 **동일**(공개키 대조).
>    · 현재 `.env` 는 `USDC_MINT=8L9feSS…`(자체발행) · `BUDGET_USDC=100` · `PER_TRADE_MAX_USDC=50`.
>
>    **▶ 절차 (이 순서 그대로)**
>    ① `.env` 백업 → `USDC_MINT` 를 `4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU` 로 바꾼다.
>       (안 바꾸면 `setup_devnet.py:269` 가 "자체발행 토큰입니다" 로 **정상 중단**한다 — 안전망이다.)
>    ② **[사용자 몫] Circle 파우셋** `https://faucet.circle.com` → Solana Devnet →
>       **trading `GQJo2qcr6nUVjFaHUGsb6ZS5XnQndUCDHTqGgZN5gQX6`** 과
>       **broker `32UBfBeTXcxwJ67Q6K1uFW5faQbS84r1DvFkrpyFKMSm`** 에 **각각 1회**.
>       ⚠ **두 주소에 따로 받는 것이 요령이다.** setup 은 broker 몫을
>       `min((trading+broker)/3, 500)` 로 잡는데(`:310-320`), broker 가 이미 그 이상이면
>       **이체를 생략**해 trading 이 받은 20 을 전액 쓸 수 있다. trading 한 곳만 받으면
>       6.67 이 broker 로 빠져나가 구매자에게 13.33 만 남는다.
>       ⚠ 파우셋 한도는 **주소당 2시간마다 20 USDC**(스크립트 주석 기준).
>    ③ `.venv\\Scripts\\python.exe scripts/setup_devnet.py` (SOL 은 이미 충분하다)
>    ④ **건별 지출을 낮춰야 한다** — 지금 기본이 **30 USDC** 인데 파우셋 1회는 20 이다.
>       `run_demo.py:186 spend_per_trade_usdc` · `web/engine.py:46 DEFAULT_RULES["spend_per_trade"]`
>       (⚠ 둘 다 **코드 상수**다. CLI 옵션이 없다 — `run_demo` 인자는 live/ticks/no-gemini/replay뿐).
>       `.env` 의 `BUDGET_USDC`·`PER_TRADE_MAX_USDC` 도 잔여 이하로.
>       **또는** 파우셋을 2시간 간격으로 두 번 더 돌려 40+ 를 모으면 상수를 안 건드려도 된다.
>    ⑤ `.venv\\Scripts\\python.exe run_demo.py --live`
>    ⚠ `setup_devnet.py:191` 이 **마지막에 `.env` 를 덮어쓴다**(USDC_MINT·STOCK_MINT 줄) —
>    ①의 백업이 그래서 필요하다. 07-29 라운드는 임시 지갑 경로로 우회했다.
> 2. **A-lite(온체인 예산 레일) 재검토** — `payments/delegation.py` · `scripts/demo_delegation.py`.
>    07-29 에 만들고 localnet 증빙 1건(`20260729_1243_…_delegation.json`)까지 남겼지만
>    그 뒤로 손대지 않았다. **아직 제품 미배선**(`wired_into_product: false`)이고, 그 경계를
>    넘는 주장(“402 Guard 의 예산이 온체인에서 집행된다”)은 **거짓**이다 — 정확한 문장은
>    "예산 상한을 체인이 집행하는 레일을 실증했고, 엔진 배선은 로드맵". 재검토할 것:
>    `test_delegation` 58종이 지금도 통과하는지 · localnet 증빙이 재현되는지 ·
>    `classify_rejection` 이 한도 초과와 잔액 부족(둘 다 `0x1`)을 여전히 갈라내는지.
>    **▶ 2026-07-31 부분 확인**: 배치 실행에서 **`test_delegation.py` rc=0**(단위 23종 전부 rc=0).
>    다만 이건 **오프라인 41종만의 통과**일 수 있다 — localnet 검증기를 안 띄우고 돌렸으므로
>    **온체인 17종이 건너뛰어졌는지 실제로 돈 것인지 아직 확인하지 않았다.** 그 구분과
>    localnet 증빙 재현이 남은 일이다.
>    ⚠ **C4(엔진 배선)는 하지 않는다** — 교차검증 파손 위험 + 촬영이 드라이런 경로다.
> 3. **온체인 결제 확인** — 배포본 세션 기록 **13건 전부 `mode=dry`**(2026-07-31 재실측,
>    리비전 `00025-xvl` 기준으로도 그대로다). 라이브
>    (온체인) 세션이 배포 URL 에서 1건도 없다. 1번이 끝난 뒤 `ALLOW_LIVE_FROM_WEB=1` 로
>    잠깐 열고 되돌리는 절차. ⚠ **세션이 도는 중에는 어떤 `gcloud run services update` 도
>    하지 않는다**(런북 §6) — env 주입은 세션 시작 전에 끝낸다.
> 4. **또 할 작업 전수 점검** — 그 뒤에 `bug-dept` · `judge-dept` 를 돌린다.
> 5. ~~**`NOTICE` 분리해서 GitHub 라이선스 배지 띄우기**~~ **COMPLETE (2026-07-31, commit 68b7e54)** — GitHub API spdx_id = MIT 확인. 아래는 착수 당시 명세다.
>    지금 저장소 페이지에 라이선스 배지가 없다(API `license.spdx_id: NOASSERTION`).
>    원인은 `LICENSE` 가 표준 MIT 뒤에 제3자 데이터 고지를 이어 붙여서다 — GitHub 의 분류기
>    (licensee)는 **본문 뒤에 딴 절이 붙으면 MIT 로 인식하지 않는다.** 공개 저장소가 심사
>    대상이라 "라이선스 없음"으로 보이는 것은 값싸게 고칠 수 있는 손해다.
>    **파일 구조는 이미 깔끔하게 나뉘어 있다**(실측):
>      · `LICENSE:1-21` = 표준 MIT 전문 · `:22` 빈 줄 · `:23` 구분선 ·
>        `:24-43` = `NOTICE — third-party data…`(Alpha Vantage CSV 예외 + "실자금 없음" 고지)
>    할 일: ①`LICENSE` 를 **1-21행만** 남긴다(⚠ 구분선·꼬리 문구까지 전부 빼야 인식된다)
>    ②`:24-43` 을 새 파일 `NOTICE` 로 옮긴다(내용은 한 글자도 바꾸지 않는다 — 데이터 예외와
>    "투자 조언이 아니다" 고지는 공개 저장소에서 실제로 필요한 문구다)
>    ③`README` 에 `## 라이선스` 절을 짧게 신설해 둘을 가리킨다 — 지금 README 에는 라이선스
>    절이 아예 없고 Alpha Vantage 는 `:259` 에서 출처로만 스친다. 분리만 하면 예외 고지가
>    아무 데서도 안 보이게 되므로 이 ③이 있어야 분리가 안전하다.
>    ④푸시 후 확인: `gh repo view Loda1002/SynapStock --json licenseInfo`(또는 API 의
>    `license.spdx_id`)가 `MIT` 로 바뀌는지. 재색인에 몇 분 걸릴 수 있다.
>
> **⚠⚑ 사용자 정정(2026-07-31): 지금 화면은 디자인 "최종본이 아니다".**
> 따라서 **소개서 PDF 출력·3분 영상 촬영은 아직 착수하지 않는다** — 최종본이 와야 의미가 있다.
> (이전 블록들에 "디자인 최종본 반영됨"으로 읽힐 서술이 있으면 그것이 틀린 것이다.)
>
> **✅ 이번 대화에서 끝낸 것 (다시 하지 말 것)**
> - **제품명 = `SynapStock`** (커밋 `5372d4f`). **`402 Guard` 는 그 안의 지출 승인 게이트
>   "기능명"** 으로 남긴다. 바꾼 것은 제품명 자리뿐이다 — 화면 `<title>`·브랜드 h1·얇은 헤더·
>   랜딩 미리보기 브랜드 바·브라우저 알림 제목 · FastAPI 문서 제목·404·CLI 설명 ·
>   README 제목/첫 문단 · `submission.md` 제출명 · 소개서 표지.
>   **가드 배너·KPI·차단 로그 태그·red_team·`payments/guard.py` 는 그대로다**(화면에 남은
>   402 Guard 12곳을 전수 확인해 전부 기능 자리임을 검증했다).
>   ⚠ **나중에 누가 또 일괄치환하지 않도록** README·submission 에 이름 규칙을 한 줄로 박아 뒀다.
> - **BUG-24 수정**(`5f4c1a6`) — 숫자형 환경변수가 빈 값이면 임포트 단계에서 앱 전체가 죽던 것.
>   `config.env_num` 신설(빈 값→기본값 복원 · 잘못된 값→**변수명을 밝히며** 중단).
>   ⚠ `MAX_BUDGET_USDC` 만 `blank_ok=False` — 배포가 1000 으로 낮춘 서버측 상한이 빈 값일 때
>   기본값 10000 으로 조용히 되돌아가면 통제가 10배 헐거워진다(리포트의 경고 그대로).
>   회귀 5묶음은 `test_dry_sell.py` 에 넣어 **테스트 파일 수 23개 불변**.
>   음성 대조 수행: 옛 방식은 `decimal.InvalidOperation`(변수명 없음)으로 죽는다.
> - **런북 `docs/deploy_cloud_run.md:35` 정정** — `autotrader-agent-2026` 은 실재하지 않는
>   값이었다. 프로젝트·서비스 모두 `synapstock`. "이미 만들어져 있으니 재배포는 §4 로" 안내 추가.
> - **`CONTROL_TOKEN` 교체** — 시크릿 버전 **2 가 현행, 버전 1 은 disabled**.
>   리비전 `synapstock-00023-92g` 부터 적용. 옛 토큰이 401 나는 것을 실측 확인.
> - **public 전환 안전성 감사 — 통과.** 275커밋 전수 blob 스캔에서 크리덴셜 0건
>   (유일한 히트는 `test_brain_select.py` 의 더미 `AIzaTestKey…NotReal`). 추적 파일 196개에
>   `secrets/`·`.env`·키페어 0건, 히스토리에 들어간 적도 0건. 이메일·로컬 절대경로 0건.
>   `.mcp.json`·`.claude/settings.json` 깨끗(`settings.local.json` 은 미추적). LICENSE 존재.
>   자해가 될 문서들의 방어 문구도 전부 잔존(`bug_latest` 스냅샷 배너 · `differentiation`
>   상태 열 · CLAUDE.md 내부 노트 배너 · `competition_research` "인용 금지").
>   **→ 남은 것은 사용자의 전환 조작뿐이다.**
>
> **▶ 사용자 몫 — ✅ 전부 처리됨(2026-07-31). 남은 것은 아래 '다음 대화' 4건뿐이다.**
> - ~~GitHub public 전환~~ **✅ 완료 — `Loda1002/SynapStock` 이 PUBLIC**(`gh repo edit`).
>   익명 접근으로 확인했다(API 200 · raw README 200). 전환 직전에 감사 이후 커밋 3건까지
>   재스캔해 크리덴셜 0건을 확인했다.
>   ⚠ **GitHub 라이선스 배지가 안 뜬다**(`spdx_id: NOASSERTION`). `LICENSE` 가 표준 MIT
>   본문 뒤에 Alpha Vantage 데이터 예외 고지를 함께 담고 있어 분류기가 못 읽는 것이다.
>   → **위 '다음 대화' 5번으로 잡아 뒀다**(사용자 요청). 내용은 정확하니 급하지는 않다.
> - ~~저장소 이름 변경~~ **✅ 완료 — `Loda1002/SynapStock`**(2026-07-31, `gh repo rename`).
>   로컬 원격 주소와 `submission.md`·`handoff.md` 링크도 함께 맞췄다. 옛 URL 은 GitHub 가
>   리다이렉트한다. ⚠ **아직 `PRIVATE`** — public 전환은 남아 있다.
> - ~~재배포~~ **✅ 완료 — 리비전 `synapstock-00024-gbg`**(2026-07-31). 개명·BUG-24 반영본이다.
>   실측: 엔드포인트 12개 200 · 정적 파일 6개 저장소와 일치 · `/static/lab/index.html` 404 ·
>   env 유지(`GEMINI_MODEL`·`ALLOW_LIVE_FROM_WEB`·`MAX_BUDGET_USDC`) ·
>   `POST /broker/orders` → 402(asset = Circle 공식 devnet USDC) ·
>   **FastAPI 문서 제목이 `SynapStock — 402 Guard(…)`** = BUG-24 반영본이 실제로 기동한 증거 ·
>   화면에서 제품명 SynapStock / 가드 배너·'402 Guard 요약 다시 보기'는 기능명 그대로 ·
>   시안 토큰(radius 18 · gap 24 · fs-big 32) · 범례 칩 5 · 아이콘 7 · 툴팁 2 · 지갑 16px ·
>   상태 열 숨김 · 상단 바 겹침 -68(해소) · 가로 넘침 0 · 콘솔 오류 0.
>
> ---
>
> **★★★★★★★★★★★★★★★ 2026-07-31 3차 — 남은 시안 규칙 7건 전부 이식 완료. 커밋 7건 전부 푸시
> (`449833c` 판단 타임라인 · `b17446b` 결제 로그 태그 · `6bf9980` 거래 표 · `afd4998` 툴팁 ·
> `06c3169` 카드 아이콘 · `fff4dc8` 요동 방지 · `1e7d369` theme.css 팔레트).
> 훅 128종 누락 0 · `node --check` 통과 · 콘솔 오류 0 · 유출 0.00 · 세 페이지 전부 실측.**
>
> **⚑ 시안 이식은 이것으로 끝났다 — `lab.css` 에 남은 규칙이 없다.** 아래 옛 블록의
> "이번에 안 옮긴 lab.css 규칙" 목록은 **전부 닫혔다. 다시 하지 말 것.**
>
> **넣은 것 (실험장 대비 달라진 점만 적는다)**
> 1. **판단 타임라인** — `addDecision` 이 `"@가격 — 사유"` 를 `.fx-at`(우측) + `p.fx-reason`
>    (다음 줄)로 나눠 그린다. `.src` 는 그대로 두고 CSS 가 16px 점으로 바꾼다(축② 증거 유지).
>    범례는 `.fx-legend` 칩 5개(실험장 마크업 그대로).
> 2. **결제 로그** — `splitLogTag` 신설. **호출부 15곳 무변경**(태그가 전부 문구 첫머리에 있다).
>    긴 태그부터 대조해 `402 Guard` 가 `402` 로 잘리지 않게 했고, 태그 안 꼬리말
>    (`[세션 시작]` 의 `시작`)은 본문 앞으로 넘긴다 — 지우면 시작·종료 줄이 구분되지 않는다.
> 3. **거래 표** — `addTradeRow` 가 방향 글자를 `span.fx-pill` 로 감싼다. 나머지는 CSS.
> 4. **툴팁** — `wireTips`. ⚠ **"로드 시 한 번 복사"로는 부족했다** — `renderNotifyBtn` 과
>    `wallet.js` 의 `render` 가 상태마다 `title` 을 다시 쓴다. `MutationObserver`(attributeFilter
>    `title`)로 따라가게 했다. 우리가 `title` 을 비우는 것도 감시에 걸리지만 옮길 글이 없어
>    바로 끝난다(무한 루프 없음을 실측 확인). 얇은 헤더 지갑 버튼은 감싸지 않았다(시안에 없다).
> 5. **카드 아이콘** — 제목 7곳에 `span.ci`. 글리프 토큰 7종 추가.
> 6. **요동 방지** — 거래 표 `flex:1 1 0` · `overflow-anchor:none` · 한도 안내 `min-height:36px`.
> 7. **theme.css 팔레트** — 색 + `--radius:18` · `--gap:24` · `--pad:24` · `--fs-big:32` ·
>    `--page-pad: clamp(16px,(100vw-1098px)/2,171px)` · Noto Sans KR.
>    `.btn-wallet-sm/-xs { --wallet-radius: 16px }` **도 함께 옮겼다**(지갑 버튼은 시안 제외 지시).
>    보존한 토큰: `--mono`·`--badge-*`·`--ma1~200`·`--card-lift*`·`--stagger-*`·`--split-*`.
>    칩 색은 `--fx-*` 로 theme.css 에 따로 뒀다(라이트·다크).
>
> **⚠ 이번에 드러난 사실 3건 — 다음 세션이 알아야 한다.**
> ① **`:not()` 이 특정도를 올린다.** `.grid > .card > h2:not(.fx-today-date)::before` 는 (0,3,2)라
>    실험장과 같은 `.grid > [data-card="log"] > h2::before`(0,2,2)로는 못 끈다. `.card` 를 덧붙여
>    (0,3,2)로 맞췄다. **실험장 CSS 를 그대로 붙여 넣으면 안 되는 사례가 실제로 나왔다.**
> ② **`.fx-today-date` 는 우리만 `h2` 다**(실험장은 `<p>`). 카드 재배치가 h2 를 손잡이로 잡기
>    때문인데, 그래서 카드 제목 규칙에서 `:not(.fx-today-date)` 로 빼지 않으면 날짜 앞에
>    사각형이 붙고 12px 라벨이 15px 제목으로 커진다.
> ③ **시세 카드 제목만 14px 이다** — h2 가 `.price-head` 안에 한 겹 더 들어가 있어
>    `.grid > .card > h2` 에 안 걸린다. **실험장도 같은 값이라 그대로 두었다**(고치려면 한 줄).
>
> **✅ 배포본 라이브 세션 1회 완주 + 상단 바 결함 1건 수정 → 최종 리비전 `synapstock-00022-2xl`.**
> **▶ 배포 URL 에서 실제로 세션을 돌렸다**(`00021-jk2`, 샌드박스·추세추종·실데이터 재생 80봉 완주).
> 가드 시도 3 · 차단 0 · 한도 거부 0 · **유출 0.00** · 실현 **+17.25 USDC**(23.06%) ·
> `persistence=firestore`(project=synapstock)에 세션 저장 · **서버 `counts.trades`=3 = 화면 표 3줄**
> (세션 경계 결함 재발 없음) · 판단 80줄 전부 점·`.fx-at`·`.fx-reason` 분리 · 로그 16줄 전부 태그
> 배지 · 거래 3행 전부 알약 · 날짜가 재생 중인 봉 날짜(2026-07-22) · 콘솔 오류 0 ·
> 세션 20초 동안 문서 높이 변화는 세션 종료 1회뿐(체결 요동 없음).
> ⚠ **배포본은 `CONTROL_TOKEN` 게이트라 URL 만 열면 세션 시작이 401 이다.** 런북 §6-1 대로
> `/app#token=…` 로 한 번 열어 localStorage 에 심어야 한다. **반드시 `/app` 경로여야 한다** —
> `/`(랜딩)로 열면 `app.js` 를 안 실어서 토큰이 저장되지 않고 주소창에 그대로 남는다(실제로 겪었다).
>
> **⚠⚠ `CONTROL_TOKEN` 을 2026-07-31 에 교체했다 — 예전에 쓰던 토큰은 더 이상 안 먹는다.**
> 위 함정 때문에 토큰이 주소창에 남아 대화 기록으로 새어서 회전시켰다. 시크릿
> `autotrader-control-token` **버전 2** 가 현행이고(버전 1 은 남겨 뒀다 — 지우지 않았지만
> 서비스가 `:latest` 를 보므로 사용되지 않는다), 리비전 **`synapstock-00023-92g`** 부터 적용된다.
> 값 확인은 이 명령으로 **사용자가 직접** 한다(Claude 는 시크릿 값을 읽지 않는다):
> ```powershell
> gcloud secrets versions access latest --secret=autotrader-control-token --project=synapstock
> ```
> ⚠ 시크릿을 또 교체하면 **새 리비전을 만들어야 반영된다**(env 주입은 인스턴스 시작 때 값을
> 읽는다). 빌드 없이 끝내는 방법:
> `gcloud run services update synapstock --region asia-northeast3 --update-secrets "CONTROL_TOKEN=autotrader-control-token:latest"`
> ⚠ 값을 넣을 때 **줄바꿈이 붙으면 안 된다** — 서버 비교가 헤더 값과 정확히 일치해야 한다
> (파일로 넣을 때 `WriteAllText` 로 32바이트를 그대로 쓴다. 파이프로 넘기면 `\r\n` 이 붙는다).
> ⚠ **세션이 도는 중에는 리비전을 바꾸지 않는다**(런북 §6). 교체 전 `/api/state` 의
> `engine.status` 가 `idle` 인지 확인했다.
>
> **▶ [수정] 상단 바 탭이 긴급정지 버튼을 42px 가리던 것**(`f3dde4e`, `.fx-tabs { overflow: hidden }`).
> 탭 글자가 nowrap 이라 자리가 모자라도 줄지 않고 상자 밖으로 넘쳐 조작부 **아래로 겹쳤다**.
> 발생 조건은 **알림 권한이 '거부' 일 때뿐**이다(그때만 라벨이 `🔕 브라우저가 알림을 막았습니다`
> 243px 로 길어진다). 기본(prompt) 상태의 `🔔 알림 받기` 에서는 네 탭이 다 보이고 겹침도 없다.
> ⚠ **시안 팔레트 이전부터 있던 결함이다** — 옛 `--page-pad` 로 되돌려 재면 16px 겹침이고,
> 본문을 1098px 로 묶으면서 16→42px 로 벌어진 것이다. 수정 후 배포본 실측 **겹침 -68px**
> (탭이 잘리고 긴급정지 버튼이 온전히 보인다).
>
> **✅ 재배포 완료 — 리비전 `synapstock-00021-jk2`** (2026-07-31, 런북 §4 한 줄 명령 그대로).
> 이번 7건은 전부 정적 파일이라 **백엔드 변경 0**이었다.
> 실측: 엔드포인트 **12개 200** · 정적 파일 6개(`/app`·`app.js`·`skeleton.css`·`theme.css`·
> `/`·`/connect`) **전부 저장소와 일치** · `/static/lab/index.html` **404**(실험장 차단 유지) ·
> env 에 **`GEMINI_MODEL=gemini-flash-latest` 유지**(`GEMINI_MODE=developer`·
> `ALLOW_LIVE_FROM_WEB=0`, 시크릿 5개) · `POST /broker/orders` → **402 + accepts[]**
> (scheme `exact` / network `solana-devnet` / asset **Circle 공식 devnet USDC** `4zMMC9sr…`) ·
> 배포본 대시보드에서 **radius 18 · gap 24 · pad 24 · fs-big 32 · page-pad 식 · 범례 칩 5개 ·
> 아이콘 7개**(6개는 그리드, `ci-session` 은 개발자 서랍 — `?lab=0` 기본이라 정상) ·
> **툴팁 래퍼 2개 다 `data-tip` 채워짐 · 지갑 버튼 16px 유지 · '상태' 열 숨김 · 콘솔 오류 0.**
> ⚠ 런북 `:35` 의 `$PROJECT_ID` 는 틀렸다 — 프로젝트·서비스 모두 `synapstock`(문서만 고치면 된다).
>
> ---
>
> **★★★★★★★★★★★★★★ 2026-07-31 — 시안 마크업 이식 5건 완료 + 재배포. 커밋 5건 전부 푸시
> (`07be7cc` 상단바·배너·12열·지갑 · `dca24c5` 오늘의 결과 · `9faf015` AI 카드 3건 ·
> `a7139f4` 거래 표 경계·손익 라벨·예산 주석 · `609f70b` CSS 범위 회귀 차단).
> 리비전 `synapstock-00019-nld`. 테스트 23종 rc=0 · 훅 125종 누락 0 · 콘솔 오류 0 · 유출 0.00.**
>
> **아래 '이식할 것 5건'은 전부 끝났다. 다시 하지 말 것.**
> 1. `today`(오늘의 결과) 카드 — 소스에서 pnl·valuation·position 앞에 두고 그 3장은 CSS 로
>    감췄다(지우지 않음). 백엔드 무변경. 실험장의 "오늘 에이전트는 N번 사고…" 한 문장은
>    **가져오지 않았다** — 서버가 주지 않는 값이라 mock.js 가 지어내던 것이다.
> 2. AI 카드 3건 — `.ai-no` 배지 · 보기 버튼(+`::after` 캐럿) · 두 피드를 한 박스로.
>    `app.js` `MORE_LABEL` 을 함께 고쳤다(이게 라벨의 단일 출처).
> 3. 상단 바(로고+탭 4개) · 402 Guard 보라 배너 · 그리드 12열. 가드 훅·구조는 그대로.
> 4. 지갑 버튼(`.btn-wallet`+`.wallet-icon`+`[data-wallet-label]`) 제목 블록·얇은 헤더 양쪽.
> 5. 거래 표 세션 경계 배선 + 손익 라벨(`pnl.basis`) + 예산 `all_in` 주석.
>
> **⚠ 이번에 드러난 사실 3건 — 다음 세션이 알아야 한다.**
> ① **`wallet.js` 는 전달서가 전제한 버전이 아니었다.** 저장소본은 `querySelector`(단수) +
>    `btn.textContent` 대입이라, 지갑 버튼을 두 자리에 두면 하나가 죽고 **아이콘 span 이
>    통째로 지워진다.** `querySelectorAll` + `[data-wallet-label]` 대입으로 고쳤다
>    (프런트 로컬본을 받으면 이 부분이 이미 되어 있는지 먼저 볼 것).
> ② **`.topbar`·`.brand h1` 규칙이 랜딩·연결 페이지로 샜다.** 랜딩이 같은 `.topbar` 골격을
>    쓰고, `.brand h1` 이 `.auth-brand h1`(20px)을 뒤에서 덮었다 → `body.page-dashboard`·
>    `.fx-brand h1` 로 좁혔다(`609f70b`). **다음에 lab.css 를 더 옮길 때 같은 함정이 있다.**
> ③ **런북 `docs/deploy_cloud_run.md:35` 의 `$PROJECT_ID = "autotrader-agent-2026"` 은 틀렸다.**
>    실제는 프로젝트·서비스 모두 `synapstock`(리전 `asia-northeast3`). 문서만 고치면 된다.
>
> **✅ [2026-07-31 2차 · 커밋 `92a760a`·`c3d0fb4`] 사용자 지적 3건 반영 — 전부 푸시.**
> - **브로커 수수료 줄 복원**(`c3d0fb4`) — `pnl` 카드를 감추며 사라졌던
>   `브로커 수수료 0.3% · 누적 N USDC` 를 **손익 타일 안**으로 옮겼다(위 ⓑ안).
>   훅 이름을 pnl 카드와 같게 둬서 **app.js 무변경**(querySelector 가 먼저 나오는 쪽을 잡는다).
>   축① 상업성의 유일한 화면 근거이자 영상 C 구간 클로즈업 대상이 되살아났다.
> - **'오늘의 결과' 날짜 + 한 줄 요약 배선**(`c3d0fb4`) — 이식 때 "서버가 안 주는 값"이라
>   뺐던 것을 **실제 값으로** 만들었다. 날짜는 **브라우저 시계가 아니라 재생 중인 봉의 날짜**
>   (`renderTodayDate`, price_tick 마다 갱신·값 같으면 안 씀). 문장의 매수·매도 건수는
>   **거래 이벤트를 그대로 센다** — 거래 표와 같은 출처·같은 세션 경계라 표 줄 수와 늘 맞는다.
>   막은 건수는 `state.guard.blocked`, 보유 여부는 `valuation.position_net_value_usdc`
>   (포커스 종목 포지션만 보면 멀티에서 틀린다).
> - **`landing.html`·`connect.html` 전달본 반영**(`92a760a`) — 회귀 0, 순수 추가.
>   landing = CTA 3곳에 `.btn-sweep`, connect = 지갑 버튼을 `.btn-wallet`+아이콘으로.
>   ⚠ 두 파일 저장소 관례는 **LF** 인데 전달본은 CRLF 였다 — 변환해 기록해서 diff 가 6/7줄.
>
> **✅ 2차분 배포 완료 — 리비전 `synapstock-00020-7w4`.** 정적 파일 6개(`/app`·`/`·`/connect`·
> `app.js`·`skeleton.css`·`wallet.js`) 전부 저장소와 **바이트 일치** · `/static/lab/index.html`
> **404**(실험장 차단 유지) · 콘솔 오류 0 · 배포본에서 수수료 줄·타일 4개·배너 흐름 배치 확인.
>
> ---
>
> # ▶▶ [✅ 완료 — 2026-07-31 3차] 남은 시안 규칙 전부 이식 (lab.css 잔여 + theme.css 팔레트)
> **아래는 착수 당시 명세다. 7건 전부 끝났다 — 결과는 이 문서 맨 위 블록.**
> 함정 서술은 대부분 맞았고, 틀렸던 것 하나는 4번의 *"로드 시 한 번 복사하는 코드"* 다
> (한 번으로는 안 되고 `MutationObserver` 가 필요했다 — 맨 위 블록 참조).
> **⚑ 사용자 지시(2026-07-31): "다 넣어 위치도 다 맞춰진거래."** = 실험장에 남은 시안 규칙을
> 전부 옮긴다. 아래 6건이고, **앞의 4건은 `app.js` 렌더 함수를 고쳐야 한다** — 실험장에서는
> `mock.js` 가 app.js 가 그린 줄을 시안 구조로 다시 짜서 넘어갔지만 본 화면에는 mock.js 가 없다.
> **분량이 커서 새 대화·넉넉한 예산 권장.** 기준 커밋 `cf7fe7a` 이후, 배포 `synapstock-00020-7w4`.
>
> **⚠ lab 폴더는 gitignore 라 `rg`·Grep 에 안 뜬다** — `rg --no-ignore` 또는 파일을 직접 열 것.
> 원본은 `web/static/lab/lab.css`(854줄) · `mock.js`(줄 재구성 로직) · `README.md`(전달서).
>
> 1. **판단 타임라인**(`lab.css:363-409`) — 출처 enum 배지 → **왼쪽 16px 점**, `@가격`은 오른쪽
>    끝(`.fx-at`), 사유는 다음 줄(`.fx-reason`), 제목 옆에 범례 칩(`.fx-legend`).
>    **`app.js addDecision` 을 고쳐야 한다** — 지금은 "@가격 — 사유"가 한 덩어리 텍스트다.
>    `.src` 는 지우지 말 것(CSS 가 점으로 바꿀 뿐, DOM 의 enum 이 축② 증거다).
>    범례 칩 마크업은 `lab/index.html:502-509` 그대로.
> 2. **결제 과정 로그**(`lab.css:411-435`) — 본문 맨 앞 `[A2A 견적]` 같은 태그 → 회색 배지
>    `.fx-tag` 로 분리. **`app.js addLog` 을 고쳐야 한다**(지금은 태그가 본문에 섞여 있다).
> 3. **거래 내역 표**(`lab.css:444-472, 514-524`) — 셀 가운데 정렬 · 방향을 점+알약(`.fx-pill`)
>    으로 · '상태' 열(`nth-child(9)`) 숨김 · 판단 칩 틴트. **`addTradeRow` 에서 방향 칸 글자를
>    `<span class="fx-pill">` 로 감싸야 한다**(나머지는 CSS).
> 4. **툴팁**(`lab.css:666-713`) — `.tip[data-tip]` 말풍선. 버튼이 `overflow:hidden` 이라
>    버튼에 직접 못 붙이고 감싼 `.tip` span 에 붙인다. `title` → `data-tip` 으로 옮기는 일을
>    실험장에서는 mock.js 가 했다 → **본 화면에서는 로드 시 한 번 복사하는 코드가 필요**하다
>    (`title` 을 비우지 않으면 브라우저 기본 툴팁과 두 개가 겹쳐 뜬다).
> 5. **카드 제목 아이콘**(`lab.css:284-291, 756-823`) — 제목 앞 26px 칸.
>    **마크업 작업만**이라 가장 싸다: 카드 h2 에 `<span class="ci ci-*">` 를 넣고
>    (`lab/index.html` 의 `ci-session`·`ci-ai`·`ci-price`·`ci-trades`·`ci-budget`·`ci-mandate`·
>    `ci-briefing`), `.ci` 규칙 + 아직 안 옮긴 `--ci-*` 토큰 7종(power·search·candles·list·
>    gauge·sliders·doc)을 `skeleton.css` 로. `--ci-shield`·`-ok`·`coins`·`arrow-*`·`box` 는 이미 있다.
> 6. **theme.css 팔레트**(`lab.css:57-112`) — **파일을 따로 받을 필요 없다.** 시안 값이 그
>    `:root` 블록에 라이트·다크 둘 다 들어 있다(값 출처를 Figma 노드로 밝혀 둠).
>    ⚠ 색만 바뀌는 게 아니다: `--radius` 16→**18** · `--gap` 16→**24** · `--pad` 20→**24** ·
>    `--fs-big` 34→**32** · `--page-pad` 를 `clamp(16px, (100vw-1098px)/2, 171px)` 로 ·
>    글꼴에 Noto Sans KR 추가. **랜딩·연결 페이지까지 함께 바뀐다** → 세 페이지 전부 실측할 것.
>    ⚠ `skeleton.css` 에 이미 `.btn-wallet-sm/-xs { --wallet-radius: var(--radius) }` 가 있어
>    `--radius` 를 18 로 올리면 지갑 버튼 모서리가 따라 바뀐다. 실험장은 16px 로 못 박아
>    두었다(`lab.css:118`) — 그 한 줄도 함께 옮길지 정할 것.
> 7. (덤) **요동 방지**(`lab.css:474-512`) — `overflow-anchor: none` · 거래 표
>    `.table-wrap { flex: 1 1 0 }` · `[data-mandate-hint] { min-height: 36px }`.
>    체결마다 문서 높이가 변해 화면이 튀는 것을 막는다. CSS 만이라 싸다.
>
> **▶ 되풀이되는 함정 (전부 이번 라운드에 실제로 밟았다)**
> - **`app.js` 가 라벨을 다시 쓴다** — `MORE_LABEL` 처럼 마크업만 고치면 첫 클릭에 되돌아간다.
> - **같은 훅이 둘이면 `querySelector` 가 문서상 먼저 나오는 것을 잡는다**(today 카드가 그 원리).
> - **전역 선택자를 그냥 옮기면 랜딩·연결 페이지로 샌다** — `.topbar`·`.brand h1` 이 실제로 샜다.
>   `body.page-dashboard` / `.fx-*` 로 좁힐 것.
> - **줄바꿈이 파일마다 다르다** — `index/skeleton/app` = CRLF, `landing/connect/wallet` = LF.
>   판별은 PowerShell 바이트 스캔만 믿는다(Git Bash 는 CR 을 놓친다).
> - **게이트**: 훅 전수 대조(현행 **128종**·누락 0) → `node --check` → dry 세션 스모크
>   (콘솔 오류 0 · 유출 0.00) → 커밋·푸시 → 재배포(런북 §4, **`GEMINI_MODEL` 포함 필수**).
>   ⚠ 런북 `:35` 의 `$PROJECT_ID` 는 틀렸다 — 프로젝트·서비스 모두 `synapstock`.
>
> **▶ [✅ 전부 닫힘 — 2026-07-31 3차] 그 라운드에 안 옮겼던 lab.css 규칙**
> 아래는 **`mock.js` 가 app.js 가 그린 줄을 다시 짜야** 완성되는 것들이라 통째로 뺐던 목록이다.
> 옮기려면 `app.js` 의 렌더 함수를 고쳐야 한다(마크업만으로는 안 된다).
> - 판단 타임라인: 출처 enum 배지 → 왼쪽 16px 점 + 제목 옆 범례 칩(`.fx-legend`),
>   `@가격`(`.fx-at`)·사유(`.fx-reason`) 분리 — 조각 나누기가 `app.js` 몫이다.
> - 결제 로그: 본문 맨 앞 `[태그]` → 회색 배지(`.fx-tag`) 분리 — 같은 이유.
> - 거래 표: 셀 가운데 정렬·방향 알약(`.fx-pill`)·'상태' 열 숨김.
> - 카드 제목 앞 26px 아이콘 칸(`.ci`·`h2::before`) — 마크업에 `<span class="ci ci-*">` 를
>   카드마다 넣어야 한다(글리프 토큰은 이미 `skeleton.css` 에 있다).
> - 툴팁(`.tip[data-tip]`) — `title` 을 `data-tip` 으로 옮기는 일을 mock.js 가 했다.
> - **theme.css 토큰 전체**(색·`--radius: 18px`·`--gap: 24px`·`--page-pad` 식) — 프런트가
>   theme.css 를 보내면 그때 한 번에. 지금 화면은 **구조는 시안 · 팔레트는 저장소(로열블루)** 다.
>
> **이미 끝난 것 (다시 하지 말 것)**
> - `web/static/lab/` 배치 — `index.html`·`lab.css`·`mock.js`·`README.md`·`bundle.py`
>   + `.gitignore`(`*`). **폴더가 gitignore 라 `git status`·`rg`·Grep 에 안 뜬다**
>   (찾을 때 `rg --no-ignore` 또는 파일을 직접 연다).
> - `web/static/css/skeleton.css` 교체 — 프런트 로컬본. **양방향 대조에서 저장소에만
>   있는 줄 0개 = 순수 추가**(34,028B → 43,592B). 들어온 것: `.btn-wallet` 계열 +
>   `--wallet-*` 변수 + `.wallet-icon`(PNG 알파 마스크) · `.btn-sweep` ·
>   **`tr.past-session`/`tr.session-divider`**(거래 표 세션 경계 CSS — app.js 배선은 아직 없다).
> - `web/static/img/phantom-icon.png`(11,844B) — 사용자가 직접 배치.
> - `theme.css` 는 **바이트 완전 동일**이라 손대지 않았다.
> - `.dockerignore`·`.gcloudignore` 에 `web/static/lab/` 차단 추가 — **없으면 배포 URL 의
>   `/static/lab/index.html` 이 완전히 동작하는 목업으로 열린다**(이 대회는 목업 심사 제외).
>   `.gcloudignore` 는 `.gitignore` 를 포함하지 않으므로 gitignore 만으로는 안 막혔다.
> - 검증: `bundle.py` rc=0(아이콘·경로 전부 해석 · 외부 참조 0) ·
>   **본 화면 렌더링 변화 0**(index/connect/landing 이 새 클래스를 쓰는 곳 0건 — CSS 는
>   마크업이 올 때까지 무효다).
>
> **전달서는 `web/static/lab/README.md`** 다(원본은 Downloads 에도 남아 있다). 전달서 원칙:
> *"확정된 것만 나중에 `skeleton.css`·`index.html` 로 옮깁니다."*
>
> **이번 라운드에 확정된 것 = AI 카드 시안 3건**(전달서 §"AI 카드 시안 맞추기 3건 — 완료")
> ①레이어 번호를 제목 글자 속 원문자(`① 판단 레이어`)에서 **제목 앞 배지 `.ai-no`** 로
> (22×22 · 모서리 7 · `#5D4EF1` · 흰 Bold 12 — 시안은 정원이 아니라 둥근 사각)
> ②보기 버튼 라벨·스타일(`AI 판단 타임라인 보기` · `협상·이벤트 로그 보기`, 레이어 안 오른쪽
> 정렬 · 높이 32 · 모서리 9 · 테두리 `#ECEEF7` · 12 Bold `#5D4EF1` · 뒤에 ▾)
> ③두 피드를 AI 카드와 한 박스로(DOM 무변경, CSS 만).
>
> **⚠⚠ 이식 함정 — 내가 소스로 실측해 확인했다(2026-07-30).**
> ②의 라벨은 `index.html` 만 고치면 **첫 클릭에 되돌아간다.** `app.js:1244` 의
> `btn.textContent = MORE_LABEL[id][open ? 1 : 0]` 가 토글할 때마다 제 라벨을 다시 쓰기
> 때문이다. 현행 값은 `app.js:1213-1216` 의
> `decisions: ["판단 타임라인 보기","판단 타임라인 접기"]` · `log: ["심사 내역 보기","심사 내역 접기"]`.
> 실험장은 `mock.js` 의 `relabelMoreBtns()` 가 클릭 리스너로 되돌려 놓는 방식이라 넘어가지만
> **본 화면에는 mock.js 가 없다** → 이식하려면 **`app.js` 의 `MORE_LABEL` 을 고쳐야 한다.**
> (▾ 도 같은 이유로 버튼 안 자식이 아니라 `::after` 로 그린다.)
> 이식 대상은 본 파일에 실재한다: `index.html:125`(`① 판단 레이어`)·`:141-142`·`:165-166`.
>
> **⚠ 절대 하면 안 되는 것 — `mock.js` 가 `web/static/` 본체로 새는 것.**
> `mock.js` 는 `fetch`·`EventSource`·지갑 provider 를 통째로 갈아끼워 백엔드에 닿지 않게 한다.
> 본 화면에 실리면 **화면이 목업이 된다** — 이 대회는 "목업은 심사 제외"가 명문이다.
> `web/static/lab/` 안에만 두고, 본 `index.html` 에서 절대 링크하지 않는다.
>
> **⚠ lab 폴더는 gitignore 라 `rg`·Claude Code 의 Grep 에서 빠진다.** 찾을 때는
> `rg --no-ignore` 를 쓰거나 파일을 직접 연다(전달서도 같은 경고를 담고 있다).
>
> **⚑ 사용자 결정(2026-07-30) — 실험장의 '기능'은 안 가져온다. 디자인만 옮긴다.**
> 프런트는 랜덤 텍스트로 화면을 채우는 실험용 페이지를 만들어 테스트했다. `mock.js`·
> `bundle.py`·`data-lab-*` 훅 4종(`data-lab-today`·`data-lab-trades-count`·
> `data-lab-guard-reopen`·`data-lab-dev-slot-disabled`)·실험장 조작 바는 **본 화면에 넣지
> 않는다.** 가져오는 것은 "화면에서 보이는 것" — 뺄 것과 넣을 것뿐이다.
> **세션 카드는 아직 미정이라 그대로 둔다**(양쪽 DOM 에 다 있고, 그리드 배치는 app.js 가 정한다).
>
> **▶ 전달본 ↔ 저장소 실측 차이 (2026-07-30, `index (2).html` 기준)**
> - **저장소에만 있는 훅 = 0** → **이번 전달본에는 회귀가 없다**(3회 연속이던 회귀가 처음으로 없다).
> - 카드 차이는 **`data-card="today"`(오늘의 결과) 1개 추가**뿐. 그리드는 4열 → **12열** 재배치.
> - **★ today 카드는 백엔드 작업이 필요 없다** — 안의 숫자가 `data-total-asset`·`data-cash`·
>   `data-position-value2`·`data-pnl`·`data-return-pct` 등 **기존 훅 재사용**이다.
>   실험장 전용은 `data-lab-today` 한 줄(안내 문구)뿐이라 그것만 빼면 된다.
> - 추가 훅 `data-wallet-label` — 지갑 버튼 라벨. ⚠ `wallet.js` 가 **그 요소에만** 글자를
>   쓰므로 라벨을 이 span 밖으로 빼면 아이콘이 지워진다(전달본 주석의 경고, 그대로 따를 것).
>
> **▶ [완료 — 2026-07-31] 이식할 것 5건** (아래는 착수 당시 명세다. 함정 서술은 전부
> 실제로 맞았다 — 특히 `MORE_LABEL`·`querySelector` 선점·`[data-wallet-label]`.)
> `lab/index.html` 과 `lab.css` 를 대조해 **본 `index.html`·`app.js`·`skeleton.css`** 로 옮긴다.
> 실험장 전용(`mock.js`·`data-lab-*` 4종·`.lab-bar`)은 가져가지 않는다.
> 1. **`today`(오늘의 결과) 카드 신설** — `pnl`·`valuation`·`position` 3장을 타일 4개로 합친 것.
>    **⚠ 소스에서 그 3장보다 앞에 둬야 한다** — `app.js` 가 같은 훅을 `querySelector` 로
>    한 번만 잡고 문서상 먼저 나오는 것을 쓴다. 뒤에 남는 옛 카드 3장은 CSS 로 감춘다
>    (지우면 안 된다 — 거기에만 있는 훅을 `app.js` 가 계속 쓴다).
>    **백엔드 작업 0** — 타일 숫자가 전부 기존 훅 재사용이다. 실험장 전용은 `data-lab-today`
>    한 줄(안내 문구)이라 그것만 빼면 된다.
> 2. **AI 카드 3건** — `.ai-no` 배지(22×22·모서리 7·`#5D4EF1`·흰 Bold 12, 정원 아님) ·
>    보기 버튼(오른쪽 정렬·높이 32·모서리 9·테두리 `#ECEEF7`·12 Bold·뒤에 ▾) ·
>    두 피드를 AI 카드와 한 박스로(DOM 무변경, CSS 만).
>    **⚠ 버튼 라벨은 `app.js:1213-1216` 의 `MORE_LABEL` 을 고쳐야 한다** — `:1244` 가 토글마다
>    `btn.textContent` 를 다시 쓰므로 `index.html` 만 고치면 첫 클릭에 되돌아간다. 현행 값은
>    `["판단 타임라인 보기","판단 타임라인 접기"]`·`["심사 내역 보기","심사 내역 접기"]`,
>    시안 값은 `AI 판단 타임라인 보기`·`협상·이벤트 로그 보기`.
>    ▾ 도 같은 이유로 버튼 안 자식이 아니라 `::after` 로 그린다.
> 3. **상단 바·배너·그리드** — 로고+탭 4개(포트폴리오·인사이트·리포트는 갈 곳이 없어
>    `aria-disabled` 모양만) · 402 Guard 를 떠 있는 패널에서 **본문 맨 위 보라 배너**로 ·
>    그리드 4열 → **12열**. ⚠ `data-guard-dock`/`-panel`/`-toggle` 훅과 이름은 그대로 둔다
>    (`app.js` 가 필수로 잡는다 — 없으면 대시보드가 죽는다).
> 4. **지갑 버튼 마크업** — `.btn-wallet` + `.wallet-icon` + `[data-wallet-label]`.
>    CSS·아이콘은 이미 저장소에 있다. ⚠ 라벨은 반드시 `[data-wallet-label]` **안**에 둔다 —
>    `wallet.js` 가 그 요소에만 글자를 써서, 밖으로 빼면 아이콘이 지워진다.
> 5. **거래 표 세션 경계** — CSS(`tr.past-session`/`tr.session-divider`)는 이미 들어와 있다.
>    남은 것은 `app.js` 배선뿐이고, 권고안은 위 '병합 후 할 화면 작업' 1번 그대로다.
>
> **▶ (참고) 프런트에 받아야 했던 것 — 전부 수령 완료**
>
> **1) `phantom-icon.png` → `web/static/img/phantom-icon.png` (파일 1개면 된다)**
> "아이콘 2개"는 **그림 2장이 아니라 화면 2군데 배치**다 — `index (2).html:34`(얇은 헤더)와
> `:91`(본문)이 **같은 클래스 `.wallet-icon`** 을 쓴다. 전달본 전체에서 참조되는 아이콘
> 파일명은 `phantom-icon.png` **하나뿐**(`lab.css` 3곳 · `bundle.py` 2곳).
> ⚠ 이름 고정 — `bundle.py:25` 가 `open(.../img/phantom-icon.png)` 로 **직접 읽고**(방어 없음)
> 없으면 공유본 생성이 `FileNotFoundError` 로 죽는다. `web/static/img/` 폴더는 **신규**다.
> ⚠ **mask-image 로 쓰므로 알파(모양)만 사용된다** — 색은 CSS 가 입힌다. 배경 투명 PNG 필요.
>
> **2) 프런트 로컬 `skeleton.css` 현재본 → `web/static/css/skeleton.css`**
> **PNG 만 넣으면 화면에 아무것도 안 나온다.** 실측: `.wallet-icon`·`.btn-wallet` 의 **본체
> 규칙이 저장소 `skeleton.css` 에 0건**이고 `--wallet-*` 변수도 **0건**이다. `lab.css` 에 있는
> 것은 경로 되잡기(`mask-image`)와 모서리(`--wallet-radius:16px`) **오버라이드뿐**이며,
> 주석이 *"skeleton.css:192 가 아이콘을 참조한다"* · *"`.btn-wallet` 은 손대지 않는다"* 라고
> 적고 있다 → **지갑 버튼 디자인 전체가 프런트 로컬에만 있고 저장소에 온 적이 없다.**
>
> **3) 프런트 로컬 `theme.css` 현재본 → `web/static/css/theme.css`**
> `lab.css:81` 이 `--radius: 18px` 로 덮으며 *"`--radius` 가 16→18 로 바뀌었으므로"* 라고
> 적는데 **저장소 `theme.css:54` 는 아직 `16px`** 이다(실측). 토큰이 어긋나 있다.
>
> **선택 — `landing.html`·`connect.html`**: 랜딩·연결 화면도 디자인을 반영할 때만.
> `index (2).html` 은 그 둘을 링크하지 않고(내부 링크는 `href="/"` 하나) `bundle.py` 도
> `lab/index.html` 만 읽으므로, 대시보드 작업에는 없어도 된다.
>
> **요구할 필요 없음** — 폴더의 `.gitignore`(`*` 한 줄)는 우리가 만든다. 나머지 참조
> (`app.js`·`reveal.js`·`to-top.js`·`wallet.js`)는 전부 저장소에 실재하고, 그 외 아이콘은
> 전부 `lab.css` 안에 **data URI** 로 심겨 있어 외부 파일이 필요 없다.
>
> ## ▶ 잔여 '낮음' 12건 + '정보' 1건 — 훑어본 결과 (2026-07-30, 사용자 요청)
> **프런트 작업이 먼저다. 아래는 그 다음 순서이고, 이번엔 손대지 않았다.**
>
> **고칠 만함 (권장 순서)**
> 1. **BUG-20 — 한도 변경이 진행 중인 예약을 버려 실패 결제의 예산이 영구 소모** (`web/engine.py`)
>    `update_limits` 가 새 authorizer 에 `spent_usdc` 만 이월하고 `_reservations` 는 안 옮긴다.
>    그 뒤 `_buy_cycle` 의 finally 가 `release(order_id)` 를 불러도 새 auth 엔 그 예약이 없어
>    **실패한 결제의 예산이 세션 끝까지 묶인다.** 방금 고친 **BUG-03 과 같은 계열**(한도가
>    조용히 사라진다)이고 `update_limits` 는 BUG-07 로 이미 손댄 자리라 맥락이 신선하다.
>    수정은 1~2줄(`new_auth._reservations = dict(old._reservations)`, 추세 멀티는 종목별).
> 2. **BUG-24 — 숫자형 환경변수를 빈값으로 주면 임포트 단계에서 앱 전체가 죽는다** (`config.py:35`)
>    `_get` 이 `os.environ.get(key, default)` 라 '키는 있고 값은 빈 문자열'이면 기본값이 아니라
>    `""` 를 돌려주고 `Decimal("")`·`int("")` 가 터진다. **재배포가 1회 남아 있다** — Cloud Run
>    env 를 하나 빈 값으로 넣는 순간 서비스가 안 뜨고, 그게 심사 URL 이다. 수정 1~2줄.
> 3. **BUG-13 — 견적 총액이 요청 지출을 최대 0.01 초과해 정상 거래가 AP2 '예산 초과'로 거부**
>    (`agents/broker_agent.py:74`) 수량만 ROUND_DOWN 하고 subtotal·fee 는 반올림이라
>    `:70` 주석의 계약이 깨진다. **'오탐 0' 주장과 충돌하는 유일한 잔여 건**(우리 견적이
>    우리 한도에 걸린다). 실측 빈도는 NVDA_bear 501봉 중 11봉.
>    **⚠ 견적 산식을 바꾸면 문서·소개서에 박힌 백테스트 수치가 흔들릴 수 있다** — 고치면
>    `+77.55%` 등 인용된 값을 재검증해야 한다. 그래서 3순위다.
> 4. **묶음 1커밋 — BUG-14·15·18·19** (전부 "NaN·0 이 검사를 통과" 계열, 각 1줄)
>    Gemini 가 `spend_usdc=NaN` → 그 틱 판단 유실 / 가격 0 봉이 피벗이면 `ta_summary` 크래시 /
>    `tick_interval_sec=NaN` 이 클램프를 통과해 틱 루프 폭주 / 적립식 금액 NaN 이 500 으로 샘.
>    전부 `is_finite()`·0 검사 한 줄이면 닫힌다. 개별 가치는 낮지만 한 번에 묶으면 싸다.
>
> **권장하지 않음 (이유 명시)**
> - **BUG-21**(`session_id` 초 단위 충돌) — 형식을 바꾸면 화면 라벨 파싱·아카이브 파일명·
>   Firestore 문서 키가 함께 흔들린다. **위험 > 가치**. 재현 조건도 '두 세션이 같은 초 시작'.
> - **BUG-22**(라이브 `start()` 동시 호출 중복 세션) — 배포본은 `ALLOW_LIVE_FROM_WEB=0` +
>   `CONTROL_TOKEN` + `max-instances 1` 이라 **도달 경로가 없다.**
> - **BUG-23**(SSE 큐 포화 시 조용한 유실) — 수정이 `app.js` 를 건드린다. **프런트 동결 중.**
>   가드 KPI 는 `fetchState` 로 복구되므로 실제 손실은 로그 줄뿐이다(적대 검증이 확인).
> - **BUG-16·17**(재생 구간·CSV 오류 메시지) — 둘 다 **요란하게 실패**하고 안내가 나간다.
> - **BUG-25**(정보 — `to_dict/from_dict` 사문화) — 런타임 영향 0. 공개 저장소 정합성 차원의
>   문서·주석 정리라 여유 시에.
>
> **⚑ 2026-07-30 사용자 결정 — 07-30 세션은 `web/static/**` 을 한 줄도 건드리지 않았다.**
> 화면 결함(아래 1·2)부터 하려 했으나 사용자가 **"프런트 부분은 건들지 마 / `web/static/**`
> 전체를 안 건드린다"** 로 범위를 좁혔다. 사유는 병합에서 **3회 연속** 회귀를 겪었기 때문이다 —
> 지금 고쳐 두면 병합 때 그 수정이 되돌아간다. **그래서 1·2 는 병합 '뒤에' 그 위에서 한다.**
>
> **병합 후 할 화면 작업 — ✅ 1·2 전부 2026-07-31 완료(`a7139f4`). 4 는 여전히 보류.**
> ⚠ 1번은 권고안(표를 비운다)이 아니라 **구분선 방식**으로 갔다 — 프런트가 보낸 CSS
> (`tr.session-divider`/`tr.past-session`)가 그 방식을 전제하고, 두 피드와 처리가 같아지며,
> '전에도 돌았다'는 증거를 버리지 않는다. 새로고침 복원 실측에서 **구분선 위 행 수 =
> `counts.trades`** 로 일치하는 것을 확인했다(결함 재현 조건 그대로).
> 1. **[유일한 실제 결함] 거래 내역이 세션 경계를 구분하지 않는다** — 아래 '화면에서 더
>    다듬을 곳' 목록 1번. **촬영 C 구간에 직접 걸린다**(서버 `counts.trades=1` 인데 표는
>    2줄, 하나는 지난 세션 것). **원인 확정(2026-07-30 소스 확인)**: `/api/state` 에 `trades`
>    키가 없어(`state_snapshot` 은 `counts.trades` 만 준다) 표가 SSE 히스토리 재전송으로만
>    채워지는데 `app.js` `case "trade"` 에 세션 필터가 없다.
>    **권고안 = `case "engine_started"` 에서 거래 표를 비운다(빈 안내행 복원, 5줄).**
>    `isFresh` 필터는 **쓰면 안 된다** — 히스토리에는 현재 세션 거래도 실려 오므로(새로고침
>    복원 경로) 걸러 내면 서버 3건인데 표가 0줄이 된다. `engine_started` 기준이면 표가
>    `counts.trades` 와 정확히 일치하고(`engine.start` 가 `self.trades = []` 하는 그 시점),
>    이벤트 버퍼(1000건 deque)가 넘쳐 `engine_started` 가 밀려나면 그보다 오래된 지난 세션
>    거래도 함께 밀려나므로 안전하다.
> 2. **손익(실현) 카드 라벨 자기모순** — `pnl.basis` 를 읽어 라벨을 전환한다. `cum-buy`
>    (조건형)는 현행 `수익률`, `initial-capital`(추세추종)은 `수익률(평가 포함)` + 툴팁.
>    실현 `0 USDC` 아래 `수익률 6.22%` 가 붙던 것이 사라진다. **예산 카드 주석 한 줄**:
>    `all_in` 일 때 "추세추종은 전량 매매라 건별 한도를 쓰지 않는다"(한도 카드와 값이 달라 보임).
> 3. ~~잔여 버그 3건(BUG-03·06·07)~~ — **✅ 2026-07-30 완료. 아래 블록 참조.**
> 4. 차트 조작부 축약 — **사용자가 "프런트 건들지 마"로 답했으므로 보류.**
>    `tAAPL` 툴팁은 **이미 구현돼 있다**(`app.js:508-511`, 종목·포지션 카드 `title`) — 이
>    항목을 미결로 다시 적지 말 것.
>
> **⚠ C4(온체인 예산 엔진 배선)는 여전히 8/3 전 금지** — 교차검증 파손 위험 + 촬영이
> 드라이런 경로다. 일정 연기로도 이 판단은 바뀌지 않는다.
>
> **⚑ 앞선 사용자 결정(유효)**: 제출 준비 3종(소개서 PDF 출력 · 영상 촬영 · 구글폼 제출)은
> 이틀 연기. **최종본이 나와야 출력·촬영이 의미가 있다.** **⚠ 마감 2026-08-03 23:59 KST 는
> 그대로다** — 연기한 것은 착수 시점이지 마감이 아니다. 소개서 PDF 는 디자인 최종본 반영 후
> 게시본의 `PDF로 저장`(배율 100% · **배경 그래픽 켜기** — 이 덱은 색이 의미를 나른다).
>
> ## ▶▶ 병합 런북 (2026-07-30 실측 기준선 포함)
>
> **⚠ 이번 실험장 라운드에는 §1 양방향 회귀 대조가 해당하지 않는다** — 전달본이 본 파일을
> **대체하지 않고** 사본으로 오기 때문이다. 이번에 쓰는 것은 **기준선 표 · §2 줄바꿈 ·
> §4 게이트**이고, 게이트는 '이식 3건을 본 파일에 반영한 뒤' 돌린다.
> 아래 §1 전문은 **다음에 본 파일 교체본이 올 때** 그대로 쓴다(3회 연속 겪은 회귀라 지우지 않는다).
>
> **저장소 기준선 — 커밋 `55b3487` 에서 PowerShell 바이트 스캔으로 실측.**
> 병합 대조의 출발점이고, 병합 후 이 값들이 어떻게 바뀌었는지가 곧 리뷰 대상이다.
>
> | 파일 | 크기 | 줄바꿈 |
> |---|---|---|
> | `web/static/index.html` | 40,070B | **CRLF** |
> | `web/static/js/app.js` | 102,149B | **CRLF** |
> | `web/static/css/skeleton.css` | 34,028B | **CRLF** |
> | `web/static/css/theme.css` | 5,138B | **CRLF** |
> | `web/static/landing.html` | 22,348B | **LF** |
> | `web/static/connect.html` | 4,657B | **LF** |
> | `web/static/login.html` | 1,727B | **LF** |
> | `web/static/js/wallet.js` | 10,992B | **LF** |
> | `web/static/js/reveal.js` | 3,398B | **LF** |
> | `web/static/js/to-top.js` | 1,826B | **LF** |
>
> **`app.js` 의 `data-*` 훅 = 123 종 · `index.html` 에 없는 것 0 개.**
> 병합 후 이 값이 유지되는지가 하드 게이트다 — 하나라도 없으면 `el.x` 가 null 이라
> **대시보드가 통째로 죽는다**(예전에 실제로 겪었다).
>
> **절차 (순서대로)**
> 1. ⚠ **양방향 회귀 대조부터 한다.** 지금까지 **3회 연속** 전달본이 저장소보다 앞선 시점
>    기준이라 최근 반영분이 옛 버전으로 되돌아와 있었다. 양쪽을 LF 로 정규화한 뒤
>    `diff | grep '^<'`(= 저장소에만 있는 줄)이 0인지부터 본다. 0이 아니면 **한 줄씩**
>    "전달서가 지시한 변경인가"를 확인한다.
>    과거에 실제로 되돌아왔던 것들(전부 되살린 이력 있음): 지갑 로그인 훅 · AI 카드
>    (`renderAi`) · `guard_semantic` · `data-guard-legs` · `.src-rule-gate` · 전략 기본값
>    추세추종 · 종목 체크박스 잠금 조건 · `data-card="history"` · `fetchHistory` ·
>    `lastEngineStatus` · `actor_label` · `invoice_semantics` · `blocked_unverified` ·
>    `.col-tx`/`body.live-mode` · `LAYOUT_KEY` 버전(현행 **_v11**) · `?lab=1` 개발자 서랍.
> 2. ⚠ **줄바꿈이 파일마다 다르다**(위 표). 정규화 없이 diff 하면 파일 전체가 바뀐 것처럼
>    보이고, 기록할 때 기존 방식에 맞추지 않으면 `git diff` 가 실제 변경분을 묻어 버린다.
>    판별은 **PowerShell 바이트 스캔만** 믿을 것 — 이 환경의 Git Bash 는 `grep -c $'\r'`·
>    `awk '/\r$/'`·`od -c` 파이프가 전부 CR 을 놓친다(두 번 오판했다). 스캔 명령:
>    ```powershell
>    Get-ChildItem web/static -Recurse -File -Include *.html,*.css,*.js | ForEach-Object {
>      $b=[System.IO.File]::ReadAllBytes($_.FullName); $crlf=0; $lf=0
>      for($i=0;$i -lt $b.Length;$i++){ if($b[$i] -eq 10){ if($i -gt 0 -and $b[$i-1] -eq 13){$crlf++}else{$lf++} } }
>      "{0,-34} {1,7}B  CRLF={2,-5} LF={3,-5}" -f $_.Name,$b.Length,$crlf,$lf }
>    ```
> 3. **배치하면 안 되는 파일 유형**: 어디서도 참조되지 않으면서 `button {}` 같은 **전역
>    선택자에 색을 하드코딩**한 CSS(지난 라운드의 `hoverme.css`·`clickme.css`). 링크하는
>    순간 화면의 모든 버튼을 덮고 다크 모드가 깨지며 `theme.css` 시안과 충돌한다.
>    참고용으로만 `docs/design/incoming/` 에 두고, 필요한 규칙은 클래스로 좁혀 흡수한다.
> 4. **병합 후 게이트** — ①훅 전수 대조(123종·누락 0) ②`node --check web/static/js/app.js`
>    ③dry 세션 스모크(`preview_start` → `/app?lab=1` → 세션 시작 → **콘솔 오류 0 · 유출 0.00**)
>    ④커밋·푸시 ⑤**재배포 1회**(런북 `docs/deploy_cloud_run.md` §4 — **`GEMINI_MODEL=
>    gemini-flash-latest` 포함 필수**. `--set-env-vars` 는 기존 env 를 전부 덮어쓰므로 빼면
>    쿼터 소진된 모델로 되돌아간다) ⑥엔드포인트 실측(§5).
>
> **✅ [해소] 백엔드 5건(BUG-03·06·07·11·12)은 2026-07-31 재배포(`synapstock-00019-nld`)에
> 함께 올라갔다.** 더 이상 배포 대기 중인 백엔드 수정은 없다.
>
> ---
>
> **★★★★★★★★★★★★★ 2026-07-30 (2) — BUG-11·12 완료. 커밋 2건 푸시
> (`7652326` BUG-11 · `8dc7325` BUG-12). 테스트 23종 rc=0 · red_team rc=0 · 화면 변경 0.**
>
> 리포트의 완전성 비평이 *"금액 기준으로만 낮음이고 심사 신뢰도 관점에서는 먼저 볼 값어치가
> 있다"* 고 지목한 2건. 뿌리가 같다 — **수량·금액 0 에 하한이 없어 모든 계층을 통과**했고,
> 둘 다 **402 Guard 가 검증하지 않은 것을 "확인했다"고 기록하는 자리**다.
>
> - **BUG-12**(`8dc7325`) 지불액이 1단위 미만이면 수량이 `0.0000` 으로 내림돼 청구액도 0 이
>   되는데, Guard 는 '청구액 == 견적'이라 통과 · AP2 는 하한이 없어 `authorize(0)` 통과 ·
>   정산은 `settled`. spent 가 0 이라 **잔여 예산이 줄지 않아 틱마다 반복**됐다(dust 예산으로
>   481틱이면 가짜 체결 481건이 거래 내역·아카이브에 쌓인다). 엔진 `_buy_cycle` 과
>   `run_demo` 에서 결제로 만들지 않고, AP2 `authorize` 에 `amount > 0` 하한을 넣었다.
>   **HTTP 경로(`broker_service.py:145`)는 이미 400 으로 막고 있었다** — 인프로세스만
>   비대칭으로 열려 있던 것을 맞춘 것이다.
>   **★ 엔진에서 먼저 끊은 이유**: 아래 계층이 먼저 잡으면 우리가 만든 dust 견적이
>   '가드 차단'·'한도 거부' KPI 에 잡혀 **대표 지표("시도 N건 중 M건 차단")가 더러워진다.**
>   회귀에 그 조건(`guard_block_count == 0 and _guard_checked == 0`)을 박아 뒀다.
> - **BUG-11**(`7652326`) `check_delivery` 의 도착 판정이 `cur - before >= expected` 라
>   기대 증가분 0 이면 항상 참이고, `balance_reader` 가 예외를 내면 `cur = last`(=before)로
>   대체되므로 **온체인을 한 번도 못 읽고도 `ok=True`** 가 나왔다. 0 이하는 즉시 보류
>   처리(무의미한 RPC 도 안 쏜다) + **'읽어 봤는데 안 왔다'와 '읽지도 못했다'를 사유에서
>   구분**(`reads_ok`). 뒤엣것을 앞엣것처럼 적으면 확인 불가를 확정 손실로 보고하게 된다.
>
> **⚠ 하드 검사 8종 카운트는 불변이다** — BUG-11 은 `check_demand` 가 아니라 정산 후 배송
> 확인 계층이고, BUG-12 의 하한은 AP2 `authorize` 에 넣었다. **`check_demand` 무수정** =
> 소개서·대본·README 의 "8종" 표기를 고칠 필요가 없다(낭독 대사와 어긋나면 재촬영 사유).
>
> **음성 대조 수행** — 세 계층을 전부 무력화하면 리포트 그대로 재현된다:
> `ok=True · 자산 +0 base units 도착`(RPC 가 계속 429 인데도) · dust 3틱 → **가짜 체결 3건**.
> 추가 검증: README 대표 명령 `run_demo --no-gemini --ticks 12` rc=0, 매수·매도 정상 동작.
> 테스트 파일 수는 **23개 그대로**(기존 파일에 회귀 추가).
>
> ---
>
> **★★★★★★★★★★★★ 2026-07-30 — 잔여 '중간' 버그 3건 정리 완료. 커밋 3건 전부 푸시
> (`bc063fd` BUG-03 · `77e8eaa` BUG-07 · `fbec112` BUG-06 · 문서 갱신 별도).
> 테스트 23종 전부 rc=0 · red_team rc=0(유출 0.00 · 오탐 0) · 화면 코드 변경 0.**
>
> **⚠ 계획 문서의 사실 오류 1건 정정**: 이 3건은 "전부 낮음"이 아니라 `bug_latest.md` 기준
> **전부 '중간'** 이다. 특히 **BUG-07 은 적대 검증에서 낮음→중간 상향**됐고, 재현 시
> **AP2 총예산을 15 USDC 초과 승인**하는 것이 실측됐다 — "한도를 낮추면 그만큼 줄어든다"는
> 402 Guard 헤드라인 주장과 정면으로 어긋나는 자리였다.
>
> - **BUG-03**(`bc063fd`) `PaymentAuthorizer.release()` 의 0 클램프 제거. 추세추종은
>   `credit_sale(allow_surplus=True)` 로 spent 를 음수까지 내려 실현이익 재투자를 표현하는데,
>   그 상태에서 매수 1건이 release 되면 음수가 0 으로 올라붙어 **벌어 놓은 운용 한도가 세션
>   끝까지 사라졌다**(로그도 안 남는다). 멱등성은 `_reservations.pop` 이 담당하므로 비잉여
>   모드에서 이 클램프는 **발동할 수 없는 죽은 코드**였다. `credit_sale` 쪽 클램프(순투입 한도
>   정의)는 의미가 있어 그대로 뒀다. 회귀 6건(`test_settlement`).
> - **BUG-07**(`77e8eaa`) `update_limits` 가 총액만 검사해, 추세 멀티에서 예산을 낮추면 한
>   종목의 몫이 그 종목 사용액보다 작아져 슬라이스 잔여가 음수가 되고 **합산에서 다른 종목의
>   양수와 상계**됐다. 각 종목의 authorize 는 제 슬라이스만 보므로 결국 총예산 초과 매수가
>   승인됐다. 재슬라이스 **앞에서** 종목별 검사(뒤면 세션 mandate 만 새 예산으로 바뀌는 부분
>   적용이 된다) + `_budget_slices` 헬퍼(검사와 적용이 같은 몫을 쓰게) + `_total_remaining`
>   음수 몫 바닥 처리. 회귀 9건 + **대조군 2건**(정상 인하는 통과 — 과잉 차단 방지).
> - **BUG-06**(`fbec112`) `_last_action` 기록을 '판단' → '체결' 시점으로 이동(`_record_action`).
>   무산된 판단이 다음 틱 프롬프트에 "몇 봉 전 매수"로 단언돼 같은 프롬프트의 보유 0 과
>   모순됐다. 기록 조건을 **포지션 변동과 같게** 묶었다(매수 settled / 매도 settled 또는
>   partial+confirmed). **매매 로직은 무변경** — `_last_action` 은 저장소 전체에서 이 파일
>   안에서만 쓰이고 소비처는 Gemini 프롬프트의 회고 한 줄뿐이다(시간청산은 `_bars_held` 가
>   센다). 규칙 판단 세션은 회고를 프롬프트에 싣지 않으므로 결과가 동일하다. 회귀 6건.
>
> **★ 3건 모두 음성 대조를 수행했다** — 옛 코드로 되돌려 리포트와 같은 값이 재현되는 것을
> 확인한 뒤 복원했다(`spent=0 / 잔여=100` · `AAPL 몫 -15.00 · 합산 36.00` · `보유 0 인데
> "1봉 전 매수 @ 95"`). 회귀 테스트가 실제로 그 결함을 잡는다는 증거다.
>
> **테스트 파일 수는 23개 그대로다** — 새 파일을 만들지 않고 기존 파일에 회귀를 넣었다.
> 소개서·대본·README 의 "**단위 테스트 23종**" 표기를 고칠 필요가 없다(촬영 대본 낭독 대사와
> 어긋나면 재촬영 사유가 된다).
>
> **Cloud Run 영향 = 재배포 필요.** 세 파일 전부 이미지에 들어가는 런타임 코드다. 다만
> 배포본은 `ALLOW_LIVE_FROM_WEB=0` 이고 드라이런 경로의 **동작 변화는 없다**(BUG-06 은
> Gemini 프롬프트 한 줄, BUG-03·07 은 실패·한도인하 경로). **다음 재배포에 자연히 포함하면
> 되고, 이것만을 위해 따로 배포할 이유는 없다.** 재배포 시 런북 §4 (`GEMINI_MODEL` 포함 필수).
>
> **`docs/reports/bug_latest.md` 상태 표 갱신** — 스캔 당시 '중간' 9건이 **전부 닫혀 잔여 0건**
> (BUG-05·09·10 이 07-28 P4 에서 닫혔는데 표에 반영돼 있지 않던 것도 함께 채웠다).
> 남은 것은 낮음 14·정보 1 이고, **BUG-11·12 는 "금액 기준으로만 낮음"** 이라는 리포트 자체의
> 경고를 표 아래에 명시했다(402 Guard 가 검증하지 않은 것을 "확인했다"고 기록하는 자리).
>
> **프런트 전달본이 도착하면 (그때 볼 것)**
> 절차 상세는 아래 07-28 블록들에 있다. 반드시 지킬 것 셋:
> - ⚠ **받는 즉시 양방향 회귀 대조부터 한다.** 지금까지 **3회 연속** 전달본이 저장소보다
>   앞선 시점 기준이라 최근 반영분이 옛 버전으로 되돌아와 있었다(지갑 로그인·AI 카드·
>   전략 기본값·종목 체크박스 잠금 조건). 양쪽 LF 정규화 후 `diff | grep '^<'`(저장소에만
>   있는 줄)이 0인지부터 본다.
> - ⚠ **줄바꿈이 섞여 있다**(저장소 `index/skeleton/theme/app`=CRLF, `landing/reveal/wallet`
>   =LF). 정규화 없이 diff 하면 파일 전체가 바뀐 것처럼 보인다. 판별은 **PowerShell 바이트
>   스캔만** 믿을 것 — 이 환경의 Git Bash 는 CR 을 놓친다.
> - 병합 후: `app.js` 훅 전수 대조(`grep -o '\[data-[a-z0-9-]*\]'` → 전부 `index.html` 에
>   존재하는지. 하나라도 없으면 대시보드가 통째로 죽는다) → `node --check` → dry 세션
>   스모크(콘솔 오류 0 · 유출 0.00) → **재배포 1회**(런북 §4, **`GEMINI_MODEL` 포함 필수**).
> - A-lite 커밋과는 **파일 교집합이 공집합**이라 서로 영향이 없다(프런트는 `web/static/**` 만).
>
> ---
>
> **★★★★★★★★★★★ 2026-07-29 낮 — 온체인 예산 집행 A-lite 구현 완료. 커밋 7건 전부 푸시
> (`cc4d3f9` C2 · `64fb86d` C1 · `da9d0d9` C3 · `24565f6` C5 · `70664f6` 문서 5곳 ·
> `2dbd183` 진행 기록 · `f58aabd` 자체 점검 보완).
> 테스트 23종 전부 통과 · red_team rc=0 · 유출 0.00 · 제품 결제 경로 무변경.**
>
> **⚑ 자체 점검에서 잡은 것 3건(`f58aabd`) — 전부 영상 컷에 나가는 줄이었다.**
> ①⑤ 구간이 `"← ④와 같은 코드입니다"` 를 **실제로 같은지 확인하지 않고** 출력했다(같을 때만
> 붙이도록 수정). ②헤더의 `"← 이 지갑에는 결제할 돈이 없다"` 가 잔액과 무관하게 박혀 있었다
> — **`--devnet` 으로 기존 지갑을 재사용하면 거짓**이 된다(실측: agent USDC 1009.44).
> ③⑤에서 한도를 **잔액 기준**으로 올린 뒤 ⑦에서 회수하는데 그 사이에 죽으면 **사용자 지갑에
> 위임이 남는다** → `finally` 에 자동 회수 안전망(⑦ 실패를 인위로 만들어 검증: revoke 2회 ·
> 최종 `delegate=None`). **`--devnet` 분기는 devnet 자산 없이 localnet 위에서 태워 확인했다.**
> 함께: 테스트 개수 표기 20/21/22 드리프트를 현재 상태 주장 4곳만 23종으로 통일 ·
> 아카이브 8→10건 · README 에 레일 절 신설(심사위원이 저장소만 보고 재현 가능해야 하는데
> `demo_delegation` 이 README 어디에도 없었다) · 설계/RFC 상태 줄 갱신.
>
> **★ 가장 중요한 한 줄 — 과장 금지.** 이 작업은 **제품 배선이 아니다.** 엔진·화면·API·
> 드라이런 어디에도 동작 변화가 없다. *"402 Guard 의 예산이 온체인에서 집행된다"* 는
> **거짓**이고, 정확한 문장은 *"예산 상한을 체인이 집행하는 레일을 실증했고, 엔진 배선은
> 로드맵"* 이다. 아카이브의 **`wired_into_product: false`** 가 그 경계를 기계 판독 가능하게
> 남긴다. 영상 '말하지 말 것' 목록에도 추가했다(`submission.md §6`).
>
> **만든 것 (설계 `docs/design/onchain_budget_design.md` 명세 그대로)**
> - **`payments/delegation.py`**(신규·호출자 0) — `read_delegation`(RPC 1회로 잔액+위임 잔여)
>   · `approve_budget`(**절대값** 의미 고정 — 감액은 전송 전 `DelegationError`. SPL approve 가
>   누적이 아니라 덮어쓰기라 '재충전' 의도가 한도를 깎는다) · `revoke_budget`(멱등) ·
>   `spl_error_code`(속성 경로 1순위 + 문자열 폴백) · `classify_rejection`(순수 함수).
>   **`GuardResult` 재사용** → C4 배선이 한 줄이고 화면 코드 변경 0. **`guard.py` 무수정 =
>   하드 검사 8종 카운트 불변**(온체인 거부는 서명 *뒤* 체인이 거절하는 것이라 정의가 다르다).
> - **`build_transfer_transaction(source_owner=)`** — 출처 ATA 를 지불자에서 분리. 기본값
>   경로는 **바이트 동일**이고, 판매자 `verify_payment` 는 출처 ATA 를 아예 안 보므로
>   **판매자측 변경 0줄**.
> - **`scripts/demo_delegation.py`**(심사 산출물) — approve → 결제 → 한도 초과 거절 →
>   **★대조군(잔액 부족, 같은 0x1)** → 자기 상향 공격 → revoke. `.env`·시크릿·Gemini 키
>   **불필요**(localnet 기본 + 임시 지갑 + 테스트 민트 자체 생성).
> - **C5** `snapshot_balances(user_pk=)` + `usdc_net_out` 추출 — 교차검증에 자금 소유자 지갑
>   합산. user 잔액이 현재 0 이라 값이 지금까지와 **완전히 동일**하다.
>
> **★ 이 작업의 핵심 명제(영상 X 구간이 보여줄 것)**: 체인은 **한도 초과와 잔액 부족을 같은
> 에러 코드(0x1)로** 거절한다. 구분하지 않으면 **지갑이 빈 것을 "체인이 한도를 집행했다"고
> 광고하게 된다.** 그래서 실패 직후 계정 상태를 1회 재조회해 갈라내고, **잔액이 충분했을
> 때만** 한도를 주장한다. 음성 대조 N1 이 "코드만 보는 순진한 분류기"를 같은 파일에서
> 나란히 돌려 그것이 실제로 거짓 광고를 한다는 것을 단언한다.
>
> **⚠⚠ 테스트 배치 실행법이 바뀌어야 한다 — 이번에 실측으로 드러났다.**
> ① **`python scripts/test_*.py` 로 돌리면 6종이 `ModuleNotFoundError` 로 실패한다**
>    (`test_brain_select`·`test_dataquality`·`test_dca_schedule`·`test_gemini_parse`·
>    `test_indicators`·`test_store` 에는 `sys.path.insert` 가 없다 — 각 파일 독스트링이
>    `python -m scripts.test_xxx` 를 지시한다). **선재 조건이지 이번 작업의 회귀가 아니다.**
> ② **시스템 python 이 아니라 `.venv\Scripts\python.exe` 가 정본이다** — 시스템 쪽에는
>    `google-genai` 가 없어 `test_brain_select` 가 실패한다(`.venv` 에는 2.13.0). solders
>    0.27.1 · solana 0.38.0 은 양쪽 동일.
> **올바른 배치 명령**:
> ```powershell
> Get-ChildItem scripts/test_*.py | ForEach-Object { & ".venv\Scripts\python.exe" -m ("scripts." + $_.BaseName) *> $null; "{0,-30} rc={1}" -f $_.Name, $LASTEXITCODE }
> ```
>
> **실측 검증**: 단위 **23종 전부 rc=0** · `test_delegation` **58종**(오프라인 41 + localnet 17)
> · `red_team --report` rc=0(유출 0.00 · 오탐 0 · KPI 불변) · **localnet 라이브 세션 1회**
> (`run_demo --live --ticks 12`) `usdc_ok/stock_ok=True`, 아카이브에 `balances_before.user` 와
> `cross_check.usdc_wallets` 존재 · 증빙 `artifacts/tx/20260729_1243_solana-localnet_delegation.json`
> (온체인 거절 3건 · 유출 0.00 · approve/transfer/revoke tx 3건).
> ※ 라이브 세션은 `.env`·`secrets/` 를 건드리지 않고 임시 지갑·임시 민트로 돌렸다
>   (`setup_devnet.py` 는 마지막에 `.env` 를 덮어쓰므로 쓸 수 없다 — `:191`).
>
> **Cloud Run 영향 = 0. 재배포 불필요.** `artifacts/` 는 `.dockerignore` 로 이미지에서 제외되고,
> `payments/delegation.py` 는 이미지에 들어가되 임포트하는 코드가 없다. C5 가 건드린
> `snapshot_balances` 는 **라이브에서만** 호출되는데 배포본은 `ALLOW_LIVE_FROM_WEB=0` +
> `CONTROL_TOKEN` 으로 라이브가 잠겨 있다 → 드라이런 경로는 RPC 증가도 동작 변화도 없다.
> (다음에 다른 이유로 재배포할 때 자연히 포함되면 된다. 런북 §4 명령에 `GEMINI_MODEL` 포함 필수.)
>
> **문서 5곳 반영**(`70664f6`): 소개서 슬라이드 11 각주(⚠ **게시본 재발행 필요** — 같은 URL
> 유지. `.frame` 넘침은 브라우저 실측으로 14장 전부 0 확인) · README:105 · 벤치마크 §4-1
> 각주(**갭은 닫히지 않았다**를 명시) · FEATURES §1-1 행 1개(상태 = ⚠ 제품 미배선) ·
> **submission §6 큐시트에 축③ `X` 구간 12초 신설**(배포 URL `curl -i` 402 원문 6초 +
> `demo_delegation` ④⑤ 6초). 재원은 **A −4 · B −3 · G −5** 이고 **D·E 는 손대지 않았다**.
> 총합 180초·구간 연속성 검산 통과. 단위 테스트 표기 20→23종 정정.
> **일부러 바꾸지 않은 것**: `landing.html:224`·`index.html:364` 의 "예산을 코드가 집행" —
> 지금도 사실이고(AP2 가 실제로 차감한다) 여기에 온체인을 붙이면 **배선하지 않은 것을 첫
> 화면이 주장**하게 된다. **화면 변경 0 = 재촬영 사유 0.**
>
> **촬영용 대본 반영 완료**(사용자 지시로 2026-07-29 수행): `docs/artifacts/demo_script.html`
> 저장소본에 **X 구간 신설 + A~G 타이머 값 갱신 + 금지 문장 + localnet 체크 항목**을 넣었다.
> 브라우저 실측 — 장면 **8개**·합계 **180초**·표시시간/길이 불일치 0·타이머가 경계 초
> (67→C · 68→X · 79→X · 80→D)에서 정확히 전환·콘솔 오류 0.
> **게시본 재발행도 완료**(2026-07-29): 데모 대본·소개서 둘 다 **같은 URL 로** 갱신했다.
> 재발행 전에 게시본을 `WebFetch` 로 받아 저장소 수정 전 상태와 동일함을 확인했다(다른
> 세션의 변경 0 — 병합할 것이 없었다). 관제탑(`status.html`)은 날짜 박힌 기록이라 손대지
> 않았고 재발행도 불필요하다.
> ⚠ 아티팩트 favicon 은 이전 값을 알 수 없어 대본 🎬 · 소개서 🛡️ 로 새로 지정했다.
> 탭 아이콘이 예전과 다르면 사용자가 알려 주면 맞춰 재발행한다.
> **X 구간 ②컷은 localnet 검증기가 떠 있어야** 찍힌다.
>
> **다음(선택)**: C4(엔진 배선)는 **8/3 전에는 하지 않는다** — 교차검증 파손 위험 + 촬영이
> 드라이런 경로다. devnet 실증(`--devnet --budget 2 --spend 1`)은 Circle 파우셋에서 **user
> 지갑**으로 USDC 를 받았을 때만. 파우셋 대기를 일정에 넣지 않는다(설계 §8-3).
>
> ---
>
> **★★★★★★★★★★ 2026-07-29 새벽 — 사용자 지정 프런트 3건 + 재배포 완료. 커밋 `a65b834` 푸시.
> 리비전 `synapstock-00018-dhr`. 엔드포인트 13개 200 · 정적 파일 5개 저장소와 바이트 일치 ·
> 콘솔 오류 0.**
>
> **반영 3건 (`a65b834`)**
> 1. **`한도 바꾸기`(AP2 재서명) 카드를 개발자 서랍 → 사용자 화면으로 되돌렸다.** 예산 한도를
>    정하는 것은 개발용 조작이 아니라 제품을 쓰는 사람의 일이다(그 한도를 코드가 집행하는 것이
>    제품 자체다). 자리는 **예산 카드 바로 뒤**. ⚠ 이것은 하루 전 `44dddee` 5번 항목을 되돌린 것이다.
> 2. **`지난 세션` 이력 카드를 그리드 → 개발자 서랍으로 옮겼다.** 세션ID·Gemini 비율은 우리 계측
>    단위라 관람자용이 아니다. **⚠ 거래다** — 축④("전에도 돌았다")·축②(Gemini 비율)의 **화면
>    증거를 라이브 URL 에서 뺐다.** 그 몫은 영상 G 구간과 소개서가 대신 진다.
>    (서랍 폭이 380px 이라 6열 표는 `.table-wrap` 안에서 가로 스크롤된다 — 깨지지는 않는다.)
>    위 둘로 `DEFAULT_LAYOUT` 이 바뀌어 **`LAYOUT_KEY` _v11**.
> 3. **`맨 위로` 버튼이 누른 직후 뚝 끊기듯 사라지던 것 수정.** 원인은 `transition` 목록에
>    **`visibility` 가 없던 것** — opacity 는 0.22초 페이드인데 visibility 만 즉시 hidden 이 되어
>    페이드가 한 프레임도 안 보였다. `visibility .22s` 한 항목 추가. ⚠ **육안 검증은 못 했다**
>    (브라우저 패널이 화면에 없으면 프레임을 안 그려 CSS 전환이 진행되지 않는다 — 기존 함정).
>    `transitionProperty` 에 포함된 것만 확인했다.
>
> **⚠ 하루 전 블록의 사실 2건이 틀렸다 — 아래를 믿을 것.**
> ① **`docs/artifacts/demo_script.html` 은 구버전이 아니다.** 저장소본과 **게시본 URL 둘 다**
>    장면 7개(A~G) 현행이고, `★ E. 공격 2 — 숫자는 다 맞는데 물건이 다르다`(축② 유일 영상 근거)가
>    들어 있으며, 금지 문장은 *금지 표시로* · 폐기 KPI(`시도 20 · 차단 6`)는 *폐기 표시로* 적혀 있다.
>    문서 자체가 "`submission.md §6` 이 정본, 2026-07-28 동기화"라고 명시한다.
>    **대본은 두 벌이 아니라 한 벌이다 — 더 이상 확정 대기 항목이 아니다.**
> ② **배포본은 `00016-k8p` 이 아니었다.** `00017-fdj` 가 07-29 00:25 에 이미 올라가 있었다
>    (= `44dddee` 반영본). 지금은 `00018-dhr`.
>
> **explorer/민트 문제도 미지가 아니다** — 대본 G 구간에 **A안(Circle 재실증을 촬영 앞으로)/
> B안(아카이브 JSON + 고지 자막)** 으로 이미 갈라져 있다. **권고는 B안**: Circle 재실증은 devnet
> 파셋 일정에 걸려 확정 불가이고, 아래 402 원문 컷에서 **광고 asset 이 Circle 공식 민트**임이
> 이미 화면에 나오므로 "광고 통화는 공식 민트 · 온체인 정산 1건은 테스트 민트"가 자연스럽게 이어진다.
>
> **배포 검증 실측(`synapstock-00018-dhr`)**: 엔드포인트 **13개 200** · `/app`·`app.js`·
> `skeleton.css`·`landing.html`·`to-top.js` **5개 전부 저장소와 바이트 일치** · **SSE 첫 바이트
> 0.0796초** · `POST /broker/orders` → **402 + accepts[1]**, scheme `exact` / network
> `solana-devnet` / asset **Circle 공식 devnet USDC**(`4zMMC9srt5Ri…`) / `maxAmountRequired`
> 9980000 / description `STOCK:tAAPL x0.0398 (fee 0.03 USDC incl.)` · `persistence=firestore` ·
> `guard`·`ai.invoice_semantics` 방출 · env 에 **`GEMINI_MODEL=gemini-flash-latest` 유지**
> (`GEMINI_MODE=developer`·`ALLOW_LIVE_FROM_WEB=0`, 시크릿 5개) · 배포본 대시보드에서
> **한도 카드 표시 · 지난 세션 비표시 · 개발자 탭 rect 0×0 · 콘솔 오류 0**.
> ※ 배포 요청 body 는 `{"symbol","spend_usdc","price_usdc","mode"}` 다(`OrderBody`, `extra=forbid`).
>   `quantity` 로 보내면 402 가 아니라 **422** 가 난다 — 검증 스크립트를 쓸 때 주의.
>
> **⚑ 촬영 흐름 1건 검증 완료(유용)**: **`?lab=1` 로 세션을 시작한 뒤 `?lab=0` 으로 새로고침하면
> 세션은 계속 돌면서 개발자 UI 만 완전히 사라진다**(실측: 틱 12 → 76, `status=running` 유지,
> 주소창은 `/app` 로 정리, 긴급정지·세션 종료는 살아 있음). **촬영본에 개발용 조작부가 한 번도
> 안 비친다.** 대본 촬영 전 점검에 없던 항목이다.
>
> # ▶ 화면에서 더 다듬을 곳 — 실측 목록 (사용자 판단 대기, 코드 변경 안 함)
> 심각도 순. **①만 결함이고 나머지는 판단 사항이다.**
> 1. **[결함] 거래 내역이 세션 경계를 구분하지 않는다 — 촬영 C 구간에 직접 걸린다.**
>    실측: 서버 `counts.trades = 1` 인데 화면 표는 **2줄**, 그중 하나는 45초 전 끝난 다른 세션 것.
>    원인은 **`/api/state` 에 `trades` 키가 아예 없어** 표가 **SSE 히스토리 재전송(`?since=0`)만으로**
>    채워지는데 `app.js` `case "trade"` 에 세션 필터가 없다는 것. **판단 피드·활동 로그는
>    `past-session` 클래스 + 구분선으로 이미 처리돼 있고 거래 표만 빠졌다.** 캔들 중복을 막았던
>    (`isFresh` + 틱 번호) 것과 같은 계열 — 그때 표는 안 봤다. 수정하면 10줄 안쪽.
> 2. **`손익 (실현)` 카드가 자기모순으로 읽힌다.** 큰 숫자 `0 USDC` 아래에 `수익률 6.22%`.
>    실측상 `pnl.return_pct`(6.22) = `valuation.unrealized_pct`(6.22) — **실현 카드에 미실현 기준
>    수익률이 붙어 있다**(`pnl.basis = "initial-capital"`). 라벨을 바꾸거나 옆 카드로 옮기면 끝.
> 3. **예산 카드와 한도 카드가 서로 다른 값을 말한다.** 예산 `건별 최대: 전량(올인)` vs 한도
>    입력칸 `50`. 둘 다 사실(`budget.all_in=true`, `per_trade_max_usdc="50"` — 추세추종이 덮어씀).
>    한도 카드를 사용자 화면으로 되돌렸으므로 이제 나란히 보인다 → 예산 카드에 한 줄 주석 필요.
> 4. **차트 조작부가 과하다** — 원본봉/2/4/8 + 60/120/240/전체 + 양봉·음봉 + MA 7개 = **노브 15개**,
>    게다가 `봉 43개` 와 `(18/80봉)` 이 진행도를 두 번 말한다. 기본 MA 를 5/20 만 켜고 나머지를
>    `고급` 뒤로 접는 안.
> 5. **가드 패널을 걷으면 첫 줄이 전부 돈 얘기**(손익·평가손익·포지션). 패널로 시선을 끄는 것은
>    **사용자 의도(2026-07-29 확정 — 일부러 거슬리게 해 확인을 유도)** 이므로 패널은 손대지 않는다.
>    착지점만 `예산(코드가 집행하는 상한)` 으로 바꾸면 "유출 0 → 한도 → 얼마 벌었나" 순서가 된다
>    (`DEFAULT_LAYOUT` 한 줄).
> 6. ~~**`tAAPL` 툴팁**(기존 I5)~~ — **이미 구현돼 있다**(2026-07-30 소스 확인:
>    `app.js:508-511` 이 종목·포지션 카드에 `devnet 테스트 토큰 — AAPL 을 토큰화한 자산` 을
>    `title` 로 붙인다). 미결 항목으로 다시 적지 말 것.
> 7. (낮음) 알림 권한이 **거부** 상태면 헤더에 `🔕 브라우저가 알림을 막았습니다` 가 상시 뜬다.
>    기본(prompt) 상태에서는 `🔔 알림 받기` 라 심사위원에게는 안 보일 가능성이 높다.
> 8. **[심사 영향 고지] 세션 시작 버튼이 개발자 서랍 안이라 심사위원이 라이브 URL 을 그냥 열면
>    세션을 못 켠다.** 의도된 설계이고 배포본은 `CONTROL_TOKEN` 이 조작 API 를 막아 실질 손실은
>    없지만, 화면에서도 사라져 **"볼 것만 있는 사이트"로 읽힐 여지**가 생겼다. 랜딩에 심사용
>    조작 URL 한 줄을 두는 선택지.
>
> # ▶ 3분 영상 — 축 매핑과 유일한 구멍
> 현행 큐시트(`docs/submission.md §6`, A~G 180초)를 축에 대조한 결과:
> A 문제 22s=축① 주장 · B 빈 자리 18s=축① 혁신성 · C 정상 동작 35s=축④(단 샌드박스라 온체인 아님) ·
> D 공격1 25s=축①+실행 로그 · **E 공격2 35s=축② 유일 근거** · F AI 권한 25s=축② · G 증빙 20s=축④.
> **→ 축③(기술 완성도·블록체인 인프라 연동)이 주축인 장면이 0초다.** 심사 기준이
> "USDC on Solana / AP2·A2A·x402" 를 병렬로 든 축인데 화면 증거가 없다(내레이션으로만 스친다).
> **처방 = D 앞에 6초 컷 신설**: 배포 URL 에 `curl -i -X POST /broker/orders` 한 방으로 나오는
> **402 원문**(위 실측 그대로). 이유 셋 — ①`402 Payment Required` + `accepts[]` 가 x402 를
> "붙였다"가 아니라 "구현했다"의 차이를 한 화면으로 보인다 ②asset 이 **Circle 공식 민트**라
> 자체발행 논란을 정면으로 비껴간다 ③**심사위원이 자기 터미널에서 그대로 재현 가능**하다.
> 시간은 **A −4초 · B −3초**로 뽑는다(D·E 는 건드리지 않는다 — E 는 대본상 절대 불가).
> **축① 상업성은 공짜로 하나 채울 수 있다** — C 구간에 이미 화면에 있는
> `브로커 수수료 0.3% · 누적 0.30 USDC` 줄을 1~2초 클로즈업하면 "수익모델이 슬라이드가 아니라
> 코드에 있다"가 된다(축①이 '중'인 사유 = 상업성 정량 근거 0건. 영상에서 메울 수 있는 유일한 지점).
> 추가 시간 0초.
>
> **남은 사용자 몫**: GitHub public 전환 · Circle USDC devnet 재실증 · 배포 라이브 세션 · 제출물 3종.
>
> ---
>
> **★★★★★★★★★ 2026-07-29 — 개발자 전용 잔여 5건 정리 완료. 커밋 `44dddee` 푸시.
> 테스트 22종 통과 · red_team `rc=0` · 유출 0.00 · 콘솔 오류 0.
> (⚠ 이 블록은 "아직 재배포 안 했다"고 적었는데 사실이 아니다 — `00017-fdj` 로 07-29 00:25 에
> 배포됐다. 그리고 아래의 "대본 구버전" 경고도 틀렸다. 위 최신 블록을 볼 것.)**
>
> **⚑ 사용자 결정 3건 (이후 이 전제로 움직인다)**
> 1. **촬영 걱정은 8/1 까지 하지 않는다.** 디자인 시안이 와야 완성이고 완성돼야 촬영이다 —
>    **재촬영은 보류·연기 대상이 아니다.** 그러니 "재촬영 사유라 손대지 말자"는 제약을 더 이상
>    적용하지 말 것(이전 블록들에 그 문구가 남아 있는데, 8/1 까지는 무효다).
>    화면 구조·문구·카드 배치를 자유롭게 바꿔도 된다.
> 2. **랜딩 `디스커버리` 탭은 그대로 둔다.** 더 이상 확인 대기 항목이 아니다.
> 3. **개발자 전용 후보 7건 중 1·2·3·4·6 제거.** 5(푸터 지갑 공개키 줄)·7(판단 출처 enum
>    범례)는 **남긴다** — 둘 다 심사 증거이고 관람에 방해되지 않는다는 판단.
>
> **반영 내용 (`44dddee`)**
> - **거래 내역 `결제 tx`·`전달 tx` 두 열 → 라이브에서만 표시**(`.col-tx` + `body.live-mode`,
>   전환은 `renderState`). 샌드박스는 전송이 없어 값이 전부 `—` 라 빈 열 둘이 늘 남아 있었다.
>   **완전 삭제는 하지 않았다** — 라이브의 explorer 링크가 "화면이 아니라 진짜 트랜잭션" 증거다.
>   양방향 실측(샌드박스 비표시 / `live-mode` 부여 시 표시) 확인. 완전 삭제를 원하면 CSS 한 줄.
> - **지난 세션 표에서 `틱` 열 제거**(엔진 내부 단위). `Gemini 비율`은 축② 근거라 유지.
>   `emptyRow` colspan 7→**6** 동반 수정(안 바꾸면 빈 표 안내가 열을 넘친다).
> - **푸터 `실시간 연결됨 (SSE)` → `실시간 연결됨`.**
> - **시세 카드 `틱 N · 간격 0.15s` → 날짜 + `(N/총봉)`.** 합성 패턴 피드(개발용)는 봉 총수가
>   없어 그때만 틱 수를 그대로 쓴다 — 그쪽은 실제로 틱이 단위다.
> - **`한도 바꾸기`(AP2 재서명) 카드 → 개발자 서랍.** 세션 설정과 같은 방식으로 `app.js` 가
>   그리드에서 옮긴다. `DEFAULT_LAYOUT` 에서 `mandate` 제거, **`LAYOUT_KEY` _v10**.
>   서랍에 카드가 둘이 되어 `.dev-drawer .card + .card` 구분선 추가.
>
> **⚠ 배포본은 아직 `synapstock-00016-k8p`** (= 이 5건이 빠진 상태). 다음 라운드에서 화면을
> 더 다듬을 것이므로 **재배포는 그때 한 번으로 묶는 것이 싸다**(빌드 5~10분 + 비용).
> 급히 배포본에 반영해야 하면 런북 §4 한 줄 명령(`GEMINI_MODEL=gemini-flash-latest` 포함 필수).
>
> # ▶ 다음 대화 = **더 다듬을 곳 찾기 + 3분 영상에서 보여줄 것 정하기**
> 사용자 지시: *"새 대화로 넘어가서 더 다듬여야 할 부분들을 찾아보자. 특히 심사에서 3분 영상에서
> 보여주면 좋은 것들에 대해서 생각해보자."*
> - **읽고 시작할 것**: 이 블록 → `docs/submission.md §6`(대본 정본) → `docs/axis2_ai_narrative.md`
>   (축② 서사·금지 문장) → `docs/reports/agentfabric_benchmark.md §5`(수상 프레이밍) →
>   `docs/reports/judging_latest.md`(축① 중 · ②③④ 강, 축①의 사유는 상업성 정량 근거).
> - **⚠ 대본이 두 벌이고 타이머가 붙은 `docs/artifacts/demo_script.html` 이 구버전이다**
>   (금지 문장으로 시작 · 폐기 KPI 낭독 · **축② 유일 영상 근거인 의미 대조 장면이 없다**).
>   영상 이야기를 시작하기 전에 **어느 쪽이 정본인지부터 확정**할 것.
> - **⚠ 촬영 순서 지뢰**: explorer 화면을 찍으면 devnet 아카이브에 **자체발행 민트 결제 1건뿐**이라
>   민트 권한 줄이 비친다. Circle 재실증을 앞으로 당기거나 그 장면을 아카이브 JSON + 고지 자막으로
>   대체할 것(이건 8/1 이후에도 유효한 제약이다 — 촬영 일정과 무관한 증빙 문제).
> - **화면 쪽 남은 후보**: **L2** 가드 패널이 첫 줄 카드를 가림(이제 촬영 제약이 없으니 실제로
>   고칠 수 있다 — 접기 기본값을 바꾸거나 dock 위치를 조정) · **I5** 종목 `tAAPL` 툴팁 ·
>   개발자 후보 5·7 은 남기기로 결정됨.
> - 남은 사용자 몫: GitHub public 전환 · Circle USDC devnet 재실증 · 배포 라이브 세션 · 제출물 3종.
>
> ---
>
> **★★★★★★★★ 2026-07-28 심야 — P5-2 병합 + 사용자 수정 9건 완료. 커밋 `56af89d` 푸시.
> 리비전 `synapstock-00016-k8p`. 테스트 22종 통과 · red_team `rc=0` · 콘솔 오류 0 · 유출 0.00.**
>
> **회귀 대조 = 되돌아간 것 0** (LF 정규화 후 양방향 `diff`): 저장소에만 있던 줄 `index.html` 0 ·
> `skeleton.css` 0 · `landing.html` 4 — 그 4줄은 전부 새 탭 메뉴로 대체된 옛 nav 링크다.
> `app.js` 는 이번 배치에 없어 저장소 현행본을 그대로 뒀다.
> **줄바꿈은 이번에도 섞여 있었다**(저장소 `index/skeleton/theme/app` = CRLF, `landing/reveal/wallet` = LF;
> 전달본은 html·css 전부 CRLF, `to-top.js` 만 LF). `landing.html` 만 LF 로 변환해 기록했고,
> 결과적으로 `git diff` 가 **120줄 추가·4줄 삭제**로 나왔다(전체 변경 오판 없음).
> ⚠ 판별은 여전히 **PowerShell 바이트 스캔만** 믿을 것 — 이 환경의 Git Bash `od -c` 파이프도
> CR 을 놓친다(이번에 전 파일이 LF 로 잘못 나왔다).
>
> **전달본에서 받은 것**: 랜딩 상단 **탭 슬라이더**(`.tabs`+`.glider`) · **맨 위로 버튼**(`.to-top`
> + 신규 `web/static/js/to-top.js`, 랜딩·대시보드 공용) · 토스트 스택을 버튼 높이만큼 띄움.
> **배치하지 않은 것 2개**: `hoverme.css`·`clickme.css` — 어디서도 참조되지 않고 **`button {}` 전역
> 선택자에 색을 하드코딩**해(`--color:#560bad` 등) 링크하는 순간 모든 버튼을 덮고 다크 모드가 깨진다.
> `docs/design/incoming/p5-2/` 에 참고용으로만 보관(untracked).
>
> **⚠ 사용자 확인 필요 — 랜딩 nav 에 `디스커버리`(`/.well-known/x402`) 탭이 되돌아왔다.**
> P5 전달서가 *"처음 온 사람이 상단 메뉴를 누르면 날 JSON 을 마주친다"* 는 이유로 뺐던 항목인데,
> 전달본 주석이 **"사용자 요청으로 다시 넣었다"** 고 적고 있다(탭 슬라이더가 3칸이라 필요했던 것으로도
> 보인다). **전달본대로 두었다** — 되돌리려면 탭을 2개로 줄이거나 3번째를 `#about` 앵커로 바꾸면 된다.
> 푸터 증빙 링크는 그대로 살아 있다.
>
> **사용자 수정 9건 — 8건 반영, 1건은 조사 결과 보고(아래 '개발자 전용 잔여')**
> 1·2·3·4(차트): 캔들 이력을 **60개만 들고 그 60개 위에서 이동평균을 구하던 것**이 "MA50 이 너무
> 짧다"의 원인이었다. 이제 **이력 전체 위에서 계산한 뒤 보이는 구간만 잘라 쓴다** → **MA100·MA200
> 추가**, MA50 이 화면 전체를 지난다. 봉이 모자란 이동평균은 **범례를 흐리게**(`.ma-off`) 표시.
> **가로 스크롤**(드래그·Shift+휠) + `최신 ▸` 복귀 + **보기 범위**(60/120/240/전체) 선택.
> 분봉·일봉 대비는 **`groupCandles`(봉 N개 묶기)** 를 분리해 화면 선택으로 노출했다 — 서버가 진짜
> 분봉/일봉을 내려주면 캔들 배열 모양이 같아 이 함수는 그대로 두고 피드만 바꾸면 된다.
> 5: 배너에서 `solana-devnet`·`엔진 대기`·`판단`·`시세` 배지 제거 → **개발자 서랍으로 이동**
> (DOM 에서 빼지 않는다 — `app.js` 가 매 갱신마다 값을 쓰므로 없애면 null 참조로 대시보드가 죽는다).
> 6: **세션 설정 카드를 개발자 서랍(`.dev-dock`)으로 이동.** `?lab=1` 로 켜고 `?lab=0` 으로 끈다 —
> **꺼져 있으면 탭도 서랍도 화면에 없다**(런칭 시 개발용 UI 감추기가 스위치 하나). 카드는
> `app.js` 가 그리드에서 서랍으로 옮긴다(스크립트가 죽으면 그리드에 남아 조작이 막히지는 않는다).
> `DEFAULT_LAYOUT` 에서 `session` 제거, **`LAYOUT_KEY` _v9**.
> 7: 화면 문구 축약 — **지우지 않고 `title` 로 내렸다**(KPI 라벨 4개·AI 카드 6개·예산·평가손익·
> 한도·손익·브리핑·종목별 요약·태그라인).
>
> **⚠ 이번에 새로 드러난 결함 3건 — 전부 이 라운드에서 닫았다.**
> ① **새로고침하면 같은 봉이 두 번 쌓였다**(실측: 501봉 세션이 **718봉**, 지난 세션까지 얹히면
> 100봉 세션이 **451봉**). SSE 가 `?since=0` 으로 이벤트 히스토리를 처음부터 재전송하는데 그게
> `/api/state` 스냅샷 위에 겹친다. **캔들 상한이 60이던 시절에는 겹친 만큼이 잘려 나가 안 보였다.**
> `isFresh(evt)`(페이지 연 시각) + 틱 번호로 **두 겹** 차단 — 틱 번호만으로는 세션이 바뀌면 못 막는다
> (이벤트 버퍼에 지난 세션이 남아 있고 틱이 1부터 다시 시작한다).
> ② 체결·가드 이벤트마다 도는 `fetchState` 가 차트 이력을 스냅샷 길이로 **되감았다** → `mergeCandles`.
> ③ 차트가 **휠을 통째로 가로채** 대시보드를 스크롤하다 커서가 차트에 닿으면 페이지가 멈췄고,
> 트랙패드 핀치가 `ctrl+휠` 로 들어와 **손대지 않아도 배율이 바뀌었다**(60봉으로 시작한 차트가
> 43봉이 되어 있었다). 그냥 휠은 가로채지 않고 확대·축소는 선택으로만.
>
> **함께 바꾼 백엔드 1줄짜리**: `engine.MAX_CANDLES` 90→**300**, 스냅샷 슬라이스 `[-60:]`→동일.
> **화면 표시 전용 버퍼**라 판단·정산 경로에 닿지 않는다(에이전트는 자기 봉 이력을 따로 든다).
>
> **차트 조작부·범례를 그래프 아래로 내렸다** — 위쪽은 가드 요약 패널(`.guard-dock`)이 떠 있는
> 자리라 **범례를 100% 가렸다**(실측). 가드 패널 자체는 사용자 결정 사항이라 손대지 않았다.
>
> **배포 검증 실측**(`synapstock-00016-k8p`): 엔드포인트 **13개 200** · `/app`·`app.js`·`skeleton.css`·
> `landing.html`·`to-top.js`·`theme.css` **6개 전부 저장소와 바이트 일치** · **SSE 첫 바이트 0.107초**
> (`: connected`) · `POST /broker/orders` → **402 + accepts[1]**, scheme `exact` / network `solana-devnet` /
> asset **Circle 공식 devnet USDC**(`4zMMC9sr…`) · `persistence=firestore` · `guard.blocked_unverified`·
> `ai.invoice_semantics` 방출 · env 에 **`GEMINI_MODEL=gemini-flash-latest` 유지**(`GEMINI_MODE=developer`·
> `ALLOW_LIVE_FROM_WEB=0`) · 배포본 대시보드 **이력 10건 렌더 · 배너 배지 0 · 개발자 서랍 비표시 ·
> 콘솔 오류 0**. 로컬 dry: 하락장 **481틱 완주**(가드 시도 38 · 차단 0 · **유출 0.00**) · 새로고침 후
> 봉 중복 0 · `?lab=0` 에서 개발자 UI 전부 사라짐 확인.
>
> **⚠⚠ 촬영 전 반드시 알아야 할 운영 변경 1건.**
> **배포 URL 을 그냥 열면 세션을 시작할 수 없다** — 세션 설정이 개발자 서랍 안에 있고 서랍은 기본
> 꺼짐이다. 촬영·시연은 **`https://…/app?lab=1`** 로 한 번 열면 되고(localStorage 가 기억한다),
> 되돌릴 때는 `?lab=0`. 배포본은 어차피 `CONTROL_TOKEN` 이 조작 API 를 막고 있어 외부인에게는
> 달라지는 것이 없다(오히려 눌러도 401 이 나던 버튼이 사라진다).
>
> # ▶ 항목 8 답 — **아직 남은 '개발자 전용' 후보** (사용자 판단 대기, 코드 변경 안 함)
> 우선순위 순. 괄호는 실제 위치.
> 1. **거래 내역의 `결제 tx`·`전달 tx` 두 열**(`index.html:463`) — 샌드박스에서는 전부 `—` 다.
>    라이브에서만 값이 생기므로 촬영본에서는 빈 열 2개가 계속 보인다. → 라이브일 때만 표시 권장.
> 2. **지난 세션의 `틱`·`Gemini 비율` 열**(`:483-484`) — '틱'은 엔진 단위, 'Gemini 비율'은 축② 근거.
>    전자는 뺄 만하고 후자는 남길 값이 있다.
> 3. **AI 판단 타임라인 범례의 원문 enum**(`:421-425` `gemini`·`rule`·`rule-fallback`·`rule-gate`·`dca`)
>    — 축② 증거라 지우면 안 되지만, 한국어 라벨을 앞세우고 enum 은 배지로만 두는 식은 가능.
>    (지금도 한국어가 앞에 있고 배지가 뒤이므로 **그대로 둬도 무방**하다는 것이 내 의견.)
> 4. **푸터 `실시간 연결됨 (SSE)`**(`app.js:1520`) — `(SSE)` 는 프로토콜 이름이다. 빼도 정보 손실 0.
> 5. **푸터 지갑 공개키 줄**(`:503`) — 증빙이라 남길 값이 있지만 관람자에겐 읽을 것이 아니다.
> 6. **시세 카드 `틱 N · 간격 0.15s`**(`app.js` tickInfo) — 관람자에게는 `(200/481봉)` 만으로 충분.
> 7. **`한도 바꾸기` 카드**(AP2 재서명) — 한도가 진짜라는 증거이자 운영 조작부다. 서랍으로 옮길지는
>    **축③ 증거를 첫 화면에서 뺄 것인가**의 문제라 사용자 판단 사항.
> **손대지 말 것으로 판단한 것**: 402 Guard KPI 4칸(재촬영 사유) · AI 카드 두 레이어 · 거래 내역의
> `판단`·`상태` 열 · 브리핑 카드 · `🔔 알림 받기`.
>
> **미결 2건(코드 아님)**: **L2** 가드 패널이 첫 줄 카드를 가림(접기 탭으로 내리면 해소 — 코드 변경 0) ·
> **I5** 종목 `tAAPL` 툴팁. **1차 촬영 7/30~31.**
> 남은 사용자 몫: GitHub public 전환 · Circle USDC devnet 재실증 · 배포 라이브 세션 · 제출물 3종.
>
> ---
>
> **★★★★★★★ 2026-07-28 밤 — P5 프런트 전달본 병합 + 재배포 완료. 커밋 `481ac4b` 푸시.
> 리비전 `synapstock-00015-j8f`. 전달서 33행 중 30행 반영 · 회귀 0 · 콘솔 오류 0 · 유출 0.00.**
>
> **회귀 대조 결과 = 되돌아간 것 0** (지난 두 번은 여기서 최근 반영분이 사라졌다).
> 저장소에서 없어진 줄(index 81 · app.js 38 · landing 2)을 한 줄씩 확인해 전부 전달서가
> 지시한 개명·이동임을 확정했고, 과거 회귀 이력 16종(지갑 로그인 훅 · AI 카드 · `renderAi` ·
> `guard_semantic` · `data-guard-legs` · `.src-rule-gate` · 전략 기본값 추세추종 · 종목 체크박스
> 잠금 조건 · `data-card="history"` · `fetchHistory` · `lastEngineStatus` · `actor_label` ·
> `invoice_semantics` · `blocked_unverified`)은 전부 잔존 확인.
>
> **⚠ 다음에 전달본을 받을 때 반드시 볼 것 — 저장소가 줄바꿈 방식이 섞여 있다.**
> `index.html`·`skeleton.css`·`app.js` 는 **CRLF**, `landing.html` 만 **LF** 다. 전달본은 4개
> 전부 CRLF 라 정규화 없이 `diff` 를 돌리면 **파일 전체가 바뀐 것처럼 보인다**(이번에 index.html
> 이 437줄 전부 변경으로 나와 오판할 뻔했다). 게다가 **이 환경의 Git Bash 는 `grep -c $'\r'` 와
> `awk '/\r$/'` 가 CR 을 못 잡는다** — 줄바꿈 판별은 **`od -c` 나 PowerShell 바이트 스캔만** 믿을 것.
> 대조는 양쪽을 LF 로 정규화한 뒤 `diff | grep '^<'`(저장소에만 있는 줄) 방향을 먼저 본다.
>
> **반영된 것**: 세션 설정 **간단/고급 분리**(간단 = 모드·속도·종목·전략 4개 / 고급 `<details data-adv>`
> = 시세 피드·데이터셋·봉 간격·AI 판단 모드·추세 신호·TA·DCA) · **`?lab=1`** 로 고급 펼침 + 라이브
> 옵션·배치 초기화 잠금 해제(`?lab=0` 해제) · 개발 용어 개명(드라이런→**샌드박스**, 목 시세→합성 패턴
> 피드, 조건형→**AI 판단**, 알림: 차단됨→**알림 받기**) · 세션ID `20260727_135727_dry`→**`07-27 13:57 세션`**
> (원문은 `title`) · 프로토콜명(AP2·A2A·x402)을 **제목→부제로 이동(삭제 아님)** · `DEFAULT_LAYOUT`
> 첫 자리 `session`→`pnl`, **`LAYOUT_KEY` _v8** · 브리핑 `fallback_detail` 을 `title` 에만 배선 ·
> 랜딩 nav 에서 x402 제거(푸터 증빙은 유지) · ABOUT CTA `/connect` 통일.
>
> **⚠⚠ 촬영 전 사용자 결정 1건 — L2 가 L1 재배열로 더 나빠졌다.**
> 전달서 실측은 1040×982 에서 `pnl`·`valuation`·`position` **각 33%** 였는데, `DEFAULT_LAYOUT`
> 첫 자리가 바뀌며 dock 아래 오는 카드가 달라졌다. **1280×1000 실측: `position` 96% ·
> `valuation` 40% · `pnl` 0%** — **포지션 카드(보유 수량·평균단가)가 첫 화면에서 사실상 완전히
> 가려진다**(배포본 스크린샷 확인). 프런트는 지시대로 `.guard-dock` 을 안 건드렸고 '디자이너 확인
> 후 유지' 결정이 살아 있어 임의로 바꾸지 않았다. **접기 탭으로 내리면 안 가려지므로 촬영 시
> 패널을 접고 찍는 것도 선택지**다(코드 변경 0).
>
> **미반영 1건**: I5(종목 `tAAPL` 툴팁) — 전달서 우선순위 **C(선택)**. A·B 는 전부 반영.
> **범위 밖 수용 1건**: `skeleton.css` 에 예산 카드 재구성(uiverse.io MIT 출처 명시)이 함께 왔다.
> 하드코딩 색 없이 `theme.css` 토큰만 쓰고 `data-budget-*` 훅 5개가 그대로라 8/2 시안과 충돌 없음.
>
> **배포 검증 실측**(`synapstock-00015-j8f`): 엔드포인트 **12개 200** · `/app` 32,665B·`app.js`
> 87,382B = 병합본과 바이트 일치 · **SSE 첫 바이트 0.232초**(`: connected`) · `POST /broker/orders`
> → **402 + accepts[]·payTo·maxAmountRequired**, asset = **Circle 공식 devnet USDC**
> (`4zMMC9sr…`) · `persistence=firestore` · `state.ai.invoice_semantics`·`guard.blocked_unverified`
> 방출 · env 에 **`GEMINI_MODEL=gemini-flash-latest` 유지**(`GEMINI_MODE=developer`·
> `ALLOW_LIVE_FROM_WEB=0`) · 배포본 대시보드 **이력 10건 렌더 + 세션ID 개명 작동** · **콘솔 오류 0**.
> 로컬 dry 스모크: 80틱 완주 · 체결 3건 · **차단 0 · 유출 0.00** · `?lab=1/0` 과 `#token` 이
> 서로의 URL 정리를 밟지 않음 확인.
>
> **[완료 — 2026-07-28 심야, 커밋 `56af89d`] P5-2 병합 + 사용자 지정 수정 9건.**
> 아래는 **착수 당시** 기록이다(발견 2건은 실제로 그대로 확인됐다). 결과는 이 문서 맨 위 블록.
>
> ~~# ▶ 다음 대화 = **P5-2 프런트 2차 전달본 병합 + 사용자 지정 수정**~~
> 2차 전달본이 **이미 `C:\Users\Tedd\Downloads` 에 있다**(07-28 22:17, 6개). 사용자가 여기에
> **자기 수정 요청 목록을 추가로 준다.** 병합은 아직 안 했다 — 저장소는 `78ec8ff` 그대로다.
>
> **2차 전달본 구성 (아래 이름 그대로 Downloads 에 있음)**
> `index (1).html` 33,492B · `landing (1).html` 22,673B · `skeleton (1).css` 29,808B ·
> **신규 `to-top.js` 1,826B** · `hoverme.css` 847B · `clickme.css` 771B.
> **`app.js` 는 이번 배치에 없다** → 저장소 현행본(`87,382B`)을 그대로 둔다.
>
> **이미 해 둔 회귀 사전 점검(양쪽 LF 정규화 후 `diff | grep '^<'`)**
> `index.html` **0** · `skeleton.css` **0** · `landing.html` **4** — 그 4줄이 문제다(아래).
>
> **⚠ 발견 1 — P1 이 되돌아갔다(병합 전에 결정할 것).** 새 `landing.html` 이 nav 를 탭 슬라이더
> (`.tabs` + `.glider`)로 새로 만들면서 **`디스커버리`(`/.well-known/x402`) 탭을 다시 넣었다.**
> 전달서 §5 P1 이 *"처음 온 사람이 상단 메뉴를 누르면 JSON 원문을 마주친다 · 같은 링크가 푸터에
> 이미 있다"* 는 이유로 뺐던 항목이다(푸터 증빙 3곳은 그대로 살아 있다). 탭 3개가 슬라이더
> 디자인에 필요했던 것으로 보인다 → **탭을 2개로 줄일지, 3번째를 다른 곳(예: `#about` 앵커)으로
> 바꿀지 사용자 결정.** P5(ABOUT CTA `/connect`)는 정상 유지 확인.
>
> **⚠ 발견 2 — `hoverme.css`·`clickme.css` 는 배치하면 안 된다(그냥 참고 자료다).**
> 어느 파일에서도 참조되지 않고(index·landing·skeleton 전부 0건), 둘 다 **`button { }` 전역
> 선택자에 색을 하드코딩**한다(`--color: #560bad` 등, 출처 주석 `From Uiverse.io`). 링크하는
> 순간 **화면의 모든 버튼을 덮어쓰고 다크 모드가 깨지며 8/2 시안(`theme.css`)과 정면 충돌**한다.
> → `web/static/css/` 에 넣지 말고 참고용으로만 두거나, 필요한 규칙만 `.to-top` 처럼 클래스로
> 좁혀 `skeleton.css` 에 흡수한다. **프런트에 의도를 먼저 물어볼 것.**
>
> **`to-top.js` 는 진짜 신규 기능이라 배치가 필요하다** — `web/static/js/to-top.js` 로 넣는다
> (index 2곳 · landing 2곳 · skeleton `.to-top` 규칙 1곳에서 참조. 안 넣으면 404 + 버튼 무동작).
>
> **⚠⚠ 병합 절차의 함정 — 저장소 줄바꿈이 섞여 있다(이번 라운드에서 실제로 오판할 뻔했다).**
> `index.html`·`skeleton.css`·`app.js` = **CRLF**, `landing.html` = **LF**. 전달본은 전부 CRLF.
> 정규화 없이 `diff` 를 돌리면 **파일 전체가 바뀐 것처럼 보인다.** 게다가 **이 환경의 Git Bash 는
> `grep -c $'\r'` 와 `awk '/\r$/'` 가 CR 을 못 잡는다** — 판별은 **`od -c` / PowerShell 바이트 스캔만**
> 믿을 것. 기록할 때도 파일별 기존 방식에 맞춰야 `git diff` 가 실제 변경분만 남는다.
>
> **병합 후 순서(이번 라운드에서 그대로 통한 절차)**: ①`app.js` 훅 전수 대조
> (`grep -o '\[data-[a-z0-9-]*\]' app.js | sort -u` → 전부 `index.html` 에 존재하는지. 하나라도
> 없으면 `el.x` 가 null 이라 대시보드가 통째로 죽는다) ②`node --check` ③로컬 dry 세션 스모크
> (`preview_start` `dashboard-fast` → `/app` → 세션 시작 → **콘솔 오류 0 · 유출 0.00**)
> ④커밋·푸시 ⑤**재배포 1회**(런북 §4 한 줄 명령 — `GEMINI_MODEL=gemini-flash-latest` 포함 필수,
> `--set-env-vars` 는 기존 env 를 전부 덮어쓴다) ⑥엔드포인트 실측.
>
> **미결 2건(코드 아님·사용자 결정)**: **L2** 가드 패널이 `position` 을 96% 가림(접기 탭으로
> 내리면 해소 — 코드 변경 0) · **I5** 종목 `tAAPL` 툴팁 미반영(전달서 우선순위 C).
>
> **1차 촬영 7/30~31.** 남은 사용자 몫: GitHub public 전환 · Circle USDC devnet 재실증 ·
> 배포 라이브 세션 · 제출물 3종.
>
> ---
>
> **★ 2026-07-28 저녁 — P5 전달서 완료. 산출물은 문서다: [`docs/frontend_p5_handoff.md`](docs/frontend_p5_handoff.md).
> `web/static/**` 은 한 줄도 안 건드렸다(프런트 소유). 기준 커밋 `3c1811d`.**
>
> **사용자 결정 2건** — ①**반영 먼저 → 촬영 7/30~31**(①촬영 먼저 안이 아니다).
> 따라서 **프런트 마감 = 7/30 오전**, 늦으면 전달서 **§7-A 만** 받고 촬영한다.
> ②**간단/고급 토글 + `?lab=1`**. **`/lab` 라우트는 채택 안 함** — `server.py` 를 건드리는 것보다
> **대시보드가 두 벌이 되어 마감 6일 전 회귀 표면이 2배**가 되는 게 더 비싸다. 채택안은 **백엔드 0줄**.
>
> **전달서**: 대시보드 28행 · 랜딩 5행. 각 행 = `data-card`·`file:line`·실측 라벨 + 3분류(유지/
> 개발용이라 뒤로/심사 증거라 위치만) + 처리(그대로·감춤(고급)·개명·이동, **개명은 문구 확정**) + 근거 한 줄.
>
> **⚠ 위 P5 초안 목록이 실측으로 2건 뒤집혔다 — 초안을 그대로 믿지 말 것.**
> ①**'긴급정지 두 벌'은 사실이 아니다**: DOM 4개지만 `scrollY=900` 에서 슬림바만 `is-shown`,
> 헤더는 화면 밖(`bottom=-747`) → 화면엔 늘 한 벌. 전달서엔 **`그대로`(건드리면 로직만 위험)**.
> ②**가드 패널 가림은 76px 이 아니라 194px**: 1040×982 에서 dock `[274,153,994,364]` 가
> `pnl`·`valuation`·`position` 을 **각각 면적 33%** 덮는다(세 카드 제목·대표 숫자가 첫 화면에서 안 보임).
> '디자이너 확인 후 유지' 결정이 있어 프런트엔 임의 변경 금지로 적었다 → **촬영 전 사용자 결정 사항.**
>
> **백엔드 몫은 이 대화에서 끝냈다 (`3c1811d`)** — 브리핑 본문에 raw 예외 문자열이 섞이던 것.
> 배포본 실측에서 사용자가 읽는 문장이 `… ClientError: 429 RESOURCE_EXHAUSTED. {'error': …` 로
> 끝나고 있었다(쿼터 소진 상태로 촬영하면 심사위원이 읽는다). `generate_briefing_text` 를 3-tuple 로
> 바꿔 본문엔 사람 말만 남기고 원문은 **`fallback_detail`** 로 분리 + 프롬프트 서사 정정
> (`자율 주식매매 에이전트 서비스` → 402 Guard). **테스트 22종 통과 · red_team `rc=0`.**
> 프런트는 `fallback_detail` 을 `title` 에만 배선하면 된다. **재배포는 프런트 병합 후 1회로 묶는다.**
>
> **★★★★★★ 2026-07-28 오후 — P2-4(저장소 정합성) · P4(최종 버그픽스) 완료. 커밋 12건 전부 푸시.
> 테스트 21종 → 22종 전부 통과 · red_team `rc=0` · cp949 콘솔 재현 통과.**
> 상세는 `docs/next_session_plan.md` 의 P2-4·P4 완료 블록과 `docs/progress_log.md` 2026-07-28.
>
> **P2-4(7건)** — public 전환 시 자해가 되는 것만 골라 닫았다. `differentiation.md` 결함 A~I 9건에
> **상태·해결 커밋 열**(9건 전부 코드에서 직접 확인) · 금지 문장 2곳 교체 · `bug_latest.md` 스냅샷
> 배너 · `FEATURES.md` 자기모순(§1 "G5 완료" vs §3 "HTTP402 0줄") 해소 + 숫자 실측화 ·
> `.env.example` 정정(`ALLOW_LIVE_FROM_WEB` 1→0 등) + 누락 키 4종 · **LICENSE(MIT) 신설**
> (CSV 는 Alpha Vantage 출처라 NOTICE 로 예외 명시) · CLAUDE.md 내부 노트 배너 ·
> `START_GUIDE.md`·`prompts/` 추적 해제. **시크릿 최종 점검 0건.**
> → **남은 것은 사용자의 GitHub public 전환뿐이다.**
>
> **P4(4건)** — **BUG-09**(끝난 세션의 마무리가 다음 세션의 긴급정지를 해제하던 경쟁 상태,
> 회귀 `test_finalize_race` 18건 신설·음성 대조 수행) · **BUG-10**(인트라바 문구가 코드보다 강하게
> 단언 — 문구만 정정, 알고리즘은 사용자 보류 결정대로 유지) · **CODE-06**(가드 정지 사유를 배지·
> 로그에 표시) · **BUG-05**(매도 partial 포지션 미차감 → 총자산 과대계상).
>
> **⚠⚠ 버그 리포트 수정안을 검증 없이 복붙하지 말 것 — 이번에 2건이 틀렸다.**
> BUG-09 의 1순위 안(`finalizing_sid` 캡처 비교)은 session_id 가 초 단위라 무효.
> BUG-05 의 제안(`status in ("settled","partial") and confirmed`)은 **드라이런 정상 매도까지
> 포지션에서 빼지 않게** 만든다(드라이런은 `confirmed=False`) — 회귀 게이트가 아니었으면
> 데모 수익률이 통째로 깨진 채 나갔다. `confirmed` 조건은 partial 에만 건다.
>
> **P1-3**(`e919e8c`) — 대시보드 **'지난 세션' 이력 카드** 신설. 서버에 기록이 쌓여 있는데
> `app.js` 가 `/api/history` 를 한 번도 안 불러서 첫 화면이 전부 0 이었다. 카드는 맨 뒤라
> 첫 화면을 안 밀어낸다. 열은 세션·모드·종목·틱·체결·Gemini 비율·시작시각 7개(유출·실현손익
> 제외 — 전자는 스키마에 없고 후자는 제품 서사와 충돌). **⚠ 갱신은 `ENGINE_STOPPED` 가 아니라
> `renderState` 의 실행→대기 전환에서 한다** — 종료 이벤트는 저장 *전에* 나간다.
> **P1-5**(`31d5361`) — 404 를 사람이 읽는 HTML 로(API·JSON 클라이언트는 기존 그대로) +
> `OPS-06`·`OPS-07`·`UX-09` 를 런북 **§6-2 시연·촬영 운영 규칙**으로 신설
> (`BROKER_HTTP_URL` 배포 env 에 넣지 않기 · 촬영 세션 60~120봉 · 촬영은 라이트 모드 고정).
>
> **✅ 재배포 완료 — 리비전 `synapstock-00014-hft`** (2026-07-28 오후, 검증 통과).
> 실측: 엔드포인트 **11개 200** · `app.js` 에 `fetchHistory`·`lastEngineStatus`·`actor_label` 존재 ·
> `index.html` 에 `data-card="history"`·BUG-10 정정 툴팁 · **404 → text/html + 복귀 링크**
> (`/api/nope` 는 그대로 JSON) · SSE 첫 바이트 **0.067초** · `POST /broker/orders` → **402 + accepts[]** ·
> `state.guard.blocked_unverified`·`state.ai.invoice_semantics` 방출 · `persistence=firestore` ·
> **env 에 `GEMINI_MODEL=gemini-flash-latest` 유지 확인** · 배포본 대시보드에서 **이력 10건 실제 렌더**·콘솔 오류 0.
>
> **잔여**: '여유 시' 버그 3건(BUG-03·06·07, 전부 낮음·심사 노출 경로 없음) · `GAP-10`(업타임 보험).
> **사용자 몫**: GitHub public 전환 · Circle USDC devnet 재실증 · 배포 라이브 세션 · 제출물 3종.
>
> # ▶ 다음 대화 = **P5 프런트 전달본 병합 + 재배포** (7/30 오전, 촬영 직전)
> 전달서(`docs/frontend_p5_handoff.md`)를 받은 프런트가 `web/static/**` 만 고쳐서 보낸다.
> **⚠ 받는 즉시 회귀 대조부터 한다** — 지난 두 번 다 전달본이 저장소보다 **앞선 시점** 기준이라
> 최근 반영분이 옛 버전으로 되돌아와 있었다(지갑 로그인·AI 카드·전략 기본값·체크박스 잠금 조건).
> 이번엔 **촬영 직전**이라 되살릴 시간이 없다. 방법: `git diff` 로 전달본에만 있는 줄과 저장소에만
> 있는 줄을 양방향으로 보고, 후자가 0인지 확인(2026-07-28 에 blob 해시 대조로 했던 절차).
> **병합 후**: dry 세션 스모크(콘솔 오류 0·유출 0.00) → 재배포 1회(런북 §4, `GEMINI_MODEL` 포함) →
> 엔드포인트 실측 → 촬영.
> ⚠ **촬영 전 사용자가 정할 것 1건**: 가드 패널이 첫 줄 카드 3개를 **면적 33%** 가리는 문제
> (위 실측). '디자이너 확인 후 유지' 결정이 있어 프런트에는 손대지 말라고 적어 두었다.
>
> **★★★★★ 2026-07-28 — P1·P2·P3 대부분 완료 + 지갑 잔액 표시. 커밋 9건 전부 푸시.
> 배포 리비전 `synapstock-00013-sgs`. 테스트 20종 → 21종 전부 통과 · red_team rc=0.**
> 상세는 `docs/next_session_plan.md` 최상단 "★ 2026-07-28 진행분" 표.
> 닫은 것: **CODE-01**(브로커 자기신고 partial 매수가 유출 KPI 에서 통째로 빠지던 결함 —
> 음성 대조로 재현 확인) · **CODE-03/07**(쿼터 보류가 '가드 차단' 에 합산되던 것 분리,
> KPI 칸은 4개 유지) · **UX-02/GAP-01**(`#token` 링크 규칙 = 런북 §6-1 신설, 랜딩에 단일
> 공용 인스턴스 고지) · **P3 상업성 정량**(경쟁 지도 1장 신설 → 소개서 13장→**14장**,
> 수익모델 단가 + 12개월 목표 월 $35,000 전부 '가정' 명시) · **P2-3**(소개서 자체발행 고지·
> 사실오류 `USDC 순변화 −4.9`→구매자 **+4.9**·금지 문장) · **DOC-03**(촬영 대본 정본 동기화,
> 6→**7장면**, 축② 근거인 장면 E 신설) · **GAP-07**(README 부서 3종 + 라이브 URL).
> **신규 기능**: 연결된 지갑의 온체인 잔액 표시(`GET /api/wallet/balance`, 헤더 배지 +
> `/connect`). 세션 쿠키의 지갑만 조회(무료 RPC 프록시화 방지) · 실패는 500 이 아니라 error ·
> 20초 서버 캐시(실패도 캐시). **세션 모드와 무관** — 샌드박스 촬영에서도 실제 값이 나온다.
> ⚠ **소개서 `.frame` 은 `overflow:hidden`** 이라 장표를 편집하면 넘침이 조용히 잘린다 —
> 브라우저에서 슬라이드별 실측 필수(이번에 수익모델이 77px 넘쳤다).
> ⚠ **아티팩트 게시본이 저장소보다 뒤처져 있었다** — 소개서·대본 수정 시 같은 URL 로 재게시할 것.
> ⚠ **`8/1 09:00 제출물 리마인드` 예약됨**(scheduled task `402guard-submission-reminder-0801`).
> **Phantom 실확장 검증 완료**(사용자). **남은 것: P2-4(공개 정합성) · P4(버그픽스) · P1-3/P1-5.**
>
> # ▶ 새 대화는 **`docs/next_session_plan.md`** 하나만 읽고 시작한다.
> 남은 작업 전체(P1 계측 잔여 → P2 놓친 것 → P3 상업성 정량 → P4 최종 버그픽스)가 근거·주의사항·
> 채택 금지안·일정까지 자족적으로 정리돼 있다. 아래 블록들은 그 배경 기록이다.
>
> **★★★★★ 2026-07-28 — 프런트 전달본 병합 + 재배포 완료. 리비전 `synapstock-00010-5qb`.**
> 커밋 `b0c0a53`(병합)·`e2be625`(reveal.js 원본 교체)·`ce105fc`(랜딩 모션)·`def229d`(런북)
> 푸시 완료. 상세는 `docs/progress_log.md` 2026-07-28.
> **화면이 바뀌었다** — 얇은 헤더(`.slim-bar`) · 402 Guard KPI 가 카드에서 **떠 있는 패널**(`.guard-dock`,
> 접기 탭·차단 상세 줄)로 이동 · 카드 hover 플로팅 · 스크롤 등장 모션(`web/static/js/reveal.js` 신설) ·
> 대시보드 좌우 여백(`--page-pad`) · 대시보드 ABOUT 제거(랜딩 `#about` 일원화).
> **2차 전달본 병합 `2304658`**(별도 CLI 세션 — AI 카드 '자세히 보기'로 두 피드를 기본 접기,
> 심사 줄 필터, 가드 패널 접힘 미기억). **`LAYOUT_KEY` _v7.** ⚠ 이 커밋은 커밋·푸시까지 끝났는데
> 관제탑 문서에만 안 적혀 있었다 — 2026-07-28 blob 해시 재대조로 **전달본 5개 파일 전부 저장소에
> 포함(누락 0줄)** 확인. 원본은 `docs/design/incoming/` 에 untracked 로 남아 있다.
> ⚠ **전달본은 저장소보다 앞선 시점 기준이라 최근 반영분이 빠져 있었다** — 되살린 것: 지갑 연결
> 로그인(`wallet.js` + 헤더 훅) · **AI 카드**(축② 근거) · `renderAi`·`guard_semantic` · 레그 분해
> (`data-guard-legs`) · `.src-rule-gate` · **전략 기본값 추세추종** · **종목 체크박스 잠금 조건**
> (전달본이 추세추종까지 잠그는 옛 버전으로 회귀해 있었다). **다음에 프런트 파일을 받을 때도 같은
> 대조를 먼저 할 것** — 그쪽은 clone 시점이 고정돼 있어 매번 같은 회귀가 실린다.
> `login.html` 은 전달본 미적용(이메일·비밀번호 폼 회귀 — 사용자 확인). 가드 패널이 첫 줄 카드
> 위쪽 76px 을 가리는 것은 **디자이너 확인 후 유지**.
> 실측 검증: 엔드포인트 11개 200 · 랜딩 `손을 떼려면,` · `persistence.enabled=true` ·
> `state.ai.invoice_semantics` 방출 · SSE 첫 바이트 **0.063초** · 서명 메시지 **`URI: https://`** ·
> 비밀번호 폼 0건 · 콘솔 오류 0 · env 에 `GEMINI_MODEL=gemini-flash-latest` 주입 확인.
> **[해결] 랜딩 등장 모션 누락**(`ce105fc`) — `landing.html` 에 `<script>` 태그가 **하나도 없어**
> reveal.js 가 안 실려 시작 페이지만 모션이 없었다(요소는 그냥 보이므로 안 깨져서 눈에 안 띈다).
> `<head>` 에 한 줄 추가. 대상은 `.hero-copy` 자식 6 · `.hero-preview` · `.landing-about` 자식 4 =
> 11개이고, 흐릿한 미리보기 안 `.card` 들은 이 페이지에 `.grid` 가 없어 대상이 아니다(실측 0).
> ⚠ **`theme.css` 주석은 "랜딩은 자체 style 에서 크고 느리게 덮어쓴다"고 적혀 있는데 실제
> `landing.html` 에 `--stagger-*` 덮어쓰기가 없다** — 지금은 대시보드 값(0.55초·12px)을 그대로
> 쓴다. 값은 디자이너 확인 사항이라 임의로 정하지 않았다.
> ⚠ **모션 검증 시 함정**: 브라우저 패널이 화면에 안 떠 있으면 프레임을 안 그려서 CSS 애니메이션이
> 진행되지 않는다 — JS 로 opacity 를 읽으면 `is-visible` 인데 `0` 으로 보여 "멈췄다"고 오판하기 쉽다
> (이번에 한 번 오판했다). **스크린샷을 찍어 합성을 일으킨 뒤 다시 읽을 것.** 실제 사용자는 화면이
> 보이는 상태이므로 해당되지 않는다.
> **런북 수정**(`def229d`): §4 명령에 `GEMINI_MODEL` 포함 + `--set-env-vars`(전체 교체) vs
> `--update-env-vars`(부분 갱신) 경고. 이제 런북을 그대로 복붙해도 모델이 되돌아가지 않는다.
>
> **★★★★★ 2026-07-27 심야 3차 — 재배포 완료. 리비전 `synapstock-00008-klh`.**
> 실측 검증: 7개 엔드포인트 200 · 랜딩 헤드라인 `손을 떼려면,` · **SSE 첫 바이트 15.06초 → 0.066초** ·
> **서명 메시지 `URI: http://` → `https://`** · 수익률 카드 0건 · 비밀번호 폼 0건.
> ⚠ 런북 §4 명령에는 `GEMINI_MODEL` 이 없다 — `--set-env-vars` 는 **기존 env 를 전부 덮어쓰므로**
> 그대로 쓰면 쿼터가 소진된 `gemini-flash-lite-latest` 로 되돌아간다. 이번 배포는 명령에
> `GEMINI_MODEL=gemini-flash-latest` 를 직접 넣었다. **다음 배포에서도 반드시 포함할 것.**
> ⚠ gcloud 실행 경로: `C:\Users\Tedd\AppData\Local\Google\Cloud SDK\google-cloud-sdk\bin\gcloud.cmd`
> (PATH 에 있으므로 PowerShell 에서 `& gcloud.cmd` 로 호출된다. `Program Files (x86)` 경로는 없다.)
> **[추가] 지갑 서명 형태 4종 흡수**(`3b0a72f`) — `signMessage` 반환이 `{signature:Uint8Array}`(Phantom)·
> raw Uint8Array·base58 문자열·JSON 직렬화 `{0:..,1:..}` 중 무엇이든 통과하고, 64바이트가 아니면
> **무엇을 받았는지 말해 주는** 오류를 던진다. 예전 코드는 `new Uint8Array(값)` 한 줄이라 나머지
> 형태에서 **길이 0짜리 서명**을 서버로 보내 원인 불명 401 이 났다. Phantom 실검증은 시도 기회가
> 사실상 한 번이라 가장 흔한 실패 원인을 미리 없앴다(브라우저에서 4종 왕복 통과 확인).
>
> **★★★★ 2026-07-27 심야 2차 — D-7 품질 갭 감사(5렌즈·48건 검증통과) + 실측 결함 8건 수정. 커밋 `b08bcee`·`b41b688`·`58586c7`·`6d39ee5`·`ddac74b` 푸시 완료.**
>
> **[감사 방법]** 워크플로우 12에이전트·오류 0(UX/문서정합/배포운영/놓친것/코드체감 5렌즈 → 렌즈별 적대검증 → 종합 → 완전성 비평). 원본 결과는 세션 스크래치패드, 계획 요지는 아래.
>
> **[수정 완료 — 전부 배포본 실측 기반]**
> 1. **SSE 첫 바이트 15.06초**(실측 `ttfb=15.064769`) — 엔진 대기 중엔 이벤트 히스토리가 비어 `wait_for(timeout=15)` 로 직행해 본문이 15초간 한 바이트도 안 나갔다. 그동안 대시보드는 "서버 연결 대기…". **심사위원이 URL 을 열고 보는 첫 15초가 죽은 화면이었다.** → `yield ": connected\n\n"`. 수정 후 **0.0024초**.
> 2. **지갑 서명 팝업에 `URI: http://…`**(실측) — Cloud Run 이 TLS 를 앞단에서 끊는데 uvicorn 기본값이 `forwarded_allow_ips=127.0.0.1` 이라 `X-Forwarded-Proto` 를 무시했다. ①https 사이트가 http 서명을 요구(피싱으로 의심할 화면) ②세션 쿠키 `secure` 플래그가 꺼진 채 발급. → `proxy_headers=True, forwarded_allow_ips="*"`.
> 3. **전략 기본값 조건형 → 추세추종** — 조건형은 Gemini 두뇌를 붙이고, 두뇌가 붙으면 의미 대조 계층도 붙는다. **무료 티어 쿼터가 소진돼 있으면 설계대로 매수가 전건 보류**된다(실측 `20260727_130702`: 97건). 아무 설정도 안 바꾸고 시작을 누른 심사위원의 "결제가 실제로 동작하는가"가 쿼터 잔량에 걸리면 안 된다. **AI 판단은 조건형을 고르면 되고 라벨에 명시.**
> 4. **[UI가 엔진보다 좁던 버그] 추세추종이면 종목 체크박스를 잠갔다**(`app.js`) — 2026-07-25 에 추세추종 멀티가 구현됐는데 UI 잠금만 남아 **검증 최고 성적인 3종목 하락장 추세추종을 웹에서 고를 수 없었다.** 엔진이 실제 거부하는 것은 라이브·목시세뿐 → 잠금 조건을 엔진과 일치. 실행 검증: **481틱 완주 +77.55%** (문서 기록값 재현, 가드 114건 검사·차단 0·유출 0).
> 5. **README 대표 명령이 갓 clone 한 저장소에서 크래시**(제출물 ② 재현 요건 직격) — `git clone` 후 `python run_demo.py` → `ValueError: stock_mint 미설정`. 웹 엔진은 `DRY_STOCK_MINT` 로 이미 닫았는데 CLI 경로에 미반영. 함께: `--ticks` 기본 4→**12**(같은 파일이 "최소 9틱 필요"를 스스로 출력하는데 기본값이 4 = 대표 명령이 거래 0건으로 끝나는 데모였다) · 배너 `AutoTrader Agent` → `402 Guard`. **순서 주의: 자리표시자 없이 틱만 올리면 '아무 일도 안 일어남'이 '크래시'로 악화된다.** 검증: 임시 폴더 clone(.env 없음) → rc=0, 틱6 BUY·틱7 BUY·틱9 SELL.
> 6. **`test_intraday`·`test_wallet_auth` 가 cp949 콘솔에서 UnicodeEncodeError** — 두 파일만 `config` 의 인코딩 안전화 경로를 안 탔다. 심사위원이 README 대로 돌리면 결과 대신 트레이스백. → 관례(`test_dca_schedule.py`)대로 `reconfigure(errors="replace")`. **비UTF-8 콘솔에서 20/20 통과 확인.**
> 7. **카피 프레이밍 교체**(벤치마크 §5) — `손을 떼려면, 한도가 있어야 합니다` + 결과절 `감시 없는 자율. 건건이 승인하지 않는 결제.` ⚠ 원안 "승인 없는 결제"는 제품명(승인 게이트)과 자기모순이라 '건건이'로 한정했다. 증명 3줄에서 `한도 초과 불가`(방어형) → **`예산을 코드가 집행`**(1위 수상작조차 온체인 미강제·자기 소스에 `advisory` 명기, 우리는 실제 차감이라 사실로 버틴다). **`유출 0.00` 전 노출 지점에 '서명 전에 가드를 태운 청구서' 한정어 부착.**
> 8. **랜딩 자해 3건** — ①`실시간 데모 미리보기` 라벨이 전부 하드코딩 목업에 붙어 있었다(목업 심사 제외 대회에서 들키면 온체인 증빙까지 의심받는다) → `대시보드 화면 예시` ②`손익 +4.90 · 수익률 +4.9%` 카드 → 가드 로그(차단 사유) ③만들 계획 없는 `곧` 메뉴 3개 제거, 푸터에 devnet explorer tx + 자체발행 고지. ④모바일에서 흐릿한 예시가 카피보다 먼저 오던 순서 되돌림. ⑤`static/login.html` 의 이메일·비밀번호 폼 → `/connect` 리다이렉트("비밀번호를 만들지 않습니다"를 내건 제품이 같은 도메인에 살아 있는 비밀번호 폼을 서빙하고 있었다).
>
> **[점검 결과 — 문제 없음으로 확인]** 무인증 GET(`/api/state`·`/api/history/sessions`) 노출 범위 점검: 시크릿·개인정보 0, 지갑 필드는 공개키뿐(explorer 에 이미 공개). `git ls-files` 시크릿 0건.
>
> **▶ 다음 순서 (2026-07-27 심야 2차 확정)**
> 0. **[즉시] 재배포 1회** — 위 8건 전부가 미반영이다. `docs/deploy_cloud_run.md §4` 한 줄 명령 → §5 로 6개 엔드포인트 200 + 랜딩 헤드라인이 `손을 떼려면` 인지 curl 확인. **Phantom 실검증은 반드시 이 재배포 이후에**(지금 검증하면 `http://` 서명 화면으로 굳는다).
> 1. **[촬영·PDF 전 필수] '검사 몇 종' 숫자 통일** — 지금 심사위원은 PDF 에서 7종·영상 대본에서 6종·README 에서 "6종"이라 쓰고 7개 나열·FEATURES/differentiation 에서 4항목을 **동시에** 본다. 실물은 8종(주문번호·자산·종목·수취인·단위·금액·의도상한·건별한도, `guard.py:154~217`)이라 통일하면 오히려 늘어난다. 치환 12지점 + **`submission.md:202`(영상 낭독 대사)·`:210`(체크리스트)·`README:274·326` 누락 주의** — 화면과 내레이션이 어긋나면 재촬영 사유다.
> 2. **[제출물 ①] 소개서 PDF** — `pitch_deck.html` 에 `print-color-adjust: exact` 없어 '배경 그래픽 켜기' 체크박스 하나에 덱 절반의 색이 걸려 있고, `@media print` 안 `.shot { background: none; }` 때문에 스크린샷 자리가 자리표시 텍스트만 인쇄된다. **결제 통화 자체발행 고지가 PDF 에만 빠져 있다**(README·submission 은 `d5f98e7` 로 이미 밝힘 — 정직성이 셀링 포인트인 제품에서 가장 비싼 불일치). 사실오류 2건: `:528` 'USDC 순변화 −4.9'(구매자 기준 +4.9) · `:422` 주문번호 검사의 '재사용 차단'(실제는 형식·바인딩 확인, 리플레이는 Memo+판매자측 dedup — **계층 합침은 이 팀의 핵심 미덕을 스스로 어긴 사례**).
> 3. **[제출물 ③] 대본 정본화 후 1차 촬영(7/29~30)** — 대본이 2종이고 **타이머가 붙어 실제로 보고 찍게 될 `demo_script.html` 이 구버전**이다(정본 `submission.md §6` 이 나중에 갱신됨). 그대로 찍으면 ①금지 문장("x402 는 파는 쪽을 보호")으로 시작 ②`status.html:487` 이 스스로 '자해였다'고 적은 폐기 KPI(시도 20·차단 6)를 말함 ③**축② 유일 영상 근거인 의미 대조 장면이 아예 없다**(`demo_script.html` 에 '의미 대조|SEMANTIC' 0건).
> 4. **[축① — 유일한 약축]** 감사 계획에도 빠져 있던 것: 축①이 '중'인 사유는 **상업성 정량 근거 0건**인데(경쟁 지도·TAM·SDK 단가 전부 0) 어느 항목도 이걸 안 채운다. `pitch_deck.html:596-616` 에 단가 가설 3줄 + 경쟁 지도 1장("우리가 확인한 범위에서" 한정어 필수).
> 5. **[public 전환 직전] 저장소 정합성** — `differentiation.md §0` 의 결함 A~I 9건이 **전부 현재형**이라 공개 시 "이 팀이 스스로 밝힌 미수정 보안 결함 9건"으로 읽힌다(실제로는 고쳐졌다). `bug_latest.md` 상단에 수정 완료 배너. `.env.example:40 ALLOW_LIVE_FROM_WEB=1`(바로 위 주석·`config.py:83` 기본값과 반대). `FEATURES.md:37`(G5 완료) vs `:125`(HTTP402 코드 0줄) 자기모순. LICENSE 신설.
>
> **[⚠ 심사위원 동시 접속 문제 — 미해결, 구조적]** 엔진이 전역 싱글턴 + `max-instances 1` 이라 한 명이 세션을 켜면 다른 심사위원은 "이미 실행 중"으로 막힌다. 마감까지 멀티테넌트 리팩터는 불가능 → **랜딩에 "단일 공용 인스턴스" 고지로 구조를 숨기지 않고 규정하는 것이 정직하고 싸다.** 아울러 `require_control` 을 dry start 에 한해 여는 안은 **채택 금지**(`/api/engine/stop` 은 여전히 토큰 게이트라 외부인이 세션 하나만 켜 두면 아무도 못 푼다).
>
> **[⚠ 촬영 순서 위험]** 큐시트 G 구간이 explorer tx 화면인데 devnet 아카이브는 **자체발행 민트 결제 1건뿐**이다. Circle 재실증이 1차 촬영보다 뒤면 **심사위원이 영상 속 explorer 에서 민트 권한 한 줄을 보게 된다.** 재실증을 촬영 앞으로 당기거나 G 를 '아카이브 JSON + 고지 자막'으로 대체할 것.
>
> **[버그 판단]** 잔여는 **중간 9건이 아니라 6건**이다(BUG-02·04·08 은 `7a2b251` 로 이미 닫힘 — 리포트가 안 갱신돼 9건으로 보였을 뿐). 그중 데모 노출 경로가 있는 것은 **BUG-09**(끝난 세션의 `_finalize` 가 await 뒤 전역 상태를 덮어써 다음 세션의 긴급정지를 해제 — 대본이 '공격 세션 → 운용 세션'이면 밟는다)와 **BUG-10**(문구 정정 0.2h)뿐. **이번 조사가 새로 찾은 `CODE-01` 이 잔여 6건보다 우선**: 오늘 BUG-01 수정이 매도 레그에만 있던 대칭을 매수 레그에 안 넣어, 브로커가 스스로 `partial` 을 신고한 매수가 유출 KPI·`GUARD_PENDING` 에서 통째로 빠진다(`engine.py:1227 == 'settled'` vs `1339 in ('settled','partial')`). **→ 7/30~7/31 '버그 수정 전용' 새 대화 1회(3.5h): CODE-01 · pause 사유 표시 · BUG-09 · BUG-10.**

> **★★★ 2026-07-27 심야 — 재배포·BUG-01·심사 부서 완주. 오늘 커밋 29건 전부 푸시, 작업트리 깨끗.**
> - **Cloud Run 재배포 완료** — 리비전 **`synapstock-00007-g6f`**. `/`·`/app`·`/connect`·`/login`·`/static/js/wallet.js`·`/.well-known/x402` **전부 200** 실측, `/api/state` 에 `ai.invoice_semantics` 방출, `/api/auth/me` 정상. **AI 카드·지갑 연결·의미 대조가 반영된 현행 버전이다.** 배포 env 에 `GEMINI_MODE=developer` + **`GEMINI_MODEL=gemini-flash-latest`**(쿼터 남은 모델) 주입됨.
> - **gcloud MCP 연결 완료** — 원인은 스코프였다(`C:/Users/Tedd` 홈에 등록돼 이 프로젝트 세션에 안 붙음). 프로젝트 루트 `.mcp.json` 으로 해결, **세션 재시작 후** `mcp__gcloud__run_gcloud_command` 로 실호출 확인.
> - **[BUG-01 높음 수정]**(`2981739`) — `settle()` 이 주식 전달 결과를 버리고 USDC 확정만 보고 `settled` 를 반환했다. 구매자가 USDC 를 보냈는데 주식이 한 주도 안 온 거래가 아카이브에 '성공'으로 남고 앱이 유령 포지션으로 계속 매매했다(매도 레그엔 이미 있던 3분기가 매수에만 빠짐). → settled/partial/failed 3분기 + 미배송 시 `delivered_amount=0`. **함께 닫은 부작용**: `broker_service` 가 non-settled 에 402 를 주는데, 대금이 이미 온체인에서 떠난 partial 에 402(=아직 결제 없음)를 주면 **이중지불을 유도**한다 → **409 `delivery_unconfirmed` + `retryPayment:false`** 로 분기. 회귀 8건 신규.
> - **★ 심사 부서 완주**(에이전트 11/11·오류 0, `docs/reports/judging_latest.md`) — **축① 중 · 축② 강 · 축③ 강 · 축④ 강.** 축②가 '중'→'강'(근거: Gemini 실호출 3지점 · 481봉 대표본 · **AI 가 사실과 다르게 단언한 매수를 코드가 차단한 로그 2건** · 의미 대조가 하드 검사 통과 청구서를 서명 전 차단).
> - **★★ 최대 단일 구멍(축③·④ 공통)**: **devnet 증빙의 'USDC' 가 구매자 지갑이 민트 권한을 쥔 테스트 토큰이다**(민트 `8L9feSS…`, 발행권한 = 구매자 `GQJo2qcr…`, supply 15억 — 부서가 RPC 2회 재확인). explorer 에서 민트 권한 한 줄만 봐도 *"자기가 찍은 돈으로 지불"* 이 성립한다. **절반은 닫았다** — README·submission 에 **우리가 먼저 밝히는 고지** 추가(`d5f98e7`). 기존엔 구매 대상(주식)만 자체발행이라 밝히고 결제 통화는 "USDC(SPL)" 로 무조건 서술해 고지가 비대칭이었다. **나머지 절반 = Circle 공식 민트(`4zMMC9sr…`)로 devnet 재실증**(사용자 실행 구간).
> - **문서가 자기 실물보다 낮게 적혀 있던 것 정정**(부서 표현: "가산점 항목을 스스로 버리고 있다") — submission 라이브 URL "미배포"→배포됨(실측), 소개서 "미착수"→`pitch_deck.html` 실재(PDF 출력만 남음), `FEATURES.md` 축 매핑표 "devnet 증빙 필요(현재 전부 localhost)"→devnet tx 10건 실재.
> - **⚠ 아직 실제 Phantom 확장으로 검증 안 됨** — 지갑 로그인은 브라우저에서 **WebCrypto Ed25519 가짜 provider** 로 왕복 확인했다(서버 검증 40건 통과). 실제 확장의 `signMessage` 반환 형태가 다르면 깨진다. **사용자 브라우저에서 1회 확인 필요(5분).**
> - **⚠ 배포본 온체인 tx 0건** — `/api/history/sessions` 12건 전부 `mode=dry`. 심사위원이 URL 에서 볼 수 있는 것은 402 청구서·서명 전 차단까지이고, 온체인 증빙은 `artifacts/tx/` 아카이브로 **분리해서** 말해야 한다.
>
> **▶ 다음 순서 (2026-07-27 심야 확정 — 디자인 일정 반영)**
> **디자인: 7/29 50% 완성본 · 8/2~8/3 최종본 도착 예정. 마감 8/3 23:59.**
> 1. **[최우선] 제출물 3종** — 없으면 심사 자체를 못 받는다(우선순위 1위). **⚠ 영상·PDF 가 UI 에 종속되는데 최종 디자인이 마감 하루 전에 온다 = 단일 실패점.** → **영상을 두 번 찍는다.** 7/29~7/30 에 현재/50% 화면으로 **제출 가능한 완본을 먼저 만들고**, 최종 디자인이 제때 오면 화면 구간만 재촬영한다. 블로킹 의존성을 선택적 업그레이드로 바꾸는 것이 요점.
> 2. **Circle USDC devnet 재실증**(사용자) — devnet SOL + Circle 파셋 → `setup_devnet`(기본값이 이미 공식 민트, 자체발행 감지 시 중단) → `--live`. 가능하면 **배포 URL 에서** 돌려 `mode=live` 세션 1건을 Firestore 에 남긴다(현재 12/12 dry).
> 3. **Phantom 실확장 검증**(사용자, 5분) → 실패하면 그 대화에서 즉시 수정.
> 4. **카피 프레이밍 교체**(개발, 2.5h) — 문안은 `docs/reports/agentfabric_benchmark.md:99-111` 에 이미 있다. `landing.html:113·132·148`. 축① 직결.
> 5. **랜딩 미리보기의 '손익(실현)+4.90 · 수익률 +4.9%' 카드 교체**(`landing.html:180-183`) — 첫 화면에서 "수익률이 아니라 지출 통제" 메시지와 정면 충돌하는 유일한 지점.
> 6. (여유) '오탐 0' KPI 화면 배선 · 버그 부서 잔여 중간 9건 · 유출 계측 아카이브 미기록(축④ — 화면엔 있는데 tx 파일엔 없어 사후에 '유출 0'을 파일로 증명 못 함).
>
> **사용자만 할 수 있는 것**: 제출물 3종 · Circle USDC 재실증 · Phantom 실검증 · **GitHub public 전환** · 디스코드 공지 재확인(영상 길이 상한·제출 규격·마감 시각·트랙 A~D 명칭) · Gemini 쿼터(모델별 계산이므로 `GEMINI_MODEL` 교체로 회피 가능).

> **★★ 2026-07-27 밤 — 축② 보강 + 지갑 로그인 완료(커밋 6개 전부 푸시: `256077c`·`c9c01b2`·`3408b14`·`28652d5`·`daa7ee1`·`afbd2af`).**
> - **[우선작업 1] gcloud MCP 는 연결돼 있지 않았다** — 등록은 됐지만 스코프가 `C:/Users/Tedd`(홈)라 이 프로젝트 세션에 안 붙었다(`mcp__gcloud__*` 도구 0개). 프로젝트 루트에 **`.mcp.json` 생성** → **세션 재시작 후 적용**. gcloud CLI 자체는 `synapstock` 프로젝트로 인증 정상.
> - **[우선작업 2] 지갑 연결 = 로그인 완료**(`256077c`). 이메일·비밀번호 자리표시 로그인을 **Phantom 지갑 서명**으로 대체. `/connect` 신설·`/login` 도 그리로 라우팅(**옛 `web/static/login.html` 파일은 안 건드림** — 프론트 Phase 5 대상이라 `/static/login.html` 로 계속 열린다). **SIWS `signIn()` 대신 `signMessage`** 를 쓴 이유: signIn 은 지갑이 EIP-4361 ABNF 로 메시지를 스스로 조립해 서버가 재구성·파싱해야 하고 공식 헬퍼가 JS 전용이다. 서버가 정본을 만들어 내려주면 검증이 "문자열 완전 일치 + ed25519" 두 줄로 끝난다(**의존성 추가 0**, solders 만 사용). `scripts/test_wallet_auth.py` 40건. 브라우저에서 WebCrypto Ed25519 가짜 provider 로 실제 JS 흐름 왕복 확인. **⚠ 실제 Phantom 확장 검증은 사용자 브라우저에서 1회 필요.**
> - **[0] 선행 버그 2건 수정**(`c9c01b2`) — ①`guard.py:140` 종목 검사가 **소속 여부만** 봐서 멀티 종목 세션이면 총액만 맞추면 다른 종목 청구서가 통과했다 → `expected_symbol` 추가(`GUARD_SYMBOL_MISMATCH`), **청구서와 견적을 둘 다 대조**(quote 도 브로커가 만든 값이라 그것만 믿으면 금액 검사가 엉뚱한 종목 가격 기준으로 통과), 매도 레그도 대칭 적용. ②`trading_agent.py:488` 의 `expected_order_id=required.order_id` **자기 자신 비교 제거** — 최초 청구서의 주문번호는 브로커 응답에서 태어나 구매자에게 독립 기준이 없다. 실질 바인딩은 온체인 Memo + 정산 후 `check_delivery` 이며 그 사실을 독스트링·주석에 명시.
> - **[1] ★ 청구서 의미 대조 = 축② 핵심**(`3408b14`, `payments/invoice_semantics.py` 신설). 하드 검사 6종은 청구서의 **사람이 읽는 설명**(x402 `description` = `PaymentRequirements.resource`)을 **한 번도 안 읽는다** → 금액·수취인·자산·종목이 전부 정상인데 "본 청구는 12개월 자동 결제 구독의 첫 회차입니다" 인 청구서가 통과했다. **숫자는 하나도 안 틀리고 물건만 다르다 = 정규식으로 못 쓴다.** LLM 은 **차단만** 가능·통과 권한 없음(`_rule_gate` 와 같은 원리). **실패 처리: 매수는 그 건 차단(`GUARD_LLM_UNVERIFIED`)·매도는 하드 검사만으로 진행**, 엔진은 안 멈춘다. **호출 예산**: 결제 시도마다만 돌고 + 숫자를 가린 '서식' 캐시 → 실측 **87건 대조 = LLM 3회**. `scripts/test_invoice_semantics.py` 50건.
> - **[⚠ 부수 발견 — 쿼터 소모 버그]** 테스트 4종(multistock·leak_kpi·dry_sell·http402)이 `brain=auto` 로 세션을 열어 **실제 Gemini API 를 호출하고 있었다**(429 시 조용히 규칙 폴백돼 안 드러남). `brain=rule` 로 고정. `engine.replace_brain()` 신설(두뇌가 판단·의미대조 두 곳에 물려 있어 한쪽만 바꾸면 나머지가 실제 API 를 부른다).
> - **[2] red_team 공격 ①'' 상품 바꿔치기**(`28652d5`) — 리포트가 같은 실행에서 **"하드 검사 6종: 전부 통과 (기존 게이트로는 못 막는다)"** 를 먼저 보인 뒤 의미 대조가 차단하는 것을 보인다. **판정자 정직성**: 기본은 실제 `gemini-flash-latest` 응답을 **녹화 재생**(오프라인·결정론) — 이건 "배선이 판정을 집행하는가"의 증거이지 "모델이 매번 옳은가"의 증거가 아니며 리포트에 그렇게 표기한다. `--live-llm` 로 실모델 판정 가능. **prevent 5건·유출 0.00·정상 14건 오탐 0·exit 0.**
> - **[3] AI 카드 + 가드 로그 배선**(`daa7ee1`) — `state.ai` 를 프론트가 **한 줄도 안 보여주던** 문제를 닫음. 두 레이어를 나란히 배치(판단/청구서). `DEFAULT_LAYOUT` 가드 다음, `LAYOUT_KEY` **_v4**. **[버그] `guard_blocked`·`guard_pending` 이벤트에 로그 처리가 아예 없었다** — 가드가 차단해도 활동 로그에 한 줄도 안 남았다(축④ 직결). **[버그] `.src-rule-gate` CSS 부재**로 배지가 안 보이던 것도 수정(`--badge-gate` 신설).
> - **[4] 축② 서사 문서** `docs/axis2_ai_narrative.md` — 소개서·발표·데모 대본이 여기서 문장을 가져다 쓴다. 예상 반문 5종 답변·정직한 경계선 5건·쓰지 말아야 할 문장 포함.
> - **⚠⚠ Gemini 쿼터 실태(중요)**: `gemini-flash-lite-latest` **일일 500건 이미 소진**(크레딧을 받아도 무료 티어 일일 한도는 안 풀렸다). **쿼터는 모델별로 따로 계산된다** — `gemini-flash-latest` 는 오늘 살아 있었다(단 CLAUDE.md 기존 메모상 20건/일). **[5] 라이브 증빙은 `GEMINI_MODEL` 을 쿼터 남은 모델로 바꿔서** 해야 한다. **그리고 `brain=gemini` 로 세션을 열었는데 그 모델 쿼터가 소진돼 있으면 설계대로 매수가 전부 보류된다**(엔진은 계속 돔) — 실측: 의미 대조 97건 시도 → 전부 검증 불가 → 매수 97건 보류. **수익 시연은 `brain=rule` 로 돌릴 것.**
> - **다음**: 부서 2종 가동 → **제출물 3종(소개서 PDF·저장소 public·3분 영상) = 최우선** → 카피 정정(benchmark §5) → devnet Circle USDC 실증.

> **★ 2026-07-27 — 프론트 병합·재배포·조사 재분석·자해 제거 완료(커밋 5개 전부 푸시: `bdd4c1d`·`1295504`·`c2ffe9a`·`e4f80bd`·`de9e000`).**
> - **프론트 Phase 0~4 병합 완료** — 402 Guard 리브랜딩·가드 KPI 카드 최상단·시안 스킨·`landing.html`. 로컬 검증(2종목 하락장, 틱 481 자동 종료, 콘솔 오류 0) 통과.
> - **첫 화면이 소개 페이지로 전환** — `/`=랜딩 · **`/app`=대시보드(신설)** · `/login`. 옛 `/static/index.html` 도 계속 열린다. 브랜딩 누락 3곳(`login.html` 제목·헤더, FastAPI title=`/docs`, 브라우저 알림)도 `402 Guard` 로 교체. **⚠ 백엔드가 `web/static/**` 4곳을 편집했다**(라우트와 분리 불가) — 프론트 Phase 5 재개 시 `login.html` 은 어차피 재작성 대상.
> - **Cloud Run 재배포 성공** — 리비전 `synapstock-00004-7jc`. **라이브 URL 은 `https://synapstock-766888967498.asia-northeast3.run.app`** (해시형 `synapstock-2phcfrdf3a-du.a.run.app` 도 같은 서비스로 열림). 402·디스커버리·랜딩 전부 정상. **배포본 402 가 광고하는 asset 은 Circle 공식 USDC** (`USDC_MINT` 미주입 → `config.py:44` 기본값).
> - ⚠ **gcloud 실행 팁**: PowerShell 에서 `gcloud` 가 서명 오류로 막히면 `& "…\google-cloud-sdk\bin\gcloud.cmd"` 로 호출한다(정책 무관). 블록 사이에 `cd`·변수가 유지되지 않으므로 **배포 명령은 한 줄에 전부** 담을 것. 런북 `docs/deploy_cloud_run.md`.
> - **[버그 A·B] Gemini 429 처리** (`c2ffe9a`) — 일일 한도(**500건/일**)를 분당 한도처럼 다뤄 `retryDelay`(41초)마다 하루 종일 헛호출했고, 오류가 100자에서 잘려 `quotaId` 가 사라져 원인 추적이 불가능했다. → `quotaId` 에 `PerDay` 면 30분 쿨다운, `_call` 이 91자 요약으로 변환. `scripts/test_quota_cooldown.py` 24건.
> - ⚠ **Gemini 예산 제약**: **일일 500건**. 481봉×2종목 세션 1회 = 962건 = 하루 예산의 2배다. **축② 증빙은 1종목·60봉 정도의 짧은 세션**(약 60건)으로 남기고, 수익률 시연은 `brain=rule` 로 돌려 한도를 아끼는 것이 사용자 합의 방침. 디스코드 크레딧 요청 병행.
> - **[자해 3건 제거]** (`de9e000`) — ①첫 화면 KPI `유출 0.00` 이 **증가 코드가 없는 상수**였다 → `_record_leak` 배선(매수/매도/기준선 3경로 + 브로커 자기신고 `partial` 포함) ②red_team 이 `guarded_leak=Decimal(0)` 하드코딩 + 회수 못한 29.99 를 합산해 되찾은 것처럼 표기 → **3계층 분리**(prevent/counterparty/detect), '② 이중청구'는 판매자측 방어로 재분류(구매자 차단 5→**4**), `[KPI-JSON]` 기계 판독 줄 신설 ③devnet USDC 가 **구매자 지갑이 민트 권한인 자체 발행 토큰** → `setup_devnet` 기본을 Circle 공식 민트로 + **발행권한 검사로 자체발행 민트 감지 시 중단**. `scripts/test_leak_kpi.py` 44건 신규, 테스트 18종 전부 통과.
> - ⚠ **아직 안 닫힌 유출 계측 한계 6건**을 `engine._record_leak` 독스트링에 명시했다(전송 후 예외 경로 미계상·**정지도 안 걸림** / HTTP 원격이 `status` 문자열을 쥠 / 매도 과소계상 / 확인불가와 확정손실 혼합 / 아카이브 미기록 / AP2 한도 부풀림). **"이 숫자가 곧 실제 유출"이라 단정 금지.**
> - **⚠ 재분석 문서 필독**: `docs/reports/competition_research_reconcile.md` — 경쟁 조사의 전제 4건이 뒤집혔다(x402 `maxAmountRequired` 는 **판매자 요구액**이지 구매자 상한이 아니다 / AP2 는 집행 지점을 **규정한다**, 다만 그 주체가 self-custody 에 없다 / **pay.sh 는 필수 아님**(공식 페이지 확인) / 총상금 **미확인**). 카피 정정 문안·8/3 일정 배치·API 레그 판정(26~34h, 축④ 답 못 됨)이 여기 있다.
> - **일정 확정**: 8/3 23:59 **최종 제출 마감** → 8/7 파이널리스트 약 10팀 → 8/10~20 멘토링 → 8/21 데모데이. **제출물 3종(소개서 PDF·저장소·3분 영상)이 8/3 에 확정되고 그게 1차 심사 대상**이라, 프론트를 멘토링으로 미루라는 조사 리포트 권고는 채택하지 않았다.
> - **첫 화면 = 소개 페이지 확정 + 캐시 결함 수정**(`7761026`, 리비전 `synapstock-00006-bdc` 배포 검증 완료). `/`=랜딩·`/app`=대시보드·`/login` 전부 200. **캐시 헤더가 아예 없어서 재배포해도 브라우저가 옛 화면을 계속 보여주던 문제**를 미들웨어로 수정(`/`·`/app`·`/login`·`/static/**` 에 `Cache-Control: no-cache`, API·SSE 는 무영향). 심사위원이 URL 을 한 번 열어둔 뒤 재배포하면 옛 화면을 보던 실제 위험이었다.
>
> **★ 2026-07-27 오후 — AgentFabric 벤치마크 + 축② 방향 확정 (`8f39b68`).** 전문 `docs/reports/agentfabric_benchmark.md` **필독**.
> - **대상**: Cronos x402 PayTech 1위 $24,000(191팀). 조사한 대회 중 **수상 근거가 원문으로 명시된 유일한 사례**.
> - **★ raw 소스로 직접 재확인한 결정적 사실**: `AgentDelegator.sol` 의 `Session` 구조체 전체 필드가 `sessionKey/allowedTargets/allowedSelectors/validAfter/validUntil/active` — **금액도 수취인도 없다.** 그리고 팀이 자기 소스에 주석으로 명기: `Advisory limits (NOT enforced on-chain, just for UI display)`. **1위의 'Max spend' 는 UI 표시용이고, 우리 AP2 예산 차감은 실제로 코드가 강제한다.** ⚠ 단 공격 톤 금지 — `budgetEnforceable` 는 그들이 정직하게 단 플래그다.
> - **겹치는 것 6건**(차별점으로 내세우면 안 되는 것들): 개인키 미접근·1회 서명 후 자율·스코프 위임·x402 레일·**테스트넷 배포**(1위도 테스트넷이 유일 — 우리만의 약점이 아니다)·제출물 구성.
> - **우리 고유 4건**(전부 file:line 근거): 의도 지출 상한 · **매수/매도 양 레그 대칭**(조사 범위 전체에서 대칭 설계 발견 0) · `_rule_gate` · 못 막은 것도 숫자로 표시.
> - **1위가 이긴 진짜 이유는 프레이밍**이다. 같은 대회에서 안전을 '자율성의 조건'으로 말한 팀 $24,000 / '보호막'으로 말한 팀 $5,000 / 'sanity check 도구' $3,000. **현행 우리 카피는 전부 $5,000 자리 문법**(`landing.html:113,132,148` "지키는·지킵니다·초과 불가"). 교체 문안은 benchmark §5.
> - **⚠ 내가 한 번 틀렸다**: "우리 AI 계측이 1위보다 낫다"고 단언했는데 오류였다. AgentFabric 의 `apps/mcp-server` 는 **실제 동작하는 MCP 서버**(Express+OAuth2·툴 레지스트리·Railway 배포)라 심사위원이 자기 Claude 로 직접 호출할 수 있다. **계측은 AI 활용이 아니다.**
> - **축②의 진짜 문제 = 우리 AI 가 설계상 제거 가능하다**(`strategy.brain="rule"` 을 기능으로 출시함). 계측 카드로는 못 메운다.
> - **처방 확정(사용자 결정)**: 스택을 늘리지 않고 **Gemini 가 규칙으로 못 하는 일**을 시킨다 — **402 청구서의 자연어 설명과 주문 의도를 LLM 이 의미 대조**(금액·수취인·자산 다 정상인데 물건만 다른 청구서는 규칙으로 표현 불가). 킥오프 Q&A 가 기준임을 사용자가 재확인(어떤 AI 든 무관·ADK 가산점 아님) → 심사기준 원문의 "Google Cloud AI 스택" 문구는 하드 요건 아님.
> - **실패 처리 설계(확정)**: 전자(fail-open)·후자(fail-closed) 이분법을 버리고 **실패의 단위를 시스템→거래 한 건으로** 내린다. LLM 검사 불가 시 **매수는 그 건 차단**(`GUARD_LLM_UNVERIFIED`)·**매도는 하드 검사만으로 진행**(못 사는 건 기회비용, 못 파는 건 실손실). 시스템은 계속 돈다 — **축④가 "결제를 완료하는지"를 보는데 Gemini 는 오늘 실제로 429 로 죽었고 데모데이는 라이브**다. LLM 은 **차단만 가능·통과 권한 없음**(`_rule_gate` 와 같은 원리 → 이 일관성 자체가 축②의 '짜임새' 증거).
> - **A안(MCP 서버)은 8/10~20 멘토링으로**. 8/7 1차 심사는 PDF·저장소·영상만 보므로 붙여볼 사람이 없고, 8/3 전에 넣으면 영상을 다시 찍어야 한다.
> - **⚠ 새로 발견한 버그 2건(축② 작업 전 선행)**: ①`payments/guard.py:140` 종목 검사가 `allowed_symbols` **소속 여부만** 본다 — 멀티 종목 세션에서 총액만 맞추면 다른 종목 청구서가 통과 ②`agents/trading_agent.py:488` 이 `expected_order_id=required.order_id` 로 **청구서 주문번호를 자기 자신과 비교**(항상 참·무의미).
> - **Gemini 크레딧 수령**(사용자) — 일일 500건 제약이 풀렸는지 라이브 세션 1회로 확인 필요.
> - **디자이너 전달용 컨셉 3줄**(확정): ①AI에게 지갑을 맡기면 사람 없이도 알아서 사고판다 ②그런데 AI는 가짜 영수증을 의심할 줄 몰라 돈이 엉뚱한 곳으로 나갈 수 있다 ③우리는 돈이 나가기 **직전에** 영수증을 검사해 이상하면 아예 서명하지 않는다. → **화면의 주인공은 "얼마 벌었나"가 아니라 "한 푼도 안 샜다".**
> - **다음 순서(사용자 합의, 재정렬)**: ①**축② 보강**(선행 버그 2건 → LLM 의미 대조 → red_team 새 공격 → AI 카드 → 서사 문서화, 6~8h) → ②**카피 정정**(benchmark §5 프레이밍 + x402·AP2·사후구제 3건 + KPI 문구로 어긋난 `differentiation.md:69,87,164,238`·`submission.md:65,66`·`pitch_deck.html:507,508`·`FEATURES.md:123`) → ③**devnet Circle USDC 실증** → ④**제출물 3종**(영향이 가장 크다 — 없으면 심사 자체를 못 받는다). 유출 계측 잔여 6건·MCP 는 8/3 이후.

> **★ 2026-07-27 최신 — 경쟁 조사(해커톤 수상작·실패작) 완료. 전문 `docs/reports/competition_research.md` 를 반드시 먼저 읽을 것.** 조사 전용 대화에서 워크플로우 병렬 분산(에이전트 14·웹 469회·오류 0)으로 수행. **이 대회는 1회차라 수상작·탈락작이 존재하지 않아** 유사 해커톤 12개로 대체 조사했다. 결과는 **아직 판단이 아니라 조사이며, 사용자가 자고 일어나 재분석할 예정**이므로 이 블록의 내용으로 개발 방향을 바꾸지 말 것(사용자 결정 대기).
> - **[대회 자체 신규 확인 — 전부 2차 매체, 공식 재확인 필수]** ①정식 주제가 **"에이전트가 클라우드 API 를 발견·인증·결제"**(공식 예시: Gemini 호출 → 402 → USDC 차감 → API 접근) ②결제 스택으로 **pay.sh** 명시, 매체는 `"Developers must build ... using Pay.sh"` 로 서술 ③**일정이 3단계 — 8/3 은 최종 마감이 아니라 본선 진출 스크리닝, 8/10~20 집중 멘토링, 8/21 데모데이 본선** ④공동주최 **Superteam Korea**, 기관 파트너 **케이에스넷(국내 PG사)**, 심사는 "기술력과 사업성" ⑤총상금 $135,000·트랙 최대 $20,000 ⑥선행 회차 = 2025-04 Seoulana Hacker House(동일 주최진).
> - **[가장 아픈 발견 3건 — 카피 정정 필요]** ①**x402 스펙에 `maxAmountRequired` + 구매자 로컬 서명이 이미 있다** → *"x402 는 파는 쪽을 보호한다"* 카피가 한 줄로 반박된다 ②**AP2 공식 스펙에 `allowed payees` 제약이 이미 정의돼 있다**(SD-JWT/VC) → *"AP2 가 pay_to 를 미검사한다"* 는 프로토콜 공백이 아니라 **우리 구현 미완성**이고, 우리 mandate 는 VC 가 아니므로 **"AP2 준수"가 아니라 "AP2 개념 기반"으로 낮춰야 한다** ③**pay.sh 가 이미 "programmatic spending ceiling" 을 홍보 문구로 갖고 있다**(살아 있는 틈 = 수취인 allowlist·청구서 대 합의견적 대조는 pay.sh 에서 미확인).
> - **[실패작 최대 경고 3건]** **GuardianOS**(ADK 2025, 온체인 승인 게이트+Sepolia 실배포+라이브 URL 갖추고 **무수상** — 용어 과적재) / **x402 Intent Firewall**(Cronos, 개념 거의 동일한데 메인 트랙 아닌 **'Dev Tooling' $3,000**, 1위는 $24,000) / **Latinum**(에이전트 결제 미들웨어, 2025 AI 1위 → **2026 명예언급**, 범주 소진). 순수 방어 프레이밍 천장은 2위(Cronos Shield $5,000), 트레이딩 풀스택 천장은 명예언급(TradeSage AI·Vigil AI).
> - **[반증되어 폐기할 기존 전제 2건]** ❌ **"직전 x402 해커톤 수상작 16건 중 트레이딩 봇 0건"**(재포지셔닝의 출발점 — **출처 미확인 + 반례 3건** Peaks·Clawpump·Agent Arc). 정확한 명제는 *"단일 전략 봇 언어로 말하면 떨어진다"* → **매매 기능을 숨길 필요는 없다.** ❌ "인프라라서 상금이 낮다"(상금표 실측 반증).
> - **[미해소 위험]** 축④ 반문 **"실제로 무엇을 산 건가요?"**(devnet 에 없어 자체 발행한 토큰, 브로커도 자기 프로세스) / **규제 질문**(PG사 파트너 대회인데 리스크 목록에 0건) / 배포본 **`GEMINI_MODE` 누락 시 라이브 URL 축② 증빙 0**.
> - **⚠ 조사 신뢰도**: 12개 해커톤 **전부 프로젝트별 심사평 비공개** → "왜 이겼다/졌다"는 대부분 추정(확정 인용 가능 4건뿐). URL 없이 단언된 제품명 다수 적출됨(CardZero·PlaiPin·x402Resolve 등). **소개서·발표에 사실로 인용 금지.**

> **2026-07-27 — ✅ Cloud Run 첫 배포 성공 + 배포에서만 드러난 버그 2건 수정(커밋 `7d6a178`·`8440a1b`·문서, 전부 푸시).**
> - **라이브 URL** `https://synapstock-766888967498.asia-northeast3.run.app` (프로젝트 `synapstock`, 리전 `asia-northeast3`, 서비스명 `synapstock`). **§5 검증 1~5 + §5-1 전부 통과** — `persistence.enabled=true`(firestore) · 드라이런 체결 · Firestore 문서 · **리비전 교체 후 세션 7건 생존**(tmpfs 이므로 곧 영속 증명) · `POST /broker/orders` → **402**+`payTo` · `/.well-known/x402` 200. 시크릿 5개(지갑 **3개** — `user.json` = AP2 위임장 서명자 추가 — + control-token + gemini-key).
> - **서비스명 `synapstock` 결정(사용자)**: 심사 기준에 이름·URL 항목이 없고, URL 만 되돌리기 비용이 크다(제품명은 마감 직전에도 바꿀 수 있다). **제품명 `402 Guard` 는 화면·소개서·README 가 담당**한다.
> - **[버그] 드라이런 매도 레그 복구** — `engine.start` 가 `live` 일 때만 `STOCK_MINT` 를 검사해 드라이런에서 `None` 이 브로커로 넘어갔고, 매 틱 `ValueError: stock_mint 미설정 — 매도 견적 불가`. **매수만 되고 익절 전무 = 수익률 시연 불가**였다. 로컬은 `.env` 에 값이 있어 안 드러나는 **배포본 전용 결함**. → `DRY_STOCK_MINT = Pubkey.default()` 자리표시(라이브는 fail-fast 유지 = 체인에 닿는 경로 없음, **가드 자산 대조는 그대로 작동**). `scripts/test_dry_sell.py` 14건(결함 재현 음성 대조 포함).
> - **[기능] 판단 두뇌 선택 `strategy.brain`** — "규칙이 AI밖에 없다"는 지적은 버그가 아니라 **기능 미노출**이었다(키가 있으면 무조건 Gemini 고정). `auto`/`rule`(키 있어도 미호출)/`gemini`(키 없거나 초기화 실패 시 **조용한 규칙 폴백 대신 즉시 오류**). `scripts/test_brain_select.py` 10건. **UI 드롭다운은 미포함** — 시안이 `index.html` 을 교체하므로 프론트 병합 대화에서 함께 붙인다(그때까지 API 로 검증 가능).
> - **⚠ 잠복 결함 `GEMINI_MODE` 누락** — `.env` 엔 `developer` 가 있는데 배포엔 없었다. 키가 `AIza` 로 시작하지 않아 **vertex 자동 선택 → 개발자 키로 미작동**. 배포본 세션이 전부 추세추종(Gemini 미사용)이라 아직 안 터졌다. 재배포 env 에 `GEMINI_MODE=developer` 추가.
> - **런북 정정 반영**(`docs/deploy_cloud_run.md`): §3 에 **`roles/cloudbuild.builds.builder`**(없으면 배포가 `could not resolve source: Error 403` 으로 빌드 전에 실패 — 실제 발생) · `cd` 는 **`-LiteralPath`**(경로의 대괄호를 PowerShell 이 와일드카드로 해석 — 실제 발생) · §4 는 검증된 **한 줄 명령** + 저장소 루트 확인 안전장치 · 낡은 "`/secrets` 파일 마운트" 서술을 환경변수 주입으로 정정 · §8 트러블슈팅 4행 추가.
> - **다음 순서(사용자 합의)**: ①**프론트 시안 적용**(최우선 — 심사 최대 갭 해소 + 제출물 스크린샷·영상이 여기에 종속) → ②**로그인+사용자 지갑 통합**(지갑 연결 = 로그인) → ③**제출물 3종**(07-31 착수 마지노선). **⚠ "사용자 지갑으로 거래" 범위 확정: 개인키 서버 등록은 하지 않는다**(규칙 10 위반이자 제품 주장과 정면 충돌). 사용자 지갑은 **위임장에 1회 서명**하고 거래는 에이전트 지갑이 한도 안에서 수행한다(매 거래 사용자 서명 = 자율 결제 전제와 모순). 엔진이 **전역 싱글턴 + `max-instances 1`** 이라 다중 사용자는 리팩터가 필요 → 마감까지는 **"지갑 1개 연결"** 범위.
>
> **2026-07-26 — ✅ 백엔드 심사 축②③ 보완 완료(커밋 8개 전부 푸시: `94fe605`·`3602015`·`70bed10`·`aaaa4d5`·`a953153`·`b1e7620`·`e81a9ce`).** 프론트는 별도 대화에서 병렬 작업 중이라 **`web/static/**` 은 한 줄도 건드리지 않았다.**
> - **[축② 규칙 게이트]** "규칙 신호 없는 개시 금지"를 프롬프트가 아니라 **코드로 강제**. `TradingAgent._rule_gate` 가 AI(`source=gemini`) 판단 중 매수기준(MA5 −N%) 미충족 매수·익절기준(평단 +N%) 미충족 매도를 `hold` 로 강등하고 출처를 **`rule-gate`** 로 남긴다(보류는 항상 통과 = AI 재량은 '멈추는 방향'만). `decide()` 는 `_sanitize`→`_rule_gate` 순서. **지출 3중 통제 완성: 개시=규칙 게이트 / 금액=AP2 mandate / 청구서=402 Guard.** `scripts/test_rule_gate.py` 28건.
> - **[축② 판단 출처 계측]** `engine._ai_stats()`(brain·by_source·by_action·gemini_calls·gemini_decisions·**gemini_gated**·rule_fallbacks·gemini_share_pct·trades_by_decision_source)를 tx 아카이브·세션 요약(Firestore)·**`GET /api/state.ai`(신규 필드, 기존 무변경)**·브리핑에 배선. `run_demo.py` 도 동일. `scripts/test_ai_stats.py` 27건.
> - **[축② 대규모 실측]** TSLA `_bear` **481봉**에서 rule −37.56% vs **gemini −39.57%**(벤치 −20.45%) → **AI 수익 기여는 대표본에서도 중립(약간 열위)**. **★ 규칙 게이트가 실제 2건 발동** — Gemini 가 "MA5 대비 3% 이상 낮아 조건 충족"이라고 **사실과 다르게 단언하며 매수 개시**를 시도(2022-10-24·2023-08-14)한 것을 코드가 차단. 폴백 29건(무료 티어 쿨다운)이라 **90.2%가 순수 Gemini 판단**. 상세 `docs/reports/strategy_validation.md` 말미.
> - **[축③ HTTP 402 레그 G5 완료]** `web/broker_service.py` = x402 자원 서버(`POST /broker/orders` → **402 Payment Required + accepts[]** / `X-PAYMENT` 헤더 시 **200 + X-PAYMENT-RESPONSE**, `GET /.well-known/x402` 디스커버리가 `notImplemented` 선공개). `payments/x402_http.py` = 와이어 포맷+클라이언트 단일 소스. 엔진은 **`BROKER_HTTP_URL` 설정 시에만** 매수 레그를 HTTP 로 보낸다(미설정=기존 인프로세스 그대로). **메인 앱에도 router 마운트** — Cloud Run 이 포트를 하나만 노출하므로 배포 URL 에서도 `curl -i`로 402 확인 가능. `scripts/test_http402.py` 53건 + `scripts/demo_http402.py`(실제 TCP 왕복·증빙 `artifacts/x402_http/`). 데모에서 원격 `payTo` 위조 → `GUARD_PAYEE_UNKNOWN` 차단·**유출 0.00**.
> - **회귀**: 테스트 **14종 전부 통과** + red_team(시도20·차단6·유출0.00·오탐0) 불변. 메인 앱 라우트 스모크 정상.
> - **⚠ 프론트에 전달할 계약 변경 2건**(`docs/frontend_backend_split.md` 반영 완료): ①`/api/state` 에 `ai` 블록 추가(추가만) ②판단 출처에 **`rule-gate`** 값 추가 → `src-rule-gate` 스타일만 넣으면 됨(없어도 안 깨짐).
> - **남은 것(사용자 진행 필요)**: ①**devnet 풀사이클 반복 아카이빙**(SOL 필요) ②**조건형+Gemini 온체인 세션 1회**(축② "온체인이 AI 로 구동됐다" 증빙 — 이제 아카이브에 `ai` 블록이 자동으로 남는다) ③**Cloud Run 배포**(`docs/deploy_cloud_run.md`, §5-1 에 402 확인 절차·§7-1 에 "배포해도 코드 수정 난이도는 안 오른다" 정리) ④(여유) AAPL `_bear` Gemini 백테스트로 표본 추가 — **무료 티어 일일 한도상 하루 1~2회 제한**.
>
> **2026-07-25 — ✅ 심사 부서 실행 + 프론트/백엔드 분리 협업 구조 확립(전부 커밋·푸시, `016b6fd`·`28651ed`·`4260fb6`).** **심사(judge-dept) 결과**: 축③④ '강'(온체인 실증·유출0·exact·Memo·AP2·A2A·Guard 결선 실물, 적대검증 32근거 반려0), 축①② '중'. **최대 갭 = 402 Guard 서사가 첫 화면에 안 보임**(백엔드 `state.guard` 이미 방출 `engine.py:1569` / 프론트 미배선 + 'AutoTrader' 트레이딩봇 브랜딩 = 차별화 전략이 잘 전달되지 않는다는 경고). 리포트 `docs/reports/judging_latest.md`. **사용자 방침 = 프론트/백 분리 개발**(백엔드=사용자, 프론트=다른 작업자·데스크탑 Claude Code·GitHub clone, 각자 작업 후 병합). **협업 계약 3문서 작성**: `docs/frontend_backend_split.md`(⚠경계 = 프론트 **`web/static/**` 만** / 백엔드 나머지, server.py·상태스키마·API 는 백엔드 단독 → 파일집합 분리로 **병합충돌 0**, `GET /api/state` 스키마 계약 동결, dry모드 시크릿없이 프론트 단독실행, 구조정합성 점검표), `docs/frontend_kickoff.md`(프론트 개발자용 시작 브리프 — 시안↔기존 12 data-card 매핑·**메시징 402 Guard 재조정 치환표**·가드 KPI 카드 스펙·작업순서), `docs/design/README.md`(시안 자리). **디자인 시안 도착**(대시보드 mockup, 멀티종목 이전 프로토타입) → **배치·컴포넌트는 채택·카피는 402 Guard 로 치환·가드KPI 최상단 추가**(사용자 강조 = 한도초과 불가·유출0.00·손실 한도격리). **⚠ 사용자가 시안 이미지를 `docs/design/dashboard_mockup.png` 로 직접 저장·커밋해야 함**(Claude 는 채팅 이미지를 파일로 못 씀). **다음 = ①프론트 개발자가 `docs/frontend_kickoff.md` 로 UI 작업(별도 데스크탑 대화) ②백엔드는 새 대화서 심사 축②③ 보완(Gemini 온체인 증빙·brain=gemini 대규모 백테스트·HTTP402 레그 G5·규칙게이트 코드강제). ⚠새 대화 백엔드 작업 시 `web/static/**` 절대 편집 금지(프론트 병렬작업 중).**
>
> **2026-07-25 — ✅ 사용자 요청 5개 성능 개선 완료(커밋 5개 전부 푸시, `794cd0d`·`aaf8acb`·`a63f48e`·`54e6d0d`).** 사용자가 심사 전 세션을 직접 돌려 수익(목표 30%)을 확인하려는 맥락. **적립형(DCA) 임시 비활성화 중**(전략 드롭다운 옵션 disabled — 성능 판단 왜곡 방지, 되돌리기 1줄, **사용자 판단 대기**). 5개: **①속도** 틱 8초→세션 시작 시 선택(초고속0.15/빠름0.3기본/보통1/느림3/발표용8, UI→StartBody.tick_interval_sec→engine 클램프[0.05,60]). **③1/5 크로스** trend_signal 에 `cross_1_5`(가격≥MA5)·`cross_5_20_1_5`(5/20 AND 1/5 둘 다 상승만 보유·하나라도 꺾이면 빠른 청산) 추가(재현 12/12). **④멀티 추세추종** — 올인이 공유예산 독식하던 문제를 **종목별 독립 authorizer(예산/N 슬라이스)**로 해결(`_decide_trend` 올인이 슬라이스만 써 자동 격리, 판단로직 무변경 · 매수/매도 사이클 `self._auth`→`agent.auth` · 예산보고 `_total_remaining/_total_spent` 구별auth 합산·공유모드 무변경 · update_limits 재슬라이스 · 웹 추세 멀티 잠금해제, 라이브만 단일). test_multistock 37건(+6). **②인트라바 합성**(사용자 선택 — 무료 실 인트라데이는 2022 하락장 미커버) `market/price_feed.IntradayReplayFeed` 실 일봉 OHLC 경로로 하루당 N봉(마지막 종가=실 일봉 종가), engine `_replay_feed`(1~12)·웹 봉간격(1/2/4/8)·backtest `--sub-bars`·test_intraday(481일 0불일치). **⑤30% 실증**: 웹 라이브 **3종목 하락장 추세추종(pxma20)=+77.55%**(가드 차단0·유출0·독립 포지션 — 단일 3종목 평균과 일치=격리의 수학적 증거). **⚠정직한 실측**: 1/5·인트라바는 정확히 동작하나 잦은 신호→휩쏘로 수익 급락(TSLA 하락장 5/20: 일봉 +81% vs 인트라바4 −35% / 1/5단독 −40%) → **30%엔 일봉+추세추종+멀티가 최적, 1/5·인트라바는 현실감 옵션**. **⚑ 사용자 결정(2026-07-25): 1/5·인트라바가 "특정 조건(추세가 오래 지속되고 휩쏘가 적은 구간)에서만 유리"한 점은 지금 고치지 않고 보류 — 제거·튜닝 없이 옵션으로 그대로 둔다.** 검증: test_intraday·test_trend(12)·test_multistock(37)·test_timeexit·test_dataquality·red_team(유출0)·test_store 전부 통과, 웹 e2e 콘솔오류0. **다음 = 사용자가 별도 새 대화에서 심사 에이전트 실행(`Workflow({scriptPath:".claude/workflows/judge-dept.js"})`). DCA 복원/유지도 사용자 판단 대기.**
>
> **2026-07-25 — ✅ 멀티 종목(동시 매수) 엔진 구현 완료(드라이+대시보드+백테스트, 전부 검증·푸시, 커밋 8개).** `web/engine.py` 를 단일 심볼 전제에서 멀티로 리팩터(`docs/multistock_plan.md` 계획대로). **설계 핵심 = "하나의 402 Guard 로 다중 지출 통제"**: 공유 1개(mandate `allowed_symbols`=전종목·auth 예산·Guard 수취인 allowlist·broker) 아래 종목별 N개 `TradingAgent`(position·이력·시간청산 **독립**, 모두 같은 trading_kp·auth·guard 공유). **1회 매수=총 spend/N**(DCA 회당도 /N)으로 한 종목의 예산 독식 방지. **방어(계획 공백 보완)**: 추세추종(올인)·라이브·목시세 + 멀티는 거부(예산 독식·온체인 멀티 범위 밖·전종목 동일가격). **N=1 하위호환 바이트 동일**(symbols 미지정=레거시 STOCK_SYMBOL 경로). **배선**: `config.py`(STOCK_SYMBOLS)·`web/engine.py`(`_resolve_symbols`·`_build_symbol_feed`·`_tick_once`/`_process_symbol` 종목순회·오류격리·일부소진 skip·전부소진 종료·`_buy/_sell_cycle` agent 파라미터화·`_valuation` 집계+`_symbol_valuation`·`state_snapshot` symbols[]+per_symbol+집계·update_limits 전종목 auth+가드정합·`start(autostart)` 테스트시드)·`web/server.py`(FeedBody.symbols)·웹 3파일(종목 체크박스·포커스 드롭다운·종목별 요약표·거래표 종목열·판단 종목태그)·`scripts/backtest.py`(`--symbols` 포트폴리오, 엔진 공유예산 모델 재현). **`scripts/test_multistock.py` 31건 전부 통과**(예산 공유·포지션 독립·피드 소진 격리·세션 종료·가드 KPI 합산·하위호환·방어·한도변경 가드정합). **웹 라이브 검증**(dashboard-fast): 3종목 bear 세션 — 종목별 10.00 매수·독립 포지션·공유 예산 차감·유출0·포커스 전환·거래표 종목열·추세/라이브 자동 단일잠금, 콘솔 오류0. **적대적 검증 워크플로우**(9에이전트·3렌즈): 확정 6건(반증0) 전부 수정(가드 mandate 정합·목시세 멀티거부·DCA 문구·거래표 종목열·포커스 되감김·라이브 잠금). **회귀**: 기존 테스트 10종+red_team(유출0) 통과, 단일 경로 무변경. **다음 = 심사 부서(`Workflow({scriptPath:".claude/workflows/judge-dept.js"})`) → 새 대화서 버그 부서(bug-dept.js).** ⚠ 매도 레그 402 Guard 부재(task_373f8717)는 별도 워크트리 진행중이라 병합 시 engine.py 충돌 가능(내 멀티 리팩터가 _sell_cycle 도 손댐).
>
> **2026-07-25 — ✅ 추세추종 전략 모드 앱 구현 완료(사용자 진짜 의도, 전부 검증·푸시).** 아래 "전략 방향 정정"에서 실증한 추세추종을 실제 앱의 새 전략 모드로 배선했다. `Strategy.mode="trend"`(현행 condition/dca 와 **병렬**), 신호 `trend_signal="pxma20"`(가격≥MA20, 기본)/`"cross_5_20"`(골든크로스5/20). **판단은 결정론적 규칙 신호(Gemini 미사용)라 검증이 그대로 재현**된다. 상승세=전량 보유(짧은 하락 무시), 하락세=전량 매도(자본 보존), 재상승=재매수. **올인/올아웃**이라 매도 대금은 운용현금으로 복리 재투자(`ap2 credit_sale allow_surplus`), 건별 한도는 세션 동안 총자산까지 열되(첫 진입 상한=예산, 재진입='가진 현금' 이내) **402 Guard 는 그대로 작동**(수취인 allowlist·청구=합의견적·의도지출 상한·자산 검사). **시간청산은 미적용**(추세 살아있는 한 태움). 배선: `agents/trading_agent.py`(`_decide_trend`·`_trend_ma`)·`payments/ap2_mandate.py`(allow_surplus)·`web/engine.py`(trend 세션·올인 한도·초기자본 대비 수익률·feed dataset bear)·`scripts/backtest.py`(`--strategy trend --trend-signal --suffix`)·웹 3파일(전략·신호·데이터셋 선택)·`scripts/test_trend.py`(재현 검증). **재현 검증(`test_trend.py`)**: 실제 `decide()→broker→auth` 경로가 explore_trend 의 진입/청산 시퀀스를 **봉 단위 정확 일치**(매매수 동일), 수익률 오차 ≤0.08%p(브로커 센트 반올림뿐). TSLA 가격>MA20 탐색 67.84% vs 실제 67.77%, NVDA 145.76 vs 145.79. **백테스트 _bear 벤치마크**: TSLA 가격>MA20 **+67.76% vs 매수후보유 −20.45% = +88.21%p**(폭락 회피 실증). **웹 라이브 검증**: "추세추종"+"하락장" 선택→하락 구간 "하락세 이탈(전량 매도)"→"하락세 관망(현금 보유)"로 자본 보존→회복 재진입, A2A→x402→settled 결제 정상, 가드 차단0·유출0. **전체 테스트10종+red_team(유출0) 통과**. 상세 `docs/reports/strategy_validation.md`. **다음 = 멀티 종목(`docs/multistock_plan.md`, 새 대화)→심사 부서→새 대화서 버그 부서.**
>
> **2026-07-25 — ⚠ 전략 방향 정정: 사용자 진짜 의도는 "추세추종"이고, 하락장 데이터로 실증 완료(커밋 c494470·a2643f7).** 사용자 피드백으로 밝혀진 바: 내가 고도화한 dip3/profit5+시간청산은 **평균회귀(눌림목 매수→짧은 익절)** 로, 사용자가 원한 **추세추종**(상승세엔 짧은 하락 무시 보유, 장기 하락세 확인 시 매도, 추세 전환 시 재매수)과 **정반대**였다. `scripts/explore_trend.py` 로 검증: **상승장(현행 _daily)에선 추세추종이 매수후보유에 전부 패배**(피할 하락장이 없어 늦은진입·휩쏘 손해만), **하락장(_bear.csv=2022 폭락+2023 회복, yfinance 조정일봉)에선 크게 승리**(TSLA 보유 -20.4% vs 가격>MA20 +67.8%[+88%p], NVDA MDD 60.8%→22.9%, 롤링 60/120봉 평균 +3.7~5.9%p·과반승). **핵심 = 어떤 단순전략도 순상승장에선 보유를 못 이기지만, 추세추종은 하락장 회피로 전체 사이클서 우위** → 402 Guard 안전서사와 직결("에이전트가 하락 전 탈출로 자본 보존"). 최선 방식=**가격>MA20** 또는 골든크로스5/20. **평균회귀 개선(dip3/profit5+시간청산)은 폐기 아님**(condition 모드 옵션으로 유지, 무해). **다음 우선순위 = 추세추종을 새 전략 모드(Strategy.mode="trend", 가격>MA20 기본)로 앱에 구현**(현행 condition/dca 와 병렬, 엔진·백테스트·웹선택·테스트, 데모는 _bear.csv 로 폭락회피 실증). 상세 `docs/reports/strategy_validation.md`.
>
> **2026-07-25 갱신 — 전략 수익 검증 + 수익 로직 고도화 완료(사용자 요청, 커밋 b9b8e91·d1b1ff4, 전부 푸시).** 데이터 부서가 건전성 확인한 CSV 로 "짧은 단일종목 세션이 확실히 흑자인가"를 실측(`scripts/validate_strategy.py` 롤링 762구간). **정직한 결론**: 짧은 단일종목은 확실한 흑자 불가(8봉 흑자 37%·26% 무거래), 종목성격이 파라미터 압도, **어떤 설정도 매수후보유 벤치마크를 평균적으로 못 이김** → 제품가치는 알파가 아니라 402 Guard. 유일한 신뢰도 레버=분산(단일 55%→3종목 분산 82% 흑자, 최악 손실 절반). 고도화 탐색(`scripts/explore_strategy.py`)에서 **손절·잦은매매·추세필터·트레일링 전부 반려**(실측 역효과), **시간청산(안전레일)만 진짜 개선**(AAPL 최악 -6.5%→-0.2%). 사용자 결정=최고수익 스택 **dip3/profit5 + 시간청산 10봉** 채택(분산 흑자 85.8%·평균 2.11%). **앱 반영**: `config.MAX_HOLD_BARS`·`Strategy.max_hold_bars`(조건형 decide 백스톱, 규칙/Gemini보다 우선)·`web DEFAULT_RULES` dip3/profit5·backtest 동기화·목 시세 경로 재설계(옛 절대가용 얕은딥→dip3 트리거, 회귀 해소)·단위테스트 `test_timeexit.py` 8건. **검증**: 전체 테스트+red_team(유출0) 통과, **실제 decide() 경로가 탐색값 정확 재현**(전체 흑자 57.9%·평균 2.10%), 목 폴백(web·run_demo) 매매 정상. 리포트 `docs/reports/strategy_validation.md`. **⚠ 파라미터 변경**: 웹 replay 세션·backtest 기본이 이제 dip3/profit5 + 시간청산10(거래빈도↓·수익·하방↑). run_demo 는 자체 dip2/3 유지(목 CLI 데모는 --ticks≥9 필요, 선재 이슈). **멀티 종목 범위 확정(사용자)**: 드라이+대시보드+백테스트(라이브 온체인 제외).
>
> **다음 단계(사용자 9단계 계획 기준, 2026-07-25 재정렬):** ①②④ 완료. **③멀티 종목(동시 매수) 엔진 = ✅ 완료(2026-07-25, 커밋 8개 전부 푸시 — 위 "2026-07-25 최신" 블록 참조. test_multistock 31건·웹 3종목 검증·적대검증 6건 수정·회귀 무). 범위: 드라이+대시보드+백테스트(라이브 온체인 제외).** → ⑤미완성 기능 판단(아래) → ⑦심사 부서 가동(`Workflow({scriptPath:".claude/workflows/judge-dept.js"})` — 빠진부분·수상가능성·보완점) → ⑧강력 보완 → ⑨**새 대화**서 버그 부서(`Workflow({scriptPath:".claude/workflows/bug-dept.js"})`). **⑤ 미완성 기능 판단(2026-07-25)**: 로그인 실인증(껍데기 유지·제출후 로드맵)·주간/월별 수익(Firestore 배포 후)·B1/B6 대화형(비핵심)·G6 pay.sh(여유시)는 **전부 지금 불필요**(402 Guard 축에 기여 낮음·마감 대비). 시간청산이 B4 리스크가드의 핵심을 이미 대체. G5(브로커 HTTP 402)만 축③(인프라)에 소폭 기여하나 여유시. **결론: 멀티 종목 외 미완성 기능은 즉시 구현 불필요, 심사 부서 결과로 재확인.**

## 현재 상태 (2026-07-23 심야 기준)

> **2026-07-25 갱신 — 데이터 수집 부서(data-dept) 구축 완료(트랙 B 에이전트 부서 3/3).** 템플릿(judge-dept·bug-dept: 결정론 수집기 + 워크플로우 + 자기검토)에 신규 스킬 2종을 접목해 강화. **ai-regression-testing**: 독립 재파싱이 아니라 **실제 소비경로 `market.price_feed.load_bars()` 로 로드**해 "데모·백테스트가 진짜 이 CSV 를 소화하는가"를 대조(사각지대 제거) + 판정 로직 회귀 테스트. **agent-self-evaluation**: 자기검토를 5축 스코어카드(정확성·완전성·명료성·실행가능성·간결성, 축별 증거 필수)로 강화. 산출물: `scripts/collect_dataquality.py`(결정론 수집기·순수 판정함수 — 소비호환·스키마·OHLC정합·연속성·이상치·충분성·신선도 7차원) · `scripts/test_dataquality.py`(28건 통과) · `.claude/workflows/data-dept.js`(Scan→Inspect[심볼별 팬아웃]→Verify[건별 적대검증]→Report[5축 자기평가]). 범위(사용자 확정): data/market/*.csv·읽기 전용(자동 수집 없음, `--fetch` 미래 여지)·테스트 추가. **실제 1회 구동 검증**(26에이전트·오류0·156만토큰): 실제 이슈 0건·관찰 20건(낮음2=MA200 여력·신선도 2거래일, 정보18)·자기평가 4.2/5, 4일 캘린더 갭 4건을 미 증시 공휴일로 정확 분류, git 작업트리 읽기 전용 준수(CSV·앱코드 무변경) 확인. 리포트 `docs/reports/data_latest.md`, 재실행 `Workflow({scriptPath:".claude/workflows/data-dept.js"})` = "데이터 부서 돌려줘". **트랙 B 에이전트 부서 3종(심사·버그·데이터) 전부 가동.** 다음: 사용자 지정(디자인 시안 도착 시 스킨 적용 / Cloud Run 배포 / 제출물 초안).
>
> **2026-07-25 새벽 갱신 — 402 Guard 버그 수정 라운드 완료(핵심 4건 BUG-01~04 + 적대검증 후속 1건, 전부 푸시).** 버그 부서가 찾은 402 Guard 본체 결함 4건을 재현→수정→회귀→커밋→푸시(버그당 1커밋)로 처리. **BUG-02**(90c5c33) 매도측 배송검증 추가(settle_sale 지급확정까지 봐야 settled·실패=partial, engine._sell_cycle 에 매수측 대칭 check_delivery + 세션정지) · **BUG-03**(74442df) 브로커 부풀리기 차단용 의도 지출 상한 GUARD_INTENT_EXCEEDED(가드 새 차단코드 7종, build_payment/engine/run_demo 가 decision.spend_usdc 전달, red_team 을 실흐름으로 재작성) · **BUG-01**(2b4014d) get_token_balance_base 가 계정미존재만 0·불명오류 전파(_is_account_not_found) + engine._buy_cycle 기준선 실패 시 예약원복·정지 · **BUG-04**(1f76aa6) ALLOW_LIVE_FROM_WEB 기본 1→0(기본 차단). **마무리 적대검증**(워크플로우 13에이전트, 버그×3렌즈+종합)이 BUG-03 에서 실회귀 1건(의도 상한 허용치 1센트 고정이 고수수료+sub-cent 지출에서 정직 견적 오탐) 적발 → **05fe683 로 허용치 2센트 정정**(재검증: 정직 16,800건 오탐0·공격 여전히 차단·테스트6종+red_team 오탐0). **⚠ 운영 변경: 웹 UI 에서 라이브(온체인) 세션 시작 시 이제 ALLOW_LIVE_FROM_WEB=1 필요**(dry 개발·run_demo CLI devnet 증빙은 무영향). 상세 progress_log. 다음: 트랙 B 3번(데이터 수집 부서) 또는 사용자 지정.
>
> **2026-07-24 심야 갱신 — 트랙 B 에이전트 부서 1·2번 완료(심사용·버그전담).** 버그 부서(`bug-dept`: `scripts/collect_bugscan.py` + `.claude/workflows/bug-dept.js`, **읽기 전용 — 찾기만·자동수정 없음**) 첫 스캔에서 확정 13건 중 **핵심 4건이 402 Guard 본체 결함**(제가 실소스 독립확인). ⚠ **수정 여부·순서는 사용자 판단 대기**: BUG-01(높음 — `x402_solana.py:309` get_token_balance_base 의 except→0 이 check_delivery 기준선 오염→미배송 오탐통과) · BUG-02(높음 — `broker_agent.py:192`/`engine.py:856` 매도 정산 배송검증 부재→USDC 지급실패해도 settled) · BUG-03(중간 — `guard.py:134` GUARD_AMOUNT_MISMATCH 가 required·guard expected 가 동일 브로커 quote 라 실 매수흐름 발화불가; red_team 은 인위주입으로 연출; 빈틈=브로커 총액 vs 사용자 의도 spend 미대조, 단 수취인 allowlist·건별한도는 실작동) · BUG-04(중간 — `config.py:73` ALLOW_LIVE_FROM_WEB 기본 열림, 주석·런북과 반대). 리포트 `docs/reports/bug_latest.md`, 재실행 `Workflow({scriptPath:".claude/workflows/bug-dept.js"})`. 다음 부서: 데이터 수집. 상세 progress_log + 아래 다음단계 블록.
>
> **2026-07-24 밤 갱신 — devnet 라이브 실증 완료(첫 온체인 증빙) + 로그인 껍데기.** 실데이터 재생(AAPL 06-01~07-22) devnet 라이브: 매수 4·매도 1 = 온체인 tx 10건 확정, explorer(cluster=devnet), 교차검증 PASS(실현 +4.9 USDC 온체인 반영), 증빙 `artifacts/tx/20260724_1643_solana-devnet_live_buy.json`. 공용 RPC 의 파우셋 차단·429 를 넘김(setup_devnet 여유지갑 자동충당+에어드랍 생략+백오프 재시도, payments/x402_solana `rpc_retry`, run_demo `--replay SYMBOL --from --to`). 로그인/회원가입은 **버튼·페이지 껍데기만**(대시보드 비차단, 실인증은 제출 후 self-custody 로드맵 — 사용자 결정). **다음(2026-07-24 밤 재정렬, 사용자 지시): ①검증·버그 수정 라운드 ②에이전트 부서 구축(402 Guard 역할별 + 심사용·버그전담·데이터수집 부서) ③제출물 초안은 디자인 완성 시점(~2026-07-31)으로 이동 ④Cloud Run 배포(사용자 gcloud)·G5. 상세는 아래 '다음 단계' 재정렬 블록.**

> **2026-07-24 저녁 갱신 — 402 Guard P0 전부 완료(G0~G4, 전부 푸시).** 새 대화는 `docs/handoff.md` §0 을 먼저 읽을 것. 요지: G1 `payments/guard.py`(check_demand 4항목+check_delivery), G2 결선(authorize 앞 guard·allowed_asset 살림·release 한도 원복), G4(Memo 바인딩·exact `!=`·서명 dedup·expires_at), G3 `scripts/red_team.py`(공격3종 매트릭스·정상 오탐0). **localnet 풀사이클 라이브 재검증 PASS**(교차검증 USDC/주식 일치). 부수: setup_devnet cp949 수정 + 브로커 USDC 운용자본 지급. 킥오프 발표자료를 `docs/differentiation.md §7`·`docs/submission.md §2-3`에 반영. 다음 후보: devnet 실증 → 제출물 초안 → (여유 시 G5 HTTP 402) → Cloud Run 배포.

완료(localnet 라이브 검증 통과): A2A+x402 **매수·매도 풀사이클**(USDC⇄주식토큰 양방향 온체인 정산, 순변화 교차검증 PASS), AP2 mandate(한도 + 매도 대금 환입 = 순투입 한도), **Gemini 판단**(`gemini-flash-lite-latest`, 무료 티어, 호출 실패 시 규칙 폴백), **거부 4종 데모**(`scripts/demo_rejections.py`), 증빙 아카이빙(`artifacts/tx/`), **P1 웹 서비스화**(`web/`: FastAPI+SSE, 실행 `python -m web.server`, `--port`/`PORT` env 지원), **P2 엔진 확장 전체**(수수료 투명화·한도 설정·알림·DCA·데일리 브리핑), **실시세 라운드 코드 전체**(2026-07-23 저녁, 웹 드라이런 검증 PASS): `ReplayPriceFeed`(실데이터 일봉 1틱=1봉 재생·워밍업 20봉·소진 시 자동 종료, UI 피드 선택·날짜축·전일 종가 대비), **지표 규칙**(매수 MA5 −2% / 매도 평단 +3% 익절 — 절대가 178/185 제거), **Gemini 입력 보강**(MA5·MA20·등락률·변동성·평단 손익률·직전 행동 회고) + **엄격/추세 모드 토글**(추세=보류 재량, 백테스트에서 재량 보류 실증), **백테스트 러너**(`scripts/backtest.py`, 규칙 vs Gemini 2모드 비교), 디자이너 브리프(`docs/design_brief.md`, 간소화 버전 발송 완료 — 시안 주말 예상). **실데이터 실측 완료(07-23 밤)**: AAPL·TSLA·NVDA 최근 100봉 커밋(무료 티어는 compact만 — full 은 프리미엄 전용으로 정책 변경 실측), AAPL 06-01~07-22 웹 드라이런 실현 +4.30 USDC(+4.78%, 4회 분할매수→익절), 같은 구간 3종 백테스트 커밋(규칙 +4.90%/MDD 7.31 · 엄격 +4.29%/폴백0 · 추세 +1.65~2.90/MDD 5.00 — 보류 재량으로 낙폭 축소). ※백테스트 Gemini 런은 **동시 실행 금지**(429 충돌로 폴백 오염 — 순차 실행). **대시보드 모듈화(07-23 밤)**: 기능 전부 data-card 모듈 11개, 기본 배치는 app.js `DEFAULT_LAYOUT` 배열 하나(디자인 시안 배치를 배열 수정으로 적용), 카드 제목 드래그 재배치+localStorage+배치 초기화, 세션 설정 카드 신설·상단바 슬림화. **TA 판단 보강(07-23 심야, 상세 `docs/ta_upgrade.md`)**: `market/indicators.py` 신설(MA 1~200일·크로스·기울기·지지/저항 2터치·차트/캔들 패턴 — 상승 쐐기=매도 정정, 매수 100%=역H&S·삼중바닥) → Gemini 프롬프트 TA 블록 + 규칙 폴백 거부권(사지마·팔지마·대기패턴), 세션 TA 토글(**기본 OFF**)·차트 MA 1(종가선)/5/10/20/50·백테스트 `--ta`, 단위테스트 44건. 실측(AAPL 36봉): **Gemini 엄격+TA +4.95% 최고**(무TA +4.29%) · 추세+TA MDD 4.84% 최저 · 규칙+TA 는 거부권이 매수 전부 차단(0%) → 권고: 기본 OFF 유지, Gemini 세션은 ON(데모 포함). 미탐지 TODO 깃발·다이아몬드·역V자. 검증 중 수정: web/server.py pydantic StrategyBody 미선언 필드 무시 버그. **Cloud Run 배포 준비 완료(07-23 심야, 코드·문서 전부 — 배포 실행만 사용자 gcloud 대기)**: Firestore 영속화(`web/store.py` — 로컬 기본 OFF no-op, FIRESTORE_ENABLED=1 시 세션 요약(dry 포함)·체결·브리핑·한도 기본값 저장 + 부팅 복원 + `/api/history` 4종, FakeStore 테스트 18건·장애 내성·서버 스모크 통과), Dockerfile(3.11-slim·TZ=Asia/Seoul·PORT 계약 실측)+.dockerignore, `docs/deploy_cloud_run.md` 런북(SDK 설치→Firestore 생성→Secret Manager 지갑 파일 마운트+GEMINI_API_KEY env→SA 권한→deploy 플래그 근거: **max-instances 1(전역 싱글턴 엔진) + no-cpu-throttling(백그라운드 틱 루프) 필수**→재시작 영속 증명 절차). 부수: 콘솔 print cp949 안전화(em-dash 함정 — test_dca_schedule 크래시 해소). GitHub 푸시(https://github.com/Loda1002/SolanaAgent, private — 제출 전 public 전환).

**배포 전 종합 검토 완료(2026-07-24) — 반드시 `docs/preflight_review.md` 를 먼저 읽을 것.** 다중 에이전트 라운드(웹 조사 7축 + 코드 감사 64건 + 적대적 검증 32건)로 얻은 결론과 수정:
- **실현가능성**: 토큰화 주식은 **메인넷에 실재**(xStocks 민트 온체인 확인, Jupiter 견적이 Pyth 가격과 0.4% 이내 일치)하나 **devnet 에는 없음**(독립 2인 검증) → 현 자체발행 토큰 설계 유지가 정답이고, "민트 상수·스왑 경로 2곳만 교체하면 실물"을 로드맵으로 제시한다. **permanentDelegate·pausable 등 발행사 사후 통제권이 상주하므로 "퍼미션리스" 라고 단정하지 말 것.** 규제: 토큰화 주식=증권(SEC 2026-01-28), 한국 제도 시행 2027-02-04, 자동매매는 반드시 **self-custody 도구**(사용자 지갑·사용자 한도·즉시 정지)로 설명 — "우리가 돈을 굴린다"로 말하면 무등록 투자일임 리스크.
- **전략 실측(백테스트에 매수후보유 벤치마크 신설 후, 80봉)**: AAPL +4.90% vs 벤치 +30.19%(**−25.29%p 열위**) · TSLA +11.22% vs +2.73%(**+8.49%p 우위**) · NVDA +20.15% vs +25.82%(−5.67%p). 눌림목 매수(평균회귀)라 강한 상승추세에서 구조적으로 진다. **손절이 코드에 없어 승률 100%는 착시**, Gemini 는 규칙 신호 위에서 보류만 가능해 **심사 2축(AI 활용도) 정량근거가 0**. → 데모 대표 종목은 TSLA 로, 손절·트레일링 + Gemini 사이징 재량이 다음 작업(사용자 승인 완료).
- **수정 완료**: 심사 재현 차단 2건(README 대표 명령 cp949 크래시 · 백테스트 날짜 불일치), 공개 배포 차단 5건(무인증 조작 API → CONTROL_TOKEN 게이트 / 한도 무제한 상향 / Infinity·NaN / feed 경로주입 + extra=forbid / 지갑키 마운트 무증상 실패 → env 주입 + 라이브 fail-fast).
- **⚠ 재포지셔닝 확정(2026-07-24) — `docs/differentiation.md` 필독**: "AI가 알아서 매매한다"로 나가면 흔한 트레이딩 봇으로 분류돼 차별점이 묻힌다. (⚠ 당시 근거로 적었던 "직전 x402 해커톤 수상작 16건 중 트레이딩 봇 0건"은 **2026-07-27 조사에서 출처 미확인 + 반례 3건으로 반증됐다** — 인용 금지. 재포지셔닝 결론 자체는 유지한다.) → **"402 Guard — 에이전트 지출 승인 게이트"**로 전환. *"x402 스펙은 파는 쪽을 보호한다. 우리는 사는 쪽(사람 없이 결제하는 AI 에이전트)을 보호한다."* 근거는 저장소에서 직접 재현한 실제 결함: 구매자가 브로커 청구서를 무검증 서명(`agents/trading_agent.py:330-346`), AP2 `authorize()`가 `pay_to`를 받고도 미검사(`payments/ap2_mandate.py:112-129`), `allowed_asset`이 죽은 필드, `exact`인데 금액 비교가 `<`, Memo·HTTP 402 부재, **에이전트가 자기 허가서를 자기 키로 서명**(규칙 10 위반). → 악성 브로커가 한도 안쪽 금액으로 청구하며 수취인만 바꾸면 통과된다. **제출물 제품명은 `402 Guard`** ("AutoTrader"를 제목 첫 단어로 두지 말 것). 첫 화면 KPI는 수익률이 아니라 `시도·차단·유출 0.00 USDC·오탐 0`.
- **새 대화는 `docs/handoff.md` → `docs/differentiation.md` → `docs/preflight_review.md` 순으로 읽고 시작한다.**
- **공식 심사 기준 확보(사용자 제공, 2026-07-24)**: ①혁신성·UX ②AI 활용도(Gemini/Google Cloud AI 스택·에이전트 프레임워크 포함) ③기술 완성도 및 블록체인·인프라 연동(**"USDC, Solana Pay, pay.sh 등"과 "AP2, A2A, x402 등"을 병렬 예시로 나열** → pay.sh 필수 아님, 우리 자체 x402+AP2+A2A 스택이 그 자체로 정합) ④실제 구동(로컬넷/테스트넷/데브넷 라이브). **결론: "자체 x402 vs pay.sh 충돌" 우려는 기각.** 단 pay.sh 는 디스코드 전용 지원 채널이 있어 붙이면 3축 가점 — `docs/differentiation.md` P1(G6)으로 유지(여유 시). ②축이 "Google Cloud AI 스택"을 명시하나 제약 7(과한 ADK/Vertex 는 가점 아님)과 균형 유지. 남은 미확인: 제출물 세부 규격·데모 영상 길이 상한·마감 시각은 디스코드 공지로 재확인 권장.

다음 단계(우선순위 순) — **상세는 `docs/preflight_review.md` §8, 이전 계획은 `docs/next_round_plan.md`**:

> **▶ 2026-07-24 밤 재정렬 (사용자 지시) — 이후 이 순서를 따른다. 각 항목은 새 대화에서 착수:**
> 1. **검증·버그 수정 라운드** — ✅ **완료(2026-07-25, 커밋 90c5c33·74442df·2b4014d·1f76aa6·05fe683)**. 버그 부서 리포트 핵심 4건(BUG-01~04, 402 Guard 본체) + 적대검증 후속 1건 수정·재검증·푸시. 상세는 위 현재 상태 블록 + progress_log. (버그 부서 저심각(낮음8·정보1) 잔여건은 미착수 — 필요 시 후속 라운드.)
> 2. **에이전트 부서 구축 (트랙 B 먼저 — 2026-07-24 사용자 확정)** — **개발/평가 지원 에이전트**(최종 앱에는 안 들어가고 개발·자가평가를 돕는 조직)로 먼저 구축. **구현 형태 = 워크플로우**(2026-07-24 인터뷰 확정: 서브에이전트식 온디맨드 호출 + 팬아웃·적대검증 결합, 토큰 감수하고 검증 허점 0). **모든 리포트는 제출 전 자기검토 단계를 거친다.**
>    - ①**심사용 부서** ✅ **완료(2026-07-24, 커밋 b00f249)** — `scripts/collect_evidence.py`(결정론적 증거수집: 테스트·red_team KPI·tx·백테스트·guard 마커, LLM·추정 0) + `.claude/workflows/judge-dept.js`(증거수집→축별 심사 팬아웃4→적대 검증→종합→자기검토→리포트). **재실행**: `Workflow({scriptPath:".claude/workflows/judge-dept.js"})` — 사용자가 "심사 부서 돌려줘"라고 하면 이 한 줄(하니스가 `.claude/workflows/*.js` 를 name 자동등록 안 함). 리포트 `docs/reports/judging_latest.md`, 부서 문서 `docs/reports/README.md`. 심사 기준 출처=킥오프 전사(hackathon_essentials §1 + FEATURES §5), 신청 페이지 상이점은 리포트 "기준 불일치" 섹션.
>    - ②**버그 전담 부서** ✅ **완료(2026-07-24, 커밋 87f6c9b·1b1d396·36384a2)** — `scripts/collect_bugscan.py`(결정론적 버그표면: git churn·테스트6종·red_team KPI·coverage_map[미커버 briefing/server]·risk_markers[bare/broad except·TODO·noqa·assert·payments float, file:line]·module_index, LLM·추정 0) + `.claude/workflows/bug-dept.js`(Scan→Hunt 모듈그룹4 팬아웃[4렌즈]→Verify 건별 적대검증[기본 real=false·python -c 재현]→종합→자기검토6항목→리포트). **읽기 전용 — 코드 자동수정 없음(수정은 승인 후 별도)**. **재실행**: `Workflow({scriptPath:".claude/workflows/bug-dept.js"})` — "버그 부서 돌려줘"=이 한 줄. 첫 실행(에이전트22·오류0): **확정 13건**(높음2·중간2·낮음8·정보1)·반려2, 핵심4건 실소스 독립확인(BUG-01 check_delivery 기준선 오염·BUG-02 매도 배송검증 부재·BUG-03 GUARD_AMOUNT_MISMATCH 실흐름 발화불가·BUG-04 ALLOW_LIVE_FROM_WEB 기본역전). 리포트 `docs/reports/bug_latest.md`, 부서 문서 `docs/reports/README.md`. **핵심4건은 402 Guard 본체 결함이라 수정 여부는 사용자 판단 대기.**
>    - ③**데이터 수집 부서** ✅ **완료(2026-07-25)** — `scripts/collect_dataquality.py`(결정론 수집기 — 시세 CSV 를 **실제 소비경로 `load_bars` 로 로드 대조** + OHLC정합·스키마·연속성·이상치·충분성·신선도 7차원, 판정은 순수 함수로 분리해 테스트가 직접 호출) + `scripts/test_dataquality.py`(탐지기 단위테스트 28건, 불량 입력 전수 검출) + `.claude/workflows/data-dept.js`(Scan→Inspect[심볼별 팬아웃]→Verify[건별 적대검증]→Report[5축 자기평가]). **읽기 전용 — 수집·수정 없음(범위: data/market/*.csv, 사용자 확정)**. 신규 스킬 접목: ai-regression-testing(실소비경로 대조·회귀 테스트)·agent-self-evaluation(5축 자기평가). **재실행**: `Workflow({scriptPath:".claude/workflows/data-dept.js"})` = "데이터 부서 돌려줘". 첫 실행(26에이전트·오류0): 실제이슈0·관찰20·자기평가4.2, 리포트 `docs/reports/data_latest.md`, 부서 문서 `docs/reports/README.md` 부서3. **트랙 B 에이전트 부서 3종(심사·버그·데이터) 전부 가동.**
>    - **제품 런타임 에이전트(트랙 A — 402 Guard 앱 안 리서치·리스크·실행)는 지금 만들지 않고, 트랙 B 가 안정화된 뒤 '혼합' 여부를 별도 논의**(마감·402 Guard 초점·제약 7 고려).
> 3. **제출물 초안** — **디자인 완성 시점(~2026-07-31)으로 이동**(완성본 아니고 마감까지 시간 여유). README 402 Guard 재작성·`docs/pitch.md` 소개서·데모 대본 3분. 디자인 스킨 반영 후 확정.
> 4. **Cloud Run 배포**(사용자 gcloud, 런북 `docs/deploy_cloud_run.md`) · **G5**(브로커 HTTP 402, 여유 시).
> (아래 기존 디자인 일정·1~4 항목은 배경 참고. devnet 실증은 2026-07-24 완료.)

> **디자인 일정 변경 (2026-07-23 밤 통보): 시안이 약 5일 뒤(≈07-28) 도착.** 마감(08-03)까지
> 6일만 남으므로 **07-24~07-28은 디자인과 무관한 작업만** 하고, 시안은 도착 즉시 "스킨 교체"로
> 흡수한다(창구 2개라 반나절 규모). 시안이 필요한 작업을 기다리며 일정을 비우지 말 것.

1. **Cloud Run 배포 실행(사용자)**: 코드·런북은 완료 — 사용자가 `docs/deploy_cloud_run.md` 순서대로 실행(gcloud SDK 설치부터, 인증·과금 명령은 사용자 몫). 배포 후 런북 §5 재시작 영속 증명까지 확인·스크린샷. 문제 나면 §8 트러블슈팅 → Claude 에게 로그 전달 → 이어서 **로그인/회원가입(Firebase Auth 권장) + 사용자별 세션 분리**. 지금 엔진은 전역 싱글턴이라 인증만 붙이면 사용자들이 같은 엔진을 공유하게 되므로 순서 주의(현 배포도 max-instances 1 전제)
2. **디자인 대기 중 당겨서 할 일 (기존 "후반부로 미룸"에서 승격 — 전부 디자인 무관)**: ①로그 일반인 가독성(요약/전문가 뷰 토글 — 구조 작업) ②데일리 브리핑 간소화(3문장+수치 카드 — 프롬프트·템플릿) ③랜딩 페이지 **뼈대·카피**(무디자인 HTML, 스킨은 시안 도착 후) ④devnet 최종 검증(.env 전환→setup→live→explorer 증빙) ⑤README·소개서 초안. 여전히 보류: DCA 고도화, 멀티 종목 전면 확장
3. **디자인 시안 적용(≈07-28 도착 후, 반나절 목표)** — 간소화 브리프 발송 완료(`docs/design_brief.md`). **적용 창구 2개**: 색·글꼴·간격 = `web/static/css/theme.css` 변수(값은 시안에서 우리가 추출), 카드 배치 = `web/static/js/app.js`의 `DEFAULT_LAYOUT` 배열(대시보드는 카드 모듈 11개로 재배치 자유). 다크 시안이 없으면 라이트 값에서 우리가 유도
4. **마감 주(≈07-29~08-03)**: 데모 영상 촬영 + public 전환 + 15분 리허설 + 제출. **버퍼 필수** — 시안이 더 밀리면 3번을 잘라내고 현재 스킨으로 제출한다(무디자인 뼈대도 심사 요건은 충족, 목업이 아닌 실제 구동이 심사 대상)

이전 로드맵(참고):

0. **사용자 피드백 처리(`docs/user_feedback_p2.md` — 2026-07-23 접수, 버그가 새 기능보다 우선)**: ~~①버그 4건~~ **완료(2026-07-23 버그 수정 라운드: 긴급정지 세션 경계+버튼 비활성, Gemini JSON 파서 견고화, 알림 버튼 권한 상태·피드백, 세션 경계 구분선·판단 출처 범례)** ~~②빠른 개선~~ **완료(2026-07-23: 평가손익·총자산 카드, 캔들차트+MA5·MA20+시세 카드 메인 확대, DCA 주기 틱/분/매일 시각 선택)** ③멀티 종목 전면 확장(보류 — 후반부) ④주간/월별 수익은 Firestore 배포와 병합 ~~⑤매매 기준(Gemini 추세 재량)~~ **완료(2026-07-23: 지표 규칙 + 엄격/추세 토글 + 실데이터 실측)**
1. Cloud Run 배포 + Secret Manager(지갑키) + Firestore(포지션/영수증) — 사용자 GCP $300 크레딧 확보됨. 배포 시 `WEB_HOST=0.0.0.0`·`PORT` 환경변수 대응(web/server.py 주석 참고), B2 장마감 자동 브리핑은 상시 서버에서 유효해짐
2. P3 대화형·감시(`docs/feature_spec.md` 기준): B1 자연어 규칙(변환 결과 승인 후 적용) → B6 에이전트 챗(행동 변경은 제안 카드로) → B4 리스크 가드(알림만/알림+자동정지 토글, 임계값 기본 제안 3틱 ±5% — 미결정, 착수 시 사용자 확인) + 디자인 적용(시안 수령 후 `web/static/css/theme.css` 교체) + 멀티종목·멀티턴(여유 시)
3. devnet 최종 검증: .env 전환 → setup → live → explorer 링크 증빙 (디스코드 SOL 요청 완료 0722 — **사용 시점은 로컬 UI/UX 적용 테스트 완료 후**, 사용자 결정)
4. 제출물: README 병합 + 소개서 + 데모 영상 + public 전환 + 15분 리허설 (8/3 23:59) — **소개서 수익모델 수치 확정 시 수수료율 0.3% vs 0.1% 재검토를 사용자에게 물어볼 것**(`docs/feature_spec.md` 미결정 메모)

## 세션 운영

- 세션 시작 시 이 파일과 `docs/`를 먼저 읽고, 저장소 실제 상태(파일 구조·테스트 통과 여부)를 확인한 뒤 작업한다.
- 문서 지도: `docs/FEATURES.md`(기능 현황·이전/현재 대비·심사축 매핑 — 큰 기능 완료/변경 시 갱신) / `docs/differentiation.md`(402 Guard 재포지셔닝·구현 명세) / `docs/handoff.md`(새 대화 인수인계) / `docs/preflight_review.md`(실현가능성·전략·검토) / `docs/feature_spec.md`(구현 계획 — 현황 아님) / `docs/progress_log.md`(날짜별 로그).
- 굵직한 진행(기능 완료, 결정 변경)이 있으면 `docs/progress_log.md`에 날짜와 함께 2~3줄 append하고, 기능이 완료/변경되면 `docs/FEATURES.md`의 해당 행도 갱신한다. 이 내용은 사용자가 Cowork(claude.ai 프로젝트) 쪽 진행상황 문서와 동기화한다.
- **대화 전환 안내 규칙 (2026-07-23 사용자 요청)**: 로드맵의 큰 단계가 완료되어 커밋과 progress_log 기록까지 마쳤으면, Claude가 먼저 "여기서 새 대화로 전환하는 게 좋겠다"고 알린다. 전환 전에 CLAUDE.md 현재 상태·다음 단계를 갱신하고, 새 대화 시작용 복붙 프롬프트(다음 단계 + 참고 문서 경로 포함)를 제공한다.
- **토큰 한도 대비 규칙 (2026-07-24 사용자 요청)**: 사용자는 사용량(rate limit) 한도에 걸려 작업이 중간에 끊기는 것을 우려한다. Claude는 한도 잔량을 직접 볼 수 없으므로, ①모든 작업을 **기능 단위로 잘게** 쪼개 각 기능마다 "구현→검증→커밋→푸시"를 완주해 **끊겨도 유실이 없게** 하고, ②각 커밋 직후 그 지점이 **안전한 중단점**임을 사용자에게 알리며(원하면 여기서 멈추고 한도 리셋 후 재개), ③큰(수 시간짜리) 작업을 새로 시작하기 전에는 "이건 새 대화에서 넉넉한 예산으로 시작하는 게 좋다"를 먼저 제안한다. ④끊길 조짐이 보이거나 한 기능이 끝나면 CLAUDE.md·docs 갱신 + 재개용 복붙 프롬프트를 남긴다. (2026-07-24 사용자 결정: "Ultracode 팬아웃 상시 금지" 문구는 삭제한다 — 이 기능은 사용자가 ultracode 키워드로 명시적으로 켜고 필요할 때만 표적 실행되므로 별도 금지 규칙이 불필요하다. 토큰 절약은 위 ①~④의 잘게 쪼개기·중단점 고지로 달성한다.)
- **승인(허용) 창 사전 설명 규칙 (2026-07-22 요청, 2026-07-23 간소화 조정)**: 사용자는 개발자이지만 쉬운 설명을 선호한다. 수락이 필요한 도구 실행 직전에 쉬운 한국어 한두 줄로 ①무엇을 실행하는지 ②왜 필요한지를 먼저 설명한다. 영향이 큰 작업(온체인 전송, 과금 가능성, 배포, 삭제·덮어쓰기, 시크릿 접근)만 ③수락하면 무엇이 바뀌는지 ④거절하면 어떻게 되는지까지 4단계로 풀 설명한다. 연속 승인이 예상되면 묶음 시작에 전체 흐름과 대략 몇 번의 허용 창이 뜰지 알린다. Bash/PowerShell 도구의 description(허용 창에 표시되는 한 줄 설명)은 한국어로 작성한다.
- **셸 인코딩·도구 선택 규칙 (2026-07-24 사용자 요청 — 한글 깨짐 수정)**: 셸 명령은 **PowerShell 도구를 기본으로** 쓴다(이 시스템에서 PowerShell 은 UTF-8·`chcp 65001` 로 한글이 깨지지 않음이 실측됨). **Bash 도구는 순수 ASCII POSIX 작업에만** 쓰고, 한글이 포함되는 명령·출력·커밋 메시지에는 쓰지 않는다(Windows Git Bash 카드가 사용자 화면 렌더링에서 한글을 깨뜨림 — 실제로 `settings.local.json` 에 깨진 채 굳은 항목이 발견됨). 보강으로 `.claude/settings.json` 에 `env.LANG = env.LC_ALL = C.UTF-8` 를 고정했다(**세션 재시작 후 적용**). 자동 생성된 `settings.local.json` 에 한글이 깨진 채 굳은 허용 항목이 있으면 정리 대상(무해하지만 지저분함).
- **소통 언어 규칙 (개인 지침 · 2026-07-24 재확인·하드 고정)**: 채팅 답변·설명은 **항상 존댓말(합니다체/해요체). 반말 금지.** (문서·커밋·코드는 무관.) 사용자 앱 설정의 개인 지침이 Claude Code 세션 컨텍스트로 전달되지 않을 수 있어(2026-07-24 실측: 홈 `.claude`에 전역 CLAUDE.md 부재, 존댓말 규칙이 자동 메모리 = "배경 정보"에만 존재해 강제력이 약했음 → 반말 슬립 발생), **저장소 차원에서 하드 규칙으로 고정**한다. 전역(모든 프로젝트) 적용은 사용자가 `~/.claude/CLAUDE.md`(Windows: `C:\Users\Tedd\.claude\CLAUDE.md`)에 직접 기재한다.
- **사람 친화 표현 규칙 (2026-07-24 사용자 요청)**: 개발 은어를 한국어로 직역해 어색해지지 않게 한다. 특히 테스트·검증 **통과를 '초록(green)' 이라 하지 않고 '통과'**(또는 '정상 통과'), 실패는 '실패'로 쓴다. 'red/green', 'PASS/FAIL' 상태어 → '실패/통과', 'fix' → '수정', 'merge' → '병합', 'commit' → '커밋(그대로)', 'fallback' → '대체 동작(폴백)', 'fan-out' → '병렬 분산', 'regression' → '회귀(재발)' 처럼 **한국어 우선, 필요 시 괄호로 원어 병기**. 축약 은어 남발 금지, 처음 나오는 전문용어는 한 번 풀어 준다.
- **CLAUDE.md 편집 범위 규칙 (2026-07-24 사용자 확정)**: Claude 가 수정할 수 있는 CLAUDE.md 는 **오직 이 프로젝트 파일(`solana-agent/CLAUDE.md`)뿐**이다. 전역 파일(`~/.claude/CLAUDE.md`, Windows: `C:\Users\Tedd\.claude\CLAUDE.md`)은 **사용자 전용**이므로 Claude 는 **절대 수정하지 않는다(읽기만 허용).** 세션 상태·규칙·다음 단계 갱신은 전부 이 프로젝트 파일에 기록한다. (전역 파일에 넣을 내용은 Claude 가 텍스트로 제안만 하고, 반영은 사용자가 직접 한다.)
- **작업 태도 규칙 (2026-07-24 사용자 요청)**: ①**세부 설계를 놓치지 말고 "실제로 작동하는 구조"를 구체화**한다(추상 서술로 끝내지 말 것 — 데이터 흐름·함수 경계·실패 처리까지). ②**준비와 마무리는 늘 철저히**(착수 전 조사·설계, 완료 후 검증·문서 갱신), **시도는 과감하게** 해도 좋다. ③어필할 **메인 기능(402 Guard — 에이전트 지출 승인 게이트)의 강점에 집중**하고, 무리한 기능 추가는 지양한다 — **단 사용자가 추가를 요청하면 추가한다.**
- **시크릿 값 미확인 규칙 (2026-07-24 사용자 요청)**: `.env` 의 `GEMINI_API_KEY` 등 시크릿 값을 grep·출력·Read 로 **확인하지 않는다**(키 재발급 완료됨). 존재 여부가 꼭 필요하면 값이 드러나지 않는 방식(설정 라인 유무만)으로 확인하고, **값 자체는 절대 출력·인용하지 않는다**. (2026-07-24 세션에서 grep 으로 키가 노출된 재발 방지.)
