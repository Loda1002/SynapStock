# CLAUDE.md — AutoTrader Agent (구글 클라우드 X 솔라나 AI 에이전틱 해커톤)

이 저장소에서 작업하는 Claude는 아래 규칙을 모든 세션에서 따른다.

## 프로젝트 한 줄 요약

**사용자가 정한 조건·예산 한도 안에서 AI 에이전트가 스스로 주식(토큰화 주식)을 사고팔고 USDC로 정산하는 멀티에이전트 시스템.** Trading Agent(구매) ↔ Broker Agent(판매), A2A 협의 + AP2 한도 + x402 온체인 정산. 언어 Python, 네트워크 Solana devnet(개발은 localnet). 상세는 `docs/architecture.md`.

## 해커톤 고정 제약 (0721 킥오프 세션 기준)

1. 마감: 최종 제출 2026-08-03(월) 23:59 KST. 모든 일정 제안은 이 마감을 기준으로 역산한다.
2. 제출물 3종을 항상 염두에 둔다: ①프로덕트 소개서(타겟 고객·문제/수익모델/아키텍처 필수 포함) ②재현 가능한 코드+README의 GitHub 저장소 ③AI 에이전트 결제 전 과정 데모 영상. 라이브 배포 URL은 가산점.
3. 목업은 심사 제외다. 결제는 반드시 실제로 동작해야 하며, "화면이 아니라 진짜 트랜잭션"이 심사 대상이다. 모든 결제 기능은 실행 로그와 트랜잭션 이력(해시)을 남기도록 구현한다.
4. 네트워크: 개발의 99%는 Localnet, 시연·제출은 Devnet. Mainnet은 사용하지 않는다. 심사 평가 네트워크는 Devnet과 Localnet이다.
5. 심사 4축에 맞춘다: ①혁신성·UX·상업성 ②Gemini 등 AI 활용도 ③인프라 연동(USDC on Solana, Solana Pay, pay.sh) ④실행 로그·이력 기반 실제 구동.
6. 결제 스택: 결제 레이어로 솔라나를 쓰는 것만 필수. pay.sh, x402, Solana Pay, 자체 스마트 컨트랙트 모두 허용된다. (현행: 자체 x402 구현. pay.sh는 비교 검토 대상이며 전환 의무 없음.)
7. 오버 엔지니어링 금지: Gemini는 API 호출 수준이면 충분하고 ADK/Vertex 같은 구조적 활용은 가산점이 아니다. 문제-솔루션 적합성이 명확한 최소 구성을 우선한다.
8. 역할 분담: 코드 개발·설계·문서는 Claude가 담당한다. 프로덕트 런타임의 AI(에이전트 판단·대화)는 Gemini API(무료 티어)를 호출하도록 구현한다. Antigravity/Gemini로 개발하지 않아도 감점 없음이 Q&A로 확인됨.
9. 산출물(소개서·README·데모 시나리오)에는 반드시 당위성을 명시한다: 왜 카드망이 아닌 블록체인인가(무인증 결제, 저수수료, 빠른 속도, 24/7 국경 무관, 에이전트는 계좌 개설 불가하지만 지갑은 코드로 생성 가능), 왜 사람 개입 없는 자율 결제인가.
10. 지갑 개인 키·시크릿은 코드에 하드코딩하지 않고 채팅에 붙여넣지 않는다. 에이전트에게 개인 키를 직접 주는 설계를 피한다. 키 파일은 `secrets/` 아래에 두고 절대 커밋하지 않는다(.gitignore로 차단됨).

## 개발 상세 규칙

- 검증 루틴: 결제 기능은 (1)로컬 전 과정 실행→영수증 확인 (2)상태 변화를 앱 화면과 온체인/DB 양쪽에서 교차 확인 (3)배포 URL에서 동일 검증 반복 (4)트랜잭션 해시·로그를 파일로 아카이빙(`artifacts/tx/`), 이 4단계를 통과해야 완료로 간주한다. `docs/verification_checklist.md` 참고.
- 구현 시작 전에 요구사항이 모호하면 인터뷰(질문 3~4개)로 구체화한 뒤 구현 계획을 먼저 제시하고 승인받는다.
- 독립적인 컴포넌트(에이전트/결제/시세/프론트)는 병렬로 나눠 작업한다.
- 배포 대상: Google Cloud Run(Dockerfile 기반). 데이터는 Firestore, 비정형 파일은 Cloud Storage. GCP 인증(gcloud/서비스 계정)은 사용자가 직접 수행하며, Claude는 명령만 제시한다.
- Gemini API는 무료 티어를 사용한다($300 크레딧으로는 Gemini API를 쓸 수 없음). 무료 티어 초과가 예상되면 사용자에게 "디스코드에 사전 문의해 추가 크레딧을 요청하라"고 안내한다.
- Devnet SOL 퍼셋은 하루 0.5~5 SOL 제한이므로 부족이 예상되면 미리(마감 직전 금지) 디스코드 솔라나 채널에 요청하도록 안내한다. 테스트용 자산은 자체 토큰 발행(무한 발행 가능)으로 해결한다.
- 참고 프로토콜 계층: A2A(에이전트 간 통신)/MCP(에이전트-툴)/AP2(mandate 기반 결제 위임)/UCP(A2A+AP2 통합)/x402·MPP(정산 레일). AP2 안에서 x402를 결제 레일로 사용할 수 있다.
- 데모데이(8/21 금, Google Startup Campus) 라이브 시연을 전제로, 데모는 네트워크 불안정 대비 폴백(로컬넷 재시연 경로)을 준비한다.

## 현재 상태 (2026-07-22 저녁 기준)

완료(localnet 라이브 검증 통과): A2A+x402 **매수·매도 풀사이클**(USDC⇄주식토큰 양방향 온체인 정산, 순변화 교차검증 PASS), AP2 mandate(한도 + 매도 대금 환입 = 순투입 한도), **Gemini 판단**(`gemini-flash-latest`, 무료 티어, 호출 실패 시 규칙 폴백 — 라이브에서 rate limit 폴백 실증됨), **거부 4종 데모**(`scripts/demo_rejections.py`: 건별 한도·위변조·금액 부족·미허용 종목), 증빙 아카이빙(`artifacts/tx/`), GitHub 푸시(https://github.com/Loda1002/SolanaAgent, private — 제출 전 public 전환).

다음 단계(우선순위 순):

1. 웹 서비스화: FastAPI 백엔드(상태 API + 실시간 이벤트) + 무디자인 대시보드 뼈대 (디자인 시안은 며칠 뒤 수령 예정 — 뼈대에 스킨만 교체하는 구조로)
2. Cloud Run 배포 + Secret Manager(지갑키) + Firestore(포지션/영수증) — 사용자 GCP $300 크레딧 확보됨
3. 디자인 적용(시안 수령 후) + 멀티종목·멀티턴 협상(여유 시)
4. devnet 최종 검증(디스코드 SOL 수령 후): .env 전환 → setup → live → explorer 링크 증빙
5. 제출물: README 병합 + 소개서 + 데모 영상 + public 전환 + 15분 리허설 (8/3 23:59)

## 세션 운영

- 세션 시작 시 이 파일과 `docs/`를 먼저 읽고, 저장소 실제 상태(파일 구조·테스트 통과 여부)를 확인한 뒤 작업한다.
- 굵직한 진행(기능 완료, 결정 변경)이 있으면 `docs/progress_log.md`에 날짜와 함께 2~3줄 append한다. 이 내용은 사용자가 Cowork(claude.ai 프로젝트) 쪽 진행상황 문서와 동기화한다.
- **승인(허용) 창 사전 설명 규칙 (2026-07-22 사용자 요청, 필수)**: 사용자는 비개발자다. 사용자의 수락이 필요한 도구 실행(명령 실행, 파일 수정·생성, 설치, 네트워크 요청, 배포 등)을 하기 **직전에** 채팅에 쉬운 한국어로 먼저 설명한다 — ①이번 허용 창이 실행하려는 것 한 줄 요약 ②왜 필요한지 ③수락하면 무엇이 바뀌는지(파일·폴더/설치/네트워크/온체인/과금 여부) ④거절하면 어떻게 되는지(거절해도 안전하다는 점과 대안). 연속 승인이 예상되면 묶음 시작에 전체 흐름과 대략 몇 번의 허용 창이 뜰지 미리 알린다. Bash/PowerShell 도구의 description(허용 창에 표시되는 한 줄 설명)도 한국어로 작성한다.
