# 402 Guard 심사 자가평가 리포트
> 생성: 2026-07-25 22:24 (Asia/Seoul)
> 근거 스냅샷: docs/reports/_evidence_20260725_221318.json
> 심사 기준 출처: 킥오프 전사(docs/hackathon_essentials_0721.md §1) + docs/FEATURES.md §5. 신청 페이지 원문 미확보(docs/handoff.md §4).

## 종합 요약
| 축 | 등급 | 한 줄 근거 |
|---|---|---|
| ① 혁신성·UX·상업성 | 중 | 포지셔닝 개념(사는 쪽 보호·서명 직전 게이트)·당위성·차단 KPI는 실물로 뚜렷하나, 첫 화면 KPI·가드 카드·402 Guard 브랜딩·UI 공격 콘솔이 웹 대시보드에 미구현이라 전달 매체 갭이 큼. |
| ② AI 활용도 (Gemini) | 중 | Gemini를 호출 수준으로 2용처(매매 판단·브리핑)에 실통합하고 파싱·폴백 엔지니어링은 견고하나, 정량 수익 기여가 규칙 대비 사실상 중립이고 대표 30% 서사는 전부 brain=rule 로 AI 미관여. |
| ③ 기술·인프라 연동 | 강 | x402 온체인 SPL 정산·exact 금액검증·Memo 바인딩·AP2 ed25519 mandate·A2A 3단계·Guard 결선이 실물 코드이고 devnet 라이브 교차검증 통과·유출 0. HTTP 402 status-code 서비스만 미구현. |
| ④ 실제 구동 | 강 | devnet·localnet 라이브 모두 실 tx·교차검증 통과, tx 아티팩트 9건, red_team KPI 직접 재현(유출0·오탐0), 테스트 11종 통과. devnet 반복성·배포 URL 재현·HTTP 402 만 미확보. |

## 축① 혁신성·UX·상업성
**등급**: 중

**부합 근거**
- 핵심 재포지셔닝이 개념적으로 뚜렷·차별적 — "x402 exact 스킴은 파는 쪽을 보호, 402 Guard 는 사는 쪽(사람 없이 결제하는 AI 에이전트)을 보호하는 서명 직전 마지막 게이트". 자기 버그 고백이 아니라 스펙 갭 지적이라 논리가 강함 — `docs/differentiation.md:42-45`
- 당위성(왜 카드망이 아니라 블록체인·왜 사람 없는 자율결제인가)이 실물 문서에 구체 서술: 인증 없는 결제→한 번의 잘못된 서명=즉시 온체인 손실→사람 대신 청구서를 검증하는 계층이 필수 — `docs/submission.md:68-79` · `docs/differentiation.md:180-197`
- '수익률이 아닌 지출 통제' KPI(시도·차단·유출 0.00 USDC·오탐 0)가 red_team CLI 도구에서 실제 산출, 같은 실행에서 정상 14건 오탐 0 동반 — `_evidence_20260725_221318.json red_team.kpi {attempts:20, blocked:6, leak_usdc:0.00, false_positives:0}`
- 공격 콘솔(심사위원 직접 입력) 실재 — `--attacker <pubkey>` 옵션으로 악성 수취인 주소 주입 가능 — `scripts/red_team.py:16,58,62`
- 백엔드 엔진이 state_snapshot 에 첫 화면 KPI 데이터(attempts/blocked/leak_usdc)를 이미 방출 — 데이터 계층은 준비됨 — `web/engine.py:1569-1573`

**갭**
- 플래그십 UX 주장('첫 화면 KPI=시도·차단·유출·오탐, 수익률 아님')이 실제 웹 대시보드에 미구현. 백엔드는 KPI 방출(engine.py:1569)하나 프론트가 소비·렌더 안 함 — `web/static/index.html`(data-card="guard" 부재) · `web/static/js/app.js:967`(guard/attempts/blocked grep 0건, DEFAULT_LAYOUT 이 pnl·valuation 을 상단 배치 — 차별화 문서 §103-104 와 정반대).
- 메인 대시보드가 여전히 'AutoTrader Agent'로 브랜딩·'한도 안에서 스스로 사고파는 AI 에이전트' 태그라인 — 차별화 전략이 경고한 트레이딩 봇 프레이밍. 402 Guard 재포지셔닝은 `web/static/login.html:23` 에만 반영, 첫 진입 화면 `web/static/index.html:7,16-17` 미반영.
- UI 공격 콘솔(심사위원이 배율·수취인을 화면에서 입력)은 미구현 — 입력은 `red_team.py --attacker` CLI 로만 가능. 가드 UI 카드(G8)는 P2 로 보류 — `docs/differentiation.md:99`. 라이브 URL 을 여는 심사위원은 가드 서사가 아니라 트레이딩 대시보드를 보게 됨.
- 상업성(수익모델) 실증이 실물 UI/증거 스냅샷으로는 미확인 — 수익모델 수치·PDF 소개서 미착수(`docs/submission.md:85-89`), 라이브 데모 URL 미배포(`docs/submission.md:113-116`).
- 데모 영상·PDF 소개서 미착수(`docs/submission.md:106-111,85-89`) — 축① UX·상업성 최종 전달 매체가 현시점 부재(마감 전 예정 작업).

**개선 TODO**
- index.html 에 data-card="guard" KPI 카드 추가 + app.js 에서 state.guard(attempts/blocked/ap2_rejected/leak_usdc — engine.py:1569 가 실제 방출하는 필드) 렌더링해 DEFAULT_LAYOUT 최상단 배치. 백엔드 데이터는 이미 준비돼 프론트 배선만 하면 됨 — 축①의 최저비용·고효과 수정.
- DEFAULT_LAYOUT 에서 pnl·valuation·position 을 mandate/decisions 아래로 내려 첫 스크롤에 '지출 통제'가 먼저 보이게 함(차별화 §101-104 미이행 0시간 작업).
- index.html 헤더 브랜드를 '402 Guard — 에이전트 지출 승인 게이트'로, 태그라인을 '사는 쪽 에이전트를 보호하는 서명 직전 게이트'로 교체(login.html:23 재사용), `<title>` 동일 정비.
- 가드 UI 공격 토글/입력 카드(G8)를 최소 형태로 라이브에 노출하거나, 첫 화면에 red_team --report 결과를 임베드해 심사위원이 UI 만으로 '차단·유출0' 확인 가능하게.
- submission.md 의 수익모델 수치(수수료율)·PDF 소개서 초안을 채워 상업성 축 실물 증거 확보.

## 축② AI 활용도 (Gemini)
**등급**: 중

**부합 근거**
- Gemini API를 호출 수준으로 실통합 — `client.models.generate_content` 로 판단 요청(모델 gemini-flash-lite-latest, developer/vertex 키 자동판별). 기준 '호출 수준이면 충분' 충족 — `agents/gemini_decider.py:202,188` · `config.py:100`
- AI 사용처가 2곳 — ①매매 판단(gemini_decider) ②데일리 브리핑 리포트 생성(별도 Gemini 호출, temperature 0.4). 단일 용도 아님 — `web/briefing.py:60` · `agents/gemini_decider.py:224`
- Gemini 주변 엔지니어링 견고 — JSON 3단 파싱(추출→복구→정규식), 형식 위반 1회 재요청(RETRY_SUFFIX/:290), 429 쿨다운(retryDelay 존중), 모든 예외 시 규칙 폴백. 파서 단위테스트 11건 통과 — `agents/gemini_decider.py:165-180,217-222,290` · `_evidence...json tests.modules[test_gemini_parse ok_count 11]`
- Gemini 는 조건형(condition) 모드에서만 작동, 추세추종·적립 모드는 코드가 명시적으로 결정론 규칙 신호만 사용('Gemini 미사용') — `agents/trading_agent.py:200-203,317` · `web/engine.py:430-432`

**갭**
- AI 정량 수익 기여가 약함 — 동일 AAPL 36봉에서 gemini-strict 4.29% 가 rule 4.90% 하회, 최선(gemini-strict-ta) 4.95% 도 rule 대비 +0.05%p. trend 재량 모드만 MDD 감소(5.00 vs 7.31)로 유의미 — `_evidence...json backtests` 다수.
- 대표 성능 백테스트(30% 서사)는 전부 brain=rule 로 Gemini 미관여 — TSLA 하락장 +81.29%·+67.76·포트폴리오 모두 규칙 — `20260725_180832_TSLA_bear...(brain rule,81.29)` · `20260725_122955_TSLA_bear...(brain rule,67.76)`
- 온체인(로컬넷/devnet) 세션에서 Gemini 가 판단을 구동했다는 증거 없음 — tx 아티팩트가 판단 출처(rule/gemini)를 기록 안 해, 9건 온체인 tx 중 Gemini 관여분 확인 불가.
- Gemini 백테스트 표본이 전부 단일 종목 AAPL 36봉 — 대표 서사의 대규모(481·1600봉) 하락장에는 Gemini 런이 없어 AI 정량 기여가 극소 표본에서만 측정됨.
- '규칙 없는 개시 금지' 제약이 프롬프트에만 존재하고 코드로 강제 안 됨 — `_sanitize` 는 매수 클램프/무보유 매도 차단만(`agents/trading_agent.py:418-427`). 모델이 프롬프트를 어기면 규칙 밖 매매 가능해 AI 기여 서사가 모델 순응에 의존.

**개선 TODO**
- 대표 성능에 쓰인 동일 대규모 하락장 데이터셋(481·1600봉)으로 brain=gemini 백테스트 실행해 rule 대비 수익/MDD/gemini_fallbacks 기록 — 36봉 AAPL 대신 실제 AI 정량 기여 수치 확보.
- 판단 출처(rule/gemini/rule-fallback) 카운트를 tx 아티팩트·세션 요약에 계측 — 온체인 세션이 Gemini 로 구동됐음을 증빙 가능하게.
- 데모용으로 조건형+Gemini 세션을 최소 1회 온체인 실행·아카이빙 — 현재 30% 서사(trend/multi)는 Gemini 사용 0이라 축② 실구동 근거가 비어 있음.
- 축② 정량 지표 헤드라인화 — trend 재량의 MDD 감소를 명시하고 수익 기여는 사실상 중립임을 정직하게 표기.
- '규칙 신호 없는 개시 금지'를 코드 게이트로 강제(_sanitize/decide 후처리에서 규칙 미충족 시 Gemini 의 buy/sell 을 hold 로 강등) — 프롬프트 의존 제거로 AI 기여 주장 견고화.

## 축③ 기술·인프라 연동
**등급**: 강

**부합 근거**
- x402 온체인 정산이 실물 코드 — SPL TransferChecked instruction 생성·서명(build_transfer_transaction) 후 실제 클러스터에 send_raw_transaction/confirm_transaction 브로드캐스트(목업 아님) — `payments/x402_solana.py:111-126,334-352`
- x402 exact 스킴 금액 검증 실재 — 검출 금액이 기대치와 다르면(초과/부족 모두) 거부 — `payments/x402_solana.py:218`
- 온체인 Memo 바인딩(AT1:{order_id})으로 대사 키 + 리플레이 방어 — `payments/x402_solana.py:38-40,181-193`
- AP2 mandate 가 실제 ed25519 서명(solders)으로 서명·검증, 건별/총예산 한도와 allowed_asset 검사가 authorize 에서 살아 있음 — `payments/ap2_mandate.py:56-64,122-133`
- A2A 가 a2a-x402 공식 3단계(payment-required→submitted→completed)를 JSON 직렬화 메시지로 구현 — `shared/a2a_messages.py:1-10,80-107`
- 402 Guard 가 check_demand(청구서 항목 대조)·check_delivery(정산 후 온체인 재조회) 구현, 죽어 있던 allowed_asset 되살림·base units 정수 정합 검사 — `payments/guard.py:110,135,152,254`
- Guard 가 별도 모듈이 아니라 실제 결제 경로에 결선 — 매수 build_payment 에서 assert_demand, 매도에서 assert_stock_transfer, 엔진 정산 후 check_delivery — `agents/trading_agent.py:444,487` · `web/engine.py:1082,1190`
- authorize 가 asset=reqs.asset 로 결선돼 allowed_asset 검사가 실흐름에서 발화 — `agents/trading_agent.py:449-451`
- Solana devnet 라이브 온체인 증빙 실재 — 5거래(매수4·매도1) 모두 settled/confirmed, payment_tx·delivery_tx 서명과 explorer?cluster=devnet 링크, USDC 순변화(-4.9)가 기대치와 교차검증 통과 — `artifacts/tx/20260724_1643_solana-devnet_live_buy.json`
- red_team 결과 유출 0.00 USDC·오탐 0(시도 20·차단 6)으로 결제 방어 실효 — `_evidence...json sections.red_team.kpi`

**갭**
- HTTP 402 를 '실제 HTTP status-code 서비스'로 노출 안 함 — x402 3단계 흐름이 인프로세스 A2A 메시지(dict)로만 구현, 표준 402 Payment Required 반환 엔드포인트 없음(코드매치 0) — `_evidence...json guard_markers.http_402_status_code.count=0`. x402의 'HTTP' 전송 정합성 측면 미완.
- pay.sh 미연동·Solana Pay 미사용 — 전사 기준상 병렬 예시라 필수는 아니나 해당 명명 스택 커버리지 0.
- devnet 온체인 실증이 1회(아티팩트 1건)뿐이고 나머지 7건은 localnet. 자체발행 토큰이라 실물 xStocks/USDC 메인넷 정산은 미검증(제약상 mainnet 미사용은 정상).
- devnet explorer 링크의 현재 조회 가능 여부는 이 세션에서 온라인 확인 못 함 — 파일 기록만 검증.

**개선 TODO**
- G5 구현: 브로커 에이전트에 실제 HTTP 402 Payment Required 응답 레이어 추가(FastAPI status_code=402 + accepts/paymentRequirements 반환, 클라이언트가 payment-submitted 로 재요청)해 x402의 'HTTP 402' 정합성을 실물 확보 → 축③ 직접 가점. 현 A2A 흐름을 얇은 HTTP 트랜스포트로 감싸면 됨.
- devnet 라이브를 매수+매도 풀사이클로 1~2회 더 실행·아카이빙하고, 데모/제출 직전 explorer 링크 생존 재확인.
- 제출물(README/소개서)에 '결제 레이어=Solana 실 온체인 정산' 재현 명령과 devnet tx 해시·explorer 링크 명시 — 축④와 함께 축③ 근거 못박기.
- (여유 시) pay.sh 를 붙여 병렬 예시 커버리지 확대(differentiation P1/G6).

## 축④ 실제 구동
**등급**: 강

**부합 근거**
- 데브넷 라이브 세션 실재 — 5건(매수4·매도1) 모두 status=settled·confirmed, 각 거래에 payment_tx+delivery_tx 서명과 cluster=devnet explorer 링크, rpc=https://api.devnet.solana.com — `artifacts/tx/20260724_1643_solana-devnet_live_buy.json`
- 데브넷 교차검증 통과 — usdc_net_out onchain -4.9 == expected -4.90(usdc_ok), stock_net_in 0==0.0000(stock_ok). 온체인 잔액 변화로 대조 — `..._live_buy.json:cross_check`
- 로컬넷 라이브도 실재·교차검증 통과 — 7건 전부 settled, usdc_net_out 86.95==86.95, stock_net_in 0.5129==0.5129, rpc=http://127.0.0.1:8899 — `artifacts/tx/20260724_1511_solana-localnet_live_buy.json:cross_check`
- tx 증빙 아카이브 9건(로컬넷 7·데브넷 1 세션 + rejections.log 1), 증거 JSON by_network 값과 실제 ls 결과 일치 — `_evidence...json:sections.tx_artifacts.by_network`
- red_team 직접 재실행 KPI 재현 — 시도 20·차단 6·유출 0.00 USDC·오탐 0, check_delivery 미도착 탐지 PASS·정상 도착 통과 PASS — `python -m scripts.red_team 실행 출력 tail`
- 정산 후 구매자 자산 잔액을 온체인 재조회해 청구서대로 도착했는지 검증하는 check_delivery 실구현(라이브는 실 RPC, 오프라인은 주입 원장) — `payments/guard.py:254`
- 테스트 11종 전부 통과(rc=0) — test_guard 23·test_settlement 22·test_multistock 37 등 — `_evidence...json:sections.tests.all_pass=true,count=11`

**갭**
- 데브넷 증빙이 세션 1건(거래 5건)뿐 — 로컬넷 7세션 대비 얕음. 심사 평가 네트워크가 Devnet·Localnet 둘 다인데 devnet 라이브 반복성 증빙 부족.
- verification_checklist.md 3단계('배포 환경 재검증' — Cloud Run 배포 URL 에서 매수/매도 재현) 미수행. 라이브 배포 URL 가산점·배포환경 실구동 증빙 부재(배포는 사용자 gcloud 대기).
- red_team 의 온체인 재조회가 실제 devnet/localnet RPC인지 주입 원장(오프라인)인지 실행 코드 경로까지는 이번에 확인 못 함 — '실제 구동' 증빙과의 연결이 문서상으로만 확인됨.
- 데브넷 explorer 링크 트랜잭션이 현재도 체인에서 조회되는지(실접속)는 이 환경에서 검증 못 함 — 아티팩트 내 서명·URL 존재만 확인.

**개선 TODO**
- 데브넷 라이브 세션을 2~3회 더 실행·아카이빙(정상 매수·정상 매도·한도초과 거부 각 1회 이상). run_demo --replay 경로로 cluster=devnet 증빙 추가해 반복성 시연.
- Cloud Run 배포(docs/deploy_cloud_run.md) 후 배포 URL 에서 매수/매도 1회씩 재현하고 tx 해시·로그 아카이빙해 verification_checklist 3단계 통과 처리.
- 리포트/데모 대본에 red_team 의 온체인 재조회가 어느 네트워크·어느 balance_reader 경로로 도는지 명시해 '실제 구동' 증빙과 명확히 연결.
- (여유 시) 브로커 HTTP 402 응답 레그(G5) 추가해 x402 스펙의 문자적 정합 확보 — 축③ 가점과도 연결.
- 제출 전 데브넷 explorer 링크 최소 1건을 실제로 열어 트랜잭션 조회 스크린샷을 증빙으로 확보.

## 기준 불일치·미검증
- **대회 공식 신청 페이지 원문 미확보** — `docs/handoff.md §4`, 사이트 JS 렌더링으로 스크래핑 실패. 아래 4축의 명칭·가중·필수요건은 모두 킥오프 전사(`docs/hackathon_essentials_0721.md §1`) + `docs/FEATURES.md §5` 에서 도출한 것으로, 공식 폼과 다를 수 있음. 이 섹션의 판단은 전사 기준으로만 유효.
- **축① 상업성 위치 불명** — 전사(§1)는 축①을 '혁신성·UX·상업성'으로 표기하나 FEATURES §5(`docs/FEATURES.md:145`)는 '① 혁신성·UX'로만 표기. '상업성/수익모델'이 축① 내 하위요소인지·별도 축인지·미평가인지 공식 기준으로 미검증 → 상업성 등급 반영은 잠정.
- **축② Google Cloud AI 스택 가중 불명** — 전사는 ②를 'Gemini 등 AI, 호출 수준이면 충분·ADK/Vertex 구조화는 가점 아님'으로 규정해 현재 통합이 충족. 그러나 전달 문구상 ②가 'AI 활용도(Gemini/Google Cloud AI 스택·에이전트 프레임워크 포함)'로 기술돼 깊은 활용을 더 가중할 가능성 — 'call-level 로 충분'은 전사 근거로만 유효하며 신청 페이지와 상이할 수 있어 단정 불가.
- **축③ HTTP 402·Solana Pay 필수 여부 불명** — 전사는 'USDC·Solana Pay·pay.sh' 와 'AP2·A2A·x402' 를 병렬 예시로 나열하므로 자체 x402+AP2+A2A 스택이 축③ 충족. 그러나 공식 페이지가 Solana Pay 나 표준 HTTP 402 challenge-flow 를 '필수'로 요구하는지 미검증 — 필수라면 HTTP 402 갭이 감점 요인. 인프라 배점에서 devnet 실증 횟수/양방향 풀사이클 요구 수준도 기준 원문 확보 후 재판단 필요.
- **축④ 데브넷 최소 건수·배포 URL 필수 여부 불명** — '실제 구동' 정의는 전사 기반(로컬넷/테스트넷/데브넷 라이브). 신청 페이지가 데브넷 최소 트랜잭션 건수나 배포 URL 필수를 다르게 규정할 수 있음. x402 의 HTTP 402 응답 코드가 필수 요건인지도 미확보로 불명 — 예시 수준이면 무관, 필수면 감점 소지(미검증).

## 적대 검증 메모
- **반려 0건 — 4개 축 32개 근거 전부 실물로 확인됨**(축①8·②8·③11·④8, verdict.unsupported 모두 빈 배열, fabrication_found 전부 false). 존재하지 않는 file:line·수치 조작·창작 인용 없음.
- **축① 검증**: 8개 근거 실물 대조 통과. differentiation.md:42-45 포지셔닝·'스펙 갭 지적'(45행) 실재, red_team KPI {20,6,0.00,0}가 증거 JSON red_team.kpi·raw_tail('정상 14건 오탐 0')과 완전 일치, GAP 3건(guard 카드 부재·AutoTrader 브랜딩·G8 P2 보류)도 index.html/app.js/login.html 실상과 정확히 부합.
- **축② 검증**: 8개 근거 통과. 백테스트 수치 전량 증거 JSON 과 일치(rule 4.90/mdd7.31, gemini-strict 4.29, gemini-strict-ta 4.95/mdd7.24, gemini-trend 2.90/mdd5.00, TSLA_bear brain=rule 81.29·67.76, gemini 런 5개 폴백 합 2건). *미세 지적(반려 아님)*: 근거3의 "형식 위반 1회 재요청"은 인용 범위(165-180·217-222)가 아닌 `:290/RETRY_SUFFIX` 에 실재 — 최종 검토에서 인용 라인에 `290` 을 보강함(주장 메커니즘은 전부 실존, unsupported 아님).
- **축③ 검증**: 11개 근거 통과. x402 브로드캐스트·exact 검증(L218)·Memo(L38-40/181-193)·AP2 ed25519·A2A 3단계·Guard 결선·devnet cross_check(usdc_ok=true)·red_team KPI·http_402 count=0 모두 실물 일치. *정직성 지적(반려 아님)*: guard.py 의 check_demand return 메시지가 '4항목'으로 표기된 오래된 문구지만 실제 검사 코드(DEMAND_CODES)는 7종(주문·자산·종목·수취인·금액·의도상한·건별한도, guard.py:41-47,49)으로, 근거의 '항목 대조' 주장은 DEMAND_CODES 7종으로 뒷받침됨.
- **축④ 검증**: 8개 근거 통과. devnet/localnet 아티팩트 교차검증 수치 일치(최종 검토에서 아티팩트 직접 열람 재확인: devnet -4.9/-4.90·localnet 86.95/0.5129), ls artifacts/tx 9건 = 로컬넷7·데브넷1·rejections.log1 로 증거 JSON by_network 와 일치, python -m scripts.red_team 직접 재실행으로 KPI(20·6·0.00·0) + check_delivery PASS 재현, 테스트 11종 all_pass=true. HTTP 402 부재는 과대주장이 아니라 정직한 한계로 서술됨.

## 증거 스냅샷
docs/reports/_evidence_20260725_221318.json — Bash 로 열어 대조: cat "docs/reports/_evidence_20260725_221318.json"
