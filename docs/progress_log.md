# 진행 로그

굵직한 진행(기능 완료, 결정 변경)마다 날짜와 함께 2~3줄 append. Cowork(claude.ai 프로젝트) 진행상황 문서와 수동 동기화.

---

## 2026-07-25
- **✅ 멀티 종목(동시 매수) 엔진 구현 완료(드라이+대시보드+백테스트, 전부 검증·푸시).** `web/engine.py` 를 단일 심볼 전제에서 멀티로 리팩터(`docs/multistock_plan.md` 계획대로). **설계 핵심**: 공유 1개(mandate `allowed_symbols`=전종목·auth 예산·Guard 수취인 allowlist·broker) 아래 종목별 N개 `TradingAgent`(position·이력·시간청산 독립, 모두 같은 trading_kp·auth·guard 공유) → "하나의 402 Guard 로 다중 지출 통제" 서사. **1회 매수=총 spend/N**(DCA 회당도 /N)으로 한 종목의 예산 독식 방지(검증 sweep_portfolio 와 동형). **방어(계획 공백 보완)**: 추세추종(올인)·라이브·목시세 + 멀티는 거부(각각 예산 독식·온체인 멀티 범위 밖·전종목 동일가격). **N=1 하위호환 바이트 동일**(symbols 미지정=레거시 STOCK_SYMBOL 경로). **배선 파일**: `config.py`(STOCK_SYMBOLS)·`web/engine.py`(핵심: `_resolve_symbols`·`_build_symbol_feed`·`_tick_once`/`_process_symbol` 종목순회·오류격리·일부소진 skip·전부소진 종료·`_buy/_sell_cycle` agent 파라미터화·`_valuation` 집계+`_symbol_valuation`·`state_snapshot` symbols[]+per_symbol+집계·update_limits 전종목 auth+가드 정합·`start(autostart)` 테스트시드)·`web/server.py`(FeedBody.symbols)·웹 3파일(종목 체크박스·포커스 드롭다운·종목별 요약표·거래표 종목열·판단 종목태그)·`scripts/backtest.py`(`--symbols` 포트폴리오, 엔진 공유예산 모델 재현). **신규 `scripts/test_multistock.py`**: 예산 공유·포지션 독립·피드 소진 격리·세션 종료·가드 KPI 합산·하위호환·방어·한도변경 가드정합 **31개 확인 전부 통과**. **웹 라이브 검증**(dashboard-fast): 3종목 bear 세션 — 종목별 10.00 USDC 매수·독립 포지션(요약표 종목별 상이 P&L)·공유 예산 차감·가드 유출0·포커스 전환 차트/가격/포지션 이동·거래표 종목열·추세추종/라이브 자동 단일잠금, 콘솔 오류0. **포트폴리오 백테스트**: AAPL+TSLA+NVDA _bear 조건형 -22.6% vs 등가중 매수후보유 30.1%(예상대로 열위 — 조건형은 알파 없음, 가치는 402 Guard+분산 하방 축소). **적대적 검증 워크플로우**(9에이전트·3렌즈·건별 재검증): 확정 6건(반증0) 전부 수정 — ①update_limits 가 공유 Guard.mandate 미갱신(선재 결함, 한도변경 후 가드 정합) ②목시세+멀티 거부 ③DCA 멀티 문구 종목별 금액(=amount/N) 정정 ④거래표 종목열·판단 종목태그 ⑤포커스 전환 차트 되감김 해소 ⑥라이브 종목 단일잠금. **회귀**: 기존 테스트 10종+red_team(시도18·차단4·유출0.00·오탐0) 전부 통과, 단일종목 경로 무변경. 커밋 8개(config·engine+test·server·web·backtest·엔진수정+test·web수정) 전부 푸시. **다음 = 심사 부서(judge-dept) → 새 대화서 버그 부서(bug-dept).** 안전한 중단점.
- **✅ 추세추종 전략 모드 앱 구현 완료(사용자 진짜 의도, 검증·푸시).** 아래 방향 정정에서 실증한 추세추종을 실제 앱 새 전략 모드로 배선. **설계 핵심**: `Strategy.mode="trend"`(condition/dca 와 병렬)·`trend_signal`(pxma20 가격≥MA20 기본 / cross_5_20 골든크로스5/20)·판단은 **결정론적 규칙 신호(Gemini 미사용)** 라 검증 재현·상승세 전량 보유·하락세 전량 매도(자본 보존)·재상승 재매수·**올인/올아웃**(매도 대금 복리 재투자=`ap2 credit_sale(allow_surplus=True)`)·건별 한도는 세션 동안 총자산까지 열되 첫 진입 상한=예산·재진입=가진 현금 이내(402 Guard 수취인·청구=합의견적·의도지출·자산 검사 그대로)·시간청산 미적용. **컴파운딩 설계 근거**: explore_trend 가 올인/올아웃 복리 모델(_bear 종목당 ~40매매)이라, 앱도 복리해야 검증 재현 — `credit_sale` floor(순투입 예산캡)를 trend 만 해제(음수 spent 허용=운용현금이 예산 초과, 초과분은 실현이익 재투자). **배선 파일**: `agents/trading_agent.py`(`_decide_trend`·`_trend_ma` 라운딩없는 MA로 시퀀스 정확 재현·`on_sale_completed` trend 복리)·`payments/ap2_mandate.py`(`credit_sale allow_surplus`)·`web/engine.py`(strat_type trend 검증·trend_signal·brain=None·세션 per_trade 올인캡·max_hold=0·초기자본 대비 수익률(`_display_return_pct`)·update_limits/state/summary trend·feed dataset daily/bear)·`scripts/backtest.py`(`--strategy trend --trend-signal --suffix`)·`web/server.py`(StrategyBody.trend_signal·FeedBody.dataset)·`web/static/index.html`+`app.js`(전략·신호·데이터셋 3선택·규칙텍스트·per_trade 전량표시). **신규 `scripts/test_trend.py`(재현 검증)**: 실제 `decide()→broker→auth` 경로가 explore_trend.simulate 의 진입/청산 시퀀스를 **봉 단위 정확 일치**(매매수 동일: AAPL 38/38·22/22, TSLA 41/41·25/25, NVDA 35/35·23/23), 수익률 오차 ≤0.08%p(브로커 소계·수수료 분리 센트반올림뿐 — 탐색은 합계 1회 반올림). **백테스트 _bear 벤치마크**: TSLA 가격>MA20 +67.76% vs 매수후보유 −20.45% = **+88.21%p 우위**(MDD 29.58%), AAPL 골든크로스5/20 +13.56% vs +10.85%. **웹 라이브 검증**(dashboard-fast 0.4s틱): "추세추종"+"하락장" 선택→하락 구간 "하락세 이탈(전량 매도, 자본 보존)"→"하락세 관망(현금 보유)"→회복 "상승세 진입(전량 매수)", A2A→x402→settled 결제 정상(올인 매수·매도), 가드차단0·유출0, 건별한도 "전량(올인)" 표시. **회귀**: 전체 테스트 10종(test_trend·timeexit·guard·settlement·dca·indicators·gemini_parse·store·dataquality)+red_team(시도18·차단4·유출0.00·오탐0) 전부 통과 — allow_surplus 는 trend 전용이라 condition/dca 무영향. 리포트 `docs/reports/strategy_validation.md`. **다음 = 멀티 종목(`docs/multistock_plan.md`, 새 대화)→심사 부서→새 대화서 버그 부서.** 안전한 중단점(커밋·푸시 완료).
- **⚠ 전략 방향 정정 — 사용자 진짜 의도는 추세추종, 하락장으로 실증(커밋 c494470·a2643f7).** 사용자 피드백: 내가 고도화한 dip3/profit5+시간청산은 **평균회귀**(눌림목 매수→짧은 익절)로, 원하던 **추세추종**(상승세 보유·장기 하락세 매도·추세전환 재매수)과 정반대였음을 정직히 인정. `scripts/explore_trend.py`(전량 진입/청산, 가격>MA20·골든크로스·MA기울기)로 검증: **상승장(현행 _daily)에선 추세추종이 매수후보유에 전부 패배**(피할 하락장 없어 늦은진입·휩쏘 손해), **하락장(_bear.csv=2022 폭락+2023 회복, yfinance 조정일봉 501봉)에선 크게 승리** — TSLA 보유 -20.4% vs 가격>MA20 +67.8%(+88%p)·골든크로스5/20 +76.4%(+97%p), NVDA MDD 60.8%→22.9%, 롤링 60/120봉서 가격>MA20 이 보유를 57.8%/62.9% 승률·평균 +3.7/+5.8%p 초과. **결론: 어떤 단순전략도 순상승장에선 보유를 못 이기나(알파는 어려움), 추세추종은 하락장 회피로 전체 사이클서 우위 = 402 Guard 안전서사 직결**. 데이터 수신은 stooq 봇차단→yfinance 전환(`scripts/fetch_bear_data.py`, 개발용). **평균회귀 개선은 condition 모드 옵션으로 유지(무해). 다음 우선순위 = 추세추종을 새 전략 모드로 앱 구현(가격>MA20 기본).** 상세 `docs/reports/strategy_validation.md`.
- **전략 수익 검증 라운드 + 수익 로직 고도화(사용자 요청).** 데이터 부서가 건전성 확인한 CSV 로 "짧은 단일종목 세션이 확실히 흑자인가"를 실측. ①`scripts/validate_strategy.py` 신설(롤링 윈도우 762구간, 규칙 두뇌 고정·미실현손익 포함으로 승률착시 제거·매수후보유 벤치마크·3종목 분산 포트폴리오 모드). **결론(정직)**: 짧은 단일종목은 확실한 흑자 불가(8봉 흑자 37%·26% 무거래), 종목성격이 파라미터 압도(TSLA 우수·AAPL 열위), **어떤 설정도 벤치마크를 평균적으로 못 이김** → 제품가치는 알파가 아니라 402 Guard. 유일한 신뢰도 레버=분산(단일 55%→분산 82% 흑자, 최악 절반). ②`scripts/explore_strategy.py` 신설(규칙 위 오버레이 실측): **손절·잦은매매·추세필터·트레일링 전부 반려**(근거: 손절은 눌림목 바닥 확정, 추세필터는 매수붕괴, 트레일링은 MDD 2배), **시간청산(안전레일)이 진짜 고도화**(AAPL 최악 -6.47%→-0.18%·MDD 7.31→1.34%로 꼬리위험 제거). ③사용자 결정=최고 수익 스택 **dip3/profit5 + 시간청산 10봉**(분산 흑자 85.8%·평균 2.11%·최악 -1.61%, 전 지표 최고). **앱 반영**: config.MAX_HOLD_BARS·Strategy.max_hold_bars(조건형 decide 백스톱, 규칙/Gemini보다 우선)·web DEFAULT_RULES dip3/profit5·backtest 기본값 동기화·목 시세 경로 재설계(옛 절대가용 얕은 딥→dip3 트리거, 회귀 해소)·단위테스트 test_timeexit 8건. **검증**: 전체 테스트+red_team(유출0) 통과, **실제 decide() 경로가 탐색값 정확 재현**(전체 흑자 57.9%·평균 2.10%), 목 폴백(web·run_demo) 매매 정상, run_demo 드라이 12틱 풀사이클 완주. 리포트 `docs/reports/strategy_validation.md`. 범위 확정(사용자): 멀티 종목은 **드라이+대시보드+백테스트**(라이브 온체인 제외). **다음: 멀티 종목 엔진(단일→다중 리팩터, 큰 작업 — 새 대화 권장) → 심사 부서 가동(빠진부분·수상가능성·보완점) → 강력 보완 → 새 대화서 버그 부서.**
- **데이터 수집 부서(data-dept) 구축 완료 — 트랙 B 에이전트 부서 3/3.** 템플릿(judge-dept·bug-dept: 결정론 수집기 + 워크플로우 + 자기검토)에 사용자가 중간 점검으로 추가한 신규 스킬 2종을 접목해 강화. **산출물 3종**: `scripts/collect_dataquality.py`(결정론 수집기 — LLM·추정 0, CSV별 소비호환·스키마·OHLC정합·연속성·이상치·충분성·신선도 7차원을 판정, 로직은 순수 함수 parse_rows/check_ohlc/find_gaps/find_anomalies 등으로 분리) · `scripts/test_dataquality.py`(탐지기 단위테스트 28건 — high<low·30%점프·주말봉·깨진헤더·중복·워밍업부족 등 불량 입력 전수 검출, 정상 입력 통과 확인) · `.claude/workflows/data-dept.js`(Scan→Inspect[심볼별 팬아웃]→Verify[건별 적대검증]→Report[5축 자기평가+기록]). **스킬 접목**: ai-regression-testing → 독립 재파싱이 아니라 **실제 소비경로 `market.price_feed.load_bars()` 로 로드**해 "데모·백테스트가 진짜 이 CSV 를 소화하는가"를 대조(사각지대 제거) + 판정 로직 회귀 테스트. agent-self-evaluation → 자기검토를 5축 스코어카드(정확성·완전성·명료성·실행가능성·간결성, 축별 증거 필수)로 강화(bug/judge 부서의 임시 체크리스트보다 엄격). **범위**(설계 인터뷰로 사용자 확정): data/market/*.csv 만·읽기 전용(자동 수집 없음, `--fetch` 미래 여지)·수집기 테스트 추가. **실제 1회 구동 검증**(26에이전트·오류0·소요 11.7분): 실제 이슈 0건·관찰 20건(낮음2=MA200 산출 여력 미달·마지막봉 2거래일 뒤처짐 신선도, 정보18)·자기평가 4.2/5. 4일 캘린더 갭 4건을 미 증시 공휴일(성금요일·메모리얼데이·준틴스·독립기념일)로 정확 분류, 6~8% 일변동을 임계(20%) 미만 정상으로 판정하되 "외부 시세 대조 안 함"을 정직하게 한계로 명시, git 작업트리 읽기 전용 준수(CSV·앱 코드 무변경, 부서 스캐폴딩 파일만) 검증. 인용 수치 독립 교차확인(AAPL -6.12%·NVDA 공휴일 갭) 일치. `_dataquality_*.json` gitignore 추가, README 부서3 예정→가동. **재실행**: `Workflow({scriptPath:".claude/workflows/data-dept.js"})` = "데이터 부서 돌려줘". 안전한 중단점(커밋·푸시 완료).

## 2026-07-24
- **402 Guard 버그 수정 라운드 완료(버그 부서 리포트 핵심 4건 BUG-01~04 + 적대검증 후속 1건, 커밋 90c5c33·74442df·2b4014d·1f76aa6·05fe683)**: 버그 부서가 찾은 402 Guard 본체 결함을 재현→수정→회귀→커밋→푸시 사이클(버그당 1커밋)로 처리. **BUG-02(높음, 90c5c33)** 매도 정산 배송검증 부재 — broker.settle_sale 이 주식 confirmed 하나로 settled 를 정하던 것을 라이브에서 지급 paid 까지 봐야 settled(실패=partial·주식미확정=failed)로 바꾸고, engine._sell_cycle 에 매수측 대칭 check_delivery(정산 후 판매자 USDC 잔액 재조회, 미확인=partial 강등·포지션·예산·실현손익 미반영·세션 정지) 추가. FakeClient(주식확정·지급실패) 회귀. **BUG-03(중간, 74442df)** GUARD_AMOUNT_MISMATCH 실흐름 미발화 — required 가 quote 에서 파생돼 둘이 늘 정합이라 브로커 부풀리기를 못 잡던 축을, 브로커와 독립적인 **의도 지출 상한**(GUARD_INTENT_EXCEEDED, 새 차단코드 7번째)으로 보강. build_payment→engine._buy_cycle/run_demo 가 decision.spend_usdc 를 가드에 전달, red_team.attack_amount_forge 를 인위주입(required≠quote) 대신 자기정합 quote+의도초과로 재작성(실흐름 발화, 데모 신뢰성 이슈 해소). **BUG-01(높음, 2b4014d)** 잔액 기준선 오라클 오염 — get_token_balance_base 가 모든 예외를 0 으로 삼켜 ATA 미존재(진짜 0)와 429/불명을 혼동하던 것을, _is_account_not_found(계정미존재 신호만) 로 계정미존재일 때만 0·그 외 전파. engine._buy_cycle 기준선 읽기를 try 로 감싸 실패 시 예약원복(release)+GUARD_BASELINE_UNREAD+세션정지+return(결제 미제출, 유출 0). **BUG-04(중간, 1f76aa6)** ALLOW_LIVE_FROM_WEB 기본 "1"(열림)→"0"(차단) — 주석·engine 에러·런북과 정합. **웹 UI 라이브 데모는 이제 ALLOW_LIVE_FROM_WEB=1 필요**(현 devnet 증빙은 run_demo CLI 라 영향 없음). **마무리 적대검증**(워크플로우 13에이전트, 버그×3렌즈+종합): 3건(01·02·04)은 실소스 재확인·오탐 유효, BUG-03 에서 **실회귀 1건**(의도 상한 허용치 1센트 고정이 고수수료+sub-cent 지출에서 정직 견적 오탐 차단, 초과 0.010006) 적발 → **05fe683 로 허용치 2센트로 정정**(subtotal·fee 이중 센트반올림 최악치 0.01+0.005+0.005=0.02, 수수료 100%까지 유효, 브로커 값 비의존). 재검증: 정직 견적 16,800건(수수료 10~10000bps×sub-cent) 오탐 0·최악 초과 0.0101, 공격(44.94 vs 의도30) 여전히 차단, 테스트 6종+red_team(시도18·차단4·유출0·오탐0) 전부 통과. 각 커밋이 안전한 중단점.
- **에이전트 부서(트랙 B) 2번 = 버그 전담 부서 완료(커밋 87f6c9b·1b1d396·36384a2)**: judge-dept 템플릿을 버그 사냥에 이식. **설계 인터뷰 4문항** 확정 — 스캔 범위=앱 전체 모듈(16개)·렌즈=4종 전부(정확성·엣지케이스·에러처리·회귀/커버리지)·재현물=**리포트 텍스트만(읽기 전용, 코드 파일 0)**·심각도=전부. ①`scripts/collect_bugscan.py`(결정론적 버그표면 수집기, LLM·추정 0): git churn(최근20커밋 파일별 변경빈도)·테스트 6종 pass/OK수·red_team KPI·**coverage_map**(모듈별 test 참조+미커버 briefing/server 확정)·**risk_markers**(bare/broad except·TODO·noqa·runtime assert·payments내 float, 각 file:line)·**module_index**(파일별 비공백·함수·클래스). collect_evidence 패턴(glob.escape 대괄호·PYTHONIOENCODING·_safe 섹션독립) 재사용, 독립 grep 교차검증(broad_except 27 일치·미커버 2건 일치). ②`.claude/workflows/bug-dept.js`: Scan(수집기)→Hunt(모듈그룹4 팬아웃 G1결제·가드/G2에이전트/G3시세·지표·공유/G4웹·설정, 4렌즈 전부)→Verify(건별 적대검증, 기본 real=false·python -c 재현·저장소 미잔류)→Report(종합+**자기검토 6항목**: file:line 실재·재현 구체성·창작0·심각도 정합·반려누락·읽기전용 준수). ③첫 end-to-end 실행(에이전트 22·오류0·40분·서브에이전트 토큰 1.88M): **확정 13건**(높음2·중간2·낮음8·정보1)·반려2. **핵심 4건 제가 실소스로 독립 교차확인**: BUG-01(높음—`x402_solana.py:309` get_token_balance_base 의 except→0 이 RPC실패/ATA미존재 혼동→check_delivery 델타오라클 기준선 오염→미배송 오탐 통과, 매수측 결함I 재개통) · BUG-02(높음—`broker_agent.py:192` settle_sale 이 confirmed[주식]에만 의존, USDC 지급 실패해도 settled + 매도 사이클 `engine.py:856`엔 매수측 check_delivery 없음=결함I 매도측 대칭구멍) · BUG-03(중간—`guard.py:134` GUARD_AMOUNT_MISMATCH 가 required·guard expected 가 **동일 브로커 quote**에서 나와 실 매수흐름 발화불가, red_team 은 required≠quote 인위주입으로 '차단됨' 연출; 빈틈=브로커총액 vs 사용자 의도 spend 미대조, 단 수취인 allowlist·건별한도는 실작동이라 무한드레인 아님) · BUG-04(중간—`config.py:73` ALLOW_LIVE_FROM_WEB 기본 "1"=열림인데 주석·런북·engine 에러는 '기본 차단' 전제). **읽기 전용 검증**: git status 에 리포트 md 만(앱코드·테스트·repro 잔재 0). 적대검증이 심각도 정직 하향(BUG-04 높음→중간·07 중간→낮음·13 낮음→정보)+오탐 2건 근거 반려. 리포트 `docs/reports/bug_latest.md`+`bug_<TS>.md`(커밋), `_bugscan_*.json` gitignore. **수정은 승인 후 별도 작업**(부서는 찾기만, 앱·제출물 불변). 다음 부서: 데이터 수집(같은 패턴). 부서2·3 착수 프롬프트는 CLAUDE.md 다음단계.
- **에이전트 부서(트랙 B) 1번 = 심사용 부서 완료(커밋 b00f249)**: 개발/평가 지원 조직의 첫 부서. **설계 인터뷰 4문항**으로 확정 — 구현 형태는 사용자 판단(토큰 감수하고 검증 허점 0)에 따라 **워크플로우 형태**(서브에이전트식 온디맨드 호출 + 팬아웃·적대검증 결합), 모든 리포트에 **제출 전 자기검토 단계** 추가. ①`scripts/collect_evidence.py`(결정론적 증거수집기, LLM·추정 0): 테스트 6종 pass/케이스수·`red_team --report` KPI·`artifacts/tx` 인벤토리(localnet vs devnet·cross_check·explorer)·`artifacts/backtests` 전략 vs 벤치마크·코드 라인·402 Guard 구현 마커(git grep)를 실물로 수집. **함정 3개 처리**: glob 이 저장소 경로의 `[...]` 를 문자클래스로 오해석(→glob.escape) · 서브프로세스 cp949 인코딩(→PYTHONIOENCODING=utf-8) · `"402 Payment Required"` 가 docs 예시에 있어 구현 오탐(→코드 디렉터리로 한정). ②`.claude/workflows/judge-dept.js`: 증거수집→축별 심사(팬아웃4)→적대 검증(축별)→종합→자기검토·기록. 심사 기준 출처는 **킥오프 전사 기반**(hackathon_essentials §1 + FEATURES §5), 신청 페이지 상이점은 "기준 불일치·미검증" 섹션으로 분리(공식 원문 미확보, handoff §4). 호출은 `Workflow({scriptPath:".claude/workflows/judge-dept.js"})` — 이 하니스는 `.claude/workflows/*.js` 를 name 자동등록 안 함(내장만). ③`docs/reports/`: 부서 문서 README + 첫 리포트(에이전트 11명·오류0). **검증**: 리포트 핵심 수치를 제가 증거 JSON·README·app.js 와 독립 교차대조 — 전부 일치(창작 0). **첫 리포트 결론**(축별 ①중②중③강④강, 적대검증 반려0): 강점=③기술(실 SPL/USDC·AP2 ed25519·guard 결선·exact `!=`·Memo)·④구동(devnet 10 tx settled+교차검증·localnet7·테스트6종). **잡아낸 실제 갭**: FEATURES §5 stale(devnet 증빙 존재하나 "전부 localhost"로 기재)·README 정문 미전환(402 Guard 언급 0)·DEFAULT_LAYOUT 수익 우선·devnet 라이브 전부 rule(Gemini 온체인 0건)·HTTP402 미구현·배포URL/데모영상 부재. 증거 스냅샷 `_evidence_*.json` 은 gitignore(재생성 가능), 리포트만 커밋. 다음 부서: 버그 전담·데이터 수집(같은 패턴, judge-dept.js 가 템플릿).
- **devnet 라이브 실증 완료 — 첫 온체인 증빙(기존 증빙 전부 localhost)**: 공용 RPC(api.devnet.solana.com)의 두 벽(파우셋 GitHub 공개repo 조건 차단 · 429 rate limit)을 넘어 실제 devnet 풀사이클 브로드캐스트. ①**setup_devnet 안정화** — 여유 지갑(trading)→broker 자동 이체(파우셋 없이 충당), 이미 충분한 지갑은 에어드랍 생략, 모든 RPC 호출 지수 백오프 재시도(429 는 solana-py 가 SolanaRpcException 으로 감싸 str 에 안 보임 → __cause__/__context__ 체인 판별). ②**payments/x402_solana `rpc_retry`** — submit_and_confirm·blockhash·잔액 조회에 동일 재시도(라이브 브로드캐스트 경로). ③**run_demo `--replay SYMBOL --from --to`** — MockPriceFeed 8스텝은 옛 절대가 규칙(185)용이라 현 MA5 규칙에선 매수 미발생(딥이 워밍업 중 소실) → ReplayPriceFeed+preload_bars 로 실데이터 재생, 데이터 소진까지 루프(결정적 CLI 증빙, 판사 재현 용이). **결과(AAPL 06-01~07-22 재생 라이브)**: 매수 4·매도 1 = 온체인 tx 10건(결제+전달) 전부 확정, explorer.solana.com?cluster=devnet 조회 가능, 교차검증 PASS(온체인 USDC 순 -4.9 == 체결 순합계 · 주식 순 0 == 순수량, 완전 왕복), 실현 +4.9 USDC 온체인 반영(trading 1000→1004.9, broker 500→495.1, 양쪽 SOL 수수료 차감), 증빙 `artifacts/tx/20260724_1643_solana-devnet_live_buy.json`. README devnet/localnet 예제를 재생 방식으로 갱신(낡은 --ticks 8 제거·faucet 자동충당 안내). 검증: 드라이런 재생 매수4·매도1 트리거 확인 → 라이브 완주, 테스트 4종 PASS. 다음: 제출물 초안(README 402 Guard 재작성·pitch.md·데모 대본).
- **로그인 껍데기 + devnet 실증 준비(이어가기 세션)**: ①**로그인 우선순위 판단** — 코드로 확인(엔진 전역 싱글턴 `web/server.py:28`, 조작 API 는 이미 CONTROL_TOKEN 게이트, 사용자 인증 없음) → 결론: 실인증은 심사축에 기여 0·전역 싱글턴 리팩터(15~30h)라 마감·402 Guard 본체와 충돌, handoff §5-2 가 Firebase Auth 를 이미 컷. **사용자 결정: 로그인 버튼·페이지 껍데기만 지금, 실인증은 제출 후 self-custody 로드맵**. ②구현: `web/static/login.html`(로그인/회원가입 탭 토글, 회원가입 시 확인 필드, 제출은 어디로도 전송 없이 인라인 "출시 예정" 안내) + `GET /login`(대시보드 비차단, 정적 서빙) + 상단바 로그인 링크 + skeleton.css auth 레이아웃(theme.css 변수 기반, 시안 흡수). 검증: /login 렌더·상호작용 PASS, 대시보드 카드 11개 로그인 없이 정상, 콘솔·서버 에러 0. ③**devnet 실증 준비** — 코드·경로는 이미 완성(localnet 검증, `explorer_tx_url` devnet URL 정확·교차검증·`artifacts/tx` 아카이빙 완비), devnet 은 RPC(기본값)+지갑 SOL(파셋)만 다름. setup_devnet 에 **SOL 선검사 fail-fast** 추가(에어드랍 파셋 실패 시 민트생성 rent 단계 크래시 방지 → 지갑주소+faucet 안내 후 종료) + docstring 경로 정정. 라이브 실행은 **사용자 차례**(현 .env=localnet → devnet 전환, trading `GQJo2q…`·broker `32UBfB…` 파셋 충전, setup→run_demo --live). 다음: devnet 라이브 증빙 → 제출물 초안(README 재작성·pitch.md·데모 대본).
- **402 Guard P0 전부 완료(G1~G4, 커밋 89cde31·9701d90·9523d19·7e2f3b8) + 킥오프 근거 반영**: 재포지셔닝의 본체 구현. **G1** `payments/guard.py` — check_demand(금액 base units 오차0·수취인 allowlist·자산·주문번호 4항목 + 종목·건별한도, 차단코드 6종)·check_delivery(정산 후 온체인 잔액 재조회 balance_reader 주입·미확인은 pending 보류), 방어 위치를 sys._getframe 로 런타임 생성(guard.py:L{n}). **G2** 결선 — build_payment 가 authorize 앞에서 guard.assert_demand, AP2 authorize 가 asset 검증으로 allowed_asset 을 실제로 읽게(결함 C), release/settle 예약 추적으로 정산 실패 시 한도 원복(결함 H), 엔진 GuardError→GUARD_BLOCKED + 라이브 배송 재조회 partial 처리(결함 I). **G4** Memo 바인딩(AT1:order_id:sig8 온체인 대사 키)·verify_payment `<`→`!=`(exact, 결함 D)·expected_order_id Memo 대사(결함 E)·브로커 used_signatures dedup·expires_at. ※의존성(이중청구 방어=Memo+dedup, dedup 은 Memo 로 tx 유일해져야 드라이런 오탐 없음)으로 **G4 를 G3 앞에 구현**. **G3** `scripts/red_team.py --report` — 공격 3종(청구위조 2변형/이중청구/정산미이행)을 실제 결제 경로에 태워 매트릭스 + 정상 14건 오탐 0 동시 산출, --attacker 로 심사위원 악성주소 입력. **검증**: 단위 test_guard 13종·test_settlement 12종 + 회귀 전종 + 엔진 드라이런 스모크(예약 회계 일치) + **localnet 풀사이클 라이브 재검증 PASS**(Memo 실린 매수6·매도1 온체인 확정, 교차검증 USDC 86.95==86.95/주식 0.5129==0.5129). red_team 실측 시도18·차단4·유출0.00·오탐0(가드없을때 피해 117.76). **부수 발견·수정**: setup_devnet write_env cp949 크래시(.env UTF-8 미지정) 재현 차단, 브로커 USDC 운용자본 500 지급(없으면 익절 매도 대금 부족으로 정산 실패 — 사전 미검증 결함). **킥오프 발표 반영**: `docs/differentiation.md §7`·`docs/submission.md §2-3` 신설 — 402 Guard 가 유준혁이 추천한 "검증·가드레일" 레이어 정합, 당위성(진짜 해자=인증 없는 자율 결제→검증 계층 필수, 마이크로페이먼트 논거 기각), 자율성 축 우위(Google 데모 "버튼 클릭과 다를 게 없다" 자평 대비), 김채린 4대 관전포인트 전건 충족으로 서사·심사축 근거 보강. 다음: G5(브로커 HTTP 402 분리, 매수 경로) 또는 devnet 실증·제출물(README/소개서/데모).
- **G0 사용자 키 분리 완료(커밋 329885f) + 세션 규칙 3건 반영**: 402 Guard P0 착수 — 결함 G 제거. `secrets/user.json` 위임자 키 신설, open mandate 를 사용자 키로 서명(에이전트 trading 키는 한도 내 결제 서명만 유지). 반영 파일 config·run_demo·web/engine(start·update_limits 재서명·스냅샷/아카이브 wallets 에 user 노출)·setup_devnet·.env.example. 검증: 드라이런·엔진 드라이런 스모크(user≠trading, 서명검증 True, update_limits 재서명 True) + 테스트 4종 통과, user.json gitignore 차단 확인. **개인 지침 반영 라운드**: 앱 설정 개인지침이 Claude Code 로 전달 안 됨을 확인(전문 에이전트 조사 + 홈 `.claude` 실측 — 전역 CLAUDE.md 부재, 존댓말 규칙이 자동 메모리="배경 정보"에만 있어 강제력 약 = 반말 슬립의 구조적 원인). → CLAUDE.md 세션 운영에 ①**존댓말 하드 규칙** ②**작업 태도 규칙**(세부 설계 구체화·철저한 준비/마무리, 강점 402 Guard 집중·무리한 기능 추가 지양·단 사용자 요청 시 추가) 추가, ③토큰 규칙의 "Ultracode 팬아웃 상시 금지" 문구 삭제(사용자 결정: ultracode 키워드로 필요 시만 켜므로 불필요). 전역 개인지침 위치 = `~/.claude/CLAUDE.md`(사용자 기재 예정). 다음: G1 `payments/guard.py`.
- **공식 심사 기준 확보(사용자 제공) + 차별화 재포지셔닝 확정**: 4축 원문 확인 — ①혁신성·UX ②AI 활용도(Gemini/Google Cloud AI 스택) ③기술·인프라 연동("USDC·Solana Pay·pay.sh 등"과 "AP2·A2A·x402 등"을 **병렬 예시**로 나열) ④실제 구동. → **"자체 x402 vs pay.sh 충돌" 우려 기각**(우리 스택이 그 자체로 정합), pay.sh 는 디스코드 지원 채널 있어 여유 시 가점(P1 유지). 차별화 라운드(에이전트 29개: 각도 6종 × 심사위원 3인 패널 + 탈락 스캔 + 비평) 완료 → **"402 Guard — 에이전트 지출 승인 게이트"** 로 전환([`docs/differentiation.md`](differentiation.md)). 근거는 저장소 실증 결함: 구매자 무검증 서명, AP2 `pay_to` 미검사, `allowed_asset` 죽은 필드, `exact` 인데 금액 `<`, Memo·HTTP 402 부재, **에이전트가 자기 허가서를 자기 키로 서명**(규칙 10 위반, 직접 확인). 심사위원 3인 시뮬레이션 전원 "현 상태 탈락" → 재포지셔닝으로 방어. **기능 명세 문서 [`docs/FEATURES.md`](FEATURES.md) 신설**(주요 기능·마일스톤 이전/현재 대비·심사축 매핑). 전략 고도화(손절·트레일링·Gemini 사이징)는 컷(수익률로는 벤치마크에 지므로 승부 축 전환 우선).
- **배포 전 종합 검토 라운드(다중 에이전트 40+)**: 웹 조사 7축 + 코드 감사 6축(발견 64건) + 적대적 검증 32건 → [`docs/preflight_review.md`](preflight_review.md). **①실현가능성 결론**: 토큰화 주식은 메인넷에 실재(xStocks 민트 온체인 확인, Jupiter 견적이 Pyth 가격과 0.4% 이내 일치)하나 **devnet 에는 없음**(2인 독립 검증) → 현 자체발행 토큰 설계가 옳고 "민트 상수·스왑 경로 2곳만 교체하면 실물"이 로드맵. 단 permanentDelegate·pausable 등 발행사 사후 통제권 상주 → "퍼미션리스" 단정 금지. **②전략 진단**: 백테스트에 매수후보유 벤치마크가 아예 없었음 → 추가 후 실측(80봉) AAPL +4.90% vs 벤치 +30.19%(**-25.29%p 열위**) · TSLA +11.22% vs +2.73%(**+8.49%p 우위**) · NVDA +20.15% vs +25.82%(-5.67%p). 손절 부재로 승률 100%는 착시, Gemini 는 구조상 보류만 가능해 심사 2축 정량근거가 0. **③미검증 2건 분리**(공식 심사기준 원문, "공식 과제=API 결제·pay.sh" 보도 — 사용자 확인 대기).
- **심사 재현 차단 버그 2건 수정**: `python run_demo.py`(README 대표 명령)가 stdout 리다이렉트 시 cp949 UnicodeEncodeError 로 크래시 → config.py 에 `enable_console_safe_output()`(errors=replace, 인코딩은 유지 — UTF-8 강제는 한글을 깬다). README 백테스트 명령 3개가 전부 ValueError(안내 구간이 커밋 CSV 범위 밖) → 인자 없는 명령을 기본 안내로 + 구간 오류를 안내 메시지로.
- **공개 배포 차단 5건 수정(보안)**: CONTROL_TOKEN 헤더 게이트(POST 만, GET·SSE 는 공개 유지 — 심사위원 관전 보장 / 로컬 미설정=무인증), 한도 서버측 상한 + per_trade<=budget, Infinity·NaN 차단(is_finite), feed.file 제거 + 심볼 정규식·realpath 검증 + 요청 모델 extra=forbid, 지갑키 환경변수 주입 경로(Cloud Run 시크릿 동일 디렉터리 마운트 제약 우회) + 라이브 fail-fast, ALLOW_LIVE_FROM_WEB 잠금. 공격 시나리오 12종 차단 확인. 런북에 --concurrency 300 추가(탭 80개면 심사위원이 페이지를 못 여는 문제), .gcloudignore 신설.

## 2026-07-23
- **Cloud Run 배포 준비 완료(심야, 배포 실행은 사용자 gcloud 대기)**: ①Firestore 영속화 — `web/store.py`(BaseStore no-op 기본 + FirestoreStore, 컬렉션 sessions/trades/briefings/state), 엔진 저장 지점(세션 요약 dry 포함·체결·브리핑·한도 기본값, fire-and-forget+실패 경고 1회)과 부팅 복원(한도+최근 브리핑), `/api/history` 4종 신설. 검증: FakeStore 통합테스트 18건·장애 내성·서버 스모크(드라이런 체결) 전부 통과, 로컬은 기본 OFF라 무변경. ②Dockerfile(3.11-slim, TZ=Asia/Seoul, PORT 계약 실측 200 OK) + .dockerignore(시크릿 차단). ③[`docs/deploy_cloud_run.md`](deploy_cloud_run.md) 런북 — SDK 설치부터 Secret Manager(지갑 파일 마운트+Gemini env)·SA 권한·deploy 플래그 근거(max 1=싱글턴 엔진, no-cpu-throttling=백그라운드 틱)·재시작 영속 증명 절차·비용(월 ≈$50, $300 크레딧 내). 부수 수정: 콘솔 print cp949 안전화(test_dca_schedule 크래시 해소). 다음: 사용자 런북 실행 → 배포 URL 검증 → Firebase Auth 라운드.
- **디자인 일정 변경 통보(밤)**: 디자이너 시안이 **약 5일 뒤(≈07-28)** 도착 — 기존 "주말 예상"에서 밀림. 마감(08-03)까지 6일만 남으므로 계획 재정렬: 07-24~07-28은 **디자인 무관 작업만**(Cloud Run+Firestore+Secret Manager → 인증·세션 분리 → 로그 가독성·브리핑 간소화·랜딩 뼈대/카피·devnet 검증·README/소개서 초안), 시안은 도착 즉시 스킨 교체(theme.css 변수 + DEFAULT_LAYOUT 배열, 반나절 목표)로 흡수. 더 밀리면 현재 스킨으로 제출(심사는 목업이 아닌 실제 구동 대상). CLAUDE.md 다음 단계 4단계로 갱신.
- **TA 판단 보강 라운드 완료(사용자 매매 기준 분석 프롬프트 적용, 심야)**: `market/indicators.py` 신설 — MA 1/5/10/20/50/100/200일·골든/데드크로스·기울기, 지지/저항(피벗+2회 터치만 유효), 차트 패턴(M형/W바닥·삼중·H&S/역H&S·삼각형·쐐기·박스·확산·상승채널), 캔들 패턴(장악·도지·적삼병/흑삼병·샛별/석별·유성/망치·장대봉)을 코드로 계산(산수=코드, 해석=판단자). Gemini 프롬프트에 "TA 신호 요약+매핑·종합 규칙" 블록(ta_mode 시 ~900자), 규칙 폴백에 거부권/보류권(사지마·팔지마·대기패턴·충돌=hold). 세션 TA 토글(기본 OFF)·차트 MA 1(종가선, 가시성 — 사용자 요청)/5/10/20/50 선·백테스트 `--ta`. ★정정: 상승 쐐기=매도(원본 이미지 오류), 매수 100% 칸=역H&S·삼중바닥(사용자 확인, 수치는 추후 조정 가능). 단위테스트 44건 통과. **백테스트 실측(AAPL 06-01~07-22, 36봉)**: Gemini 엄격+TA **+4.95%/MDD 7.24**(전 모드 최고 수익률, 무TA 엄격 +4.29% 대비 +0.66%p) · 추세+TA +2.19%/**MDD 4.84**(최저 낙폭) · **규칙+TA 0%**(경직된 거부권이 상승장 딥바잉 매수 4건 전부 차단 — 같은 TA를 "증거"로 받은 Gemini 는 엣지 유지 → "규칙만으론 부족, AI 판단이 가치를 더한다" 소개서 서사의 실측 근거). 권고 확정: 토글 기본 OFF, Gemini 세션은 ON. 검증 중 서버 버그 발견·수정(pydantic StrategyBody 가 미선언 ta_mode 를 조용히 버림). 미탐지 TODO: 깃발형·다이아몬드·역V자. 상세 [`docs/ta_upgrade.md`](ta_upgrade.md). Cloud Run 라운드는 다음 대화로 이월(사용자 지시: 분량 초과 시 이월).
- **실시세 라운드 실측까지 완료 + 대시보드 모듈화 (밤)**: ①Alpha Vantage 무료 정책 변경 실측(full=프리미엄 전용) → compact(최근 100봉)로 AAPL·TSLA·NVDA 수집·커밋(2026-02-27~07-22). ②실데이터 웹 드라이런(AAPL 06-01~07-22, 36봉): Gemini 판단으로 6월 조정 4회 분할매수 → 7월 회복 308.63 전량 익절, **실현 +4.30 USDC(+4.78%)**, 재생 소진 자동 종료+브리핑. ③같은 구간 3종 백테스트 실측 커밋: 규칙 +4.90%(MDD 7.31) / Gemini 엄격 +4.29%(폴백 0, 규칙 준수 실증) / Gemini 추세 +1.65~+2.90(2회, MDD 5.00 — **매수 보류 재량으로 낙폭 축소, 보수적 거래**). 교훈: 백테스트 동시 실행 시 무료 티어 429 충돌로 폴백 오염 — 순차 실행 필수. ④**대시보드 전면 모듈화**(사용자 요청 "구성이 난잡, 모듈화해 위치를 바꿀 수 있게"): 기능 전부를 data-card 모듈 11개로 분리, 기본 순서는 app.js `DEFAULT_LAYOUT` 배열 하나(시안 배치를 배열 수정으로 적용), 카드 제목 드래그 재배치+localStorage 저장+배치 초기화 버튼, 세션 설정(모드·시세·전략·판단모드·시작/종료)을 카드로 이동해 상단바 슬림화. 합성 DragEvent로 실경로 검증, 모바일 375px 1열 확인. 디자이너 브리프는 사용자가 별도 대화에서 간소화("느낌만 전달+자율성")해 발송 완료, 시안 주말 예상.
- **실시세 라운드(§1·§2) 코드 전체 완료 — 데이터 파일만 API 키 대기**: 사용자 결정 4건 확정(AAPL 일봉 2년+TSLA·NVDA 예비 / CSV 전체 커밋+출처 표기 / 규칙 = MA5 −2% 매수·평단 +3% 익절 / Gemini 엄격·추세 토글, 추세=보류 재량). 구현 5커밋: ①`scripts/fetch_market_data.py`(Alpha Vantage 일봉, 25콜/일 보호) ②`ReplayPriceFeed`(1틱=1봉 실제 OHLC, 워밍업 20봉 preload, 소진 시 세션 자동 종료+브리핑, UI 피드 선택·날짜축·전일 종가 대비·워밍업 반투명, `/api/engine/start` feed 파라미터, PORT env 지원) ③지표 규칙 전환(절대가 178/185 전면 제거, 매도 우선 검사, 워밍업 hold — 단위테스트 6종+DCA 회귀 PASS) ④Gemini 입력 보강(MA5/MA20·5봉 등락률·변동성·평단 손익률·직전 행동 회고)+모드 토글(strict/trend, UI 셀렉트·아카이브 표기) ⑤`scripts/backtest.py`(규칙 vs Gemini 엄격 vs 추세 같은 구간 비교 — 총손익·승률·MDD·AP2 거부·폴백 수, 무료 티어 4s 페이싱). **웹 드라이런 검증**: 목 시세에서 MA5 −2% 3회 분할매수→평단 +3% 익절(실현 +3.01 USDC), **추세 모드 백테스트에서 규칙 신호에도 "낙폭 확대 — 바닥 확인까지 보류" 재량 2회 실증**(같은 구간 규칙 두뇌는 매수 — 소개서 비교 서사 확보). 남은 것: 사용자 `ALPHAVANTAGE_API_KEY` 등록 → fetch 실행 → CSV 커밋 → 실데이터 웹 검증+3종 백테스트 실측. 디자이너 브리프 발송 패키지 [`docs/design_brief.md`](design_brief.md) 작성(복붙 메시지+토큰 표+체크리스트).
- **방향 재정렬(사용자 피드백) → [`docs/next_round_plan.md`](next_round_plan.md) 신설**: ①목 시세의 반복 패턴으로 판단을 검증하는 건 무의미 → **실시세 데이터 도입 후 Gemini 매수·매도 프로토콜 검증이 최우선**(DCA 고도화는 보류). 데이터 조사 실측: **Stooq CSV 직링크는 봇 차단(JS 검증)으로 스크립트 접근 불가** → Alpha Vantage(무료 25콜/일)나 Alpaca(무료 키)로 1회 수집해 `ReplayPriceFeed` 로 재생하는 방식 권장(오프라인 폴백·결정적 재현 유지). 실데이터 도입 시 규칙 기본값(178/185)·예산 스케일·캔들 축·데모 구간 재설계 필요. ②**로그인·회원가입 요구 신설** → Cloud Run+Firestore+Secret Manager 배포가 선행(현 엔진은 전역 싱글턴이라 사용자별 세션 분리 필요, Firebase Auth 권장). ③**디자인 의뢰 가능**(디자이너는 앱 경험만, 웹 처음) → 정보구조 3화면(랜딩/로그인/대시보드)과 디자이너 브리프 초안 작성, 시안은 theme.css 변수 매핑으로 요청. ④로그 일반인 가독성·데일리 브리핑 간소화는 후반부 작업으로 지정. 마감(8/3) 역산 순서와 열린 결정 7건도 문서에 정리.
- **빠른 개선 3건 완료(항목당 구현→검증→커밋)**: ①평가손익(미실현)·총자산 카드 — 지금 전량 매도 시 수령액(수수료 차감) − 실효 평단 기준이라 실현손익과 숫자가 일관, 총자산 = 가용 예산 + 주식 평가액(라이브는 온체인 잔액 병기), 틱마다 실시간 갱신, B2 브리핑에도 반영(Gemini가 실제 인용 확인). ②캔들차트 — 목 시세가 틱당 단일가라 `TICKS_PER_CANDLE=2`로 OHLC 집계, 시세 카드를 `card-full` 메인으로 확대(큰 현재가 + 세션 시작가 대비 등락), SVG 직접 렌더(외부 CDN 없음): 양봉·음봉, MA5·MA20, 가격 눈금·격자·현재가선·범례. 검증은 틱 0.5초로 가속(.env 임시 추가 후 원복) — 93틱 → 캔들 47개 서버·화면 일치. ③적립식 주기 선택 — `dca_unit`: 틱마다/N분마다/매일 HH:MM, 대기 사유를 타임라인에 표기(남은 시간·오늘 집행 완료), 회귀 테스트 `scripts/test_dca_schedule.py` 7케이스 통과 + 웹 드라이런 2케이스 실증. 남은 피드백: 멀티 종목(별도 대화), 주간/월별 수익(Firestore 배포와 병합), Gemini 추세 재량(설계 논의).
- **버그 수정 전용 라운드 완료(사용자 보고 오류 4건 + 추가사항 4, 재현→수정→웹 드라이런 검증)**: ①긴급정지가 세션 경계를 넘어 유지되던 문제 — 세션 종료 시 `trading_enabled`·`pause_info` 리셋(종료 로그에 "정지 해제됨" 표기), 대기 중 pause/resume 은 409 거부 + 버튼 비활성(추가사항 4 동시 처리). ②Gemini `Invalid \uXXXX escape` — `parse_decision_json` 신설(코드펜스·설명문 제거 → 잘못된 이스케이프·제어문자 정화 후 재파싱 → 정규식 필드 추출), 그래도 실패하면 1회만 재요청 후 규칙 폴백. 회귀 테스트 `scripts/test_gemini_parse.py` 11케이스 전부 통과. ③알림 버튼 무반응 — 저장값과 실제 권한이 어긋나면 라벨이 안 바뀌던 desync 수정(버튼이 권한 상태 표시: 켜짐/꺼짐/차단됨/미지원), 모든 분기에서 토스트 피드백 + 차단 해제 방법 안내, 권한 변경 시 자동 갱신. ④dca 배지 혼동 — 타임라인·로그에 세션 경계 구분선(전략·판단 출처 명시) + 이전 세션 항목 흐리게, 타임라인 헤더에 판단 출처 범례. 검증: 드라이런 2세션(조건형→적립형) + 새로고침 복원, 콘솔·서버 오류 0.
- **P2 직후 사용자 피드백 접수 → [`docs/user_feedback_p2.md`](user_feedback_p2.md) 정리**: 버그 4건(긴급정지 세션 경계·알림 버튼·Gemini JSON 폴백·dca 배지 혼동), 수익 리포트 확장(주간/월별/종목별/평가손익), 캔들차트+이동평균+시세 카드 확대, DCA 시간/날짜 기반, 멀티 종목 전면 확장, Gemini 추세 재량 검토. 다음 대화는 버그 수정 전용으로 시작(작업 사이클 규칙). GitHub 푸시 완료.
- **P2 엔진 확장 완료(localnet 라이브 검증 통과, 기능당 1커밋 × 5)**: ①A8 수수료 투명화 — `BROKER_FEE_BPS=30`(0.3%), 매수 `총액=소계+수수료`·매도 `수령액=소계−수수료` 양방향(사용자 확정), AP2 검사는 수수료 포함 총액 기준, 평단=실효단가 → 실현손익=순손익, 대시보드에 견적 분리 표기·수수료 컬럼·누적 수수료(브로커 수익) 카드. **0.1% 인하안은 최종 제출 전 재검토(소개서 수익모델 수치 확정 시 사용자에게 질문할 것)**. ②A3 한도 설정 — `/api/mandate`로 새 mandate 재서명(spent 이월), 실행 중엔 긴급정지 상태에서만 적용, 대기 중엔 다음 세션 적용, 변경 이력 이벤트+아카이브. ③A4 알림 — 토스트+Web Notification(백그라운드 탭), SSE 히스토리 재생분 억제. ④B7 적립식 DCA — 세션 전략 선택(조건형/적립형), N틱마다 정액 매수(Gemini 미사용 라벨, `dca` 배지), mandate 동일 경로. ⑤B2 브리핑 — Gemini 한국어 리포트(실패 시 템플릿 폴백), 수동 버튼+세션 종료 자동+장 마감 시각(`DAILY_BRIEFING_TIME`) 3경로, `artifacts/briefings/` 저장. 검증: 오프라인 스모크(수수료 9건·브리핑 8건 PASS) → 웹 드라이런(기능별) → **localnet 라이브 2세션**: 조건형 매수4·매도1 + DCA 정액 3건 전부 온체인 확정, **교차검증 USDC/주식 PASS×2**(수수료 반영 순변화 54.22 일치), 증빙 `artifacts/tx/20260723_1355·1357_*_web_session.json`. 시나리오 실증: 한도 20으로 낮추자 수수료 포함 총액 29.98>20 AP2 거부 → 정지 중 100/40 재서명 → 재개 후 통과.
- 기능 선택 확정(사용자): **A 전부(A1~A8) + B1·B2·B4·B6·B7** (B3·B5 제외). 필수군 A1~A5 / 어필군 A6~A8·B전체 구분. B4는 "알림만/알림+자동정지" 토글 설계로 고민 해소, B6은 mandate 우회 불가·규칙 재서명 연결 원칙. 구체 설계 [`docs/feature_spec.md`](feature_spec.md)(P1→P2→P3) — 구현은 새 대화(웹 서비스화)에서 시작.
- CLAUDE.md 세션 운영에 **대화 전환 안내 규칙** 추가(큰 단계 완료 시 Claude가 먼저 새 대화 전환을 안내하고 문서 갱신 + 스타터 프롬프트 제공).
- **P1 웹 서비스화 완료(라이브 검증 통과)**: FastAPI+SSE `web/` 신설(engine/events/server + 무디자인 대시보드, 기존 코어 무수정 재사용). A1 대시보드·A2 긴급정지(주체 기록, 시세는 계속)·A5 거래 테이블(explorer 링크)·A6 판단 타임라인(gemini/rule-fallback 배지)·A7 실현손익 전부 동작. 엔진은 대시보드에서 세션 시작/종료(연속 루프, 기본 8초 틱 — 사용자 선택). localnet 라이브: 매수4+매도1 온체인 확정, +7 USDC 실현손익 온체인 반영, 교차검증 PASS, `artifacts/tx/20260723_1220_..._web_session.json`. 드라이런에서 새로고침 피드 복원(SSE 재전송)·rate limit 규칙 폴백도 실증. 디자인 시안은 `web/static/css/theme.css` 변수 교체로 적용 예정.
- **버그 수정 라운드(사용자 보고 3건, 당일 해결·재검증)**: ①"Gemini 계속 호출 실패" 원인 = `gemini-flash-latest`가 gemini-3.6-flash로 연결되고 이 모델 무료 티어가 **하루 20회**뿐 → 소진. 모델을 `gemini-flash-lite-latest`로 교체(한도 넉넉, 판단 품질 충분 — 재검증에서 판단 전건 gemini 배지), 429 시 retryDelay 존중 쿨다운 추가(틱마다 헛호출 방지), 타임라인에 실제 원인 문구 표면화, `check_gemini.py` cp949 크래시 수정. ②스크롤 부자연 = 피드·테이블이 무한히 자라 페이지가 밀림 + 스크롤 체이닝 → 고정 높이+내부 스크롤+`overscroll-behavior: contain`+sticky 테이블 헤더+`scrollbar-gutter`. ③"버튼 빼고 어두움" = `color-scheme` 미선언 상태에서 다크 모드 브라우저가 페이지를 강제 반전 → 라이트·다크 팔레트 정식 지원(theme.css 변수, 다크 렌더링 검증 완료). CLAUDE.md에 **작업 사이클 규칙** 명문화(기능 단위 구현→즉시 검증·버그 수정→커밋, 사용자 보고 버그 최우선, 폴리시는 디자인 시안 후 일괄).

## 2026-07-21
- MVP 코어 완료(오프라인 검증 통과): A2A+x402 3단계 결제, AP2 mandate 한도, x402 결제 코어, 규칙 기반 판단, 목 시세 피드. devnet 라이브 경로(setup_devnet.py + run_demo.py --live) 준비됨.

## 2026-07-22
- 0721 킥오프 세션 분석 반영: Gemini는 무료 티어 API 호출로 확정(ADK/Vertex 불필요), 평가 네트워크 Devnet/Localnet, 목업 심사 제외, 검증 4단계 루틴 채택.
- Claude Code 로컬 개발 체제로 전환. 시작 패키지(CLAUDE.md, docs, .gitignore, .env.example) 적용.
- Claude Code 첫 세션: 로컬 환경(Win11·Python 3.10.8) 검증 통과 — 오프라인 데모 전 과정 재현 OK. 수정: requirements.txt 한글 주석이 한국어 Windows(cp949)에서 pip 파싱을 깨는 버그 → ASCII화, solana-py를 3.10 호환 0.38로 조정, 지갑 경로 `.wallets/`→`secrets/` 통일, .env.example 정비(WALLET_DIR 추가·Gemini 무료티어 키로 교체), .gitignore 보강(secrets/·*-keypair.json·test-ledger/), architecture.md §6 실제 구조로 갱신. localnet 라이브 tx 계획 수립, 승인 대기.
- GitHub 원격 연결·푸시 완료: https://github.com/Loda1002/SolanaAgent (private — 제출 전 public 전환). 바탕화면에 검증기 켜기/끄기 바로가기 생성.
- 결정·확인(사용자): 최종 산출물은 **웹 애플리케이션**(대시보드 → Cloud Run). 진행자 가이드 재확인 — localnet 위주 개발, devnet은 마지막 시연·제출 검증. GitHub 저장소 `SolanaAgent` 생성(원격 연결 예정). 비개발자용 검증기 온/오프 더블클릭 스크립트(scripts/start·stop_localnet.bat) 추가. 브랜치 main 개명.
- 결정(사용자): 디스코드 Devnet SOL 요청 완료. devnet 착수는 **로컬에서 UI/UX 적용 테스트까지 끝난 뒤**로 확정(진행자 가이드의 "localnet 99% → 마지막 devnet"과 일치). 허용 창 사전 설명 규칙이 CLAUDE.md 세션 운영에 명문화됨.
- **매도 사이클 + 거부 케이스 완료**: x402 역방향(주식 전송→브로커 온체인 검증→USDC 지급) 구현, 매도 대금은 AP2 예산에 환입(예산=순투입 한도). localnet 라이브 풀사이클 검증 — 매수 4·매도 1 전부 온체인 확정, 순변화 교차검증 PASS(+7 USDC 수익 시나리오). `scripts/demo_rejections.py`: 건별 한도 초과·mandate 위변조·금액 부족·미허용 종목 4종 정상 차단(로그 아카이브). 특기: 라이브 중 Gemini 무료 티어 rate limit로 3틱이 규칙 폴백 처리 — 폴백 설계 실전 검증. CLAUDE.md 현재 상태·다음 단계 갱신(1=웹 서비스화, 2=Cloud Run, 3=디자인, 4=devnet, 5=제출물).
- **다음 단계 2번(Gemini 교체) 완료**: `agents/gemini_decider.py` 신설 — Gemini API(무료 티어, `gemini-flash-latest`, developer 모드)가 시세 흐름·규칙·예산을 보고 매수/매도/보류 판단 + 한국어 이유 생성. 호출 실패 시 규칙 기반 자동 폴백(데모데이 네트워크 대비). localnet 라이브 재검증: **Gemini 판단 매수 3건 온체인 확정**, 교차검증 PASS, 아카이브에 판단 주체·이유 기록. 참고: 신규 계정 키는 `AQ.` 새 형식이고 gemini-2.5 계열이 신규 사용자에게 차단돼 `gemini-flash-latest` 별칭 사용. 연결 확인은 `scripts/check_gemini.py`.
- **다음 단계 1번(라이브 tx) localnet 완료**: WSL Ubuntu에 Agave 4.1.1 설치 → solana-test-validator 기동 → setup → `run_demo.py --live` 성공. 매수 3건 = 온체인 tx 6건(USDC 결제+주식 전달) 확정, 전후 잔액 RPC 교차검증 PASS, `artifacts/tx/20260722_1547_solana-localnet_live_buy.json` 아카이브. 수정 2건: 클라이언트 커밋먼트 Confirmed 통일(에어드랍 자금 미인식 해결), `preflight_commitment=Confirmed` 명시(Finalized 뱅크 preflight의 "Blockhash not found" 해결). 증빙 보강: 주식 전달 tx 서명 수집(기존엔 버려짐), 라이브 미확정 시 status=failed 처리, 전후 잔액 스냅샷·교차검증·JSON 아카이빙 추가. git 저장소 초기화. devnet은 SOL 확보(디스코드 요청) 후 .env 전환만 하면 됨.
