# 진행 로그

굵직한 진행(기능 완료, 결정 변경)마다 날짜와 함께 2~3줄 append. Cowork(claude.ai 프로젝트) 진행상황 문서와 수동 동기화.

---

## 2026-07-29

- **★ 사용자 지정 프런트 3건 + 재배포 — 커밋 `a65b834`, 리비전 `synapstock-00018-dhr`.** ①**`한도 바꾸기`(AP2 재서명) 카드를 개발자 서랍 → 사용자 화면으로 되돌렸다**(하루 전 `44dddee` 5번 항목을 되돌린 것). 예산 한도를 정하는 것은 개발용 조작이 아니라 제품을 쓰는 사람의 일이고, 그 한도를 코드가 집행하는 것이 제품 자체다 — 자리는 예산 카드 바로 뒤. ②**`지난 세션` 이력 카드를 그리드 → 개발자 서랍으로.** 세션ID·Gemini 비율은 우리 계측 단위라 관람자용이 아니다. **⚠ 거래다** — 축④("전에도 돌았다")·축②(Gemini 비율)의 **화면 증거를 라이브 URL 에서 뺐고**, 그 몫은 영상 G 구간과 소개서가 진다. 위 둘로 **`LAYOUT_KEY` _v11**. ③**`맨 위로` 버튼이 누른 직후 뚝 끊기듯 사라지던 것 수정** — 원인은 `transition` 목록에 **`visibility` 가 없던 것**으로, opacity 는 0.22초 페이드인데 visibility 만 즉시 hidden 이 되어 페이드가 한 프레임도 안 보였다(visibility 는 값 사이를 잇지 않고 끝에서 한 번에 바뀌므로 목록에 넣으면 페이드가 끝난 뒤 꺼진다). **⚠ 육안 검증은 못 했다** — 브라우저 패널이 화면에 없으면 프레임을 안 그려 CSS 전환이 진행되지 않는다(기존 함정). `transitionProperty` 포함만 확인.
- **배포 검증 실측(`synapstock-00018-dhr`)**: 엔드포인트 **13개 200** · 정적 파일 **5개 전부 저장소와 바이트 일치**(`/app`·`app.js`·`skeleton.css`·`landing.html`·`to-top.js`) · **SSE 첫 바이트 0.0796초** · `POST /broker/orders` → **402 + accepts[1]**(scheme `exact` / network `solana-devnet` / asset **Circle 공식 devnet USDC** `4zMMC9srt5Ri…` / `maxAmountRequired` 9980000) · `persistence=firestore` · env 에 `GEMINI_MODEL=gemini-flash-latest` 유지 · 배포본 대시보드에서 **한도 카드 표시 · 지난 세션 비표시 · 개발자 탭 rect 0×0 · 콘솔 오류 0**. ※ 요청 body 는 `{"symbol","spend_usdc","price_usdc","mode"}`(`OrderBody`, `extra=forbid`) — `quantity` 로 보내면 **422** 가 난다.
- **⚑ 촬영 흐름 검증**: **`?lab=1` 로 세션을 시작한 뒤 `?lab=0` 으로 새로고침하면 세션은 계속 돌면서 개발자 UI 만 완전히 사라진다**(실측: 틱 12 → 76, `status=running`, 주소창 `/app` 로 정리, 긴급정지·세션 종료 유지). 촬영본에 개발용 조작부가 한 번도 안 비친다.
- **⚠ 전날 기록의 사실 2건 정정.** ①**`docs/artifacts/demo_script.html` 은 구버전이 아니다** — 저장소본과 게시본 URL 둘 다 장면 7개(A~G) 현행이고 축② 유일 근거인 `★ E. 공격 2` 가 들어 있으며, 금지 문장은 *금지 표시로* · 폐기 KPI(`시도 20 · 차단 6`)는 *폐기 표시로* 적혀 있다. **대본은 한 벌이고 확정 대기 항목이 아니다.** ②**배포본은 `00016-k8p` 이 아니었다** — `00017-fdj` 가 07-29 00:25 에 이미 올라가 있었다(= `44dddee` 반영본).
- **화면 다듬을 곳 실측 목록(사용자 판단 대기, 코드 변경 없음).** **[결함] 거래 내역이 세션 경계를 구분하지 않는다** — 서버 `counts.trades=1` 인데 화면 표는 **2줄**이고 하나는 45초 전 끝난 다른 세션 것. `/api/state` 에 `trades` 키가 아예 없어 표가 **SSE 히스토리 재전송(`?since=0`)만으로** 채워지는데 `case "trade"` 에 세션 필터가 없다. 판단 피드·활동 로그는 `past-session` 구분선으로 이미 처리돼 있고 **거래 표만 빠졌다**(캔들 중복을 막았던 것과 같은 계열). 나머지는 판단 사항: `손익(실현)` 카드의 `0 USDC` + `수익률 6.22%` 자기모순(`pnl.return_pct` = `valuation.unrealized_pct`) · 예산 `전량(올인)` vs 한도 입력칸 `50` 불일치 · 차트 노브 15개 + 진행도 2중 표기 · 첫 줄 3장이 전부 돈 얘기 · `tAAPL` 툴팁(I5) · 세션 시작이 서랍 안이라 심사위원이 라이브 URL 에서 세션을 못 켠다. **가드 패널이 첫 줄을 가리는 것은 사용자 의도(일부러 확인을 유도)라 손대지 않는다.**
- **3분 영상 축 매핑 — 구멍은 축③ 하나다.** A 22s=①주장 · B 18s=①혁신성 · C 35s=④(샌드박스라 온체인 아님) · D 25s=①+로그 · **E 35s=② 유일 근거** · F 25s=② · G 20s=④ → **축③(블록체인·인프라 연동)이 주축인 장면이 0초**다. 처방은 **D 앞 6초 컷 신설**: 배포 URL 에 `curl -i -X POST /broker/orders` 로 나오는 **402 원문**(x402 를 "붙였다"가 아니라 "구현했다"의 차이 · asset 이 Circle 공식 민트라 자체발행 논란을 비껴감 · **심사위원이 그대로 재현 가능**). 시간은 A −4초·B −3초(E 는 절대 불가). **축① 상업성은 C 구간의 `브로커 수수료 0.3% · 누적 0.30 USDC` 줄을 1~2초 클로즈업하면 추가 시간 0초로 채워진다.**
- **개발자 전용 잔여 5건 정리 — 커밋 `44dddee`.** 전날 보고한 후보 7건 중 사용자가 **1·2·3·4·6 제거**를 지정하고 5(푸터 지갑 공개키)·7(판단 출처 enum 범례)은 남기기로 했다. ①**거래 내역 `결제 tx`·`전달 tx` 두 열을 라이브에서만 표시**(`.col-tx` + `body.live-mode`) — 샌드박스는 전송이 없어 값이 전부 `—` 라 빈 열 둘이 늘 남아 있었다. **완전 삭제는 하지 않았다**: 라이브의 explorer 링크가 "화면이 아니라 진짜 트랜잭션" 증거라, 감추되 만들기는 그대로 둬 모드 전환에 바로 따라오게 했다(양방향 실측 확인). ②**지난 세션 표의 `틱` 열 제거**(엔진 내부 단위) — `emptyRow` colspan 7→**6** 동반 수정. `Gemini 비율`은 축② 근거라 유지. ③푸터 `실시간 연결됨 (SSE)` → `실시간 연결됨`. ④시세 카드 `틱 N · 간격 0.15s` → 날짜 + `(N/총봉)`(합성 패턴 피드는 봉 총수가 없어 그때만 틱 수를 그대로 쓴다 — 그쪽은 실제로 틱이 단위다). ⑤**`한도 바꾸기`(AP2 재서명) 카드를 개발자 서랍으로** — `DEFAULT_LAYOUT` 에서 `mandate` 제거·**`LAYOUT_KEY` _v10**·서랍 카드 구분선 추가. 검증: 테스트 **22종 통과** · red_team `rc=0`(유출 0.00 · 오탐 0) · 로컬 dry 80틱 완주 · 콘솔 오류 0.
- **⚑ 사용자 결정 — 촬영 걱정은 8/1 까지 하지 않는다.** 디자인 시안이 와야 완성이고 완성돼야 촬영이므로 **재촬영은 보류·연기 대상이 아니다.** 이전 기록에 남아 있는 "재촬영 사유라 손대지 말 것" 제약은 8/1 까지 무효 — 화면 구조·문구·카드 배치를 자유롭게 바꿔도 된다. 랜딩 `디스커버리` 탭도 **그대로 두기로 확정**(확인 대기 해제). **배포본은 아직 `synapstock-00016-k8p`** 로 이 5건이 빠져 있고, 다음 라운드에서 화면을 더 다듬을 예정이라 **재배포는 그때 한 번으로 묶는다**.

## 2026-07-28

- **★ P5-2 프런트 2차 전달본 병합 + 사용자 수정 9건 — 커밋 `56af89d`, 리비전 `synapstock-00016-k8p`.** 전달본(07-28 22:17)은 실질이 두 가지였다: 랜딩 상단 **탭 슬라이더**(`.tabs`+`.glider`)와 **맨 위로 버튼**(`.to-top` + 신규 `web/static/js/to-top.js`, 랜딩·대시보드 공용). 회귀 대조(LF 정규화 후 양방향 `diff`)에서 **되돌아간 것 0** — 저장소에만 있던 줄은 `landing.html` 4줄뿐이고 전부 새 탭 메뉴로 대체된 옛 nav 링크였다. `app.js` 는 이번 배치에 없어 저장소 현행본 유지. **`hoverme.css`·`clickme.css` 는 배치하지 않았다** — 어디서도 참조되지 않고 `button {}` 전역 선택자에 색을 하드코딩(`--color:#560bad`)해 링크하는 순간 모든 버튼을 덮고 다크 모드가 깨진다(`docs/design/incoming/p5-2/` 에 참고용 보관). **⚠ 랜딩 nav 에 `디스커버리` 탭이 되돌아왔다** — P5 전달서가 "처음 온 사람이 메뉴를 누르면 날 JSON 을 마주친다"는 이유로 뺐던 항목인데 전달본 주석이 "사용자 요청으로 다시 넣었다"고 적고 있어 **그대로 두고 사용자 확인 대기**.
- **차트를 실제 주식 화면처럼 — 사용자 요청 1~4.** "MA50 이 너무 짧다"의 원인은 **캔들 이력을 60개만 들고 그 60개 위에서 이동평균을 구한 것**이었다(MA50 은 오른쪽 끝 10여 점, MA100·200 은 성립조차 못 함). 이제 이력을 전부 들고 **전체 위에서 계산한 뒤 보이는 구간만 잘라 쓴다** → **MA100·MA200 추가**, MA50 이 화면 전체를 지난다. 봉이 모자란 이동평균은 범례를 흐리게(`.ma-off`) 표시해 "선이 왜 없지"를 화면에서 답한다. **가로 스크롤**(드래그·Shift+휠)·`최신 ▸` 복귀·**보기 범위**(60/120/240/전체). 분봉·일봉 대비는 **`groupCandles`(봉 N개 묶기)** 를 분리해 화면 선택으로 노출 — 서버가 진짜 분봉/일봉을 내려주면 캔들 배열 모양이 같아 이 함수는 그대로 두고 피드만 바꾸면 된다.
- **개발자 전용 서랍 신설 — 사용자 요청 5·6.** 상단 배너에서 `solana-devnet`·`엔진 대기`·`판단`·`시세` 배지를 빼고, **세션 설정 카드를 통째로 서랍(`.dev-dock`)으로** 옮겼다. `?lab=1` 로 켜고 `?lab=0` 으로 끄며 **꺼져 있으면 탭도 서랍도 화면에 없다** — 런칭 때 개발용 UI 감추기가 스위치 하나로 끝난다. **DOM 에서 빼지는 않는다**(`app.js` 가 매 갱신마다 값을 쓰므로 없애면 null 참조로 대시보드가 통째로 죽는다). 카드 이동은 `app.js` 가 하고, 스크립트가 죽으면 그리드에 남아 조작이 막히지는 않는다. `DEFAULT_LAYOUT` 에서 `session` 제거·**`LAYOUT_KEY` _v9**. 요청 7(문구 축약)은 **지우지 않고 `title` 로 내리는** 방식으로 처리했다(KPI 라벨 4개·AI 카드 6개·예산·평가손익·한도·손익·브리핑·종목별 요약·태그라인).
- **⚠ 이 라운드에서 새로 드러난 결함 3건 — 전부 닫았다.** ①**새로고침하면 같은 봉이 두 번 쌓였다**(실측: 501봉 세션이 **718봉**, 지난 세션까지 얹히면 100봉 세션이 **451봉**). SSE 가 `?since=0` 으로 이벤트 히스토리를 처음부터 재전송하는데 그게 `/api/state` 스냅샷 위에 겹친다 — **캔들 상한이 60이던 시절에는 겹친 만큼이 잘려 나가 안 보였다.** `isFresh(evt)` + 틱 번호로 두 겹 차단(틱 번호만으로는 세션이 바뀌면 못 막는다 — 이벤트 버퍼에 지난 세션이 남아 있고 틱이 1부터 다시 시작한다). ②체결·가드 이벤트마다 도는 `fetchState` 가 차트 이력을 스냅샷 길이로 **되감던 것** → `mergeCandles`. ③차트가 **휠을 통째로 가로채** 대시보드를 스크롤하다 커서가 차트에 닿으면 페이지가 멈췄고, 트랙패드 핀치가 `ctrl+휠` 로 들어와 **손대지 않아도 배율이 바뀌었다**(60봉으로 시작한 차트가 43봉이 되어 있었다) → 그냥 휠은 가로채지 않고 확대·축소는 선택으로만. 함께 `engine.MAX_CANDLES` 90→**300**(화면 표시 전용 버퍼라 판단·정산 경로에 닿지 않는다). **차트 조작부·범례는 그래프 아래로 내렸다** — 위쪽은 가드 요약 패널이 떠 있는 자리라 범례를 **100% 가렸다**(실측).
- **검증**: 테스트 **22종 전부 통과** · red_team `rc=0`(공격 7 · 서명 전 차단 5 · **유출 0.00** · 정상 14건 오탐 0) · 로컬 dry 하락장 **481틱 완주**(가드 시도 38 · 차단 0 · 유출 0.00) · 새로고침 후 봉 중복 0 · 콘솔 오류 0. 배포본 `synapstock-00016-k8p`: 엔드포인트 **13개 200** · 화면 파일 **6개 전부 저장소와 바이트 일치** · SSE 첫 바이트 **0.107초** · `POST /broker/orders` → **402 + accepts[1]**(scheme `exact`, asset = Circle 공식 devnet USDC `4zMMC9sr…`) · `persistence=firestore` · env 에 `GEMINI_MODEL=gemini-flash-latest` 유지 · 배포본 대시보드 이력 10건 렌더 · 배너 배지 0 · 개발자 서랍 비표시 · 콘솔 오류 0.
- **⚠ 촬영 전 운영 변경 1건**: **배포 URL 을 그냥 열면 세션을 시작할 수 없다**(세션 설정이 서랍 안, 서랍은 기본 꺼짐). 촬영·시연은 `https://…/app?lab=1` 로 한 번 열면 되고 localStorage 가 기억한다. 배포본은 어차피 `CONTROL_TOKEN` 이 조작 API 를 막고 있어 외부인에게는 달라지는 것이 없다(오히려 눌러도 401 이 나던 버튼이 사라진다).
- **⚠ 줄바꿈 판별 정정**: 이 환경에서는 **Git Bash 의 `od -c` 파이프도 CR 을 놓친다**(이번에 전 파일이 LF 로 잘못 나와 오판할 뻔했다). **PowerShell 바이트 스캔만** 믿을 것. 저장소는 `index/skeleton/theme/app` = CRLF, `landing/reveal/wallet/to-top` = LF 로 섞여 있고, 각 파일의 기존 방식에 맞춰 기록해야 `git diff` 가 실제 변경분만 남는다(이번 결과: 120줄 추가·4줄 삭제).
- **관제탑 아티팩트를 `next_session_plan.md` 구조(P1~P4)로 전면 재편 — 체크리스트 41건.** 오늘 할일 대화에서, 계획서의 각 항목이 **실제로 어디까지 됐는지 저장소 코드를 직접 grep 해 확인**한 뒤 반영했다(추정 없음). 그룹 6개: **오늘(P1 잔여·P3 상업성) 5** / **제출물 3종 11** / **public 전환 전 정합성 8** / **최종 버그픽스 5** / **배포·실증 7** / **여유·멘토링 5**. 완료 표시 5건(검사 종수 통일·PDF 색 유실·bug_latest 배너·프런트 병합 재배포·지갑 서명 4종 흡수). **실측으로 확정한 미착수 판정**: `blocked_unverified` 0건(P1-2) · `app.js` 의 `/api/history` 호출 0건(P1-3) · pitch_deck 에 AgentFabric·Circle·Kyvern 0건이고 수익모델 3박스가 전부 정성 서술(P3) · pitch_deck `:538` 'USDC 순변화 −4.9'·`:696` "파는 쪽을 보호합니다"·`:563`·`:569` 스크린샷 자리표시자 그대로(P2-3) · `demo_script.html` 에 '의미 대조' 0건이고 `:360` 이 폐기 KPI(P2-2) · `differentiation.md:3` '트레이딩 봇 0건' 잔존 · `FEATURES.md:37`(G5 완료) vs `:125`(HTTP402 0줄) 자기모순 · `.env.example:40` 여전히 `=1` · `START_GUIDE.md`·`prompts/` 추적 중 · LICENSE 없음 · **`engine.py:1227` 매수 `== "settled"` vs `:1339` 매도 `in ("settled","partial")` — CODE-01 미수정 재확인**. 반대로 P1-1 의 `#token` 해시 처리는 `app.js:1167-1173` 에 **이미 있다**(남은 건 운영 규칙뿐).
- **실증 KPI 를 07-28 `red_team --report` 실행값으로 교체.** 의미 대조 공격이 추가돼 **공격 7 · 구매자 서명 전 차단 5 · 판매자측 1 · 사후 탐지 1 · 서명 전 유출 0.00 · 회수 불가 29.99 · 정상 14건 오탐 0 · 가드 없을 때 551.96**(직전 표기 공격 6·차단 4 는 낡았다). 테스트 20종. 심사 4축은 부서 완주 결과대로 **축① 중 · ②③④ 강**으로 갱신하고, 축① 카드에 "사유가 상업성 정량 근거 0건 하나로 특정됨"을 명시했다. 잔여 버그 표도 9건 → **6건 + 신규 CODE-01** 로 정정(BUG-02·04·08 은 `7a2b251` 로 닫힘). 공지 2개를 **"화면을 바꾸는 창은 1차 촬영 전·2차 촬영 전 두 개뿐"** 과 **"촬영 순서 지뢰(explorer 에 자체발행 민트가 비친다) — A안 재실증 당기기 / B안 G 구간 대체"** 로 교체. 저장 키 `v4`.
- **프런트 전달본 병합 + 재배포 완료 (커밋 `b0c0a53`·`e2be625`, 리비전 `synapstock-00009-tm6`).** 프런트에서 재작업해 보내온 5개 파일(app.js·index.html·login.html·theme.css·skeleton.css + 뒤이어 reveal.js)을 병합. 전달본이 저장소의 조금 앞선 시점을 기준으로 작업돼 최근 반영분이 빠져 있어, **빠진 것만 채우고 나머지는 그대로** 적용했다. 새로 들어온 것: 얇은 헤더(`.slim-bar`)·402 Guard KPI 를 카드에서 **떠 있는 패널**(`.guard-dock`)로 이동(접기 탭·차단 상세 줄)·카드 hover 플로팅·스크롤 등장 모션·대시보드 좌우 여백(`--page-pad`)·대시보드 ABOUT 제거(랜딩 `#about` 일원화).
- **되살린 것 7건(전부 전달본에 없던 우리 현행 기능).** ①**지갑 연결 = 로그인**(`wallet.js` 로드 + 헤더 `[data-wallet-status]`/`[data-wallet-connect]`) — 전달본은 `/login` 링크였다. 얇은 헤더 쪽만 `/connect` 링크로 둔 이유는 `wallet.js` 가 버튼을 하나만 배선해(`querySelector`) 두 개를 두면 한쪽 라벨이 "지갑 연결"에 굳어 눌러도 반응하지 않기 때문. ②**AI 카드**(`data-card="ai"`, 판단/청구서 두 레이어) — 심사 축② 근거. ③`renderAi()`·`SOURCE_LABEL`·`guard_semantic` 로그. ④가드 차단 레그 분해(`data-guard-legs`). ⑤`.src-rule-gate` + `--badge-gate`(없으면 배경 없는 흰 글자로 안 보임). ⑥**전략 기본값 추세추종**(전달본은 조건형 — 쿼터 소진 시 매수 전건 보류). ⑦**종목 체크박스 잠금 조건** — 전달본이 추세추종까지 잠그는 옛 버전으로 되돌아가 있었다(엔진이 실제 거부하는 건 라이브·목시세뿐). 카피는 저장소 현행본 유지.
- **`reveal.js` 는 전달 누락이었다** — 전달본 `index.html`·`skeleton.css` 가 부르는데 파일이 없었다(`<html>.reveal-ready` 를 붙이는 주체라 없으면 숨김 CSS 가 아예 안 걸려 모션만 사라진다). 임시로 채웠다가 원본 수령 후 교체. 원본이 나은 점 3가지: IntersectionObserver 콜백 배열은 문서 순서를 보장하지 않는데 직접 정렬해 시차가 위→아래로 보장 · `--stagger-step` 을 각 요소에서 읽어 영역별 덮어쓰기 가능 · 모션 최소화/IO 미지원을 `is-done` 즉시 표시로 한 곳에서 처리.
- **`login.html` 은 전달본을 적용하지 않았다**(사용자 확인). 전달본이 이메일·비밀번호 자리표시 폼이라, `58586c7` 에서 `/connect` 리다이렉트로 바꾼 것을 되돌리게 된다 — "비밀번호를 만들지 않습니다"를 내건 제품이 같은 도메인에 살아 있는 비밀번호 폼을 서빙하는 자기모순. **가드 패널이 첫 줄 카드 위쪽 76px 을 가리는 것은 디자이너 확인 후 그대로 유지**(실측 패널 y153~315 vs 카드 y239).
- **검증**: 로컬 — 리소스 9개 200·콘솔 오류 0·드라이런 세션(틱 47/80, 체결 1건, 가드 시도 1·차단 0·유출 0.00)·AI 카드 실데이터 반영("판단 4건 — 규칙 4")·시차 실측 0/0.05/0.1/0.15/0.2초. 배포본 — **엔드포인트 11개 전부 200** · 랜딩 헤드라인 `손을 떼려면,` · `persistence.enabled=true`(firestore) · `state.ai.invoice_semantics` 방출 · `state.guard` 에 `blocked_buy`/`blocked_sell` · **SSE 첫 바이트 0.063초** · **서명 메시지 `URI: https://`** · 비밀번호 폼 0건 · `/static/login.html` → `/connect` · 콘솔 오류 0. env 에 `GEMINI_MODEL=gemini-flash-latest` 주입 확인.
- **[해결] 랜딩 등장 모션 누락 + 재배포 (커밋 `ce105fc`·`def229d`, 리비전 `synapstock-00010-5qb`).** 사용자 제보로 확인 — **`landing.html` 에 `<script>` 태그가 하나도 없어** reveal.js 가 안 실렸다. `<html>` 에 `reveal-ready` 가 안 붙으니 숨김 규칙이 애초에 안 걸려 **요소는 그냥 보이고 모션만 통째로 빠지는** 형태라 눈에 안 띄었다. `<head>` 에 한 줄 추가. 등장 대상은 `.hero-copy` 자식 6 · `.hero-preview` · `.landing-about` 자식 4 = **11개**이고, 흐릿한 미리보기 안의 `.card` 들은 선택자가 `.grid > .card` 인데 이 페이지에 `.grid` 가 없어 대상이 아니다(실측 0개 — 목업 카드가 따로 떠오르지 않는다). 배포본 실측: 히어로 시차 `hero-title=0s · hero-sub=0.05 · hero-cta=0.1 · hero-claim=0.15 · hero-note=0.2 · hero-proof=0.25 · hero-preview=0.3`, 스크롤 후 `opacity 0` 잔류 0개, 엔드포인트 7개 200, SSE 첫 바이트 0.068초, `GEMINI_MODEL` 유지 확인.
- **런북 수정 (`def229d`)**: `docs/deploy_cloud_run.md §4` 배포 명령에 `GEMINI_MODEL=gemini-flash-latest` 를 포함시키고, `--set-env-vars` 는 **기존 env 를 전부 덮어쓴다**는 경고를 달았다(부분 갱신은 `--update-env-vars`). 지난 두 번의 배포에서 매번 손으로 다시 넣어야 했던 자리 — 이제 런북을 그대로 복붙해도 쿼터 소진 모델로 되돌아가지 않는다.
- **★ P2-4(저장소 정합성) 완료 — 커밋 7건.** public 전환은 되돌릴 수 없으므로 "지금 공개하면 스스로 감점되는 것"만 골라 닫았다. `73042e4` **DOC-08**: `differentiation.md §0` 의 결함 A~I 9건이 전부 현재형이라 "이 팀이 스스로 밝힌 현행 보안 결함"으로 읽혔다 — 9건 전부 **코드에서 해결 여부를 직접 확인한 뒤** '상태·해결 커밋' 열 신설(A `89cde31`/`9701d90` · B,C,H `9701d90` · D,E `9523d19` · F `70bed10` · G `329885f` · I `90c5c33`/`2981739`/`8fa20e1`) + 문서 최상단에 단일 출처 안내. 같은 커밋에서 **금지 문장 2곳**("x402 는 파는 쪽을 보호한다" — 스펙의 `maxAmountRequired`·구매자 로컬 서명으로 반박됨)을 소개서 정본과 같은 문안으로 교체하고, **GAP-06** 버그 리포트 상단에 스냅샷 배너 + 수정 완료 4건 표를 달았다. `6cef4a7` **DOC-07**: `FEATURES.md` 가 §1 에서 "G5 완료"라 적고 §3 에서 "HTTP 402 가 코드에 0줄"이라 적어 **축③ 최강 증거를 자기가 부정**하고 있었다 — G5·devnet 행을 완료로 옮기고 숫자를 전부 실측화(증빙 7→**8건**, 카드 11→**13개**, 테스트 20→**21종**, G3 KPI 를 07-28 실행값으로). 축① 강도도 자평("재포지셔닝으로 상승")에서 부서 판정 '중'으로 되돌렸다. `38542cd` **DOC-06/GAP-09**: `.env.example` 이 바로 위 주석·`config.py` 기본값과 반대(`ALLOW_LIVE_FROM_WEB=1`)였고 setup_devnet 설명이 정반대(지금은 Circle 공식 민트를 그대로 쓰고 자체 발행 감지 시 중단)였다 + 누락 키 4종. `7dce70c` **GAP-08**: LICENSE(MIT) 신설 — `data/market/*.csv` 는 Alpha Vantage 출처라 재배포권을 줄 수 없어 NOTICE 절로 "데모·평가 목적" 예외를 명시, 금융 성격 오해 방지 문단 포함. README 의 `<배포URL>` 자리표시자를 실행 가능한 curl 로 교체(배포 URL 에서 **402 + accepts[]** 실측). `5693f0e`·`3bd0c67` **DOC-10**: CLAUDE.md 최상단에 "내부 작업 노트이며 제품 설명이 아님" 배너 + 자기 파괴적 표현 5곳 중립화 + 반증된 근거("수상작 16건 중 트레이딩 봇 0건")에 인용 금지 표시, `START_GUIDE.md`·`prompts/` 추적 해제(사용자 승인). **시크릿 최종 점검 통과** — 추적 파일 중 0건, 유일한 `AIza` 패턴은 `test_brain_select.py:61` 의 더미(길이 41·접두사 뒤 영문자만).
- **★ P4(최종 버그픽스) 심사 노출건 완료 — 커밋 4건 + 회귀 게이트.** `0bec828` **BUG-09**: `_finalize` 가 status 를 먼저 idle 로 내린 뒤 세션 요약을 await 해서, 그 창(배포본은 실제 Firestore 왕복)에 새 세션이 통과하면 **끝난 세션의 마무리 코드가 실행 중인 세션의 긴급정지를 해제**하고 남의 실적으로 ENGINE_STOPPED 를 쐈다. 엔진 소유권을 놓는 두 줄을 finally 맨 마지막으로 옮겼다(`stop()` 은 어차피 `await self._task` 라 응답이 안 느려진다). 회귀 `scripts/test_finalize_race.py` 18건 신설 — **음성 대조 수행**(옛 순서를 임시 복원하면 3건 실패하고 그중 하나가 `attempt={'started': True}` = 다음 세션이 실제로 시작된다). `400296b` **BUG-10**: 독스트링·UI 툴팁이 "실제 고가·저가를 지나는" 이라 단언했는데 표본 격자가 2/4/8 이라 고가·저가 웨이포인트(t=1/3·2/3)를 건너뛴다 — 인트라바 자체는 사용자 보류 결정이라 손대지 않고 문구를 사실에 맞추고 `test_intraday` 에 검증 5건 추가(sub=3·6 일치 / 2·4·8 불일치를 단언). `2a2b983` **CODE-06**: 가드가 멈춰도 `🛑 매매 정지됨 (guard)` 뿐이라 **왜 멈췄는지가 어디에도 없었다** — `pause(reason=)` + 액터 표시명(human→사용자, guard→402 Guard, 저장소에 없는 `risk-guard` 는 매핑 안 함) + 가드 정지 3곳에 사유 문장. 브라우저 실측으로 배지·로그 렌더 확인. `a8dcdb8` **BUG-05**: 매도 partial 은 주식 전송이 온체인 확정된 상태인데 포지션이 안 빠져 총자산이 부풀었다(유출 KPI 는 정확한데 총자산만 낙관 = 두 숫자가 모순).
- **⚠ 버그 리포트 수정안을 검증 없이 복붙하면 안 된다는 사례 2건.** BUG-09 의 1순위 안(`finalizing_sid` 캡처 비교)은 session_id 가 초 단위라 **이 버그를 못 막는다**. BUG-05 의 제안(`status in ("settled","partial") and confirmed`)은 그대로 넣었더니 **드라이런 정상 매도가 포지션에서 안 빠졌다**(드라이런은 confirmed=False) — 회귀 게이트에서 `test_dry_sell`·`test_trend` 6종 시퀀스 불일치로 잡혔다. 회귀 게이트를 안 돌렸으면 데모 수익률이 통째로 깨진 채 나갔을 상황이다.
- **★ P1-3·P1-5 완료 — 커밋 2건.** `e919e8c` **P1-3**: 대시보드에 **'지난 세션' 이력 카드**. 서버(Firestore)에는 세션 기록이 쌓여 있는데 `app.js` 가 `/api/history` 를 **한 번도 호출하지 않아**(grep 0건) 심사위원의 첫 화면이 전부 0 이었다. 열은 계획서 경고대로 좁게 — 세션·모드·종목·틱·체결·Gemini 비율·시작시각(유출 열은 **세션 레코드 스키마에 아예 없어** undefined 가 나가고, 실현손익은 "수익률이 아니라 지출 통제"라는 첫 문장과 충돌한다). 카드는 맨 뒤라 첫 화면(가드 KPI→결과→AI 근거)을 밀어내지 않고, `LAYOUT_KEY` 는 올리지 않았다(`applyLayout` 이 새 카드를 자동으로 맨 뒤에 붙이므로 저장 배치를 리셋할 이유가 없다). **⚠ 갱신 시점이 함정이었다** — `ENGINE_STOPPED` 는 세션 요약 **저장 전에** 나가므로(BUG-09 수정으로 확정된 순서) 그 이벤트에서 이력을 다시 읽으면 방금 끝난 세션이 없다. 서버는 저장이 끝난 뒤에야 status 를 idle 로 내리므로 `renderState` 의 실행→대기 전환에서 갱신한다. `31d5361` **P1-5**: 404 가 `{"detail":"Not Found"}` 라 주소를 잘못 친 심사위원이 막다른 길을 봤다 → 브라우저에는 복귀 링크가 있는 HTML, `/api/*`·`/broker/*`·`Accept: application/json` 에는 기존 JSON(커스텀 detail 보존, 경로는 `html.escape`). `OPS-06`·`OPS-07`·`UX-09` 는 코드가 아니라 **운영 결정**이라 런북 **§6-2** 로 신설했다(`BROKER_HTTP_URL` 을 배포 env 에 넣지 않기 — 넣으면 무인증 POST 로 대기열을 채우는 경로가 열린다 / 촬영 세션 60~120봉 — 클라이언트 상한이 서버보다 낮아 긴 세션은 초반이 화면에서 사라진다 / 촬영은 라이트 모드 고정 — 다크 배지 대비 1.86:1 은 8/2 시안이 덮어쓰므로 디자이너 제약으로 넘긴다).
- **다음 라운드 정의 — P5 '제품 화면으로 다듬기'(`next_session_plan.md §P5` 신설).** 배포본 `synapstock-00014-hft` 를 브라우저로 직접 훑어 **개발용이 제품 화면 자리에 있는 것**을 목록화했다(추정 아님): 세션 설정 드롭다운 **10개가 전부 실험 노브**(목 시세·데이터셋 상승장/하락장·인트라바 2·4·8봉·재생 속도 0.15~8초·AI 판단 모드·추세 신호 4종·적립 주기) 중 **`적립형 (성능 평가 위해 임시 비활성화)` 는 내부 사정이 옵션 라벨에 그대로 적혀 있다** · 판단 출처 범례가 내부 enum(`gemini·rule·rule-fallback·rule-gate`) · 세션ID `20260727_135727_dry` · 브리핑 카드에 **윈도우 파일 경로**(`artifacts\briefings\…`) · 푸터의 배치 드래그 안내 · 긴급정지/재개 버튼이 **두 벌**(실측 4개) · 카드 제목이 개발 용어(`예산 (AP2 한도)`·`AP2 mandate 재서명`). **방향은 지우기가 아니라 분리**(간단/고급) — 이 노브들은 실제 검증에 쓰므로 없애면 재현성이 떨어진다. **제약 3가지를 함께 못 박았다**: ①촬영 창과 충돌하므로 1차 촬영과의 순서를 먼저 정할 것 ②8/2 시안이 `theme.css` 를 덮어쓰므로 **색·글꼴·간격은 건드리지 않고** 정보 구조·카피·노출 범위만 ③축②③④ 증거는 위치 이동까지만, 첫 화면 KPI 4칸은 재촬영 사유라 칸 수 불변.
- **✅ 재배포 완료 — 리비전 `synapstock-00014-hft`.** 배포 전에 `/api/state` 로 엔진이 idle 인지 먼저 확인했다(`OPS-05`: 세션이 도는 중에 새 리비전을 올리면 구 인스턴스가 종료되면서 그 세션이 저장되지 않는다). 런북 §4 한 줄 명령을 그대로 사용 — `GEMINI_MODEL=gemini-flash-latest` 가 명령에 포함돼 있어 이번엔 손으로 다시 넣을 필요가 없었다(`def229d` 의 런북 수정이 실제로 값을 했다). **검증 실측**: 엔드포인트 11개 200 · `app.js` 에 `fetchHistory`·`lastEngineStatus`·`actor_label` 존재 · `index.html` 에 `data-card="history"` 와 BUG-10 정정 툴팁 · **404 가 text/html + 복귀 링크**이고 `/api/nope` 는 그대로 `application/json` · SSE 첫 바이트 **0.067초** · `POST /broker/orders` → **402 + accepts[]** · `state.guard.blocked_unverified`·`state.ai.invoice_semantics` 방출 · `persistence.enabled=true(firestore)` · 배포 env 에 `GEMINI_MODEL` 유지 확인 · **배포본 대시보드에서 이력 카드가 실제 세션 10건을 렌더**(드라이런/라이브 라벨·틱·체결·Gemini 비율·시각 전부 정상) · 콘솔 오류 0.
- **회귀 게이트(P4-4)**: 테스트 **22종 전부 통과**(`test_finalize_race` 신설) · red_team `rc=0` 불변(공격 7 · 서명 전 차단 5 · 판매자측 1 · 사후 탐지 1 · 유출 0.00 · 정상 14건 오탐 0 · 가드 없을 때 551.96) · **cp949 콘솔에서도 변경 5종 rc=0**(심사위원 환경 재현).
- **[집계 누락 정정] 프런트 2차 전달본 병합 `2304658` 이 문서에만 빠져 있었다.** 별도 CLI 세션에서 병합·커밋·푸시까지 정상 완료됐는데(2026-07-28 13:44, AI 카드 '자세히 보기' — 판단 타임라인·협상 로그를 기본 접기 + 청구서 레이어로 열면 심사 줄만 남기는 필터 + `LAYOUT_KEY` _v7), 관제탑 문서(`CLAUDE.md`·`next_session_plan.md`·이 로그)에 한 줄도 없어 07-28 진행분이 커밋 9건으로 집계돼 있었다. **git blob 해시로 재대조해 적용 여부를 확정**: `docs/design/incoming/` 의 전달본 5개 중 `theme.css`·`reveal.js` 는 저장소와 완전 동일(파일 크기 69바이트 차이는 CRLF/LF 뿐), `app.js`·`index.html`·`skeleton.css` 는 전달본 **+11/+6/+1 줄**이고 그 줄들은 커밋 메시지가 스스로 "병합 즉시 수정(전달본에 없던 것)"이라 밝힌 심사 줄 0건 안내다. **전달본에만 있고 저장소에 없는 줄은 0** = 디자인 반영 누락 없음. 전달본 원본 5개는 `docs/design/incoming/` 에 untracked 로 남아 있다(`.gitignore` 규칙 없음 — `git add` 된 적이 없을 뿐).
- **⚠ 모션 검증 시 함정(내가 한 번 오판함)**: 브라우저 패널이 화면에 떠 있지 않으면 프레임을 그리지 않아 **CSS 애니메이션이 진행되지 않는다.** 그 상태에서 JS 로 opacity 를 읽으면 `is-visible` 은 붙었는데 값이 `0` 이라 "재생이 멈췄다"고 잘못 판단하게 된다. **스크린샷을 찍어 합성을 강제한 뒤 다시 읽으면** 곧바로 `opacity 1`·`is-done` 이 된다. 실제 사용자는 화면이 보이는 상태이므로 해당되지 않는 조건이다.
- **★ P5 전달서 작성 — `docs/frontend_p5_handoff.md` (화면 파일 0줄 편집).** `/app`·`/` 에서 개발·검증용과 이용자용을 갈라 프런트가 그대로 작업할 수 있는 문서로 만들었다. **대시보드 28행 · 랜딩 5행**, 각 행이 `data-card`·`file:line`·실측 라벨 + 3분류(유지/개발용이라 뒤로/심사 증거라 위치만) + 처리(그대로·감춤(고급)·개명·이동, **개명은 문구 확정**) + 근거 한 줄. 배포본 `synapstock-00014-hft` 를 브라우저로 직접 훑은 실측이다. **사용자 결정 2건**: ①**반영 먼저 → 촬영 7/30~31**(프런트 마감 = 7/30 오전, 늦으면 §7-A 만 받고 촬영) ②**간단/고급 토글 + `?lab=1`** — `/lab` 라우트는 대시보드가 두 벌이 되어 마감 6일 전 회귀 표면이 2배가 되므로 채택하지 않았다(채택안은 백엔드 0줄).
- **⚠ P5 초안 목록이 실측으로 2건 뒤집혔다.** ①**'긴급정지 두 벌'은 사실이 아니다** — DOM 은 4개지만 `scrollY=900` 에서 슬림바만 `is-shown` 이고 헤더는 화면 밖(`bottom=-747`)이라 **화면에는 늘 한 벌**이다 → 전달서에 `그대로`(건드리면 로직만 위험)로 적었다. ②**가드 패널 가림은 76px 이 아니라 194px** — 1040×982 에서 dock `[274,153,994,364]` 가 `pnl`·`valuation`·`position` 을 **각각 면적 33%** 덮어 세 카드의 제목과 대표 숫자가 첫 화면에서 안 보인다. '디자이너 확인 후 유지' 결정이 있어 프런트에는 임의 변경 금지로 적고 **촬영 전 사용자 결정 사항**으로 남겼다.
- **[백엔드 몫 즉시 처리] 브리핑 본문에 raw 예외 문자열이 섞이던 것 수정 — `3c1811d`.** 배포본 `/api/history/briefings` 실측에서 사용자가 읽는 문장이 `… (Gemini 호출 실패 ClientError: 429 RESOURCE_EXHAUSTED. {'error': {'code': 429, 'message': 'You exceeded your cu → 템플릿 요약)` 로 끝나고 있었다(`briefing.py:69-72` 가 예외 80자를 본문에 이어 붙였고 그 본문이 화면·Firestore·아카이브에 그대로 남았다). **쿼터가 소진된 채 촬영하면 심사위원이 읽는다.** `generate_briefing_text` 를 3-tuple 로 바꿔 본문에는 사람 말 한 문장만 남기고 원문은 **`fallback_detail` 필드**로 분리(진단은 버리지 않는다 — 프런트가 `title` 툴팁에 배선). 함께 프롬프트 역할 문장의 옛 서사(`자율 주식매매 에이전트 서비스`)를 402 Guard 로 정정했다. 검증: 폴백 4경로 직접 확인(429·빈 응답·정상·키 없음, 네트워크 호출 0) · **테스트 22종 전부 통과** · red_team `rc=0` 불변. **재배포는 프런트 병합 후 1회로 묶는다**(화면에 안 보이는 변경).
- **★ P5 프런트 전달본 병합 완료 — 커밋 `481ac4b`(파일 4개: `index.html`·`landing.html`·`skeleton.css`·`app.js`).** 전달서 33행 중 **30행 반영**, 미반영은 I5(종목 툴팁) 1건뿐이고 그건 우선순위 C(선택)다. **회귀 대조를 가장 먼저 했다** — 지난 두 번 전달본이 저장소보다 앞선 시점 기준이라 최근 반영분이 되돌아와 있었기 때문. 이번엔 **되돌아간 것 0**: 저장소에서 사라진 줄(index 81 · app.js 38 · landing 2)을 한 줄씩 확인해 전부 전달서가 지시한 개명·이동임을 확정했고, 과거 회귀 이력 항목 16종(지갑 로그인 훅·AI 카드·`renderAi`·`guard_semantic`·`data-guard-legs`·`.src-rule-gate`·전략 기본값 추세추종·종목 체크박스 잠금 조건·`data-card="history"`·`fetchHistory`·`lastEngineStatus`·`actor_label`·`invoice_semantics`·`blocked_unverified` 등)은 전부 잔존을 grep 으로 확인했다.
- **⚠ 대조 절차에서 걸린 함정 — 저장소가 줄바꿈 방식이 섞여 있다.** `index.html`·`skeleton.css`·`app.js` 는 **CRLF**, `landing.html` 만 **LF** 다. 전달본은 4개 전부 CRLF 라, 정규화 없이 `diff` 를 돌리면 **파일 전체가 바뀐 것처럼 보인다**(실제로 처음에 index.html 이 437줄 전부 변경으로 나와 오판할 뻔했다). 또 이 환경의 Git Bash 는 `grep -c $'\r'` 와 `awk '/\r$/'` 가 CR 을 못 잡아 줄바꿈 판별에 쓸 수 없다 — **`od -c` 나 PowerShell 바이트 스캔이 유일하게 믿을 수 있는 방법**이다. 병합은 각 파일의 기존 방식에 맞춰 기록해(landing 만 LF 변환) `git diff` 를 실제 변경분(323+/121−)으로만 남겼다.
- **병합 전 기계 검증 2종.** ①`app.js` 가 찾는 `[data-*]` 훅 **116개 전부** `index.html` 에 존재(하나라도 없으면 `el.x` 가 null 이라 대시보드가 통째로 죽는다) ②`node --check` 문법 통과.
- **로컬 dry 세션 스모크 통과.** 80틱 완주 · 체결 3건 · 가드 시도 3 · **차단 0 · 한도 거부 0 · 유출 0.00** · 실현손익 +17.25 · **콘솔 오류 0**(랜딩도 0). 대기 상태에서 규칙 줄이 드롭다운 기준으로 정확히 렌더(`지금 설정 — 샌드박스 · 실데이터 재생 · 상승장 일봉 · 추세추종 · 빠름 · …`)되고 실행 중에는 서버 값으로 바뀌는 S13 동작도 확인. 고급을 펼치면 감춘 옵션(⚡초고속·느림·적립형)이 `hidden` 해제되고 값(`replay`/`daily`/`1`/`strict`/`pxma20`)이 전부 보존된다.
- **`?lab` 의 URL 정리 충돌 없음 확인(전달서가 경고한 지점).** `?lab=1` → 고급 펼침·localStorage 저장·주소창에서 파라미터 제거·라이브 옵션 잠금 해제. `?lab=0#token=…` 을 함께 넣어도 **lab 은 해제되고 token 은 정상 캡처**된다 — `captureLab()` 이 `captureTokenFromHash()` 보다 먼저 돌면서 hash 를 보존하고, 뒤 함수는 그때 이미 lab 이 빠진 search 를 넘기기 때문에 서로의 정리를 밟지 않는다.
- **⚠ L2(가드 패널 가림)가 L1 재배열로 더 나빠졌다 — 촬영 전 사용자 결정 필요.** 전달서 실측은 1040×982 에서 `pnl`·`valuation`·`position` **각 33%** 였는데, `DEFAULT_LAYOUT` 첫 자리가 `session`→`pnl` 로 바뀌면서 dock 아래 오는 카드가 달라졌다. **1280×1000 실측: `position` 96% · `valuation` 40% · `pnl` 0%** — 포지션 카드(보유 수량·평균단가)가 첫 화면에서 **사실상 완전히 가려진다**(스크린샷 확인). 프런트는 지시대로 `.guard-dock` 을 건드리지 않았고 '디자이너 확인 후 유지' 결정이 살아 있어 임의로 바꾸지 않았다. 접기 탭으로 내리면 가려지지 않으므로 **촬영 시 패널을 접고 찍는 것도 선택지**다.
- **범위 밖 변경 1건(무해로 판단해 수용).** 전달서 §0-2 가 "색·글꼴·간격은 손대지 않는다"였는데 `skeleton.css` 에 **예산 카드 재구성**(`.budget-card`·`.budget-hero`·`.budget-foot`, 배치 출처 uiverse.io MIT 명시)이 함께 왔다. 하드코딩 색 없이 기존 `theme.css` 토큰만 참조하고 `data-budget-*` 훅 5개가 전부 그대로라 8/2 시안 교체와 충돌하지 않는다.

## 2026-07-27

- **★ 경쟁 조사 완료 — 해커톤 수상작·실패작 (전문 `docs/reports/competition_research.md`).** 조사 전용 대화에서 워크플로우 병렬 분산(6개 각도 × 검색→원문 정독→건별 적대적 검증 → 종합 → 완전성 비평, 에이전트 14·웹 469회·오류 0·48분). **이 대회는 1회차라 수상작·탈락작이 존재하지 않아**(2026-07-17 공고, 결과 자체가 없음) Solana/Colosseum·x402 특화·Google Cloud/Gemini 계열 **유사 해커톤 12개**로 대체했다. 산출: 수상작 12건·실패작 14건·수상 패턴 11·실패 패턴 15(402 Guard 해당도 판정 포함)·리스크 12·액션 15·미확인 7건.
- **경쟁 조사 — 대회 자체에 대한 신규 확인 6건(전부 2차 매체, 공식 재확인 필요).** ①정식 주제가 **"에이전트가 클라우드 API 를 발견·인증·결제"**(공식 예시: Gemini 호출 → 402 → USDC 차감 → API 접근) ②결제 스택으로 **pay.sh** 명시, 매체 서술은 `"Developers must build ... using Pay.sh"` ③**일정 3단계 — 8/3 은 최종 마감이 아니라 본선 진출 스크리닝, 8/10~20 집중 멘토링, 8/21 데모데이 본선**(→ 프론트 시안 공수 L 을 8/3 전에 몰던 기존 배치가 잘못된 전제 위에 있었다) ④공동주최 **Superteam Korea**, 기관 파트너 **케이에스넷(국내 PG사)** → 결제 실무·규제 질문 확률이 높은데 리스크 목록에 규제가 0건이었다 ⑤총상금 $135,000·트랙 최대 $20,000 ⑥선행 회차 = 2025-04 Seoulana Hacker House(동일 주최진, 수상작 명단 확보 실패).
- **경쟁 조사 — 카피 정정이 필요한 발견 3건.** ①**x402 v2 스펙에 `maxAmountRequired` + 구매자 로컬 서명 + facilitator 변조 시 서명 실패가 이미 있다** → *"x402 는 파는 쪽을 보호한다"* 가 한 줄로 반박된다. 정확한 방어선은 *"x402 는 금액 상한은 주지만 **수취인·청구서 정합성의 검증 지점을 정의하지 않는다**"*. ②**AP2 공식 스펙(SD-JWT/VC)에 `allowed payees`·budget caps 가 이미 정의돼 있다** → *"AP2 가 `pay_to` 를 미검사한다"* 는 프로토콜 공백이 아니라 **우리 구현의 미완성**이고, 우리 mandate 는 VC 형식이 아니므로 **"AP2 준수"가 아니라 "AP2 개념 기반"으로 표기를 낮춰야 한다**(Google 주최 대회에서 Google 스펙을 잘못 구현했다 고친 것을 혁신으로 발표하는 그림이 된다). ③**pay.sh 가 이미 "programmatic spending ceiling" 을 홍보 문구로 갖고 있다** — 살아 있는 틈은 수취인 allowlist·청구서 대 합의견적 대조(pay.sh 문서에서 미확인).
- **경쟁 조사 — 실패작 최대 경고 3건.** **GuardianOS**(ADK 2025: 온체인 승인 게이트 + Sepolia 실배포 + 라이브 URL + ADK 멀티에이전트를 **다 갖추고 무수상**, 화면이 자체 용어로 뒤덮임 / 같은 계열 그랑프리는 '장바구니로 레시피 추천') · **x402 Intent Firewall**(Cronos: 개념이 거의 동일한데 메인 트랙이 아닌 **'Best Dev Tooling Layer' $3,000**, 1위는 같은 문제를 '자율성을 가능케 하는 권한 레이어'로 말해 $24,000) · **Latinum**(에이전트 결제 미들웨어로 2025 AI 트랙 1위 → **2026 명예언급으로 하락** = 범주 소진). 순수 방어 프레이밍 실측 천장은 2위(Cronos Shield $5,000), 트레이딩 풀스택 천장은 명예언급(TradeSage AI·Vigil AI).
- **경쟁 조사 — 반증되어 폐기할 기존 전제 2건.** ❌ **"직전 x402 해커톤 수상작 16건 중 트레이딩 봇 0건"** — 402 Guard 재포지셔닝의 출발점인데 **출처를 찾지 못했고 반례가 3건**(Peaks·Clawpump = Frontier Top 25, Agent Arc = Breakout AI 3위) 나왔다. 정확한 명제는 *"자동매매라서 떨어진다"*가 아니라 ***"단일 전략 봇 언어로 말하면 떨어진다"*** → **재포지셔닝 방향은 유지하되 매매 기능을 숨길 필요는 없다.** ❌ "인프라·미들웨어라서 상금이 낮다" — Berlin 대회 상금표 실측으로 반증(Infrastructure 1위 = Agentic Commerce 1위 동일 $2,500).
- **경쟁 조사 — 미해소 위험 3건.** ①축④ 반문 **"실제로 무엇을 산 건가요?"** — 대회의 결제 대상은 실재하는 클라우드 API 인데 우리는 devnet 에 없어 자체 발행한 토큰을 자기 브로커에게서 산다(트랜잭션은 진짜지만 상대와 상품이 자기 자신). 실제 Gemini 호출 1건을 x402 로 결제하면 축③·축④·이 반문이 한 번에 해소된다. ②**규제 질문**(무등록 투자일임·자금 분리보관) — PG사가 파트너인데 리스크 목록에 0건이었다. ③배포본 **`GEMINI_MODE` 누락 시 라이브 URL 에서 축② 증빙이 통째로 0**.
- **경쟁 조사 — 신뢰도 한계(인용 전 필독).** 조사한 **12개 해커톤 전부가 프로젝트별 심사평을 비공개**한다 → "왜 이겼다/졌다"의 대부분은 순위·상금·카테고리 배정에서 역산한 **추정**이고, 심사 근거로 확정 인용 가능한 것은 4건뿐이다(AgentFabric 공지 문구 / Colosseum Top 25 기준 문장 / SKALE 심사 4축 / 제미나이 3 서울 해커톤의 반복 질문). 자기 비평 단계에서 **URL 없이 단언된 제품명 다수가 적출**됐다(CardZero·PlaiPin·World of Geneva·Volt402·x402Resolve·TruthKeeper·CompilanceOS 등). **소개서·발표에 사실로 인용하지 말 것.** 이 조사는 판단이 아니라 자료이며, **사용자가 재분석 후 방향을 결정한다**(결정 전까지 개발 방향 변경 금지).
- **✅ Cloud Run 첫 배포 성공 — 라이브 URL 확보(가산점 항목).** 프로젝트 `synapstock`(기존 GCP 프로젝트 재사용, 결제 연결 확인), 리전 `asia-northeast3`, 서비스명 **`synapstock`**, URL `https://synapstock-766888967498.asia-northeast3.run.app`. 서비스명은 사용자 결정 — 심사 기준에 이름/URL 항목이 없고 되돌리기 비용이 URL 쪽만 비대칭으로 크므로(제품명은 마감 직전에도 바꿀 수 있지만 URL 은 바꾸면 죽는다), 어느 브랜딩으로 가도 견디는 `synapstock` 을 선택했다. 제품명 `402 Guard` 는 화면·소개서·README 가 담당한다. **§5 검증 1~5 + §5-1 전부 통과**: 대시보드 200 · `persistence.enabled=true`(backend firestore, project=synapstock) · 드라이런 세션 체결 발생 · Firestore 세션 문서 기록 · **새 리비전 교체 후에도 `/api/history/sessions` 에 세션 7건 생존**(컨테이너 파일시스템이 tmpfs 이므로 이게 곧 Firestore 영속 증명) · `POST /broker/orders` → **402** + `accepts[0].payTo`(scheme exact, network solana-devnet) · `/.well-known/x402` 200. 시크릿 5개 등록(지갑 3개 + control-token + gemini-key) — 런북은 지갑 2개였으나 **`user.json`(AP2 위임장 서명자)을 추가**했다. 없으면 재시작마다 새 임의 키가 생겨 "사용자가 서명한 위임장"의 주체가 바뀐다.
- **배포 중 실제로 막힌 지점 2건 → 런북에 반영.** ①`gcloud run deploy --source .` 이 `INVALID_ARGUMENT: could not resolve source: googleapi: Error 403` 으로 **빌드 시작 전에** 실패 — Google 이 소스 배포의 기본 빌드 실행 계정을 컴퓨트 기본 계정으로 바꾼 뒤로 `roles/cloudbuild.builds.builder` 가 없으면 업로드된 소스를 읽지 못한다(런북 §3 이 Firestore·Secret 권한만 주고 있었다). 빌드 이전 단계라 `gcloud builds list` 에도 아무것도 안 남아 오진하기 쉽다. ②PowerShell `Set-Location -Path` 가 저장소 경로의 `[Google Cloud X Solana]` 대괄호를 **와일드카드로 해석**해 "경로 없음"으로 실패 — `-LiteralPath` 필요(따옴표로는 해결되지 않는다). 둘 다 §8 트러블슈팅에 증상·조치로 추가했고, §4 배포 명령은 **검증된 한 줄 형태**(백틱 줄바꿈은 뒤 공백 하나로 깨진다)와 저장소 루트 확인 안전장치(`Test-Path .\Dockerfile` — 다른 폴더에서 `--source .` 을 실행하면 그 폴더가 통째로 업로드된다)를 포함하도록 교체했다. §4 의 "지갑키는 `/secrets` 파일로 마운트된다"는 낡은 서술도 실제 동작(환경변수 주입)으로 정정.
- **✅ 버그: 드라이런 매도 레그 복구(커밋 `7d6a178`, 푸시).** 배포본 세션에서 매 틱 `틱 처리 실패(tAAPL): ValueError: stock_mint 미설정 — 매도 견적 불가` 가 떴다. 원인은 `engine.start` 가 `live` 일 때만 `STOCK_MINT` 를 검사하고 드라이런에서는 `None` 을 그대로 브로커에 넘긴 것(`broker_agent.make_stock_required` 가 거부). **매수는 되고 매도만 전부 실패**해서 익절이 일어나지 않아 수익률 시연 자체가 불가능했다. 로컬은 `.env` 에 값이 있어 드러나지 않고 **배포본에서만** 나타나는 종류였다. 수정: 드라이런은 온체인을 건드리지 않으므로 자리표시 민트(`DRY_STOCK_MINT = Pubkey.default()`)를 쓰고, 라이브는 기존 fail-fast 를 유지해 자리표시가 체인에 닿는 경로를 없앤다. **가드의 자산 대조(`guard.check_stock_transfer`)는 자리표시에서도 동일하게 작동**하므로 402 Guard 는 약화되지 않는다. `scripts/test_dry_sell.py` 14건 — 결함 재현(음성 대조: 민트가 `None` 이면 실제로 `ValueError`)·수정 확인(매도 `settled`)·라이브 fail-fast 유지·설정값 우선.
- **✅ 기능: 판단 두뇌 선택 `strategy.brain`(커밋 `8440a1b`, 푸시).** 사용자 지적("규칙이 AI밖에 없다") 확인 결과 버그가 아니라 **기능 미노출**이었다 — 두뇌 선택이 "`GEMINI_API_KEY` 가 있으면 무조건 Gemini"로 고정돼 있어 같은 데이터에서 규칙 vs AI 를 나란히 돌릴 방법이 웹/API 에 없었다(백테스트에는 `--brain` 이 있다). `auto`(기존 동작) / `rule`(키가 있어도 Gemini 미호출, 두뇌 표기에 "사용자 지정" 명시) / `gemini`(키가 없거나 초기화 실패면 **조용히 규칙으로 떨어뜨리지 않고 즉시 오류** — "설정했는데 반영이 안 되는" 부류의 버그 방지). 적립형·추세추종은 결정론적 규칙이라 무관. `scripts/test_brain_select.py` 10건. **UI 드롭다운은 넣지 않았다** — 곧 적용할 디자인 시안이 `index.html` 을 교체하므로 프론트 병합 대화에서 함께 붙인다(그때까지는 API 로 실행·검증 가능).
- **⚠ 배포 잠복 결함 발견: `GEMINI_MODE` 누락.** `.env` 에는 `GEMINI_MODE=developer` 가 있는데 배포 환경변수에는 없었다. `gemini_decider.__init__` 은 모드가 비면 **키가 `AIza` 로 시작하는지로 developer/vertex 를 자동 판별**하는데 이 프로젝트 키는 `AIza` 로 시작하지 않아 **Vertex 경로로 잡히고 개발자 키로는 동작하지 않는다.** 아직 안 터진 이유는 배포본에서 돌린 세션이 전부 추세추종(Gemini 미사용, `by_source: {rule: 33}` · `gemini_calls: 0`)이었기 때문 — 조건형을 돌리는 순간 드러난다. 재배포 환경변수에 추가.
- **✅ 디자이너 재브리핑 문서 작성 + 관제탑 아티팩트 갱신(오늘 할일 전용 대화).** 디자이너가 아직 "Auto Trader" 로 알고 있어(시안 발송 시점 기준) 회의 전에 컨셉을 다시 이해시켜야 했다. **신규 `docs/design/concept_brief_402guard.md`** — 비개발자용 브리프: 5분 요약표 / 법인카드 비유(한도 안쪽 금액 + 수취인만 바꾼 청구서 = 한도는 지켜지고 전액 손실) / 시안에서 **살아남는 것과 바뀌는 것 8줄** / 신규 요청 6건(가드 KPI 행·**차단 순간의 시각화**·랜딩·로그인+지갑 연결·멀티종목·아키텍처 다이어그램) / 복붙 가능한 카피 치환표 / 금지 문장 / 회의 진행 10분 순서. **경쟁 조사 결과를 반영해 이전 브리프(`frontend_kickoff.md` §3)에서 두 가지를 정정**했다: ①**"매매를 숨기지 말 것"**(Peaks·Clawpump·Agent Arc 수상 = 자동매매 자체는 감점이 아니고 '단일 전략 봇 언어'가 문제) ②**"막는다"가 아니라 "그래서 맡길 수 있다"**(순수 방어 프레이밍의 실측 천장이 2위 $5,000, 1위는 같은 문제를 '자율성을 가능케 하는 레이어'로 서술). 또한 로그인 화면에 **"매 거래 승인 버튼"을 넣지 말 것**을 명시했다(무인 자율 결제 전제가 무너지고, 그 방식은 스폰서 보너스에 그친 실제 사례가 있다).
- **관제탑 아티팩트(`docs/artifacts/status.html`) 07-27 갱신.** 체크리스트를 19건 → **36건**으로 재구성: 그룹 신설 2개(**오늘 07-27** 4건 = 디자이너 브리핑·프론트 병합·로그인+지갑·기능 경계 구체화 / **경쟁 조사 후속** 8건 = pay.sh 확인·카피 3건 정정·API 결제 레그·아키텍처 다이어그램·브랜드 정렬·Q&A 카드·데모 대본 재작성·폴백 2중), 완료 반영 7건(규칙 게이트·판단 출처 계측·481봉 백테스트·HTTP 402·brain 선택·드라이런 매도 복구·Cloud Run 배포), 신규 위험 항목 `GEMINI_MODE` 재배포 추가. 심사 4축 카드에 경쟁 조사 위험을 병기하고(축② '중·위험', 축③ '강·재검토'), 데모데이 D-day 표시와 **"08-03이 스크리닝일 수 있음(공식 확인 전까지는 최종 마감으로 간주)"** 공지를 넣었다. 항목이 바뀌어 localStorage 키를 `v2` 로 올렸다(이전 체크 초기화). ⚠ **07-27 현재 원격에 프론트 작업물이 없다** — `origin` 에 `main` 외 내용 있는 브랜치가 0이고 `web/static` 최근 커밋은 07-25(`54e6d0d`), 가드 KPI 카드도 아직 없다. 프론트 병합은 작업자 전달 대기 상태.
- **관제탑 아티팩트 재갱신(같은 날 2차) — 프론트 병합·자해 제거·조사 재분석 반영.** 체크리스트 36 → **41건**(완료 16건, 39%). 반영: 오늘 할일 ②프론트 병합 완료 체크 · 프론트 그룹 5/6 완료(남은 것은 전략 옵션 '고급 설정' 접기 하나) · 백엔드에 Gemini 429 구분(`c2ffe9a`)·자해 3건 제거(`de9e000`) 추가 · 배포에 devnet Circle USDC 실증 신설 · '경쟁 조사 후속' 그룹을 **'정직성·카피'** 로 재편(카피 3건 정정·KPI 문구 어긋난 곳·매도 레그 비대칭·mandate payees 봉인·유출 계측 잔여 6건·아키텍처 다이어그램·Q&A 카드) · 제출물에 데모 대본 재작성·폴백 2중 추가. **실증 KPI 를 `python scripts/red_team.py --report` 로 직접 돌려 실측값으로 교체**했다 — 예전의 뭉뚱그린 "차단 6 / 유출 0.00" 대신 **서명 전 유출 0.00 · 구매자 차단 4/6(판매자측 1·사후 1) · 회수 불가 29.99 · 오탐 0 · 가드 없을 때 피해 519.86**. 심사 4축은 축① '중→상승'(첫 화면 갭 해소), 축③④ '강·단서'(devnet USDC 자체발행 / 라이브 URL 은 드라이런이라 `check_delivery` 미실행)로 조정. 일정 공지는 **"8/3 최종 제출 마감"으로 확정 표기**(조사 리포트의 '스크리닝일 뿐'은 운영 판단으로 쓰면 위험 — 제출물 3종이 8/3 에 확정되고 그것이 1차 심사 대상). 항목이 바뀌어 저장 키를 `v3` 로 올렸다.
- **다음 단계 합의(사용자).** ①프론트 시안 적용(최우선 — 심사 최대 갭인 "402 Guard 서사가 첫 화면에 없음"을 해소하고, 제출물의 스크린샷·영상이 화면 완성에 종속된다) → ②로그인+사용자 지갑을 **하나로 통합**(지갑 연결 = 로그인. Web3 관례상 지갑이 신원이므로 별도 인증 스택 불필요) → ③제출물 3종(07-31 착수 마지노선). **⚠ "사용자 지갑으로 거래"의 범위를 못박음**: 개인키를 서버에 등록하는 방식은 **하지 않는다**(규칙 10 위반이자 "에이전트에게 무제한 지출 권한을 주지 마라"는 제품 주장과 정면 충돌). 사용자 지갑은 **위임장(AP2 mandate)에 1회 서명**하고 실제 거래는 에이전트 지갑이 한도 안에서 수행한다 — 매 거래를 사용자가 서명하는 구조는 "사람 없는 자율 결제"라는 제품 전제와 모순이다. 숨은 비용으로 엔진이 **전역 싱글턴 + `max-instances 1`** 이라 진짜 다중 사용자는 인스턴스 분리 리팩터가 필요하므로, 마감까지는 "지갑 1개 연결" 범위로 좁힌다.

## 2026-07-26

- **✅ Gemini 대규모 하락장 백테스트 실측 — 축② 정량 근거 확보(커밋 `e81a9ce`, 푸시).** 심사 지적("대표 30% 서사는 전부 brain=rule, Gemini 표본은 AAPL 36봉뿐")에 대응해 **대표 하락장 TSLA `_bear` 481봉(2022-02-01~2023-12-29)** 에서 rule·gemini(strict) 각 1회 실행(순차, 4.5초 간격, 약 60분). **수익**: rule −37.56% / gemini −39.57%(벤치마크 매수후보유 −20.45%) — **AI 기여는 481봉에서도 중립(약간 열위)** 로, 36봉 소표본 결론과 일치한다. Gemini 는 32회 덜 샀지만(보수적) 수익은 2.01%p 낮았다. 조건형(눌림목)은 하락장에서 구조적으로 지고 두뇌를 바꿔도 그 구조는 안 바뀐다 → **알파는 판매 포인트가 아니다**(같은 데이터셋에서 +67.76%를 내는 건 AI 가 아니라 추세추종 전략이다. 두 사실을 섞어 말하지 않는다). **★ 판단 출처 `{gemini 434, rule 16(워밍업), rule-fallback 29, rule-gate 2}` — 규칙 게이트가 실제로 2건 발동**했다: 2022-10-24(211.25 > 기준 208.59)·2023-08-14(239.76 > 236.61) 두 봉에서 Gemini 가 *"MA5 대비 3% 이상 낮아 매수 조건을 충족합니다"* 라고 **사실과 다르게 단언하며 매수를 개시**하려 했고, 코드가 실제 지표와 대조해 보류로 강등했다. 프롬프트는 이미 그 행위를 금지하고 있었고 모델은 지시를 어긴 게 아니라 **조건 충족 여부를 잘못 판정**했다 — 지시를 아무리 잘 써도 판정 오류는 남으므로 **지출의 개시는 코드가 잠근다**는 설계의 실측 근거(481봉 중 2건 = 0.42%). **폴백 29건(6.0%)** 은 무료 티어 쿨다운(146번째 봉부터 간헐)으로, 이 런의 **90.2%가 순수 Gemini 판단**이고 폴백 설계가 없었으면 런이 중간에 죽었다. 상세·재현 명령 `docs/reports/strategy_validation.md` 말미, 증빙 `artifacts/backtests/20260726_{175252_TSLA_bear_rule,185409_TSLA_bear_gemini-strict}.json`. ⚠ Gemini 런은 동시 실행 금지(429 폴백 오염) + 481봉 1회가 무료 티어 일일 한도를 상당히 소모 → 하루 1~2회 제한.
- **✅ 아티팩트 3종 신설(`docs/artifacts/`) — 루틴은 쓰지 않기로 결정.** 사용자 요청으로 아티팩트·루틴의 효율을 검토했다. **루틴(스케줄 자동 실행)은 미채택**: ①앱이 켜져 있을 때만 실행돼 지킴이 역할이 불가 ②매 실행이 대화 기억 없는 새 세션이라 사용량 한도를 잠식(CLAUDE.md 토큰 규칙과 충돌) ③남은 8일 작업이 전부 사람 주도 일회성이고 3개 부서는 이미 온디맨드 워크플로우. 예외로 마감 전 일회성 리마인더만 가치 있음(미등록, 사용자 판단). **아티팩트 3종 제작·배포**(전부 자체 완결 HTML, 라이트/다크 양쪽 설계, 외부 요청 0): ①`status.html` 관제탑 — D-day 카운트다운·심사 4축 등급·실증 KPI·부서 리포트 요약·남은 일 19건 체크리스트(localStorage 저장, 그룹별 진행률) ②`demo_script.html` 3분 촬영 대본 — 장면 6개(20/30/40/40/30/20초) 대사·화면 조작·증거 + 카운트다운 타이머(스페이스바 시작, 장면 자동 추적, 초과 시 경고) ③`pitch_deck.html` 소개서 13장 — A4 가로 고정 크기라 브라우저 인쇄가 곧 제출용 PDF(`@page size: A4 landscape`, 화면은 뷰포트에 맞춰 스케일). 내용은 하이브리드(문제→위협→해결 스토리 + 뒤쪽 증빙·한계·로드맵). **검증**: 13장 전부 A4 안에 들어감(최소 여백 61px), 타이머 실동작 확인, 체크리스트 저장·복원 확인. **정직성 주의**: 오늘 다른 대화에서 G5(HTTP 402)가 완료돼(`70bed10`) 초안의 "HTTP 402 미구현" 서술을 3곳 정정했다(축③ 갭 없음·한계 슬라이드는 "매도 레그만 A2A"로 교체·로드맵 항목 교체). 데모 대본 4장면에도 `curl -i /broker/orders` → 402 장면을 추가. ⚠ 스크린샷 2곳은 촬영 후 삽입할 자리표시자이고, 표지에 팀/발표자 정보는 비어 있다(사실 미확인이라 임의로 채우지 않음).
- **✅ 브로커 HTTP 402 레그(G5) 구현 — 축③의 유일한 명시적 갭을 닫음(커밋 `70bed10`, 푸시).** 심사 지적: x402 3단계가 인프로세스 A2A dict 로만 구현돼 표준 402 를 반환하는 엔드포인트가 없었다(`http_402_status_code` 코드매치 0). **신규 `web/broker_service.py`** = x402 자원 서버 — `POST /broker/orders` 가 결제 없으면 **402 Payment Required + accepts[]**(공식 필드명 `x402Version`·`scheme`·`maxAmountRequired`·`payTo`·`asset`·`extra.orderId`), `X-PAYMENT` 헤더(base64 JSON)가 붙으면 **200 + `X-PAYMENT-RESPONSE`**. `GET /.well-known/x402` 디스커버리는 **미구현 항목(`notImplemented`)을 먼저 공개**(매도 레그는 방향이 반대라 A2A 인프로세스 / facilitator 미사용). 결제 로직은 기존 `BrokerAgent` 를 그대로 호출하고 이 파일은 전송(상태코드·헤더)만 담당한다. 청구서는 **1회용**(정산 후 폐기)이라 같은 결제 재제출은 402 `unknown_order`, 온체인 정산은 기본 잠김(`BROKER_SERVICE_ALLOW_LIVE=1` 로만 개방). **신규 `payments/x402_http.py`** = 와이어 포맷 + 클라이언트를 한 파일에 둬 서버·클라이언트 포맷이 갈라질 수 없게 함(테스트용 `transport` 주입 seam 포함). **엔진 배선**: `BROKER_HTTP_URL` 이 설정된 세션만 매수 레그를 HTTP 로 보내고 미설정(기본)이면 기존 인프로세스 경로가 그대로다. **메인 앱에도 같은 router 를 마운트**(`web/server.py`) — Cloud Run 은 컨테이너당 포트를 하나만 노출하므로 배포 URL 에서도 `curl -i <url>/broker/orders` 로 402 확인이 가능해야 하기 때문. 실행 두 가지가 **같은 코드·같은 브로커 지갑**(`secrets/broker.json`)을 쓴다: 시연은 별도 프로세스(`python -m web.broker_service --port 8402`), 배포는 마운트. **검증**: `scripts/test_http402.py` **53건**(402 상태코드·accepts 필드·X-PAYMENT 왕복·리플레이 차단·잘못된 입력 4종·클라이언트 규약 검증·메인 앱 마운트·엔진 매수 사이클 HTTP 경로) + `scripts/demo_http402.py` 가 **실제 TCP 소켓**(uvicorn 별도 스레드)에서 전 과정을 돌려 증빙 아카이빙(`artifacts/x402_http/20260726_180159_http402_cycle.json`). **핵심 서사 강화**: 전송이 HTTP 가 되면 상대가 진짜 원격이 되고, 구매 에이전트는 원격 `accepts[]` 를 믿지 않고 자기 견적과 대조한 뒤에야 서명한다 — 데모 ⑥단계에서 원격 응답의 `payTo` 를 악성 지갑으로 바꿔도 `GUARD_PAYEE_UNKNOWN` 으로 서명 전 차단, **유출 0.00 USDC**. 회귀: 테스트 14종 + red_team(시도20·차단6·유출0.00·오탐0) 전부 통과.
- **✅ 판단 출처 계측 — 온체인 증빙에 AI 관여도를 기록(커밋 `3602015`, 푸시).** 심사 지적: tx 아티팩트가 판단 출처를 기록하지 않아 온체인 9건 중 Gemini 관여분을 확인할 수 없었다. `engine.source_counts`/`action_counts` 누적 카운터(판단 타임라인은 메모리 상한 500건에서 잘리므로 집계를 분리)와 `engine._ai_stats()` 신설 — `brain`·`decisions_total`·`by_source`·`by_action`·`gemini_calls`·`gemini_decisions`·`gemini_gated`·`rule_fallbacks`·`gemini_share_pct`·`trades_by_decision_source`. **배선**: tx 아카이브(`_archive`)·세션 요약(Firestore)·`GET /api/state` 의 신규 `ai` 필드(추가만, 기존 필드 무변경 — 프론트 계약서 §2.2 갱신)·브리핑 근거. `run_demo.py`(devnet/localnet CLI 증빙)도 같은 `ai` 블록과 `brain` 라벨을 남긴다. `scripts/test_ai_stats.py` 27건(가짜 두뇌로 드라이 세션 결정론 스텝 — 강등·보류·폴백·규칙전용·타임라인 절단·체결출처·세션리셋).
- **✅ 규칙 게이트 — "규칙 신호 없는 개시 금지"를 프롬프트가 아니라 코드로 강제(커밋 `94fe605`, 푸시).** 심사 지적: 그 제약이 Gemini 프롬프트(MODE_RULES)에만 있어 AI 기여 서사가 모델의 순응에 의존했다. `TradingAgent._rule_gate` 가 AI(`source=gemini`) 판단 중 **매수기준(MA5 −N%) 미충족 매수**·**익절기준(평단 +N%) 미충족 매도**를 `hold` 로 강등하고 출처를 **`rule-gate`** 로 바꿔 계측에 남긴다. `hold` 는 어느 모드에서도 통과 — AI 재량은 '멈추는 방향'으로만 열려 있다. `decide()` 는 `_sanitize`(한도 클램프) → `_rule_gate`(개시 통제) 순서로 결선했고, 규칙 판단·규칙 폴백·시간청산은 정의상 규칙 안이라 대상이 아니다. 결과적으로 지출이 3중 통제된다: **개시=규칙 게이트 / 금액=AP2 mandate / 청구서=402 Guard**. `scripts/test_rule_gate.py` 28건(가짜 두뇌로 `decide()` 전 경로 — 강등·통과·경계값·폴백·시간청산·예산소진). ⚠ 프론트는 판단 출처에 `rule-gate` 값이 새로 나올 수 있으므로 `src-rule-gate` 스타일만 추가하면 된다(없어도 텍스트로 보임 — 계약서에 명시).

## 2026-07-25
- **✅ 심사 부서(judge-dept) 실행 + 프론트/백엔드 분리 협업 구조 확립(전부 커밋·푸시).** **심사 결과**: 축③(인프라)·④(실제구동) '강'(x402 온체인 SPL 정산·exact·Memo·AP2 ed25519·A2A 3단계·Guard 결선 실물, devnet/localnet 라이브 교차검증 통과, red_team 유출0.00·오탐0 직접 재현, 테스트11종 통과), 축①(혁신·UX)·②(AI) '중'. **최대 갭 = 402 Guard 서사가 첫 화면에 안 보임** — 백엔드는 `state.guard`(attempts/blocked/ap2_rejected/leak_usdc) 이미 방출(`engine.py:1569`)하나 프론트 미배선(`app.js` guard 렌더 0건)이고 대시보드가 여전히 'AutoTrader Agent' 트레이딩봇 브랜딩(차별화 전략이 탈락위험으로 경고한 프레이밍). 축② = Gemini 2용처 통합했으나 정량 수익기여 규칙 대비 중립·30% 서사 전부 brain=rule. 리포트 `docs/reports/judging_latest.md`(적대검증 32근거 반려0·창작0). **프론트/백 분리(사용자: 백엔드=본인, 프론트=다른 작업자 데스크탑 Claude Code, 각자 작업 후 병합)**: `docs/frontend_backend_split.md`(경계 = 프론트 `web/static/**` / 백엔드 나머지, 유일 공유 server.py 는 백엔드 단독 → 파일집합 분리로 병합충돌 0·상태스키마 계약 동결·dry모드 시크릿없이 프론트 단독실행·병합 워크플로우·구조정합성 점검표), `docs/frontend_kickoff.md`(프론트 개발자용 — 시안↔기존 12 data-card 매핑·⚠메시징 402 Guard 재조정 치환표·가드 KPI 카드 스펙·작업순서 가드카드→리브랜딩→스킨→랜딩→로그인/지갑·완료기준), `docs/design/README.md`(시안 자리). **디자인 시안 도착**(대시보드 mockup, 멀티종목 이전 기준 프로토타입) — **배치·색·컴포넌트는 채택하되 옛 'Auto Trader/돈넣으면 AI가 사고팜' 카피는 402 Guard 서사로 치환·가드KPI 최상단 추가**(사용자 강조점 = 한도초과 불가·유출0.00·손실 한도격리). **부수**: node_modules·package.json(omniroute stray) gitignore, 백테스트 증거 19건+심사리포트 커밋. **다음 = 프론트 개발자 UI 작업(별도) / 백엔드는 새 대화서 축②③ 보완(Gemini 온체인 증빙·HTTP402 레그 G5·코드게이트).** 안전한 중단점.
- **⚑ 결정: 인트라바·1/5 "특정 조건에서만 유리" 성질은 수정 보류(옵션 유지) — 상세 [`docs/intraday_synthesis.md`](intraday_synthesis.md).** 사용자 결정(2026-07-25): 인트라바 합성·1/5 골든크로스는 둘 다 "더 자주 반응"해 휩쏘로 대부분 구간 수익을 깎지만(추세 지속·저휩쏘 구간에서만 유리), **제거·튜닝하지 않고 지금 상태 그대로 옵션으로 둔다.** 수익 시연·30% 확인은 일봉+추세추종+멀티로 한다. 인트라바 메커니즘(상승일 O→L→H→C / 하락일 O→H→L→C, 마지막 인트라바 종가=실 일봉 종가로 일 궤적 보존)·사용법·성능표·보류 근거를 `docs/intraday_synthesis.md` 에 전용 문서로 기록. **다음 = 별도 새 대화에서 심사 에이전트(judge-dept) 실행.**
- **✅ 사용자 요청 5개 개선 완료(성능 평가용, 커밋 5개 전부 푸시).** 사용자가 심사 전 세션을 직접 돌려 수익(목표 30%)을 확인하려는 맥락. 먼저 **적립형(DCA)을 임시 비활성화**(성능 판단 왜곡 방지 — 전략 드롭다운 옵션 disabled, 되돌리기 1줄, `794cd0d`에 포함). 이어 5개: **① 속도 대폭 향상(`794cd0d`)** — 틱 간격 기본 8초 → 세션 시작 시 속도 선택(초고속0.15/빠름0.3기본/보통1/느림3/발표용8), UI→StartBody.tick_interval_sec→engine.start 안전범위[0.05,60] 클램프. 웹 검증 tick_interval_sec=0.3 반영·80틱 소화. **③ 골든크로스 1/5 단독+결합(`aaf8acb`)** — trend_signal 에 `cross_1_5`(가격≥MA5)·`cross_5_20_1_5`(5/20 AND 1/5 둘 다 상승일 때만 보유, 하나라도 꺾이면 빠른 청산) 추가. 배선: `_decide_trend`·engine 검증/라벨·backtest choices·웹 드롭다운·`explore_trend.desired_long`(재현 기준). test_trend 4신호×3종목=12조합 봉단위 정확 재현. **④ 멀티 종목 추세추종(`a63f48e`)** — 올인이 공유예산을 독식하던 문제를 **종목별 독립 authorizer(예산/N 슬라이스)**로 해결: `_decide_trend` 올인이 그 슬라이스만 쓰게 되어 자동 격리(판단로직 무변경), 매수/매도 사이클 `self._auth`→`agent.auth`, 예산보고 `_total_remaining/_total_spent`(구별되는 auth 합산 — 공유 모드 무변경), update_limits 재슬라이스, 웹 UI 추세 멀티 잠금 해제(라이브만 단일). test_multistock 37건(+6 격리·재슬라이스). **② 일봉→인트라바 합성(`54e6d0d`, 사용자 선택)** — 무료 실 인트라데이는 2022 하락장 미커버라 실 일봉 OHLC 를 지나는 결정론 경로로 하루당 N봉 합성(마지막 봉 종가=실 일봉 종가, 일 궤적은 실데이터). `market/price_feed.IntradayReplayFeed`+engine `_replay_feed`(1~12 클램프)+웹 봉간격 선택(1/2/4/8)+backtest `--sub-bars`+`test_intraday`(481일 종가 0불일치). **⑤ 30% 목표 실증**: 웹 라이브 **3종목(AAPL·TSLA·NVDA) 하락장 추세추종(pxma20) = +77.55%**(총자산 177.55, 실현 73.85), **가드 차단0·유출0**, 종목별 독립 포지션 — 이 +77.55%가 단일 pxma20 3종목 수익 평균(+77.5%)과 일치해 격리의 **수학적 증거**. **⚠ 정직한 실측**: 1/5·인트라바는 정확히 동작하나 **더 잦은 신호→휩쏘(속임수 매매) 폭증→수익 급락**(TSLA 하락장 5/20: 일봉 +81% vs 인트라바4 −35% / 1/5단독 −40%). 30% 목표엔 **일봉+추세추종(pxma20 또는 5/20)+멀티**가 최적, 1/5·인트라바는 "현실감/옵션". 검증: test_intraday·test_trend(12)·test_multistock(37)·test_timeexit·test_dataquality·red_team(유출0)·test_store 전부 통과, 웹 e2e 콘솔오류0. **다음 = 사용자가 세션 직접 실행 후 판단(DCA 복원/유지 포함) → 심사 부서.** 안전한 중단점.
- **✅ 402 Guard 매도 레그 counterparty 검증 부재(medium) 수정 — 방어 비대칭 해소, 검증·푸시.** 추세추종 검증 워크플로우가 확정한 결함(추세추종 이전부터 존재): 매수 레그는 서명 직전 `guard.assert_demand`로 청구서(자산·수취인·금액·의도지출·주문번호)를 검증했으나, **매도 레그 `build_stock_transfer`는 브로커의 `required.requirements`(asset·amount·pay_to)를 무검증 서명**했다. 악용(live+예산 초과 유휴 USDC+악성 브로커): 매도 시 청구서 `asset=USDC민트·amount=유휴 전액·pay_to=자기지갑`으로 설정→에이전트가 무검증 서명→유휴 USDC 유출(브로커 settle_sale 의 verify_payment 는 자기정합이라 통과, check_delivery 는 정산 후라 이미 온체인 전송됨). **수정(매수 대칭)**: `payments/guard.py`에 `check_stock_transfer`/`assert_stock_transfer` 추가 — 서명 직전 ①자산(합의된 주식 민트인가, USDC 등이면 차단 = 핵심 방어)②수취인(신뢰 브로커 allowlist)③수량(보유·합의 수량과 base units 정합)을 **엔진의 독립 기준**(세션 `self._stock_mint`·보유 `qty`, 브로커 응답 아님)과 대조, 불일치 시 `GuardError`로 서명 차단. `agents/trading_agent.py`(build_stock_transfer 가 파라미터 있을 때 가드 호출, 하위호환)·`web/engine.py`(`_sell_cycle` 독립 기준 전달 + `GuardError`→`GUARD_BLOCKED` 차단·`guard_block_count`)·`run_demo.py`(매도 경로 동일 배선)·`scripts/red_team.py`(매도측 공격 2종: 자산 스왑→`GUARD_ASSET_MISMATCH`, 수취인 스왑→`GUARD_PAYEE_UNKNOWN`)·`scripts/test_guard.py`(신규 `test_stock_transfer` — 정상 통과 + 자산/수취인/수량/주문번호 4차단). **검증**: test_guard·test_settlement·red_team(시도20·차단6·유출0.00·오탐0, 매도측 2종 포함)·test_trend(6/6 봉단위 정확 재현)·나머지 5종 전부 통과, 드라이 목 매도 사이클 스모크(정상 매도 settled·가드 오탐0). 현 배포는 브로커가 동일 프로세스 신뢰 코드라 원격 악용 경로는 없으나 제품 헤드라인("402 Guard=사는 쪽 보호")을 매도 방향에서 무력화하던 비대칭을 닫음. 안전한 중단점(커밋·푸시 완료).
- **✅ 멀티 종목(동시 매수) 엔진 구현 완료(드라이+대시보드+백테스트, 전부 검증·푸시).** `web/engine.py` 를 단일 심볼 전제에서 멀티로 리팩터(`docs/multistock_plan.md` 계획대로). **설계 핵심**: 공유 1개(mandate `allowed_symbols`=전종목·auth 예산·Guard 수취인 allowlist·broker) 아래 종목별 N개 `TradingAgent`(position·이력·시간청산 독립, 모두 같은 trading_kp·auth·guard 공유) → "하나의 402 Guard 로 다중 지출 통제" 서사. **1회 매수=총 spend/N**(DCA 회당도 /N)으로 한 종목의 예산 독식 방지(검증 sweep_portfolio 와 동형). **방어(계획 공백 보완)**: 추세추종(올인)·라이브·목시세 + 멀티는 거부(각각 예산 독식·온체인 멀티 범위 밖·전종목 동일가격). **N=1 하위호환 바이트 동일**(symbols 미지정=레거시 STOCK_SYMBOL 경로). **배선 파일**: `config.py`(STOCK_SYMBOLS)·`web/engine.py`(핵심: `_resolve_symbols`·`_build_symbol_feed`·`_tick_once`/`_process_symbol` 종목순회·오류격리·일부소진 skip·전부소진 종료·`_buy/_sell_cycle` agent 파라미터화·`_valuation` 집계+`_symbol_valuation`·`state_snapshot` symbols[]+per_symbol+집계·update_limits 전종목 auth+가드 정합·`start(autostart)` 테스트시드)·`web/server.py`(FeedBody.symbols)·웹 3파일(종목 체크박스·포커스 드롭다운·종목별 요약표·거래표 종목열·판단 종목태그)·`scripts/backtest.py`(`--symbols` 포트폴리오, 엔진 공유예산 모델 재현). **신규 `scripts/test_multistock.py`**: 예산 공유·포지션 독립·피드 소진 격리·세션 종료·가드 KPI 합산·하위호환·방어·한도변경 가드정합 **31개 확인 전부 통과**. **웹 라이브 검증**(dashboard-fast): 3종목 bear 세션 — 종목별 10.00 USDC 매수·독립 포지션(요약표 종목별 상이 P&L)·공유 예산 차감·가드 유출0·포커스 전환 차트/가격/포지션 이동·거래표 종목열·추세추종/라이브 자동 단일잠금, 콘솔 오류0. **포트폴리오 백테스트**: AAPL+TSLA+NVDA _bear 조건형 -22.6% vs 등가중 매수후보유 30.1%(예상대로 열위 — 조건형은 알파 없음, 가치는 402 Guard+분산 하방 축소). **적대적 검증 워크플로우**(9에이전트·3렌즈·건별 재검증): 확정 6건(반증0) 전부 수정 — ①update_limits 가 공유 Guard.mandate 미갱신(선재 결함, 한도변경 후 가드 정합) ②목시세+멀티 거부 ③DCA 멀티 문구 종목별 금액(=amount/N) 정정 ④거래표 종목열·판단 종목태그 ⑤포커스 전환 차트 되감김 해소 ⑥라이브 종목 단일잠금. **회귀**: 기존 테스트 10종+red_team(시도18·차단4·유출0.00·오탐0) 전부 통과, 단일종목 경로 무변경. 커밋 8개(config·engine+test·server·web·backtest·엔진수정+test·web수정) 전부 푸시. **다음 = 심사 부서(judge-dept) → 새 대화서 버그 부서(bug-dept).** 안전한 중단점.
- **✅ 추세추종 전략 모드 앱 구현 완료(사용자 진짜 의도, 검증·푸시).** 아래 방향 정정에서 실증한 추세추종을 실제 앱 새 전략 모드로 배선. **설계 핵심**: `Strategy.mode="trend"`(condition/dca 와 병렬)·`trend_signal`(pxma20 가격≥MA20 기본 / cross_5_20 골든크로스5/20)·판단은 **결정론적 규칙 신호(Gemini 미사용)** 라 검증 재현·상승세 전량 보유·하락세 전량 매도(자본 보존)·재상승 재매수·**올인/올아웃**(매도 대금 복리 재투자=`ap2 credit_sale(allow_surplus=True)`)·건별 한도는 세션 동안 총자산까지 열되 첫 진입 상한=예산·재진입=가진 현금 이내(402 Guard 수취인·청구=합의견적·의도지출·자산 검사 그대로)·시간청산 미적용. **컴파운딩 설계 근거**: explore_trend 가 올인/올아웃 복리 모델(_bear 종목당 ~40매매)이라, 앱도 복리해야 검증 재현 — `credit_sale` floor(순투입 예산캡)를 trend 만 해제(음수 spent 허용=운용현금이 예산 초과, 초과분은 실현이익 재투자). **배선 파일**: `agents/trading_agent.py`(`_decide_trend`·`_trend_ma` 라운딩없는 MA로 시퀀스 정확 재현·`on_sale_completed` trend 복리)·`payments/ap2_mandate.py`(`credit_sale allow_surplus`)·`web/engine.py`(strat_type trend 검증·trend_signal·brain=None·세션 per_trade 올인캡·max_hold=0·초기자본 대비 수익률(`_display_return_pct`)·update_limits/state/summary trend·feed dataset daily/bear)·`scripts/backtest.py`(`--strategy trend --trend-signal --suffix`)·`web/server.py`(StrategyBody.trend_signal·FeedBody.dataset)·`web/static/index.html`+`app.js`(전략·신호·데이터셋 3선택·규칙텍스트·per_trade 전량표시). **신규 `scripts/test_trend.py`(재현 검증)**: 실제 `decide()→broker→auth` 경로가 explore_trend.simulate 의 진입/청산 시퀀스를 **봉 단위 정확 일치**(매매수 동일: AAPL 38/38·22/22, TSLA 41/41·25/25, NVDA 35/35·23/23), 수익률 오차 ≤0.08%p(브로커 소계·수수료 분리 센트반올림뿐 — 탐색은 합계 1회 반올림). **백테스트 _bear 벤치마크**: TSLA 가격>MA20 +67.76% vs 매수후보유 −20.45% = **+88.21%p 우위**(MDD 29.58%), AAPL 골든크로스5/20 +13.56% vs +10.85%. **웹 라이브 검증**(dashboard-fast 0.4s틱): "추세추종"+"하락장" 선택→하락 구간 "하락세 이탈(전량 매도, 자본 보존)"→"하락세 관망(현금 보유)"→회복 "상승세 진입(전량 매수)", A2A→x402→settled 결제 정상(올인 매수·매도), 가드차단0·유출0, 건별한도 "전량(올인)" 표시. **회귀**: 전체 테스트 10종(test_trend·timeexit·guard·settlement·dca·indicators·gemini_parse·store·dataquality)+red_team(시도18·차단4·유출0.00·오탐0) 전부 통과 — allow_surplus 는 trend 전용이라 condition/dca 무영향. 리포트 `docs/reports/strategy_validation.md`. **다음 = 멀티 종목(`docs/multistock_plan.md`, 새 대화)→심사 부서→새 대화서 버그 부서.** 안전한 중단점(커밋·푸시 완료).
- **⚠ 전략 방향 정정 — 사용자 진짜 의도는 추세추종, 하락장으로 실증(커밋 c494470·a2643f7).** 사용자 피드백: 내가 고도화한 dip3/profit5+시간청산은 **평균회귀**(눌림목 매수→짧은 익절)로, 원하던 **추세추종**(상승세 보유·장기 하락세 매도·추세전환 재매수)과 정반대였음을 정직히 인정. `scripts/explore_trend.py`(전량 진입/청산, 가격>MA20·골든크로스·MA기울기)로 검증: **상승장(현행 _daily)에선 추세추종이 매수후보유에 전부 패배**(피할 하락장 없어 늦은진입·휩쏘 손해), **하락장(_bear.csv=2022 폭락+2023 회복, yfinance 조정일봉 501봉)에선 크게 승리** — TSLA 보유 -20.4% vs 가격>MA20 +67.8%(+88%p)·골든크로스5/20 +76.4%(+97%p), NVDA MDD 60.8%→22.9%, 롤링 60/120봉서 가격>MA20 이 보유를 57.8%/62.9% 승률·평균 +3.7/+5.8%p 초과. **결론: 어떤 단순전략도 순상승장에선 보유를 못 이기나(알파는 어려움), 추세추종은 하락장 회피로 전체 사이클서 우위 = 402 Guard 안전서사 직결**. 데이터 수신은 stooq 봇차단→yfinance 전환(`scripts/fetch_bear_data.py`, 개발용). **평균회귀 개선은 condition 모드 옵션으로 유지(무해). 다음 우선순위 = 추세추종을 새 전략 모드로 앱 구현(가격>MA20 기본).** 상세 `docs/reports/strategy_validation.md`.
- **전략 수익 검증 라운드 + 수익 로직 고도화(사용자 요청).** 데이터 부서가 건전성 확인한 CSV 로 "짧은 단일종목 세션이 확실히 흑자인가"를 실측. ①`scripts/validate_strategy.py` 신설(롤링 윈도우 762구간, 규칙 두뇌 고정·미실현손익 포함으로 승률착시 제거·매수후보유 벤치마크·3종목 분산 포트폴리오 모드). **결론(정직)**: 짧은 단일종목은 확실한 흑자 불가(8봉 흑자 37%·26% 무거래), 종목성격이 파라미터 압도(TSLA 우수·AAPL 열위), **어떤 설정도 벤치마크를 평균적으로 못 이김** → 제품가치는 알파가 아니라 402 Guard. 유일한 신뢰도 레버=분산(단일 55%→분산 82% 흑자, 최악 절반). ②`scripts/explore_strategy.py` 신설(규칙 위 오버레이 실측): **손절·잦은매매·추세필터·트레일링 전부 반려**(근거: 손절은 눌림목 바닥 확정, 추세필터는 매수붕괴, 트레일링은 MDD 2배), **시간청산(안전레일)이 진짜 고도화**(AAPL 최악 -6.47%→-0.18%·MDD 7.31→1.34%로 꼬리위험 제거). ③사용자 결정=최고 수익 스택 **dip3/profit5 + 시간청산 10봉**(분산 흑자 85.8%·평균 2.11%·최악 -1.61%, 전 지표 최고). **앱 반영**: config.MAX_HOLD_BARS·Strategy.max_hold_bars(조건형 decide 백스톱, 규칙/Gemini보다 우선)·web DEFAULT_RULES dip3/profit5·backtest 기본값 동기화·목 시세 경로 재설계(옛 절대가용 얕은 딥→dip3 트리거, 회귀 해소)·단위테스트 test_timeexit 8건. **검증**: 전체 테스트+red_team(유출0) 통과, **실제 decide() 경로가 탐색값 정확 재현**(전체 흑자 57.9%·평균 2.10%), 목 폴백(web·run_demo) 매매 정상, run_demo 드라이 12틱 풀사이클 완주. 리포트 `docs/reports/strategy_validation.md`. 범위 확정(사용자): 멀티 종목은 **드라이+대시보드+백테스트**(라이브 온체인 제외). **다음: 멀티 종목 엔진(단일→다중 리팩터, 큰 작업 — 새 대화 권장) → 심사 부서 가동(빠진부분·수상가능성·보완점) → 강력 보완 → 새 대화서 버그 부서.**
- **데이터 수집 부서(data-dept) 구축 완료 — 트랙 B 에이전트 부서 3/3.** 템플릿(judge-dept·bug-dept: 결정론 수집기 + 워크플로우 + 자기검토)에 사용자가 중간 점검으로 추가한 신규 스킬 2종을 접목해 강화. **산출물 3종**: `scripts/collect_dataquality.py`(결정론 수집기 — LLM·추정 0, CSV별 소비호환·스키마·OHLC정합·연속성·이상치·충분성·신선도 7차원을 판정, 로직은 순수 함수 parse_rows/check_ohlc/find_gaps/find_anomalies 등으로 분리) · `scripts/test_dataquality.py`(탐지기 단위테스트 28건 — high<low·30%점프·주말봉·깨진헤더·중복·워밍업부족 등 불량 입력 전수 검출, 정상 입력 통과 확인) · `.claude/workflows/data-dept.js`(Scan→Inspect[심볼별 팬아웃]→Verify[건별 적대검증]→Report[5축 자기평가+기록]). **스킬 접목**: ai-regression-testing → 독립 재파싱이 아니라 **실제 소비경로 `market.price_feed.load_bars()` 로 로드**해 "데모·백테스트가 진짜 이 CSV 를 소화하는가"를 대조(사각지대 제거) + 판정 로직 회귀 테스트. agent-self-evaluation → 자기검토를 5축 스코어카드(정확성·완전성·명료성·실행가능성·간결성, 축별 증거 필수)로 강화(bug/judge 부서의 임시 체크리스트보다 엄격). **범위**(설계 인터뷰로 사용자 확정): data/market/*.csv 만·읽기 전용(자동 수집 없음, `--fetch` 미래 여지)·수집기 테스트 추가. **실제 1회 구동 검증**(26에이전트·오류0·소요 11.7분): 실제 이슈 0건·관찰 20건(낮음2=MA200 산출 여력 미달·마지막봉 2거래일 뒤처짐 신선도, 정보18)·자기평가 4.2/5. 4일 캘린더 갭 4건을 미 증시 공휴일(성금요일·메모리얼데이·준틴스·독립기념일)로 정확 분류, 6~8% 일변동을 임계(20%) 미만 정상으로 판정하되 "외부 시세 대조 안 함"을 정직하게 한계로 명시, git 작업트리 읽기 전용 준수(CSV·앱 코드 무변경, 부서 스캐폴딩 파일만) 검증. 인용 수치 독립 교차확인(AAPL -6.12%·NVDA 공휴일 갭) 일치. `_dataquality_*.json` gitignore 추가, README 부서3 예정→가동. **재실행**: `Workflow({scriptPath:".claude/workflows/data-dept.js"})` = "데이터 부서 돌려줘". 안전한 중단점(커밋·푸시 완료).

## 2026-07-24
- **402 Guard 버그 수정 라운드 완료(버그 부서 리포트 핵심 4건 BUG-01~04 + 적대검증 후속 1건, 커밋 90c5c33·74442df·2b4014d·1f76aa6·05fe683)**: 버그 부서가 찾은 402 Guard 본체 결함을 재현→수정→회귀→커밋→푸시 사이클(버그당 1커밋)로 처리. **BUG-02(높음, 90c5c33)** 매도 정산 배송검증 부재 — broker.settle_sale 이 주식 confirmed 하나로 settled 를 정하던 것을 라이브에서 지급 paid 까지 봐야 settled(실패=partial·주식미확정=failed)로 바꾸고, engine._sell_cycle 에 매수측 대칭 check_delivery(정산 후 판매자 USDC 잔액 재조회, 미확인=partial 강등·포지션·예산·실현손익 미반영·세션 정지) 추가. FakeClient(주식확정·지급실패) 회귀. **BUG-03(중간, 74442df)** GUARD_AMOUNT_MISMATCH 실흐름 미발화 — required 가 quote 에서 파생돼 둘이 늘 정합이라 브로커 부풀리기를 못 잡던 축을, 브로커와 독립적인 **의도 지출 상한**(GUARD_INTENT_EXCEEDED, 새 차단코드 7번째)으로 보강. build_payment→engine._buy_cycle/run_demo 가 decision.spend_usdc 를 가드에 전달, red_team.attack_amount_forge 를 인위주입(required≠quote) 대신 자기정합 quote+의도초과로 재작성(실흐름 발화, 데모 신뢰성 이슈 해소). **BUG-01(높음, 2b4014d)** 잔액 기준선 오라클 오염 — get_token_balance_base 가 모든 예외를 0 으로 삼켜 ATA 미존재(진짜 0)와 429/불명을 혼동하던 것을, _is_account_not_found(계정미존재 신호만) 로 계정미존재일 때만 0·그 외 전파. engine._buy_cycle 기준선 읽기를 try 로 감싸 실패 시 예약원복(release)+GUARD_BASELINE_UNREAD+세션정지+return(결제 미제출, 유출 0). **BUG-04(중간, 1f76aa6)** ALLOW_LIVE_FROM_WEB 기본 "1"(열림)→"0"(차단) — 주석·engine 에러·런북과 정합. **웹 UI 라이브 데모는 이제 ALLOW_LIVE_FROM_WEB=1 필요**(현 devnet 증빙은 run_demo CLI 라 영향 없음). **마무리 적대검증**(워크플로우 13에이전트, 버그×3렌즈+종합): 3건(01·02·04)은 실소스 재확인·오탐 유효, BUG-03 에서 **실회귀 1건**(의도 상한 허용치 1센트 고정이 고수수료+sub-cent 지출에서 정직 견적 오탐 차단, 초과 0.010006) 적발 → **05fe683 로 허용치 2센트로 정정**(subtotal·fee 이중 센트반올림 최악치 0.01+0.005+0.005=0.02, 수수료 100%까지 유효, 브로커 값 비의존). 재검증: 정직 견적 16,800건(수수료 10~10000bps×sub-cent) 오탐 0·최악 초과 0.0101, 공격(44.94 vs 의도30) 여전히 차단, 테스트 6종+red_team(시도18·차단4·유출0·오탐0) 전부 통과. 각 커밋이 안전한 중단점.
- **에이전트 부서(트랙 B) 2번 = 버그 전담 부서 완료(커밋 87f6c9b·1b1d396·36384a2)**: judge-dept 템플릿을 버그 사냥에 이식. **설계 인터뷰 4문항** 확정 — 스캔 범위=앱 전체 모듈(16개)·렌즈=4종 전부(정확성·엣지케이스·에러처리·회귀/커버리지)·재현물=**리포트 텍스트만(읽기 전용, 코드 파일 0)**·심각도=전부. ①`scripts/collect_bugscan.py`(결정론적 버그표면 수집기, LLM·추정 0): git churn(최근20커밋 파일별 변경빈도)·테스트 6종 pass/OK수·red_team KPI·**coverage_map**(모듈별 test 참조+미커버 briefing/server 확정)·**risk_markers**(bare/broad except·TODO·noqa·runtime assert·payments내 float, 각 file:line)·**module_index**(파일별 비공백·함수·클래스). collect_evidence 패턴(glob.escape 대괄호·PYTHONIOENCODING·_safe 섹션독립) 재사용, 독립 grep 교차검증(broad_except 27 일치·미커버 2건 일치). ②`.claude/workflows/bug-dept.js`: Scan(수집기)→Hunt(모듈그룹4 팬아웃 G1결제·가드/G2에이전트/G3시세·지표·공유/G4웹·설정, 4렌즈 전부)→Verify(건별 적대검증, 기본 real=false·python -c 재현·저장소 미잔류)→Report(종합+**자기검토 6항목**: file:line 실재·재현 구체성·창작0·심각도 정합·반려누락·읽기전용 준수). ③첫 end-to-end 실행(에이전트 22·오류0·40분·서브에이전트 토큰 1.88M): **확정 13건**(높음2·중간2·낮음8·정보1)·반려2. **핵심 4건 제가 실소스로 독립 교차확인**: BUG-01(높음—`x402_solana.py:309` get_token_balance_base 의 except→0 이 RPC실패/ATA미존재 혼동→check_delivery 델타오라클 기준선 오염→미배송 오탐 통과, 매수측 결함I 재개통) · BUG-02(높음—`broker_agent.py:192` settle_sale 이 confirmed[주식]에만 의존, USDC 지급 실패해도 settled + 매도 사이클 `engine.py:856`엔 매수측 check_delivery 없음=결함I 매도측 대칭구멍) · BUG-03(중간—`guard.py:134` GUARD_AMOUNT_MISMATCH 가 required·guard expected 가 **동일 브로커 quote**에서 나와 실 매수흐름 발화불가, red_team 은 required≠quote 인위주입으로 '차단됨' 연출; 빈틈=브로커총액 vs 사용자 의도 spend 미대조, 단 수취인 allowlist·건별한도는 실작동이라 무한드레인 아님) · BUG-04(중간—`config.py:73` ALLOW_LIVE_FROM_WEB 기본 "1"=열림인데 주석·런북·engine 에러는 '기본 차단' 전제). **읽기 전용 검증**: git status 에 리포트 md 만(앱코드·테스트·repro 잔재 0). 적대검증이 심각도 정직 하향(BUG-04 높음→중간·07 중간→낮음·13 낮음→정보)+오탐 2건 근거 반려. 리포트 `docs/reports/bug_latest.md`+`bug_<TS>.md`(커밋), `_bugscan_*.json` gitignore. **수정은 승인 후 별도 작업**(부서는 찾기만, 앱·제출물 불변). 다음 부서: 데이터 수집(같은 패턴). 부서2·3 착수 프롬프트는 CLAUDE.md 다음단계.
- **에이전트 부서(트랙 B) 1번 = 심사용 부서 완료(커밋 b00f249)**: 개발/평가 지원 조직의 첫 부서. **설계 인터뷰 4문항**으로 확정 — 구현 형태는 사용자 판단(토큰 감수하고 검증 허점 0)에 따라 **워크플로우 형태**(서브에이전트식 온디맨드 호출 + 팬아웃·적대검증 결합), 모든 리포트에 **제출 전 자기검토 단계** 추가. ①`scripts/collect_evidence.py`(결정론적 증거수집기, LLM·추정 0): 테스트 6종 pass/케이스수·`red_team --report` KPI·`artifacts/tx` 인벤토리(localnet vs devnet·cross_check·explorer)·`artifacts/backtests` 전략 vs 벤치마크·코드 라인·402 Guard 구현 마커(git grep)를 실물로 수집. **함정 3개 처리**: glob 이 저장소 경로의 `[...]` 를 문자클래스로 오해석(→glob.escape) · 서브프로세스 cp949 인코딩(→PYTHONIOENCODING=utf-8) · `"402 Payment Required"` 가 docs 예시에 있어 구현 오탐(→코드 디렉터리로 한정). ②`.claude/workflows/judge-dept.js`: 증거수집→축별 심사(팬아웃4)→적대 검증(축별)→종합→자기검토·기록. 심사 기준 출처는 **킥오프 전사 기반**(hackathon_essentials §1 + FEATURES §5), 신청 페이지 상이점은 "기준 불일치·미검증" 섹션으로 분리(공식 원문 미확보, handoff §4). 호출은 `Workflow({scriptPath:".claude/workflows/judge-dept.js"})` — 이 하니스는 `.claude/workflows/*.js` 를 name 자동등록 안 함(내장만). ③`docs/reports/`: 부서 문서 README + 첫 리포트(에이전트 11명·오류0). **검증**: 리포트 핵심 수치를 제가 증거 JSON·README·app.js 와 독립 교차대조 — 전부 일치(창작 0). **첫 리포트 결론**(축별 ①중②중③강④강, 적대검증 반려0): 강점=③기술(실 SPL/USDC·AP2 ed25519·guard 결선·exact `!=`·Memo)·④구동(devnet 10 tx settled+교차검증·localnet7·테스트6종). **잡아낸 실제 갭**: FEATURES §5 stale(devnet 증빙 존재하나 "전부 localhost"로 기재)·README 정문 미전환(402 Guard 언급 0)·DEFAULT_LAYOUT 수익 우선·devnet 라이브 전부 rule(Gemini 온체인 0건)·HTTP402 미구현·배포URL/데모영상 부재. 증거 스냅샷 `_evidence_*.json` 은 gitignore(재생성 가능), 리포트만 커밋. 다음 부서: 버그 전담·데이터 수집(같은 패턴, judge-dept.js 가 템플릿).
- **devnet 라이브 실증 완료 — 첫 온체인 증빙(기존 증빙 전부 localhost)**: 공용 RPC(api.devnet.solana.com)의 두 벽(파우셋 GitHub 공개repo 조건 차단 · 429 rate limit)을 넘어 실제 devnet 풀사이클 브로드캐스트. ①**setup_devnet 안정화** — 여유 지갑(trading)→broker 자동 이체(파우셋 없이 충당), 이미 충분한 지갑은 에어드랍 생략, 모든 RPC 호출 지수 백오프 재시도(429 는 solana-py 가 SolanaRpcException 으로 감싸 str 에 안 보임 → __cause__/__context__ 체인 판별). ②**payments/x402_solana `rpc_retry`** — submit_and_confirm·blockhash·잔액 조회에 동일 재시도(라이브 브로드캐스트 경로). ③**run_demo `--replay SYMBOL --from --to`** — MockPriceFeed 8스텝은 옛 절대가 규칙(185)용이라 현 MA5 규칙에선 매수 미발생(딥이 워밍업 중 소실) → ReplayPriceFeed+preload_bars 로 실데이터 재생, 데이터 소진까지 루프(결정적 CLI 증빙, 판사 재현 용이). **결과(AAPL 06-01~07-22 재생 라이브)**: 매수 4·매도 1 = 온체인 tx 10건(결제+전달) 전부 확정, explorer.solana.com?cluster=devnet 조회 가능, 교차검증 PASS(온체인 USDC 순 -4.9 == 체결 순합계 · 주식 순 0 == 순수량, 완전 왕복), 실현 +4.9 USDC 온체인 반영(trading 1000→1004.9, broker 500→495.1, 양쪽 SOL 수수료 차감), 증빙 `artifacts/tx/20260724_1643_solana-devnet_live_buy.json`. README devnet/localnet 예제를 재생 방식으로 갱신(낡은 --ticks 8 제거·faucet 자동충당 안내). 검증: 드라이런 재생 매수4·매도1 트리거 확인 → 라이브 완주, 테스트 4종 PASS. 다음: 제출물 초안(README 402 Guard 재작성·pitch.md·데모 대본).
- **로그인 껍데기 + devnet 실증 준비(이어가기 세션)**: ①**로그인 우선순위 판단** — 코드로 확인(엔진 전역 싱글턴 `web/server.py:28`, 조작 API 는 이미 CONTROL_TOKEN 게이트, 사용자 인증 없음) → 결론: 실인증은 심사축에 기여 0·전역 싱글턴 리팩터(15~30h)라 마감·402 Guard 본체와 충돌, handoff §5-2 가 Firebase Auth 를 이미 컷. **사용자 결정: 로그인 버튼·페이지 껍데기만 지금, 실인증은 제출 후 self-custody 로드맵**. ②구현: `web/static/login.html`(로그인/회원가입 탭 토글, 회원가입 시 확인 필드, 제출은 어디로도 전송 없이 인라인 "출시 예정" 안내) + `GET /login`(대시보드 비차단, 정적 서빙) + 상단바 로그인 링크 + skeleton.css auth 레이아웃(theme.css 변수 기반, 시안 흡수). 검증: /login 렌더·상호작용 PASS, 대시보드 카드 11개 로그인 없이 정상, 콘솔·서버 에러 0. ③**devnet 실증 준비** — 코드·경로는 이미 완성(localnet 검증, `explorer_tx_url` devnet URL 정확·교차검증·`artifacts/tx` 아카이빙 완비), devnet 은 RPC(기본값)+지갑 SOL(파셋)만 다름. setup_devnet 에 **SOL 선검사 fail-fast** 추가(에어드랍 파셋 실패 시 민트생성 rent 단계 크래시 방지 → 지갑주소+faucet 안내 후 종료) + docstring 경로 정정. 라이브 실행은 **사용자 차례**(현 .env=localnet → devnet 전환, trading `GQJo2q…`·broker `32UBfB…` 파셋 충전, setup→run_demo --live). 다음: devnet 라이브 증빙 → 제출물 초안(README 재작성·pitch.md·데모 대본).
- **402 Guard P0 전부 완료(G1~G4, 커밋 89cde31·9701d90·9523d19·7e2f3b8) + 킥오프 근거 반영**: 재포지셔닝의 본체 구현. **G1** `payments/guard.py` — check_demand(금액 base units 오차0·수취인 allowlist·자산·주문번호 4항목 + 종목·건별한도, 차단코드 6종)·check_delivery(정산 후 온체인 잔액 재조회 balance_reader 주입·미확인은 pending 보류), 방어 위치를 sys._getframe 로 런타임 생성(guard.py:L{n}). **G2** 결선 — build_payment 가 authorize 앞에서 guard.assert_demand, AP2 authorize 가 asset 검증으로 allowed_asset 을 실제로 읽게(결함 C), release/settle 예약 추적으로 정산 실패 시 한도 원복(결함 H), 엔진 GuardError→GUARD_BLOCKED + 라이브 배송 재조회 partial 처리(결함 I). **G4** Memo 바인딩(AT1:order_id:sig8 온체인 대사 키)·verify_payment `<`→`!=`(exact, 결함 D)·expected_order_id Memo 대사(결함 E)·브로커 used_signatures dedup·expires_at. ※의존성(이중청구 방어=Memo+dedup, dedup 은 Memo 로 tx 유일해져야 드라이런 오탐 없음)으로 **G4 를 G3 앞에 구현**. **G3** `scripts/red_team.py --report` — 공격 3종(청구위조 2변형/이중청구/정산미이행)을 실제 결제 경로에 태워 매트릭스 + 정상 14건 오탐 0 동시 산출, --attacker 로 심사위원 악성주소 입력. **검증**: 단위 test_guard 13종·test_settlement 12종 + 회귀 전종 + 엔진 드라이런 스모크(예약 회계 일치) + **localnet 풀사이클 라이브 재검증 PASS**(Memo 실린 매수6·매도1 온체인 확정, 교차검증 USDC 86.95==86.95/주식 0.5129==0.5129). red_team 실측 시도18·차단4·유출0.00·오탐0(가드없을때 피해 117.76). **부수 발견·수정**: setup_devnet write_env cp949 크래시(.env UTF-8 미지정) 재현 차단, 브로커 USDC 운용자본 500 지급(없으면 익절 매도 대금 부족으로 정산 실패 — 사전 미검증 결함). **킥오프 발표 반영**: `docs/differentiation.md §7`·`docs/submission.md §2-3` 신설 — 402 Guard 가 유준혁이 추천한 "검증·가드레일" 레이어 정합, 당위성(진짜 해자=인증 없는 자율 결제→검증 계층 필수, 마이크로페이먼트 논거 기각), 자율성 축 우위(Google 데모 "버튼 클릭과 다를 게 없다" 자평 대비), 김채린 4대 관전포인트 전건 충족으로 서사·심사축 근거 보강. 다음: G5(브로커 HTTP 402 분리, 매수 경로) 또는 devnet 실증·제출물(README/소개서/데모).
- **G0 사용자 키 분리 완료(커밋 329885f) + 세션 규칙 3건 반영**: 402 Guard P0 착수 — 결함 G 제거. `secrets/user.json` 위임자 키 신설, open mandate 를 사용자 키로 서명(에이전트 trading 키는 한도 내 결제 서명만 유지). 반영 파일 config·run_demo·web/engine(start·update_limits 재서명·스냅샷/아카이브 wallets 에 user 노출)·setup_devnet·.env.example. 검증: 드라이런·엔진 드라이런 스모크(user≠trading, 서명검증 True, update_limits 재서명 True) + 테스트 4종 통과, user.json gitignore 차단 확인. **개인 지침 반영 라운드**: 앱 설정 개인지침이 Claude Code 로 전달 안 됨을 확인(전문 에이전트 조사 + 홈 `.claude` 실측 — 전역 CLAUDE.md 부재, 존댓말 규칙이 자동 메모리="배경 정보"에만 있어 강제력 약 = 반말 슬립의 구조적 원인). → CLAUDE.md 세션 운영에 ①**존댓말 하드 규칙** ②**작업 태도 규칙**(세부 설계 구체화·철저한 준비/마무리, 강점 402 Guard 집중·무리한 기능 추가 지양·단 사용자 요청 시 추가) 추가, ③토큰 규칙의 "Ultracode 팬아웃 상시 금지" 문구 삭제(사용자 결정: ultracode 키워드로 필요 시만 켜므로 불필요). 전역 개인지침 위치 = `~/.claude/CLAUDE.md`(사용자 기재 예정). 다음: G1 `payments/guard.py`.
- **공식 심사 기준 확보(사용자 제공) + 차별화 재포지셔닝 확정**: 4축 원문 확인 — ①혁신성·UX ②AI 활용도(Gemini/Google Cloud AI 스택) ③기술·인프라 연동("USDC·Solana Pay·pay.sh 등"과 "AP2·A2A·x402 등"을 **병렬 예시**로 나열) ④실제 구동. → **"자체 x402 vs pay.sh 충돌" 우려 기각**(우리 스택이 그 자체로 정합), pay.sh 는 디스코드 지원 채널 있어 여유 시 가점(P1 유지). 차별화 라운드(에이전트 29개: 각도 6종 × 심사위원 3인 패널 + 탈락 스캔 + 비평) 완료 → **"402 Guard — 에이전트 지출 승인 게이트"** 로 전환([`docs/differentiation.md`](differentiation.md)). 근거는 저장소 실증 결함: 구매자 무검증 서명, AP2 `pay_to` 미검사, `allowed_asset` 죽은 필드, `exact` 인데 금액 `<`, Memo·HTTP 402 부재, **에이전트가 자기 허가서를 자기 키로 서명**(규칙 10 위반, 직접 확인). 심사위원 3인 시뮬레이션 전원 "현 상태 탈락" → 재포지셔닝으로 방어. **기능 명세 문서 [`docs/FEATURES.md`](FEATURES.md) 신설**(주요 기능·마일스톤 이전/현재 대비·심사축 매핑). 전략 고도화(손절·트레일링·Gemini 사이징)는 컷(수익률로는 벤치마크에 지므로 승부 축 전환 우선).
- **배포 전 종합 검토 라운드(다중 에이전트 40+)**: 웹 조사 7축 + 코드 감사 6축(발견 64건) + 적대적 검증 32건 → [`docs/preflight_review.md`](preflight_review.md). **①실현가능성 결론**: 토큰화 주식은 메인넷에 실재(xStocks 민트 온체인 확인, Jupiter 견적이 Pyth 가격과 0.4% 이내 일치)하나 **devnet 에는 없음**(2인 독립 검증) → 현 자체발행 토큰 설계가 옳고 "민트 상수·스왑 경로 2곳만 교체하면 실물"이 로드맵. 단 permanentDelegate·pausable 등 발행사 사후 통제권 상주 → "퍼미션리스" 단정 금지. **②전략 진단**: 백테스트에 매수후보유 벤치마크가 아예 없었음 → 추가 후 실측(80봉) AAPL +4.90% vs 벤치 +30.19%(**-25.29%p 열위**) · TSLA +11.22% vs +2.73%(**+8.49%p 우위**) · NVDA +20.15% vs +25.82%(-5.67%p). 손절 부재로 승률 100%는 착시, Gemini 는 구조상 보류만 가능해 심사 2축 정량근거가 0. **③미검증 2건 분리**(공식 심사기준 원문, "공식 과제=API 결제·pay.sh" 보도 — 사용자 확인 대기).
- **심사 재현 차단 버그 2건 수정**: `python run_demo.py`(README 대표 명령)가 stdout 리다이렉트 시 cp949 UnicodeEncodeError 로 크래시 → config.py 에 `enable_console_safe_output()`(errors=replace, 인코딩은 유지 — UTF-8 강제는 한글을 깬다). README 백테스트 명령 3개가 전부 ValueError(안내 구간이 커밋 CSV 범위 밖) → 인자 없는 명령을 기본 안내로 + 구간 오류를 안내 메시지로.
- **공개 배포 차단 5건 수정(보안)**: CONTROL_TOKEN 헤더 게이트(POST 만, GET·SSE 는 공개 유지 — 심사위원 관전 보장 / 로컬 미설정=무인증), 한도 서버측 상한 + per_trade<=budget, Infinity·NaN 차단(is_finite), feed.file 제거 + 심볼 정규식·realpath 검증 + 요청 모델 extra=forbid, 지갑키 환경변수 주입 경로(Cloud Run 시크릿 동일 디렉터리 마운트 제약 우회) + 라이브 fail-fast, ALLOW_LIVE_FROM_WEB 잠금. 공격 시나리오 12종 차단 확인. 런북에 --concurrency 300 추가(탭 80개면 심사위원이 페이지를 못 여는 문제), .gcloudignore 신설.

## 2026-07-23
- **Cloud Run 배포 준비 완료(심야, 배포 실행은 사용자 gcloud 대기)**: ①Firestore 영속화 — `web/store.py`(BaseStore no-op 기본 + FirestoreStore, 컬렉션 sessions/trades/briefings/state), 엔진 저장 지점(세션 요약 dry 포함·체결·브리핑·한도 기본값, fire-and-forget+실패 경고 1회)과 부팅 복원(한도+최근 브리핑), `/api/history` 4종 신설. 검증: FakeStore 통합테스트 18건·장애 내성·서버 스모크(드라이런 체결) 전부 통과, 로컬은 기본 OFF라 무변경. ②Dockerfile(3.11-slim, TZ=Asia/Seoul, PORT 계약 실측 200 OK) + .dockerignore(시크릿 차단). ③[`docs/deploy_cloud_run.md`](deploy_cloud_run.md) 런북 — SDK 설치부터 Secret Manager(지갑 파일 마운트+Gemini env)·SA 권한·deploy 플래그 근거(max 1=싱글턴 엔진, no-cpu-throttling=백그라운드 틱)·재시작 영속 증명 절차·비용(월 ≈$50, $300 크레딧 내). 부수 수정: 콘솔 print cp949 안전화(test_dca_schedule 크래시 해소). 다음: 사용자 런북 실행 → 배포 URL 검증 → Firebase Auth 라운드.
- **디자인 일정 변경 통보(밤)**: 디자이너 시안이 **약 5일 뒤(≈07-28)** 도착 — 기존 "주말 예상"에서 밀림. 마감(08-03)까지 6일만 남으므로 계획 재정렬: 07-24~07-28은 **디자인 무관 작업만**(Cloud Run+Firestore+Secret Manager → 인증·세션 분리 → 로그 가독성·브리핑 간소화·랜딩 뼈대/카피·devnet 검증·README/소개서 초안), 시안은 도착 즉시 스킨 교체(theme.css 변수 + DEFAULT_LAYOUT 배열, 반나절 목표)로 흡수. 더 밀리면 현재 스킨으로 제출(심사는 목업이 아닌 실제 구동 대상). CLAUDE.md 다음 단계 4단계로 갱신.
- **TA 판단 보강 라운드 완료(사용자 매매 기준 분석 프롬프트 적용, 심야)**: `market/indicators.py` 신설 — MA 1/5/10/20/50/100/200일·골든/데드크로스·기울기, 지지/저항(피벗+2회 터치만 유효), 차트 패턴(M형/W바닥·삼중·H&S/역H&S·삼각형·쐐기·박스·확산·상승채널), 캔들 패턴(장악·도지·적삼병/흑삼병·샛별/석별·유성/망치·장대봉)을 코드로 계산(산수=코드, 해석=판단자). Gemini 프롬프트에 "TA 신호 요약+매핑·종합 규칙" 블록(ta_mode 시 ~900자), 규칙 폴백에 거부권/보류권(사지마·팔지마·대기패턴·충돌=hold). 세션 TA 토글(기본 OFF)·차트 MA 1(종가선, 가시성 — 사용자 요청)/5/10/20/50 선·백테스트 `--ta`. ★정정: 상승 쐐기=매도(원본 이미지 오류), 매수 100% 칸=역H&S·삼중바닥(사용자 확인, 수치는 추후 조정 가능). 단위테스트 44건 통과. **백테스트 실측(AAPL 06-01~07-22, 36봉)**: Gemini 엄격+TA **+4.95%/MDD 7.24**(전 모드 최고 수익률, 무TA 엄격 +4.29% 대비 +0.66%p) · 추세+TA +2.19%/**MDD 4.84**(최저 낙폭) · **규칙+TA 0%**(경직된 거부권이 상승장 딥바잉 매수 4건 전부 차단 — 같은 TA를 "증거"로 받은 Gemini 는 엣지 유지 → "규칙만으론 부족, AI 판단이 가치를 더한다" 소개서 서사의 실측 근거). 권고 확정: 토글 기본 OFF, Gemini 세션은 ON. 검증 중 서버 버그 발견·수정(pydantic StrategyBody 가 미선언 ta_mode 를 조용히 버림). 미탐지 TODO: 깃발형·다이아몬드·역V자. 상세 [`docs/ta_upgrade.md`](ta_upgrade.md). Cloud Run 라운드는 다음 대화로 이월(사용자 지시: 분량 초과 시 이월).
- **실시세 라운드 실측까지 완료 + 대시보드 모듈화 (밤)**: ①Alpha Vantage 무료 정책 변경 실측(full=프리미엄 전용) → compact(최근 100봉)로 AAPL·TSLA·NVDA 수집·커밋(2026-02-27~07-22). ②실데이터 웹 드라이런(AAPL 06-01~07-22, 36봉): Gemini 판단으로 6월 조정 4회 분할매수 → 7월 회복 308.63 전량 익절, **실현 +4.30 USDC(+4.78%)**, 재생 소진 자동 종료+브리핑. ③같은 구간 3종 백테스트 실측 커밋: 규칙 +4.90%(MDD 7.31) / Gemini 엄격 +4.29%(폴백 0, 규칙 준수 실증) / Gemini 추세 +1.65~+2.90(2회, MDD 5.00 — **매수 보류 재량으로 낙폭 축소, 보수적 거래**). 교훈: 백테스트 동시 실행 시 무료 티어 429 충돌로 폴백 오염 — 순차 실행 필수. ④**대시보드 전면 모듈화**(사용자 요청 "구성이 난잡, 모듈화해 위치를 바꿀 수 있게"): 기능 전부를 data-card 모듈 11개로 분리, 기본 순서는 app.js `DEFAULT_LAYOUT` 배열 하나(시안 배치를 배열 수정으로 적용), 카드 제목 드래그 재배치+localStorage 저장+배치 초기화 버튼, 세션 설정(모드·시세·전략·판단모드·시작/종료)을 카드로 이동해 상단바 슬림화. 합성 DragEvent로 실경로 검증, 모바일 375px 1열 확인. 디자이너 브리프는 사용자가 별도 대화에서 간소화("느낌만 전달+자율성")해 발송 완료, 시안 주말 예상.
- **실시세 라운드(§1·§2) 코드 전체 완료 — 데이터 파일만 API 키 대기**: 사용자 결정 4건 확정(AAPL 일봉 2년+TSLA·NVDA 예비 / CSV 전체 커밋+출처 표기 / 규칙 = MA5 −2% 매수·평단 +3% 익절 / Gemini 엄격·추세 토글, 추세=보류 재량). 구현 5커밋: ①`scripts/fetch_market_data.py`(Alpha Vantage 일봉, 25콜/일 보호) ②`ReplayPriceFeed`(1틱=1봉 실제 OHLC, 워밍업 20봉 preload, 소진 시 세션 자동 종료+브리핑, UI 피드 선택·날짜축·전일 종가 대비·워밍업 반투명, `/api/engine/start` feed 파라미터, PORT env 지원) ③지표 규칙 전환(절대가 178/185 전면 제거, 매도 우선 검사, 워밍업 hold — 단위테스트 6종+DCA 회귀 PASS) ④Gemini 입력 보강(MA5/MA20·5봉 등락률·변동성·평단 손익률·직전 행동 회고)+모드 토글(strict/trend, UI 셀렉트·아카이브 표기) ⑤`scripts/backtest.py`(규칙 vs Gemini 엄격 vs 추세 같은 구간 비교 — 총손익·승률·MDD·AP2 거부·폴백 수, 무료 티어 4s 페이싱). **웹 드라이런 검증**: 목 시세에서 MA5 −2% 3회 분할매수→평단 +3% 익절(실현 +3.01 USDC), **추세 모드 백테스트에서 규칙 신호에도 "낙폭 확대 — 바닥 확인까지 보류" 재량 2회 실증**(같은 구간 규칙 두뇌는 매수 — 소개서 비교 서사 확보). 남은 것: 사용자 `ALPHAVANTAGE_API_KEY` 등록 → fetch 실행 → CSV 커밋 → 실데이터 웹 검증+3종 백테스트 실측. 디자이너 브리프 발송 패키지 [`docs/design_brief.md`](design_brief.md) 작성(복붙 메시지+토큰 표+체크리스트).
- **방향 재정렬(사용자 피드백) → [`docs/next_round_plan.md`](next_round_plan.md) 신설**: ①목 시세의 반복 패턴으로 판단을 검증하는 건 무의미 → **실시세 데이터 도입 후 Gemini 매수·매도 프로토콜 검증이 최우선**(DCA 고도화는 보류). 데이터 조사 실측: **Stooq CSV 직링크는 봇 차단(JS 검증)으로 스크립트 접근 불가** → Alpha Vantage(무료 25콜/일)나 Alpaca(무료 키)로 1회 수집해 `ReplayPriceFeed` 로 재생하는 방식 권장(오프라인 폴백·결정적 재현 유지). 실데이터 도입 시 규칙 기본값(178/185)·예산 스케일·캔들 축·데모 구간 재설계 필요. ②**로그인·회원가입 요구 신설** → Cloud Run+Firestore+Secret Manager 배포가 선행(현 엔진은 전역 싱글턴이라 사용자별 세션 분리 필요, Firebase Auth 권장). ③**디자인 의뢰 가능**(디자이너는 앱 경험만, 웹 처음) → 정보구조 3화면(랜딩/로그인/대시보드)과 디자이너 브리프 초안 작성, 시안은 theme.css 변수 매핑으로 요청. ④로그 일반인 가독성·데일리 브리핑 간소화는 후반부 작업으로 지정. 마감(8/3) 역산 순서와 열린 결정 7건도 문서에 정리.
- **빠른 개선 3건 완료(항목당 구현→검증→커밋)**: ①평가손익(미실현)·총자산 카드 — 지금 전량 매도 시 수령액(수수료 차감) − 실효 평단 기준이라 실현손익과 숫자가 일관, 총자산 = 가용 예산 + 주식 평가액(라이브는 온체인 잔액 병기), 틱마다 실시간 갱신, B2 브리핑에도 반영(Gemini가 실제 인용 확인). ②캔들차트 — 목 시세가 틱당 단일가라 `TICKS_PER_CANDLE=2`로 OHLC 집계, 시세 카드를 `card-full` 메인으로 확대(큰 현재가 + 세션 시작가 대비 등락), SVG 직접 렌더(외부 CDN 없음): 양봉·음봉, MA5·MA20, 가격 눈금·격자·현재가선·범례. 검증은 틱 0.5초로 가속(.env 임시 추가 후 원복) — 93틱 → 캔들 47개 서버·화면 일치. ③적립식 주기 선택 — `dca_unit`: 틱마다/N분마다/매일 HH:MM, 대기 사유를 타임라인에 표기(남은 시간·오늘 집행 완료), 회귀 테스트 `scripts/test_dca_schedule.py` 7케이스 통과 + 웹 드라이런 2케이스 실증. 남은 피드백: 멀티 종목(별도 대화), 주간/월별 수익(Firestore 배포와 병합), Gemini 추세 재량(설계 논의).
- **버그 수정 전용 라운드 완료(사용자 보고 오류 4건 + 추가사항 4, 재현→수정→웹 드라이런 검증)**: ①긴급정지가 세션 경계를 넘어 유지되던 문제 — 세션 종료 시 `trading_enabled`·`pause_info` 리셋(종료 로그에 "정지 해제됨" 표기), 대기 중 pause/resume 은 409 거부 + 버튼 비활성(추가사항 4 동시 처리). ②Gemini `Invalid \uXXXX escape` — `parse_decision_json` 신설(코드펜스·설명문 제거 → 잘못된 이스케이프·제어문자 정화 후 재파싱 → 정규식 필드 추출), 그래도 실패하면 1회만 재요청 후 규칙 폴백. 회귀 테스트 `scripts/test_gemini_parse.py` 11케이스 전부 통과. ③알림 버튼 무반응 — 저장값과 실제 권한이 어긋나면 라벨이 안 바뀌던 desync 수정(버튼이 권한 상태 표시: 켜짐/꺼짐/차단됨/미지원), 모든 분기에서 토스트 피드백 + 차단 해제 방법 안내, 권한 변경 시 자동 갱신. ④dca 배지 혼동 — 타임라인·로그에 세션 경계 구분선(전략·판단 출처 명시) + 이전 세션 항목 흐리게, 타임라인 헤더에 판단 출처 범례. 검증: 드라이런 2세션(조건형→적립형) + 새로고침 복원, 콘솔·서버 오류 0.
- **P2 직후 사용자 피드백 접수 → [`docs/user_feedback_p2.md`](user_feedback_p2.md) 정리**: 버그 4건(긴급정지 세션 경계·알림 버튼·Gemini JSON 폴백·dca 배지 혼동), 수익 리포트 확장(주간/월별/종목별/평가손익), 캔들차트+이동평균+시세 카드 확대, DCA 시간/날짜 기반, 멀티 종목 전면 확장, Gemini 추세 재량 검토. 다음 대화는 버그 수정 전용으로 시작(작업 사이클 규칙). GitHub 푸시 완료.
- **P2 엔진 확장 완료(localnet 라이브 검증 통과, 기능당 1커밋 × 5)**: ①A8 수수료 투명화 — `BROKER_FEE_BPS=30`(0.3%), 매수 `총액=소계+수수료`·매도 `수령액=소계−수수료` 양방향(사용자 확정), AP2 검사는 수수료 포함 총액 기준, 평단=실효단가 → 실현손익=순손익, 대시보드에 견적 분리 표기·수수료 컬럼·누적 수수료(브로커 수익) 카드. **0.1% 인하안은 최종 제출 전 재검토(소개서 수익모델 수치 확정 시 사용자에게 질문할 것)**. ②A3 한도 설정 — `/api/mandate`로 새 mandate 재서명(spent 이월), 실행 중엔 긴급정지 상태에서만 적용, 대기 중엔 다음 세션 적용, 변경 이력 이벤트+아카이브. ③A4 알림 — 토스트+Web Notification(백그라운드 탭), SSE 히스토리 재생분 억제. ④B7 적립식 DCA — 세션 전략 선택(조건형/적립형), N틱마다 정액 매수(Gemini 미사용 라벨, `dca` 배지), mandate 동일 경로. ⑤B2 브리핑 — Gemini 한국어 리포트(실패 시 템플릿 폴백), 수동 버튼+세션 종료 자동+장 마감 시각(`DAILY_BRIEFING_TIME`) 3경로, `artifacts/briefings/` 저장. 검증: 오프라인 스모크(수수료 9건·브리핑 8건 PASS) → 웹 드라이런(기능별) → **localnet 라이브 2세션**: 조건형 매수4·매도1 + DCA 정액 3건 전부 온체인 확정, **교차검증 USDC/주식 PASS×2**(수수료 반영 순변화 54.22 일치), 증빙 `artifacts/tx/20260723_1355·1357_*_web_session.json`. 시나리오 실증: 한도 20으로 낮추자 수수료 포함 총액 29.98>20 AP2 거부 → 정지 중 100/40 재서명 → 재개 후 통과.
- 기능 선택 확정(사용자): **A 전부(A1~A8) + B1·B2·B4·B6·B7** (B3·B5 제외). 필수군 A1~A5 / 어필군 A6~A8·B전체 구분. B4는 "알림만/알림+자동정지" 토글 설계로 고민 해소, B6은 mandate 우회 불가·규칙 재서명 연결 원칙. 구체 설계 [`docs/feature_spec.md`](feature_spec.md)(P1→P2→P3) — 구현은 새 대화(웹 서비스화)에서 시작.
- CLAUDE.md 세션 운영에 **대화 전환 안내 규칙** 추가(큰 단계 완료 시 Claude가 먼저 새 대화 전환을 안내하고 문서 갱신 + 스타터 프롬프트 제공).
- **P1 웹 서비스화 완료(라이브 검증 통과)**: FastAPI+SSE `web/` 신설(engine/events/server + 무디자인 대시보드, 기존 코어 무수정 재사용). A1 대시보드·A2 긴급정지(주체 기록, 시세는 계속)·A5 거래 테이블(explorer 링크)·A6 판단 타임라인(gemini/rule-fallback 배지)·A7 실현손익 전부 동작. 엔진은 대시보드에서 세션 시작/종료(연속 루프, 기본 8초 틱 — 사용자 선택). localnet 라이브: 매수4+매도1 온체인 확정, +7 USDC 실현손익 온체인 반영, 교차검증 PASS, `artifacts/tx/20260723_1220_..._web_session.json`. 드라이런에서 새로고침 피드 복원(SSE 재전송)·rate limit 규칙 폴백도 실증. 디자인 시안은 `web/static/css/theme.css` 변수 교체로 적용 예정.
- **버그 수정 라운드(사용자 보고 3건, 당일 해결·재검증)**: ①"Gemini 계속 호출 실패" 원인 = `gemini-flash-latest`가 gemini-3.6-flash로 연결되고 이 모델 무료 티어가 **하루 20회**뿐 → 소진. 모델을 `gemini-flash-lite-latest`로 교체(한도 넉넉, 판단 품질 충분 — 재검증에서 판단 전건 gemini 배지), 429 시 retryDelay 존중 쿨다운 추가(틱마다 헛호출 방지), 타임라인에 실제 원인 문구 표면화, `check_gemini.py` cp949 크래시 수정. ②스크롤 부자연 = 피드·테이블이 무한히 자라 페이지가 밀림 + 스크롤 체이닝 → 고정 높이+내부 스크롤+`overscroll-behavior: contain`+sticky 테이블 헤더+`scrollbar-gutter`. ③"버튼 빼고 어두움" = `color-scheme` 미선언 상태에서 다크 모드 브라우저가 페이지를 강제 반전 → 라이트·다크 팔레트 정식 지원(theme.css 변수, 다크 렌더링 검증 완료). CLAUDE.md에 **작업 사이클 규칙** 명문화(기능 단위 구현→즉시 검증·버그 수정→커밋, 사용자 보고 버그 최우선, 폴리시는 디자인 시안 후 일괄).

## 2026-07-21
- MVP 코어 완료(오프라인 검증 통과): A2A+x402 3단계 결제, AP2 mandate 한도, x402 결제 코어, 규칙 기반 판단, 목 시세 피드. devnet 라이브 경로(setup_devnet.py + run_demo.py --live) 준비됨.

## 2026-07-22
- 0721 킥오프 세션 분석 반영: Gemini는 무료 티어 API 호출로 확정(ADK/Vertex 불필요), 평가 네트워크 Devnet/Localnet, 목업 심사 제외, 검증 4단계 루틴 채택.
- Claude Code 로컬 개발 체제로 전환. 시작 패키지(CLAUDE.md, docs, .gitignore, .env.example) 적용.
- Claude Code 첫 세션: 로컬 환경(Win11·Python 3.10.8) 검증 통과 — 오프라인 데모 전 과정 재현 OK. 수정: requirements.txt 한글 주석이 한국어 Windows(cp949)에서 pip 파싱을 깨는 버그 → ASCII화, solana-py를 3.10 호환 0.38로 조정, 지갑 경로 `.wallets/`→`secrets/` 통일, .env.example 정비(WALLET_DIR 추가·Gemini 무료티어 키로 교체), .gitignore 보강(secrets/·*-keypair.json·test-ledger/), architecture.md §6 실제 구조로 갱신. localnet 라이브 tx 계획 수립, 승인 대기.
- GitHub 원격 연결·푸시 완료: https://github.com/Loda1002/SolanaAgent (private — 제출 전 public 전환). 바탕화면에 검증기 켜기/끄기 바로가기 생성.
- 결정·확인(사용자): 최종 산출물은 **웹 애플리케이션**(대시보드 → Cloud Run). 진행자 가이드 재확인 — localnet 위주 개발, devnet은 마지막 시연·제출 검증. GitHub 저장소 `SolanaAgent` 생성(원격 연결 예정). 비개발자용 검증기 온/오프 더블클릭 스크립트(scripts/start·stop_localnet.bat) 추가. 브랜치 main 개명.
- 결정(사용자): 디스코드 Devnet SOL 요청 완료. devnet 착수는 **로컬에서 UI/UX 적용 테스트까지 끝난 뒤**로 확정(진행자 가이드의 "localnet 99% → 마지막 devnet"과 일치). 허용 창 사전 설명 규칙이 CLAUDE.md 세션 운영에 명문화됨.
- **매도 사이클 + 거부 케이스 완료**: x402 역방향(주식 전송→브로커 온체인 검증→USDC 지급) 구현, 매도 대금은 AP2 예산에 환입(예산=순투입 한도). localnet 라이브 풀사이클 검증 — 매수 4·매도 1 전부 온체인 확정, 순변화 교차검증 PASS(+7 USDC 수익 시나리오). `scripts/demo_rejections.py`: 건별 한도 초과·mandate 위변조·금액 부족·미허용 종목 4종 정상 차단(로그 아카이브). 특기: 라이브 중 Gemini 무료 티어 rate limit로 3틱이 규칙 폴백 처리 — 폴백 설계 실전 검증. CLAUDE.md 현재 상태·다음 단계 갱신(1=웹 서비스화, 2=Cloud Run, 3=디자인, 4=devnet, 5=제출물).
- **다음 단계 2번(Gemini 교체) 완료**: `agents/gemini_decider.py` 신설 — Gemini API(무료 티어, `gemini-flash-latest`, developer 모드)가 시세 흐름·규칙·예산을 보고 매수/매도/보류 판단 + 한국어 이유 생성. 호출 실패 시 규칙 기반 자동 폴백(데모데이 네트워크 대비). localnet 라이브 재검증: **Gemini 판단 매수 3건 온체인 확정**, 교차검증 PASS, 아카이브에 판단 주체·이유 기록. 참고: 신규 계정 키는 `AQ.` 새 형식이고 gemini-2.5 계열이 신규 사용자에게 차단돼 `gemini-flash-latest` 별칭 사용. 연결 확인은 `scripts/check_gemini.py`.
- **다음 단계 1번(라이브 tx) localnet 완료**: WSL Ubuntu에 Agave 4.1.1 설치 → solana-test-validator 기동 → setup → `run_demo.py --live` 성공. 매수 3건 = 온체인 tx 6건(USDC 결제+주식 전달) 확정, 전후 잔액 RPC 교차검증 PASS, `artifacts/tx/20260722_1547_solana-localnet_live_buy.json` 아카이브. 수정 2건: 클라이언트 커밋먼트 Confirmed 통일(에어드랍 자금 미인식 해결), `preflight_commitment=Confirmed` 명시(Finalized 뱅크 preflight의 "Blockhash not found" 해결). 증빙 보강: 주식 전달 tx 서명 수집(기존엔 버려짐), 라이브 미확정 시 status=failed 처리, 전후 잔액 스냅샷·교차검증·JSON 아카이빙 추가. git 저장소 초기화. devnet은 SOL 확보(디스코드 요청) 후 .env 전환만 하면 됨.

## 2026-07-29 (2) — 온체인 예산 집행 A-lite 구현 완료 (커밋 5건: C2→C1→C3→C5→문서)

- **제품 결제 경로 무변경.** 엔진·화면·API·드라이런 어디에도 동작 변화가 없다. 정확한 문장은 "예산 상한을 체인이 집행하는 레일을 실증했고, 엔진 배선은 로드맵"이다. 아카이브의 `wired_into_product: false` 가 그 경계를 기계 판독 가능하게 남긴다.
- C2 `payments/delegation.py` 신규 — `read_delegation`(RPC 1회로 잔액+위임 잔여) · `approve_budget`(절대값 의미 고정, 감액은 전송 전 거부) · `revoke_budget`(멱등) · `spl_error_code` · `classify_rejection`. `GuardResult` 재사용이라 화면 코드 변경 0. `guard.py` 무수정 = 하드 검사 8종 카운트 불변.
- C1 `build_transfer_transaction(source_owner=)` — 출처 ATA 를 지불자에서 분리. 기본값 경로는 **바이트 동일**(수정 전 payload 를 골든 상수로 박아 증명). 판매자 `verify_payment` 는 출처 ATA 를 안 보므로 판매자측 변경 0줄.
- C3 `scripts/demo_delegation.py` — approve → 결제 → 한도 초과 거절 → **대조군(잔액 부족, 같은 0x1)** → 자기 상향 공격 → revoke. localnet 무설정 재현. 실측 rc=0 · 온체인 거절 3건 · 유출 0.00(온체인 차분으로 계산).
- C5 `snapshot_balances(user_pk=)` + `usdc_net_out` 추출 — 교차검증에 자금 소유자 지갑 합산. 현재 user 잔액이 0 이라 값이 지금까지와 동일하고, 옛 아카이브 재계산도 기록값과 일치(−4.9).
- **음성 대조 3건**: N1 코드만 보는 순진한 분류기가 잔액 부족을 '한도 거부'로 광고한다는 것 / N2 골든 payload 바이트 동일 / N3 옛 식은 user 지출을 0 으로 셈.
- 검증(`.venv` 정본 환경): 단위 테스트 **23종 전부 rc=0** · `test_delegation` 58종(오프라인 41 + localnet 17) · red_team rc=0(유출 0.00 · 오탐 0) · localnet 라이브 세션 1회 교차검증 PASS.
- ⚠ **테스트 배치 실행 주의**: `python scripts/test_*.py` 로 돌리면 6종이 `ModuleNotFoundError` 로 실패한다(그 파일들엔 `sys.path.insert` 가 없다). `python -m scripts.test_*` 형식으로, **`.venv\Scripts\python.exe` 로** 돌릴 것 — 시스템 python 에는 `google-genai` 가 없어 `test_brain_select` 가 실패한다.
- 문서 5곳(소개서·README·벤치마크·FEATURES·submission §6). 영상 큐시트에 **축③ X 구간 12초 신설**(402 원문 6초 + 위임 ④⑤ 6초), 재원 A −4 · B −3 · G −5, D·E 무변경. 랜딩·대시보드 문구는 일부러 그대로 뒀다(배선 전이라 첫 화면이 주장하면 과장).

## 2026-07-29 (3) — 촬영 대본 X 구간 반영 + 게시본 재발행 + 일정 조정 (커밋 3건)

- 촬영 대본(`docs/artifacts/demo_script.html`)에 축③ **X 구간 12초 신설** + A·B·G 시간 조정 반영. 브라우저 실측: 장면 8개 · 합계 180초 · 표시시간/길이 불일치 0 · 타이머 경계 전환 정확(67→C · 68→X · 79→X · 80→D) · 콘솔 오류 0. D·E 는 손대지 않았다.
- 게시본 2건(데모 대본·소개서) **같은 URL 로 재발행**. 재발행 전 게시본을 받아 저장소 수정 전 상태와 동일함을 확인(다른 세션 변경 0). 관제탑은 날짜 박힌 기록이라 무변경·재발행 불필요. ⚠ favicon 은 이전 값을 알 수 없어 새로 지정(대본 🎬 · 소개서 🛡️).
- **사용자 결정: 제출 준비 항목(소개서 PDF·촬영·구글폼)을 이틀 연기.** 최종본이 나와야 출력·촬영이 의미가 있다. **마감 8/3 23:59 KST 는 그대로** — 연기한 것은 착수 시점이다.
- **프런트는 이제 막 착수했고 전달본이 아직 없다.** 파일을 기다리며 일정을 비우지 않는다 — 다음 대화는 **남은 코드 구현**만 진행하고, 전달본은 도착 시점에 병합한다. 1순위는 거래 내역이 세션 경계를 구분하지 않는 결함(촬영 C 구간 직결·10줄). ⚠ 그 수정들이 `web/static/**` 이라 나중 전달본과 파일이 겹친다 — 병합 시 되돌아가지 않는지 확인할 것.

## 2026-07-28 (2) — P1·P2·P3 대부분 완료 + 지갑 잔액 표시 (커밋 9건)

- CODE-01: 브로커 자기신고 `partial` 매수가 배송 검증 블록에 못 들어가 유출 KPI·GUARD_PENDING 에서 빠지던 결함 수정. 음성 대조로 재현 확인(옛 조건에서 leak=0 / total=9.99). tx 아카이브에 guard 블록 추가.
- CODE-03/07: 쿼터 소진 보류(GUARD_LLM_UNVERIFIED)를 `blocked_unverified` 부분집합으로 분리 방출. KPI 칸은 4개 유지. 가드 패널에 '이번 세션 기준' 명시.
- UX-02/GAP-01: 런북 6-1 신설(#token 링크 규칙·촬영 규칙·토큰 로테이션·채택 금지안), 랜딩에 단일 공용 인스턴스 고지, 401 문구 교체.
- P3 상업성 정량: 경쟁 지도 슬라이드 신설(소개서 13→14장), 수익모델 3단 단가 + 12개월 목표 월 \,000(전부 '가정' 명시, TAM 없음), 타겟 고객 3층에 목표 수치.
- P2-3: 소개서에 결제 통화 자체발행 고지 신설, 사실오류 정정(USDC 순변화 −4.9 → 구매자 +4.9, 아카이브 재확인), 금지 문장 제거, 커버에 라이브 URL.
- DOC-03: 촬영 대본을 정본(submission.md 6장)과 동기화 — 6→7장면(A~G), 금지 문장·폐기 KPI 제거, 축② 근거인 장면 E(의미 대조) 신설, G 구간 explorer 조건부 경고.
- GAP-07: README 에 부서 3종 소개 + 라이브 URL. `python scripts/collect_evidence.py` 가 sys.path 문제로 크래시하던 것 수정(두 실행 형태 모두 rc=0).
- 신규: 연결 지갑 온체인 잔액 표시(`GET /api/wallet/balance` + 헤더 배지 + /connect). 세션 쿠키의 지갑만 조회, 실패는 error 필드, 20초 캐시(실패 포함). test_wallet_balance 20건 신설 → 테스트 21종.
- 배포 리비전 `synapstock-00013-sgs`. 엔드포인트 11개 실측 정상, `/broker/orders` POST → 402 + accepts[].
- 8/1 09:00 제출물 리마인드 예약(scheduled task).

## 2026-07-30 — 잔여 중간 버그 3건 정리 (BUG-03·06·07, 커밋 3건)

- 사용자 결정: 프런트 전달본이 아직 없고 병합 회귀를 3회 연속 겪었으므로, 이번 대화는 `web/static/**` 을 한 줄도 건드리지 않고 백엔드만 진행. 화면 쪽 우선순위 1(거래 내역 세션 경계)·2(손익 라벨·예산 주석)는 전달본 병합 후로 미룸.
- BUG-03 (`bc063fd`): `PaymentAuthorizer.release()` 의 0 클램프 제거. 추세추종은 `credit_sale(allow_surplus=True)` 로 spent 를 음수까지 내려 실현이익 재투자를 표현하는데, 그 상태에서 매수 1건이 release 되면 음수가 0 으로 올라붙어 벌어 놓은 운용 한도가 세션 끝까지 사라졌다. 멱등성은 `_reservations.pop` 이 담당하므로 비잉여 모드에서 클램프는 발동 불가능한 죽은 코드였다. 회귀 6건(test_settlement).
- BUG-07 (`77e8eaa`): `update_limits` 가 총액만 검사해, 추세 멀티에서 예산을 낮추면 한 종목의 몫이 그 종목 사용액보다 작아져 슬라이스 잔여가 음수가 되고 합산에서 다른 종목 양수와 상계됐다. 결과적으로 **총예산을 넘는 매수가 승인**됐다(재현 15 USDC 초과). 재슬라이스 **전에** 종목별 검사(부분 적용 방지) + `_budget_slices` 헬퍼 + `_total_remaining` 음수 바닥 처리. 회귀 9건 + 대조군 2건(test_multistock).
- BUG-06 (`fbec112`): `_last_action` 기록을 '판단'에서 '체결'로 이동. 무산된 판단이 다음 틱 프롬프트에 "몇 봉 전 매수"로 단언돼 같은 프롬프트의 보유 0 과 모순됐다. 기록 조건은 포지션 변동과 동일하게 묶었다. 매매 로직 무변경(회고는 Gemini 프롬프트에만 쓰이고 시간청산은 `_bars_held` 가 센다). 회귀 6건(test_rule_gate).
- 3건 모두 **음성 대조 수행** — 옛 코드로 되돌려 리포트와 같은 값(`spent=0` / `AAPL 몫 -15.00` / `보유 0 인데 "1봉 전 매수 @ 95"`)이 재현되는 것을 확인한 뒤 복원.
- 검증: 단위 23종 전부 rc=0 · red_team rc=0(유출 0.00 · 오탐 0). 테스트 파일 수는 23개 그대로(기존 파일에 회귀를 넣어 "23종" 표기를 유지).
- `docs/reports/bug_latest.md` 상태 표 갱신 — 스캔 당시 '중간' 9건이 전부 닫혀 잔여 0건. 낮음 14·정보 1 은 남아 있고, BUG-11·12 는 금액만 낮음이라는 리포트 자체의 경고를 함께 명시.

## 2026-07-30 (2) — BUG-11·12 (커밋 2건)

- 프런트가 전달본을 보류(더 수정한 뒤 한꺼번에 주기로) → 대기 중 백엔드 작업. `web/static/**` 변경 0.
- BUG-12 (`8dc7325`): 지불액이 1단위 미만이면 수량이 0.0000 으로 내림돼 청구액도 0 이 되는데, Guard 는 '청구액 == 견적'이라 통과·AP2 는 하한이 없어 `authorize(0)` 통과·정산은 settled. spent 가 0 이라 **잔여 예산이 줄지 않아 틱마다 반복**됐다. 엔진 `_buy_cycle`·`run_demo` 에서 결제로 만들지 않고, AP2 `authorize` 에 `amount > 0` 하한 추가. HTTP 경로(`broker_service.py:145`)는 이미 400 으로 막고 있어 인프로세스만 비대칭이었다. **엔진에서 먼저 끊은 이유** = 아래 계층이 잡으면 우리가 만든 dust 견적이 '가드 차단'·'한도 거부' KPI 로 잡혀 대표 지표가 더러워진다(회귀에 그 조건을 박았다).
- BUG-11 (`7652326`): `check_delivery` 의 도착 판정이 `cur - before >= expected` 라 기대 증가분 0 이면 항상 참이고, 잔액 조회가 예외를 내면 `cur = last` 로 대체돼 **온체인을 한 번도 못 읽고도 ok=True** 가 나왔다. 0 이하는 즉시 보류(무의미한 RPC 도 안 쏨) + '미도착'과 '재조회 실패'를 사유에서 구분. **`check_demand` 는 무수정 = 하드 검사 8종 카운트 불변**(소개서·대본의 "8종" 표기 유지).
- 음성 대조 수행 — 세 계층 무력화 시 `ok=True · 자산 +0 도착`, dust 3틱 → 가짜 체결 3건으로 리포트 그대로 재현.
- 검증: 단위 23종 rc=0 · red_team rc=0(유출 0.00 · 오탐 0) · README 대표 명령 `run_demo --no-gemini --ticks 12` rc=0. 테스트 파일 수 23개 유지.
- 잔여: '낮음' 12건 · '정보' 1건. 백엔드 5건(BUG-03·06·07·11·12)이 재배포 대기 중 — 프런트 병합 후 재배포 1회에 함께 올라간다.

## 2026-07-31 — 시안 마크업 이식 5건 + 재배포 (커밋 5건 전부 푸시)

실험장(`web/static/lab`)에서 확정한 시안 마크업을 본 화면(`web/static/index.html`·`app.js`·
`skeleton.css`)으로 옮겼다. 실험장 전용(`mock.js`·`data-lab-*` 4종·`.lab-bar`)은 가져오지
않았고, `theme.css`(색·글꼴·간격 토큰)는 프런트 전달을 기다리느라 손대지 않았다 —
지금 화면은 **구조는 시안 · 팔레트는 저장소(로열블루)** 다.

- `07be7cc` 상단 바(로고+탭 4개) · 402 Guard 를 떠 있는 패널에서 **본문 맨 위 보라 배너**로 ·
  그리드 4열→**12열** · 지갑 버튼(`.btn-wallet`+`.wallet-icon`+`[data-wallet-label]`).
  가드 훅(`data-guard-dock/-panel/-toggle`)은 이름·구조 그대로 두고 CSS 로만 흐름에 놓았다
  (`position:static` → app.js 의 인라인 `top` 무효). '닫기'가 배너 안이라 접히면 함께
  사라지므로 접혔을 때만 나타나는 손잡이 `[data-guard-reopen]` 을 신설해 app.js 가 원래
  버튼을 대신 누르게 배선했다.
  **⚠ `wallet.js` 를 함께 고쳐야 했다** — 저장소본은 `querySelector`(단수) + `btn.textContent`
  대입이라 지갑 버튼을 두 자리에 두면 하나가 죽고 **아이콘 span 이 통째로 지워진다**.
  `querySelectorAll` + `[data-wallet-label]` 대입으로 바꿨다(전달서는 이미 고쳐진 버전을
  전제하고 있었다 — 그 버전은 저장소에 온 적이 없다).
- `dca24c5` `today`(오늘의 결과) 카드 신설 — 손익·평가손익·포지션 3장 + 총자산을 타일 4개로.
  **백엔드 작업 0**(타일 숫자가 전부 기존 훅 재사용). 소스에서 옛 3장보다 앞에 두어야
  `querySelector` 가 새 카드를 잡는다. 옛 3장은 지우지 않고 CSS 로 감췄다(거기에만 있는 훅을
  app.js 가 계속 쓴다). `DEFAULT_LAYOUT` 맨 앞 + `LAYOUT_KEY` **_v12**.
  실험장의 "오늘 에이전트는 N번 사고…" 한 문장은 가져오지 않았다 — 서버가 주지 않는 값이라
  `mock.js` 가 가짜 상태로 지어내던 것이다.
- `9faf015` AI 카드 3건 — `.ai-no` 배지(22×22·모서리 7) · 보기 버튼(오른쪽 정렬·높이 32·
  `::after` 캐럿) · 두 피드를 AI 카드와 한 박스로(DOM 무중첩, CSS 만).
  **버튼 라벨은 `app.js` 의 `MORE_LABEL` 이 단일 출처**라 그것을 함께 고쳤다 —
  `renderFeedToggle` 이 토글마다 `btn.textContent` 를 다시 써서 `index.html` 만 고치면
  첫 클릭에 되돌아간다(캐럿을 `::after` 로 그리는 것도 같은 이유).
- `a7139f4` 화면 결함 3건. ①**거래 내역 세션 경계** — `/api/state` 에 `trades` 키가 없어
  표가 SSE 히스토리 재전송만으로 채워져 새로고침하면 지난 세션 체결이 섞여 보이던 것.
  `engine_started` 에서 기존 행을 `.past-session` 으로 바꾸고 구분선 1줄을 넣는다(피드의
  `sessionBoundary` 와 같은 처리). ⚠ 계획서 권고안은 '표를 비운다'였는데 프런트가 보낸 CSS
  (`tr.session-divider`/`tr.past-session`)가 구분선 방식을 전제해 그쪽으로 갔다.
  ②손익 수익률 라벨을 `state.pnl.basis` 로 전환(`initial-capital` → `수익률(평가 포함)`).
  ③예산 카드 '건별 최대'가 올인일 때 한도 카드와 달라 보이는 것에 설명 `title` 추가.
- `609f70b` **회귀 차단** — `.topbar` 규칙이 랜딩(`.topbar.landing-topbar`)으로 새고,
  `.brand h1`(18px)이 같은 특이도의 `.auth-brand h1`(20px)을 뒤에서 덮어 connect/login 제목이
  작아지고 있었다. `body.page-dashboard`·`.fx-brand h1` 로 좁혔다.

**검증**: 단위 테스트 23종 전부 rc=0 · `node --check` 통과 · app.js 훅 **125종**(신규
`data-guard-reopen`·`data-return-label`) 전부 `index.html` 에 존재 · 줄바꿈 유지
(index/skeleton/app CRLF · wallet LF) · 로컬 dry 세션에서 타일 9개 값이 `/api/state` 와 일치 ·
한 박스 이음매 세로 0 · 가로 0 실측 · 새로고침 복원 후 **구분선 위 행 수 = `counts.trades`** ·
세 페이지 콘솔 오류 0 · 유출 0.00.

**재배포 완료 — 리비전 `synapstock-00019-nld`.** 엔드포인트 12개 200 · 정적 파일 7개 저장소와
**바이트 일치** · `/static/lab/index.html` **404**(실험장 차단 유지) · `POST /broker/orders` →
**402**, scheme `exact` / network `solana-devnet` / asset **Circle 공식 devnet USDC**
(`4zMMC9sr…`) / `maxAmountRequired` 9990000 · env 에 `GEMINI_MODEL=gemini-flash-latest`·
`GEMINI_MODE=developer`·`ALLOW_LIVE_FROM_WEB=0` + 시크릿 5개 유지 · 배포본 대시보드 실측
(배너 흐름 배치 · 타일 4 · 12열 · 탭 4 · 지갑 버튼 2 · AI 배지 1·2 · 콘솔 오류 0).
**백엔드 5건(BUG-03·06·07·11·12)이 이 배포에 함께 올라갔다 — 배포 대기 잔여 없음.**

**⚠ 이식 부수 손실 1건(사용자 판단 대기)**: `pnl` 카드를 감추면서 `브로커 수수료 0.3% ·
누적 N USDC` 줄이 화면에서 빠졌다. 3분 영상 C 구간에서 **축① 상업성을 채울 유일한 지점**으로
지목됐던 줄이다. 되살리려면 숨김 한 줄을 빼거나 손익 타일에 한 줄 더 붙이면 된다.

**⚠ 런북 오류 발견**: `docs/deploy_cloud_run.md:35` 의 `$PROJECT_ID = "autotrader-agent-2026"`
은 틀렸다 — 실제는 프로젝트·서비스 모두 `synapstock`(리전 `asia-northeast3`).

### 2026-07-31 (2차) — 사용자 지적 3건 반영 (`92a760a`·`c3d0fb4`)

- **`landing.html`·`connect.html` 전달본 반영**(`92a760a`). 양방향 대조 결과 저장소에만 있는
  줄은 landing 3 · connect 1 이고 전부 전달본이 대체한 그 줄이었다(**회귀 0**).
  landing = CTA 3곳에 `.btn-sweep`, connect = 'Phantom 으로 연결' 평범한 버튼을 대시보드와
  같은 `.btn-wallet` + `.wallet-icon` + 라벨 span 으로. connect 의 라벨에는
  `[data-wallet-label]` 이 없어도 되는데, 그 페이지 스크립트는 `disabled` 만 토글하기 때문이다.
  ⚠ 저장소 관례가 **LF** 인 두 파일에 전달본이 CRLF 로 와서 변환해 기록했다(diff 6줄 / 7줄).
- **브로커 수수료 줄 복원**(`c3d0fb4`) — 1차 이식에서 `pnl` 카드를 감추며 화면에서 사라졌던
  `브로커 수수료 0.3% · 누적 N USDC` 를 **손익 타일 안**으로 옮겼다. 훅 이름을 pnl 카드와
  같게 둬서 app.js 는 무변경이다(querySelector 가 문서상 먼저 나오는 쪽을 잡는다).
- **'오늘의 결과' 날짜 + 한 줄 요약 배선**(`c3d0fb4`) — 1차 이식에서 "서버가 주지 않는 값"
  이라 뺐던 것을 **전부 실제 값으로** 만들었다.
  · 날짜(`renderTodayDate`) = **브라우저 시계가 아니라 재생 중인 봉의 날짜**. 시세 카드가
    이미 같은 날짜를 보여준다. `price_tick` 마다 갱신하되 값이 같으면 쓰지 않는다.
  · 문장(`renderToday`) = 매수·매도 건수는 **거래 이벤트를 그대로 센다**(거래 표와 같은 출처·
    같은 세션 경계 → 표 줄 수와 늘 일치). 막은 건수 `state.guard.blocked`, 보유 여부는
    `valuation.position_net_value_usdc`(포커스 종목 포지션만 보면 멀티에서 틀린다).
    `innerHTML` 은 쓰지 않았다 — 값이 전부 우리가 만든 숫자여도 이 파일 규칙에 예외를 두지 않는다.

**검증**: `node --check` 통과 · 훅 **127종** 누락 0 · 날짜가 봉을 따라 2026.04.09 → 06.03
이동 · 문장 "2번 사고 1번 팔았고"가 표의 매수·매도·매수 3줄 및 서버 `counts.trades=3` 과 일치 ·
누적 수수료 1.00 USDC = 서버 `fees.cum_fee_usdc` · 세션 재시작 시 건수 0 복귀 · 새로고침
복원 후에도 건수·표 줄 수·서버 값 3자 일치 · 랜딩/연결 페이지 회귀 0 · 콘솔 오류 0 ·
단위 테스트 23종 rc=0.

**⚠ 이 2차분은 아직 배포본에 없다** — 배포는 `synapstock-00019-nld`(1차분까지).

**2차분 배포 완료 — 리비전 `synapstock-00020-7w4`.** 정적 파일 6개 저장소와 바이트 일치 ·
`/static/lab/index.html` 404 · 콘솔 오류 0.

**다음 라운드 = 남은 시안 규칙 전부 이식**(사용자 지시 "다 넣어 위치도 다 맞춰진거래").
6건 중 앞의 4건(판단 타임라인 점·범례 / 결제 로그 태그 배지 / 거래 표 알약·가운데 정렬 /
툴팁)은 **`app.js` 렌더 함수를 고쳐야 한다** — 실험장에서는 `mock.js` 가 app.js 가 그린 줄을
다시 짜서 넘어갔지만 본 화면에는 mock.js 가 없다. 5번(카드 제목 아이콘)은 마크업 작업뿐이고,
6번(theme.css 팔레트)은 `lab.css:57-112` 에 시안 값이 라이트·다크 다 있어 파일을 더 받을
필요가 없다 — 다만 `--radius`·`--gap`·`--pad`·`--fs-big`·`--page-pad` 가 함께 바뀌어
랜딩·연결 페이지까지 영향을 받는다. 상세는 CLAUDE.md 최상단 "다음 대화" 블록.

## 2026-07-31 (3차) — 남은 시안 규칙 7건 전부 이식

실험장(`web/static/lab/`)에 남아 있던 시안 규칙을 본 화면으로 전부 옮겼다. 커밋 7건
(`449833c`·`b17446b`·`6bf9980`·`afd4998`·`06c3169`·`fff4dc8`·`1e7d369`) 전부 푸시.
백엔드 변경 0 — 손댄 파일은 `index.html`·`app.js`·`skeleton.css`·`theme.css`·`landing.html` 뿐이다.

- 판단 타임라인: `addDecision` 이 "@가격 — 사유"를 `.fx-at`·`.fx-reason` 으로 나눠 그린다.
  출처 배지는 DOM 에 남기고 CSS 가 16px 점으로 바꾼다(축② 증거 유지). 범례는 칩 5개.
- 결제 로그: `splitLogTag` 로 본문 앞 `[태그]` 를 회색 배지로 분리(호출부 15곳 무변경).
- 거래 표: 방향을 점+알약(`.fx-pill`)으로, 셀 가운데 정렬, '상태' 열 숨김, 판단 칩 틴트.
- 툴팁: `.tip[data-tip]` 말풍선. `title`→`data-tip` 이동은 한 번으로 부족해
  `MutationObserver` 로 따라가게 했다(app.js·wallet.js 가 상태마다 title 을 다시 쓴다).
- 카드 제목 아이콘: 제목 7곳에 `span.ci` + 글리프 토큰 7종.
- 요동 방지: 거래 표 `flex:1 1 0` · `overflow-anchor:none` · 한도 안내 `min-height:36px`.
  세션 중 문서 높이 변화 0회를 100ms×120회 표본으로 확인.
- theme.css: 시안 팔레트 + `--radius:18`·`--gap:24`·`--pad:24`·`--fs-big:32`·`--page-pad` 식.
  지갑 버튼은 시안 제외 지시라 `--wallet-radius: 16px` 로 못 박았다.

검증: 훅 128종 누락 0 · `node --check` 통과 · 대시보드/랜딩/연결 세 페이지 실측(라이트·다크) ·
콘솔 오류 0 · dry 세션 유출 0.00 · 줄바꿈 관례 유지(landing LF · 나머지 CRLF).
남은 일은 재배포 1회(배포본은 아직 `synapstock-00020-7w4`).

재배포 완료 — 리비전 `synapstock-00021-jk2`. 엔드포인트 12개 200 · 정적 파일 6개 저장소와 일치 ·
`/static/lab/index.html` 404(실험장 차단 유지) · env 에 `GEMINI_MODEL=gemini-flash-latest` 유지 ·
`POST /broker/orders` → 402 + accepts[](asset = Circle 공식 devnet USDC) · 배포본 대시보드에서
시안 토큰·범례 칩·아이콘 7개·툴팁·지갑 버튼 16px 확인 · 콘솔 오류 0.

배포 URL 라이브 세션 1회 완주(샌드박스·추세추종·80봉): 가드 시도 3 · 차단 0 · 유출 0.00 ·
실현 +17.25 USDC(23.06%) · Firestore 저장 · 서버 counts.trades=3 = 화면 표 3줄 · 콘솔 오류 0.
⚠ 배포본은 CONTROL_TOKEN 게이트라 `/app#token=…` 로 한 번 열어야 세션을 켤 수 있다
(런북 §6-1). `/`(랜딩)로 열면 app.js 를 안 실어 토큰이 저장되지 않는다.

그 과정에서 상단 바 탭이 긴급정지 버튼을 42px 가리는 것을 발견해 수정(`f3dde4e`,
`.fx-tabs { overflow: hidden }`). 알림 권한이 '거부' 일 때만 나타나는 결함이고 시안 팔레트
이전부터 있었다(옛 --page-pad 로 재면 16px). 재배포 후 최종 리비전 `synapstock-00022-2xl`,
겹침 -68px 로 해소 · 정적 파일 6개 저장소와 일치 · 실험장 404 · env 유지 · 콘솔 오류 0.

CONTROL_TOKEN 교체(2026-07-31). 토큰 URL 을 랜딩(`/`)으로 열어 주소창에 `#token=` 이 남는
바람에 값이 대화 기록으로 샜다 → Secret Manager `autotrader-control-token` 버전 2 를 추가하고
리비전 `synapstock-00023-92g` 로 반영. 옛 토큰으로 `POST /api/engine/start` 를 쳐서 401 을 확인해
교체가 실제로 먹은 것을 검증했고, 브라우저 패널의 죽은 토큰도 지웠다. 버전 1 은 남겨 뒀다
(서비스가 `:latest` 를 보므로 사용되지 않는다). 교체 전 `engine.status=idle` 확인(런북 §6).
값은 `gcloud secrets versions access latest --secret=autotrader-control-token` 로 사용자가 직접 본다.

## 2026-07-31 (4차) — BUG-24 · 런북 오타 · 제품명 SynapStock · public 감사

- BUG-24(`5f4c1a6`): 숫자형 환경변수가 빈 값이면 임포트 단계에서 앱 전체가 죽던 것.
  `config.env_num` 신설 — 빈 값은 기본값 복원, 잘못된 값은 변수명을 밝히며 중단.
  `MAX_BUDGET_USDC` 만 `blank_ok=False`(서버측 상한이 조용히 10배로 풀리면 안 된다).
  회귀 5묶음은 `test_dry_sell.py` 에(테스트 파일 23개 불변). 음성 대조 수행.
- 런북 `deploy_cloud_run.md:35` 의 `$PROJECT_ID` 정정 — 실재하지 않는
  `autotrader-agent-2026` → 실제 값 `synapstock`.
- 제품명 확정(`5372d4f`): **SynapStock = 제품 · 402 Guard = 지출 승인 게이트 기능**.
  제품명 자리(타이틀·브랜드·알림 제목·README 제목·제출명·소개서 표지)만 교체하고
  게이트를 가리키는 자리는 전부 유지. 화면에 남은 12곳을 전수 확인.
- CONTROL_TOKEN 버전 1 비활성화(버전 2 가 현행).
- public 전환 안전성 감사 통과 — 275커밋 전수 스캔에서 크리덴셜 0건, 개인정보 0건,
  방어 문구 잔존 확인. 남은 것은 사용자의 전환 조작뿐.
- ⚠ 사용자 정정: 현재 화면은 디자인 최종본이 아니다 → 소개서 PDF·영상 촬영은 아직.
- 다음 대화: devnet USDC 재실증 → A-lite 재검토 → 온체인 결제 확인 → 잔여 점검 →
  그 뒤에 버그부서·심사부서.

재배포 완료 — 리비전 `synapstock-00024-gbg`(SynapStock 개명 + BUG-24 반영).
엔드포인트 12개 200 · 정적 파일 6개 저장소와 일치 · 실험장 404 · env 유지 ·
`POST /broker/orders` → 402(Circle 공식 devnet USDC) · FastAPI 문서 제목이
`SynapStock — 402 Guard(…)` 로 바뀐 것이 새 코드가 실제로 기동한 증거 ·
화면 제품명 SynapStock / 가드 문구는 402 Guard 그대로 · 콘솔 오류 0.

GitHub 저장소 이름 변경 완료 — `Loda1002/SolanaAgent` → `Loda1002/SynapStock`
(`gh repo rename`). 로컬 원격 주소와 submission.md·handoff.md 링크도 함께 맞췄다.
옛 URL 은 GitHub 가 리다이렉트한다. ⚠ 아직 PRIVATE — public 전환은 남아 있다.
