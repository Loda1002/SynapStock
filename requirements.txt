# --- MVP core (no GCP required) ---
# NOTE: keep this file ASCII-only. pip decodes it with the OS locale codec
# (cp949 on Korean Windows), so non-ASCII comments break `pip install -r`.
# solana 0.39+ requires Python >=3.11; this range also works on Python 3.10.
solana>=0.36.6,<0.39
solders>=0.26,<0.28
python-dotenv>=1.0.0

# --- Gemini free tier (runtime AI brain) ---
google-genai>=1.0.0

# --- Web dashboard (FastAPI + SSE) ---
fastapi>=0.110,<1.0
uvicorn>=0.29

# --- Cloud Run persistence (Firestore, off by default via FIRESTORE_ENABLED) ---
google-cloud-firestore>=2.16
